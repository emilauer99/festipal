---
phase: 05.1-quiks-rename-ci-v1-0-rollout
verified: 2026-08-11T12:55:00Z
status: passed
score: 16/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 14/16
  gaps_closed:
    - "Die CI-Typo-Rollen sind voll angeglichen — inklusive Outfit-Tracking −4 % bis −2 % (D-08, UI-SPEC § Typography)"
    - "CLAUDE.md beschreibt den Zustand nach dieser Phase (Light-Mode-Satz korrigiert)"
  gaps_remaining: []
  regressions: []
human_verification: []
---

# Phase 05.1: quiks Rename & CI v1.0 Rollout — Verification Report (Runde 2)

**Phasenziel:** Die Codebasis trägt den quiks-Namen und die verbindliche CI v1.0 Ende zu Ende — Pakete/Bundle-IDs per ADR-024 umbenannt, `packages/ui`-Tokens auf Beere/Amber mit Sunset als einzigem Verlauf, hell-first (Light) als Default-Fläche in `apps/mobile` — und alle bestehenden Screens laufen weiterhin durch Lint, Typecheck und Tests.
**Verifiziert:** 2026-08-11 (Runde 2)
**Status:** passed
**Re-Verifikation:** Ja — nach Gap-Closure durch Quick-Task `260811-jz6` (Commits `a799d4b`, `8202efd`, `e28d150`, `3f1bc4f` auf `docs/quiks-rebrand-ci`)

## Goal Achievement

**Das Phasenziel ist erreicht.** Beide Runde-1-Gaps sind am Code geschlossen und unabhängig nachgeprüft — nicht aus dem Quick-Task-Summary übernommen. Die vormals verifizierten 14 Must-haves wurden regressionsgeprüft (Alt-Namen-Sweep, app.json, Paketnamen, Typecheck, volle Mobile-Suite in dieser Session), keine Regression gefunden.

### Gap-Closure-Nachweis (unabhängig verifiziert, diese Session)

**Gap 1 — Outfit-Tracking (vormals Truth 16, DISCONNECTED → FLOWING):**

- **13 Tracking-Sites in 10 Dateien**, per Grep bestätigt: jedes `letterSpacing: typeRoles.<rolle>.letterSpacing` steht direkt neben seinem `fontSize: typeRoles.<rolle>.size`. Parität pro Datei und Rolle exakt (welcome 2/2, home 2/2, [festivalSlug] 2/2, übrige je 1/1). Der Runde-1-Gap-Report hatte mit „~10 Stellen in 9 Dateien" tatsächlich `AvatarTile.tsx` übersehen — die 13/10-Zählung des Quick-Tasks ist die korrekte.
- **Kein Umgehungspfad:** Ein Voll-Grep über alle `typeRoles.(wordmark|display2|title2)`-Zugriffe in `apps/mobile` zeigt außer den 13 Style-Paaren nur Geometrie (`SPLASH_GLYPH_WIDTH`), lineHeight-Arithmetik, einen Doc-Kommentar in `lib/fonts.ts` (außerhalb der Scan-Roots) und Test-Assertions. Keine Style-Site setzt eine getrackte Rollengröße ohne Tracking.
- **Guard-Test ist nicht vakuös** (`apps/mobile/lib/__tests__/type-tracking.test.ts`, 8 Tests, kritisch gelesen):
  - Token-Korridor: negativ, |Wert|/Größe in 2–4 % ± Toleranz (−2.24/56 = 4 %, −1.76/44 = 4 %, −0.52/26 = 2 % — alle im Korridor);
  - Kopplung: exakt die überwachten Rollen tragen ein letterSpacing-Token (`withTracking === TRACKED_ROLES`) — eine vierte Rolle mit Token, aber ohne Überwachung, macht rot;
  - Scan: pro Datei/Rolle muss Größen-Zuweisungszahl == Tracking-Zuweisungszahl; **Anti-Vakuitäts-Assertions** `sourceFiles.length > 0` und `sizeUses > 0` pro Rolle — bei Umbenennung des Zugriffsmusters läuft der Guard nicht leer durch, sondern schlägt fehl.
  - **Mutationstest dieser Session selbst ausgeführt:** `letterSpacing`-Zeile aus `AvatarTile.tsx` entfernt → Guard rot mit exakter Offender-Meldung (`components\AvatarTile.tsx: 1 title2 size use(s) vs 0 tracking use(s)`, 1 failed | 7 passed); wiederhergestellt → 8/8 grün. Die RED-Fähigkeit ist damit an beiden Enden belegt, unabhängig vom Executor-Protokoll.
- **Unverändert wie behauptet:** `git diff` über die Quick-Task-Commits zeigt KEINE Änderung an `packages/ui/src/tokens.ts` (letterSpacing-Werte −2.24/−1.76/−0.52 mit derived-Markern intakt), `apps/mobile/lib/wordmark-glyph.ts`, `pnpm-lock.yaml` und `.claude/CLAUDE.md`. Commit `8202efd` ist rein additiv (13 Ein-Zeilen-Inserts + 2 Testdateien, 166 Insertions, 0 Deletions).
- **`@types/node` NICHT hinzugefügt:** `apps/mobile/tsconfig.json` behält `"types": ["react"]`; `apps/mobile/package.json` ohne `@types/node`; Lockfile-Treffer sind vorbestehende Toolchain-Auflösungen (vitest/api). Stattdessen `node-builtins.d.ts` mit lokalen Ambient-Deklarationen (dokumentierte Wartungsschuld mit Auflösungsanweisung im Kopfkommentar).

**Gap 2 — CLAUDE.md (vormals Truth 17):**

- „Light mode is not wired up" → **0 Treffer** in `CLAUDE.md`; ersetzt durch den gelieferten Zustand (Zeilen 24–26: `apps/mobile/lib/theme.ts` löst das Geräteschema auf, nur exakt `dark` liefert das Nachtschicht-Set, beide Modi live). Zusätzlich dokumentiert Zeile 27–28 die neue Tracking-Regel samt Guard.
- `.claude/CLAUDE.md` bewusst unangetastet (dort stand die falsifizierte Aussage nie) — bestätigt.

### Observable Truths (alle 16 erneut bewertet)

| # | Truth | Status | Evidence (Runde 2) |
|---|-------|--------|--------------------|
| 1 | Rename Ende zu Ende (ADR-024): kein Alt-Name außerhalb `.planning/`, `docs/`, Lockfile | ✓ VERIFIED | `git grep -l -i festipal` (Runde-1-Exclusions) = **0 Treffer**, diese Session erneut ausgeführt |
| 2 | Scheme ↔ trustedOrigins gekoppelt; genau EIN App-Scheme-Eintrag | ✓ VERIFIED | Regression: keine der Dateien vom Quick-Task berührt; api-Suite laut uncached Gate-Lauf 45/45 |
| 3 | Bundle-IDs `at.quiks.app` (iOS+Android), Name/Slug `quiks` | ✓ VERIFIED | `app.json` diese Session enumeriert: name/slug/scheme=`quiks`, beide IDs `at.quiks.app`, `userInterfaceStyle: automatic`, adaptiveIcon `#F7F5F2` |
| 4 | 7 Pakete unter `@quiks/*`, Root `quiks`, Lockfile konsistent | ✓ VERIFIED | Stichprobe 4 package.json korrekt; `pnpm-lock.yaml` von den Fix-Commits unberührt; frozen install Exit 0 (Quick-Task-Gate) |
| 5 | MMKV-IDs harter Schnitt auf `quiks-*`, kein Shim (D-14) | ✓ VERIFIED | Regression: Dateien unberührt seit Runde 1 |
| 6 | tokens.ts: Beere/Amber, Sunset einziger Verlauf, derived-Marker | ✓ VERIFIED | `tokens.ts` von Fix-Commits unberührt (git diff leer); typeRoles-Block erneut gelesen, Werte intakt |
| 7 | Limette/Violett gelöscht, per Test abgesichert (D-11) | ✓ VERIFIED | Mobile-Suite **148/148 in dieser Session** (inkl. `theme.test.ts`-Verbots-Assertions) |
| 8 | Hell-first-Auflösung (D-01) getestet, Provider oberhalb Splash | ✓ VERIFIED | `theme.test.ts` in der 148er-Suite grün; `_layout.tsx`-Änderung war eine reine letterSpacing-Zeile |
| 9 | Farben pro Render aufgelöst statt beim Import eingefroren | ✓ VERIFIED | Regression: nur additive Style-Zeilen innerhalb bestehender createStyles-Factories |
| 10 | Kein rohes Hex-/rgba-Literal in app+components (ADR-015) | ✓ VERIFIED | Alle 13 neuen Zeilen lesen Token-Werte, keine Literale (Grep-Output vollständig gesichtet) |
| 11 | FloatingNav-Glas + BlurView-Tint modusabhängig (D-03) | ✓ VERIFIED | Regression: Datei unberührt |
| 12 | Sunset-Hero: Token-SVG, textOnGradient, surfaceCard; kein dritter Einsatz | ✓ VERIFIED | `FestivalCard.tsx`-Änderung ist eine letterSpacing-Zeile im nameHero-Style; Sunset-Pfade unberührt |
| 13 | Font-Resolver: Rolle → gewichtsspezifische Datei, kein numerisches fontWeight (D-10) | ✓ VERIFIED | `fonts.ts` unberührt; `fonts.test.ts` in 148er-Suite grün |
| 14 | Brand-Glyph: EINE Geometrie, 6 PNGs korrekt, adaptiveIcon Papier | ✓ VERIFIED | `wordmark-glyph.ts` + Assets von Fix-Commits unberührt; Runde-1-PNG-Maßprüfung bleibt gültig |
| 15 | Phasen-Gate grün: frozen install, Lint, Typecheck, Tests, Export; Geräteabnahme erfolgt | ✓ VERIFIED | Quick-Task-Gates uncached (lint 10/10, typecheck 10/10, test 7/7, frozen install Exit 0) **plus eigene Session-Läufe:** `tsc --noEmit` mobile Exit 0, vitest mobile 148/148 |
| 16 | CI-Typo voll angeglichen **inklusive Outfit-Tracking −4 %/−2 %** (D-08, UI-SPEC) | ✓ VERIFIED | **Gap geschlossen:** 13 Sites in 10 Dateien wenden die Token an (Grep-Parität exakt); Datenfluss FLOWING; Guard 8/8 grün + eigener Mutationstest RED/GREEN. Geräte-Optik läuft korrekt als offener Ledger-Eintrag WINDOWS 31 (siehe unten) |
| 17 | Beide CLAUDE.md beschreiben den Nach-Phasen-Zustand | ✓ VERIFIED | **Gap geschlossen:** „not wired up" 0 Treffer, Ersatztext beschreibt `theme.ts`-Auflösung korrekt; `.claude/CLAUDE.md` korrekt unangetastet |
| 18 | WINDOWS-Ledger spiegelt die Realität; D-15 dokumentiert, nicht ausgeführt | ✓ VERIFIED | Eintrag **31 `open`** beschreibt exakt die offene Geräteaussage (Umbrüche Wortmarke/Display/Titel, FestivalCard-Hero-Höhen, Splash-Wortmarke, AvatarTile-Zentrierung ≈0,26 pt — hell UND dunkel); `open_count` 22→23 wie behauptet; 25/30 weiter offen |

**Score:** 16/16 Kern-Must-haves verifiziert (18 Zeilen, weil zwei Truths in je zwei Teilaussagen geprüft werden)

### Required Artifacts

Alle Runde-1-Artefakte regressionsgeprüft unverändert bzw. nur additiv berührt. Neu bzw. geändert in Runde 2:

| Artefakt | Erwartet | Status | Details |
|----------|----------|--------|---------|
| letterSpacing-Anwendung in Consumern | UI-SPEC/D-08 | ✓ VERIFIED | 13 Sites / 10 Dateien, Parität exakt (vormals ✗ MISSING) |
| `apps/mobile/lib/__tests__/type-tracking.test.ts` | Nicht-vakuöser Regressions-Guard | ✓ VERIFIED | 8 Tests; Korridor + Kopplung + Scan mit Anti-Vakuitäts-Assertions; Mutationstest bestanden |
| `apps/mobile/lib/__tests__/node-builtins.d.ts` | lokale Ambient-Typen statt @types/node | ✓ VERIFIED | existiert; Dependency-Graph unberührt (Lockfile-Diff leer) |
| `CLAUDE.md` | Nach-Phasen-Zustand | ✓ VERIFIED | Light-Mode-Satz korrigiert, Tracking-Regel ergänzt (vormals ⚠️ PARTIAL) |
| `packages/ui/src/tokens.ts` | unverändert, Werte korrekt | ✓ VERIFIED | git diff leer; −2.24/−1.76/−0.52 mit derived-Markern |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `typeRoles.*.letterSpacing` | wordmark-/display2-/title2-Styles | Style-Konsum | ✓ WIRED (vormals ✗ NOT_WIRED — 13 Leser, Guard erzwingt Parität) |
| Alle übrigen Runde-1-Links (Scheme↔Origin, Paketnamen↔Imports, Theme-Kette, Sunset-Token, dangerText, fontFamilyForRole) | — | — | ✓ WIRED (Regression: von den Fix-Commits nicht berührt; Gates grün) |

### Data-Flow Trace (Level 4)

| Artefakt | Datenwert | Quelle | Fließt? | Status |
|----------|-----------|--------|---------|--------|
| `typeRoles.wordmark/display2/title2.letterSpacing` | −2.24/−1.76/−0.52 | tokens.ts → 13 Style-Sites | **Ja** | ✓ FLOWING (vormals ✗ DISCONNECTED) |
| Übrige Runde-1-Flüsse (Farben, Sunset, PNGs) | — | — | Ja | ✓ FLOWING (unverändert) |

### Behavioral Spot-Checks (Runde 2, eigene Läufe)

| Verhalten | Kommando | Ergebnis | Status |
|-----------|----------|----------|--------|
| Guard grün auf aktuellem Stand | `vitest run lib/__tests__/type-tracking.test.ts` | 8/8 passed | ✓ PASS |
| Guard-RED-Fähigkeit (Mutationstest) | letterSpacing-Zeile aus AvatarTile entfernt → Lauf → restauriert → Lauf | 1 failed mit Offender-Pfad+Rolle, danach 8/8 | ✓ PASS |
| Mobile-Suite vollständig | `vitest run` (apps/mobile, einmalig) | **148/148** (14 Dateien) | ✓ PASS |
| Mobile-Typecheck | `tsc --noEmit` (apps/mobile) | Exit 0 | ✓ PASS |
| Alt-Namen-Sweep | `git grep -l -i festipal` (Exclusions wie Runde 1) | 0 Treffer | ✓ PASS |
| app.json-Felder | Node-Enumeration | alle korrekt | ✓ PASS |

### Probe Execution

Keine `scripts/*/tests/probe-*.sh` im Repo, keine von Plänen deklariert — nicht anwendbar.

### Requirements Coverage

| Requirement | Source Plan | Beschreibung | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| ADR-024 (Substitution) | 05.1-01, 02, 07 | Voller Rename | ✓ SATISFIED | Truths 1–5, 18 |
| ADR-023 (Substitution) | 05.1-03, 04, 05, 06, 07 | CI v1.0 inkl. Typo/Tracking | ✓ SATISFIED | Truths 6–14, 16 — Tracking-Lücke geschlossen |

Keine REQ-IDs in `.planning/REQUIREMENTS.md` für 05.1; die Requirement-ID-Substitution über ADR-023/ADR-024 ist in allen sieben Plänen sichtbar protokolliert — kein Coverage-Versagen, keine verwaisten Requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Keine TBD/FIXME/XXX in den geänderten Dateien | — | Debt-Marker-Gate bestanden |
| `apps/mobile/lib/__tests__/node-builtins.d.ts` | 1–37 | Handgeschriebene Node-Typ-Stubs | ℹ️ Info | Dokumentierte, begründete Wartungsschuld (Alternative hätte Lockfile-Churn erzeugt und das frozen-install-Gate gefährdet — Abwägung im Quick-Task-Summary belegt) |
| `apps/api/src/auth/auth.instance.ts` | 34–35 | `exp://` ungegated in Produktion (WR-03, vorbestehend) | ⚠️ Warning | Kein Phasen-Gap; Hardening-Follow-up |
| 5 Mobile-Dateien | — | Prettier-Drift (JSX-Umbrüche, vorbestehend; `OtpBoxes.tsx` nie berührt, 0 letterSpacing-Zeilen im Prettier-Diff) | ℹ️ Info | Prettier ist kein Turbo-Gate; eigener Format-Commit vor dem PR empfohlen |
| Repo-Root | — | untrackte `tsconfig.json` (Expo-Root-Verschmutzung) liegt weiterhin da | ⚠️ Warning | `git status` bestätigt `?? tsconfig.json` — vor dem PR löschen (07-SUMMARY-Auflage) |
| diverse | — | IN-01/IN-02/IN-03 aus Runde 1 unverändert | ℹ️ Info | Follow-ups, keine Phasen-Gaps |

### Human Verification Required

Keine offenen Punkte **für diese Phasen-Verifikation**. Zur Transparenz:

- Die verpflichtende Geräteabnahme (05.1-07 Task 2) wurde vom Entwickler gefahren und als Ganzes freigegeben. Diese Freigabe liegt VOR der Tracking-Änderung — die **visuelle** Wirkung des Trackings auf echten Screens ist damit ausdrücklich ungeprüft und läuft korrekt als **offener Ledger-Eintrag WINDOWS 31** (Umbrüche in Wortmarke/Display/Titel, FestivalCard-Hero-Höhen, Splash-Wortmarke, AvatarTile-Initialen-Zentrierung ≈0,26 pt; hell UND dunkel). Der Eintrag wurde am Ledger verifiziert, nicht aus dem Summary übernommen. Das Phasenziel selbst („Codebasis trägt CI v1.0 … Lint/Typecheck/Tests grün") ist eine Code- und Gate-Aussage und davon nicht blockiert; das Ledger ist der etablierte Sink für Geräteaussagen (analog WINDOWS 25/30).

### Gaps Summary

Keine offenen Gaps. Beide Runde-1-Gaps sind geschlossen und unabhängig bestätigt:

1. **Tracking (WR-01):** 13 Style-Sites lesen die Tokens; der Guard macht die Rückfall-Art „Token da, niemand liest ihn" nachweislich rot (eigener Mutationstest) und kann nicht leer durchlaufen (Anti-Vakuitäts-Assertions). `tokens.ts`, `wordmark-glyph.ts`, Lockfile und Dependency-Graph unverändert.
2. **CLAUDE.md (WR-02):** falsifizierter Light-Mode-Satz ersetzt, Tracking-Regel ergänzt, `.claude/CLAUDE.md` korrekt unangetastet.

Keine Regressionen an den 14 vormals verifizierten Must-haves (Sweep 0, Typecheck Exit 0, Suite 148/148, Commit-Scope rein additiv).

---

_Verified: 2026-08-11T12:55:00Z (Runde 2)_
_Verifier: Claude (gsd-verifier)_
