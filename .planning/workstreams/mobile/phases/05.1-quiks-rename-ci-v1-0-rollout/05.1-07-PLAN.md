---
phase: 05.1-quiks-rename-ci-v1-0-rollout
plan: 07
type: execute
wave: 4
depends_on: ["05.1-02", "05.1-04", "05.1-05", "05.1-06"]
files_modified:
  - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md
autonomous: false
requirements: [ADR-023, ADR-024]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The whole monorepo passes a frozen-lockfile install plus lint, typecheck and the full test suite, and the mobile app still exports for both platforms — the phase's own success criterion from the roadmap goal."
    - "The phase is verified on a real Android device, not by a node-environment test run: this phase changes the bundle ID, the URL scheme, native colour-mode configuration and the app icons, none of which a node-env vitest pass can observe (CONTEXT.md Specifics, and the Phase-5 lesson recorded in project state)."
    - "The old-bundle-ID installation is removed from the test device before acceptance, so the two apps cannot be confused and no stale MMKV data influences the result."
    - "Sign-in and sign-out both succeed after the scheme change — the better-auth origin check is the one thing this phase can break invisibly — and a deep link plus a force-quit cold start both still resolve."
    - "Every existing screen is walked in BOTH colour modes; light mode has never been rendered by this codebase before, so it is new surface, not a regression check."
    - "FloatingNav tab labels are checked in both locales on device."
    - statement: "FloatingNav overflow: four fixed flex:1 items with minWidth hitMin and no numberOfLines on the label, so a longer translated label can wrap or clip. Held out of the spec deliberately and resolved by the device walk-through in both locales (UI-SPEC E3/overflow)."
      verification: backstop
    - "The Home hero renders the Sunset gradient with legible Ink text at both ends of the gradient, and the nav glass reads as milky-light in light mode rather than dark-tinted."
    - "The new app icons are correct on the launcher, including the Android adaptive mask — the safe-zone percentage used by the generator is an unverified assumption and only a real device settles it (RESEARCH Assumptions Log A1)."
    - "The white-card-on-Papier separation at 8% border opacity is confirmed on device; if it reads as invisible the sanctioned fix is raising borderSubtle opacity, never adding a shadow or elevation token (D-04)."
    - "The splash mark mount is explicitly confirmed or rejected by the developer rather than shipped on a planner interpretation (see plan 05.1-06's flagged assumption)."
    - "D-15 (repo directory and git remote rename) is delivered as a documented, ordered checklist and explicitly scheduled AFTER the merge — executing it mid-phase would break running paths, open terminals and the Metro watcher, which is D-15's own stated reason for the ordering."
  artifacts:
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md (ordered post-merge repo/remote/IDE rename steps)
    - a signed-off device acceptance record in the plan SUMMARY
  key_links:
    - "frozen-lockfile install -> turbo lint/typecheck/test across all packages -> the roadmap goal's 'all existing screens still pass' criterion"
    - "expo prebuild --clean + run:android from apps/mobile -> a native build carrying the new bundle ID, scheme, userInterfaceStyle and icons -> the only place the phase's native changes become observable"
    - "docker-compose top-level name (plan 02) -> the post-merge folder rename in the checklist -> the volume docker compose binds afterwards"
---

<objective>
Close the phase: prove the whole monorepo is green under a clean install, rebuild natively, run the
mandatory on-device acceptance, and hand the developer the ordered post-merge repo/remote rename
checklist.

Purpose: this phase changes native identity, native colour-mode configuration and launcher icons.
The project's own recorded lesson from Phase 5 is that React Native routing and native behaviour
must be verified on the device — a green node-environment vitest run proves nothing about any of
them. CONTEXT.md makes the device acceptance mandatory, not optional.
Output: a green full-stack gate, a device sign-off, and `05.1-RENAME-CHECKLIST.md`.

Requirement-ID substitution (visible, recorded): no REQ-IDs exist for this phase; ADR-023 and
ADR-024 are the traceability anchors (see plan 05.1-01 objective for the full note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-CONTEXT.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-VALIDATION.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-UI-SPEC.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-02-SUMMARY.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-04-SUMMARY.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-05-SUMMARY.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-06-SUMMARY.md
</context>

## Artifacts this phase produces (plan 07 share)

| Kind | Symbol / path | Note |
|---|---|---|
| doc | `.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md` | **new file** — post-merge operational steps for D-15 |

<tasks>

<task type="auto">
  <name>Task 1: Full-stack gate — clean install, root lint/typecheck/test, mobile export, leftover sweeps</name>
  <precondition>The local docker compose Postgres stack is running and healthy under the renamed project (plan 05.1-02 complete) — the apps/api suite in the root test run connects to it.</precondition>
  <files>(no source files — this task only runs gates and fixes anything they surface)</files>
  <read_first>
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-VALIDATION.md (Sampling Rate and the phase-gate definition)
    - The four prior SUMMARY files listed in `<context>` (for anything a plan flagged as deferred or unresolved)
    - .planning/WINDOWS.md (open unrun-verify rows relevant to this phase)
  </read_first>
  <action>
    Run the phase gate from the repo root and fix anything it surfaces (fixes stay in scope only
    when they are required for the gate to pass — anything larger is reported, not improvised):

    1. `pnpm install --frozen-lockfile` — proves the committed lockfile matches the renamed
       package graph. A failure here means a package.json and the lockfile drifted apart.
    2. `pnpm lint`, `pnpm typecheck`, `pnpm test` at root (turbo across all packages).
    3. `pnpm --filter ./apps/mobile build` — the Expo export for both platforms; this is the
       bundler-level proof that Metro resolves every renamed workspace import and the new
       react-native-svg render paths.
    4. Leftover sweeps, all of which must come back empty:
       - the old product name anywhere in tracked files outside `.planning/`, `docs/` and the
         lockfile;
       - hex or rgba literals outside comments anywhere in `apps/mobile/app` and
         `apps/mobile/components`;
       - a hardcoded dark blur tint in the floating navigation.
    5. Record the results in the SUMMARY, including anything that had to be fixed.

    Do not start Metro or a device build in this task — task 2 owns the native rebuild.
    Windows note (project memory): stop any running Expo/Metro watcher before the install, or it
    fails ENOENT on pnpm's temp directories.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile 2>&1 | tail -10</automated>
    <automated>pnpm lint 2>&1 | tail -15</automated>
    <automated>pnpm typecheck 2>&1 | tail -15</automated>
    <automated>pnpm test 2>&1 | tail -30</automated>
    <automated>pnpm --filter ./apps/mobile build 2>&1 | tail -15</automated>
    <automated>test "$(git grep -l -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml' | wc -l)" -eq 0</automated>
    <automated>! grep -rnE "#[0-9A-Fa-f]{3,8}'|rgba\(" apps/mobile/app apps/mobile/components --include=*.tsx | grep -vE "^[^:]+:[0-9]+:\s*(//|\*)"</automated>
    <automated>! grep -Eq 'tint="dark"' apps/mobile/components/FloatingNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` exits 0 (no lockfile drift after the scope rename, including plan 06's `pnpm-lock.yaml` update for the icon rasteriser)
    - `pnpm lint`, `pnpm typecheck` and `pnpm test` each exit 0 at repo root
    - `pnpm --filter ./apps/mobile build` exits 0 for both platforms
    - `git grep -l -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml' | wc -l` returns `0` <!-- planner-discipline-allow: festipal -->
    - `grep -rnE "#[0-9A-Fa-f]{3,8}'|rgba\(" apps/mobile/app apps/mobile/components --include=*.tsx` returns matches only inside comments — this is the PHASE-LEVEL combined sweep, jointly satisfied by plan 04 (components) and plan 05 (app), and it runs here because it is the first point after both have merged
    - `! grep -Eq 'tint="dark"' apps/mobile/components/FloatingNav.tsx` succeeds
    - The SUMMARY lists every command run with its exit status, and any fix applied to make a gate pass
  </acceptance_criteria>
  <done>A clean frozen install, root lint/typecheck/test and the mobile export are all green, and no old-name or raw-colour leftovers remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory on-device acceptance — native rebuild, both colour modes, auth, deep link, icons</name>
  <what-built>
    The full quiks rename (packages, bundle ID, URL scheme, MMKV storage IDs, local docker stack) plus
    CI v1.0: Beere/Amber tokens, a hell-first light default that follows the system setting, a
    Sunset gradient on the Home hero card, mode-aware nav glass, the CI type scale with real
    weight-specific font files, and six regenerated app icons. Everything automatable is green
    (Task 1). Nothing above is observable in a node-environment test run.
  </what-built>
  <how-to-verify>
    **Rebuild (order matters):**
    1. Uninstall the OLD app from the test device. The bundle ID changed, so the old build stays
       installed as a separate app with its own icon and its own MMKV data — leaving it there makes
       every result ambiguous.
    2. From `apps/mobile` (NEVER from the repo root — a root run creates a stale root `android/`
       with the old package and breaks the build):
       `npx expo prebuild --clean` then `npx expo run:android`.
    3. Start Metro with a cleared cache: `npx expo start -c`.

    **Auth and routing (the coupling that breaks invisibly):**
    4. Sign in with an email OTP end to end.
    5. Sign OUT. A 403 here means the app scheme and the server's trusted-origin entry disagree —
       report it immediately, do not work around it.
    6. Open a deep link into a festival using the NEW scheme and confirm it resolves to the right
       screen.
    7. Force-quit and relaunch (cold start) and confirm you land where you left off, with no
       unmatched-route screen.

    **Colour modes — walk EVERY existing screen in BOTH modes** (switch the device system theme):
    welcome, email, verify (OTP), complete-profile, festivals (all + saved), festival home, home
    (hero + rail), and the splash on a cold start.
    8. Light mode: the app background is warm Papier, not white and not dark. Text is legible
       everywhere; error copy (force one — e.g. a wrong OTP) is legible, not washed out.
    9. Dark mode: unchanged from before apart from the new brand hues.
    10. Splash: background and wordmark follow the mode, and the trailing dot is Beere in both.
    11. **Confirm or reject the brand mark above the splash wordmark.** This mount is a planner
        interpretation of the design contract, not something you decided — if you want the splash
        to stay text-only, say so and it is removed in one line (the component and the icon
        generator stay either way).

    **CI surfaces:**
    12. Home hero: the Sunset gradient is visible, runs Amber to Beere, and the card name/caption
        are dark Ink and readable at BOTH ends of the gradient. The "open festival" pill below the
        card stays solid Beere with white text.
    13. Floating nav in LIGHT mode: milky-light glass, not a dark-tinted blur. Active tab reads as
        active; the two disabled decorative items are visible but clearly inert.
    14. Nav labels in BOTH app languages (German and English): no wrapping, no clipping, no
        overlap between the four items.
    15. Festival list in light mode: white cards on Papier separated only by a hairline border. If
        the separation reads as invisible, say so — the sanctioned fix is a stronger border, never
        a shadow.
    16. Typography: headlines are noticeably larger than before and NOT smeared/faux-bold. Check
        the OTP screen's six-box row specifically — the larger type is the most likely place a
        layout broke.
    17. The status green (used for success states) does not read as leftover old branding next to
        Beere and Amber.

    **Icons:**
    18. Launcher icon: the `q` mark in the Sunset gradient on a Papier background with the Beere
        dot. On Android check the adaptive mask at several icon shapes (circle, squircle, rounded
        square) — nothing may be clipped. The safe-zone margin used by the generator is an
        unverified assumption; this is where it gets settled.
    19. Recents/task-switcher and settings-app icon look correct too.

    Report per numbered item: pass, or what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" when every item passes, or list the failing item numbers with what you observed. Include your decision on item 11 (keep or remove the splash mark) either way.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the ordered post-merge repo, remote and IDE rename checklist (D-15)</name>
  <reversibility rating="costly">D-15's rating, carried forward: the remote URL, every local clone and the IDE configuration live outside the repo. GitHub creates redirects, but each local environment has to be pulled across by hand. The decision is locked in CONTEXT.md — recorded, not re-asked.</reversibility>
  <files>.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-CONTEXT.md D-15 and the Specifics note about the still-open Phase-5 pull request
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md § Common Pitfalls 1 (why the compose project name had to be pinned before this step) and § Runtime State Inventory (the IDE datasource files, which are not versioned)
    - docs/DEVELOPMENT_DECISIONS.md ADR-024 (the parts of the rename scope this checklist closes, and the parts it explicitly excludes)
    - docs/GIT_CONVENTIONS.md (branch and merge rules)
  </read_first>
  <action>
    Write `05.1-RENAME-CHECKLIST.md` in the phase directory. It documents the steps that must NOT
    run during the phase, in the order D-15 requires, with the reason for the ordering stated at the
    top: renaming the working directory mid-phase breaks in-flight paths, open terminals and the
    Metro watcher, which is why this is a post-merge operation.

    Content, as an ordered, tickable list:
    1. Merge this phase's pull request first (and note the still-open Phase-5 pull request that
       CONTEXT.md flags as needing to land before this one).
    2. Rename the repository on GitHub.
    3. Update the local `git remote` URL and verify with a fetch.
    4. Close every terminal, editor and Metro/Expo process rooted in the old directory, then rename
       the local working directory.
    5. Re-open and re-verify: a fresh `pnpm install`, `docker compose ps` (confirming the stack
       still binds the SAME named volume, because the compose file now pins the project name
       explicitly — this is the step that would have silently orphaned the volume without that fix),
       and one `pnpm --filter ./apps/api test` run.
    6. Update the machine-local IDE datasource configuration, which still names the old database and
       is not versioned.
    7. Note explicitly what is NOT covered, per ADR-024: store listings, EAS build/submit profiles,
       domains, and the historical design-asset filenames.

    Do NOT execute any of these steps in this task. State clearly at the top of the file that
    executing it is a developer action after the merge.
  </action>
  <verify>
    <automated>test -f .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md</automated>
    <automated>grep -c "docker compose ps" .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RENAME-CHECKLIST.md` exists and contains an ordered, tickable list
    - The file states at the top that execution happens AFTER the merge and gives D-15's reason for that ordering
    - The order is: merge, GitHub rename, remote URL, close processes, local directory rename, re-verify, IDE datasources
    - The re-verify step includes a docker compose check that the stack still binds the same named volume
    - The file lists the explicitly out-of-scope items (store listings, EAS profiles, domains, historical design-asset filenames)
    - `git remote -v` output is unchanged by this task (nothing was executed)
  </acceptance_criteria>
  <done>The post-merge rename procedure is written down in the order D-15 requires, and nothing in it was executed during the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile app -> API `/api/auth/*` | The device acceptance is the only place the origin check is exercised against a real native build |
| OS deep-link handler -> app router | An external URL enters the app through the new custom scheme |
| old app installation -> test device | A stale build under the previous bundle ID with its own local storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-01 | Spoofing | app scheme x `trustedOrigins` on a real native build | high | mitigate | Device acceptance items 4-6 exercise sign-in, sign-out and a deep link on the rebuilt app; a 403 on sign-out is called out as a stop-and-report signal rather than something to work around |
| T-05.1-04 | Information Disclosure | old-bundle-ID installation retained on the test device | low | mitigate | Acceptance step 1 uninstalls it before anything else, so no stale MMKV data or icon confusion can influence the result |
| T-05.1-15 | Tampering | native build produced from the wrong working directory | medium | mitigate | The rebuild steps explicitly require running from the mobile app directory, never the repo root — a root run produces a stale native project carrying the previous package identity |
| T-05.1-10 | Information Disclosure (failed communication) | error copy legibility on the new default light surface | medium | mitigate | Acceptance step 8 requires deliberately forcing an error and confirming it is legible, rather than assuming the measured contrast values hold in situ |
| T-05.1-16 | Denial of Service (local infra) | post-merge directory rename orphaning the docker volume | medium | mitigate | Plan 02 pinned the compose project name explicitly; the checklist's re-verify step confirms the same named volume is still bound after the rename |

Schema-push gate: not applicable — no ORM schema file is touched in this plan.

API-coverage checkpoint: no external API integration (detector returned `detected: false` for this
phase; the phase renames identifiers and swaps design tokens inside an existing stack).
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test` and the mobile export all exit 0.
2. The leftover sweeps (old product name, raw colour literals, hardcoded blur tint) all come back empty.
3. The device acceptance checkpoint is approved, with an explicit decision recorded on the splash mark.
4. `05.1-RENAME-CHECKLIST.md` exists and nothing in it was executed.
</verification>

<success_criteria>
- The phase goal is met and proven: the codebase carries the quiks name and CI v1.0 end to end, and every existing screen still passes lint, typecheck and tests.
- The native and perceptual changes are signed off on a real device, not inferred from a node-environment test run.
- D-15 is delivered as a documented, ordered procedure scheduled after the merge, exactly as CONTEXT.md requires.
</success_criteria>

<output>
Create `.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-07-SUMMARY.md` when done.
</output>
