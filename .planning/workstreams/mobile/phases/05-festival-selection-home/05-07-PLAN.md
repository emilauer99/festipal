---
phase: 05-festival-selection-home
plan: 07
type: execute
wave: 4
depends_on: [05-03, 05-04, 05-05]
files_modified:
  - apps/mobile/lib/select-next-festival.ts
  - apps/mobile/lib/__tests__/select-next-festival.test.ts
  - apps/mobile/app/(tabs)/home.tsx
autonomous: true
requirements: [HOME-01]
estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Home tab is the post-login landing and renders a lean overview from existing data only: a 'Dein nächstes Festival' hero (FestivalCard hero variant + 'Festival öffnen') and a 'Meine Festivals' horizontal rail, sourced from listMyFestivals (D-04)."
    - "The hero is chosen by a pure, tested selectNextFestival(festivals, today) helper with explicit deterministic rules: prefer the earliest festival whose startDate is on/after today (date-only compare, no UTC shift); ties broken by name; when none is upcoming (or startDate is null) fall back to a deterministic order (e.g. by name), NOT the API's unspecified row order (REVIEW 05-07 MEDIUM). The remaining saved festivals render in the rail, never a second hero (zero-one-many / overflow)."
    - "Every rail card is a saved festival, so each is passed saved={true} consistently and carries no Save affordance of its own (rail items come from listMyFestivals) (REVIEW 05-07 MEDIUM)."
    - "Opening a festival from the hero/rail enters it gate-lessly (persist active slug + push /f/:slug), landing on its festival home (HOME-01)."
    - "With zero saved festivals the hero+rail are replaced by a single shared empty-state block ('Noch kein Festival gespeichert' + a 'Alle Festivals ansehen' CTA) that navigates to /festivals?segment=all so the Festivals tab actually OPENS on the Alle segment (the shared segment-param contract, REVIEW 05-06/05-07 HIGH — a plain tab navigation would land on Meine); no duplicate empty UI."
    - "The rail 'Alle' see-all action also navigates to /festivals?segment=all (same shared contract)."
    - { statement: "While listMyFestivals is pending the Home tab shows the existing festivals loading-text pattern (not a new skeleton); on error it shows the existing network-error copy + retry.", verification: backstop }
    - { statement: "selectNextFestival returns a stable, deterministic hero for empty input (none), a single festival, all-past festivals, null/invalid startDates, and exact-today startDate (adjacency), covered by unit tests (REVIEW 05-07 MEDIUM; edge probes FEST/HOME).", verification: backstop }
  artifacts:
    - apps/mobile/lib/select-next-festival.ts (pure tested hero-selection helper)
    - apps/mobile/lib/__tests__/select-next-festival.test.ts
    - apps/mobile/app/(tabs)/home.tsx (lean overview: hero + rail + empty state)
  key_links:
    - "listMyFestivals -> selectNextFestival(festivals, today) -> hero; the rest -> rail (deterministic order)"
    - "hero/rail onEnter -> saveActiveFestivalSlug + push /f/:slug -> festival home (HOME-01)"
    - "empty-state CTA + rail see-all -> push('/festivals?segment=all') -> Festivals tab opens on Alle"
  prohibitions:
    - "Do NOT rely on the API's row order for the hero fallback; use the deterministic selectNextFestival ordering (REVIEW 05-07 MEDIUM)."
    - "Do NOT navigate the empty-state CTA to the Festivals tab without the segment=all param (it would land on Meine, REVIEW 05-07 HIGH)."
    - "Do NOT give rail cards a Save affordance; every rail item is already saved (pass saved={true}) (REVIEW 05-07 MEDIUM)."
    - "Do NOT add an Artists rail, global News feed, 'Könnte dir gefallen' recommendations, or a social row (D-04 — omitted this phase)."
    - "Do NOT render a Ticket button or any ticket/wallet framing in the hero (D-04, ADR-014)."
    - "Do NOT render a second hero for additional festivals; extra saved festivals go in the rail."
    - "Do NOT introduce a new loading skeleton or error copy; reuse the existing festivals patterns (backstop)."
    - "Do NOT hardcode user-facing strings; all copy via Lingui per the Copywriting Contract."
    - "Do NOT use violet/secondary color or JetBrains Mono (UI-SPEC #10/#12)."
---

<objective>
Build the lean global Home tab (D-04): the post-login landing showing a single "next festival" hero
and a "Meine Festivals" rail, both from the existing `listMyFestivals` data, plus a clean empty state
that routes to the Festivals tab. A thin composition over the `FestivalCard` primitive — no new
endpoints, no out-of-scope sections.

Purpose: give the app a real home surface now (part of the shell stood up this phase) that opens any
saved festival in one tap, without pulling in Artists/News/recommendations that have no backing data.
Output: `select-next-festival` (+ test) and the finished `(tabs)/home.tsx`.
</objective>

<review_incorporation>
Cross-AI review (05-REVIEWS.md, Codex) findings addressed in THIS plan:
- **HIGH — empty-state CTA cannot select the "Alle" segment.** INCORPORATED: the empty-state CTA and
  the rail see-all navigate to `/festivals?segment=all`; 05-06 initializes its SegmentedControl from
  that typed param, so the Festivals tab actually opens on Alle (shared cross-tab contract).
- **MEDIUM — "earliest upcoming" underspecified.** INCORPORATED: a pure `lib/select-next-festival.ts`
  `selectNextFestival(festivals, today)` (Task 1) with explicit rules — date-only compare, "today"
  defined, null/invalid startDate handled, ascending, name tie-break, deterministic fallback — covered
  by a unit test (also the LOW "no hero-selection test" finding).
- **MEDIUM — saved order from the API is unspecified.** INCORPORATED: the hero + rail order comes from
  `selectNextFestival`'s deterministic ordering, not the API's `listMyFestivals` row order (which has
  no `orderBy`); no reliance on "first saved."
- **MEDIUM — rail Save affordance is unnecessary.** INCORPORATED: rail cards receive `saved={true}`
  consistently (every rail item is from `listMyFestivals`); they carry no Save control.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@apps/mobile/app/(tabs)/home.tsx
@apps/mobile/components/FestivalCard.tsx
@apps/mobile/lib/date-range.ts
@apps/mobile/lib/active-festival-storage.ts
@apps/mobile/lib/api-client.ts
</context>

## Artifacts this phase produces (this plan)

- `apps/mobile/lib/select-next-festival.ts` — pure, tested `selectNextFestival(festivals, today)` hero-selection helper.
- `apps/mobile/lib/__tests__/select-next-festival.test.ts` — unit test covering empty/single/all-past/null-date/exact-today cases.
- `apps/mobile/app/(tabs)/home.tsx` — the lean Home overview (hero + rail + empty state), replacing the 05-05 placeholder.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selectNextFestival pure hero-selection helper (deterministic, tested)</name>
  <files>apps/mobile/lib/select-next-festival.ts, apps/mobile/lib/__tests__/select-next-festival.test.ts</files>
  <read_first>
    - apps/mobile/lib/otp-error.ts + apps/mobile/lib/__tests__/otp-error.test.ts (the pure-fn + node-env Vitest pattern to mirror; no RN imports)
    - apps/mobile/lib/date-range.ts (the date-only y/m/d local-parse idiom from 05-03 — reuse the same parsing approach for the on/after-today comparison, no `new Date(str)` UTC parse)
    - packages/contracts/src/schemas.ts (the `Festival` type; startDate/endDate/place are `string | null`)
    - apps/mobile/vitest.config.ts (node-env, `lib/**/__tests__/**/*.test.ts` scope)
  </read_first>
  <behavior>
    - selectNextFestival([], today) returns undefined (empty input).
    - selectNextFestival([a], today) returns a (single element, even if its startDate is null or past — see fallback).
    - Given festivals with startDate on/after `today`, returns the earliest by date-only comparison; an exact-today startDate counts as upcoming (adjacency inclusive).
    - Ties on the same startDate are broken by festival name, then festival id; duplicate names therefore never fall back to input order.
    - When NO festival is upcoming (all past, or all null/empty/malformed startDate), returns a deterministic fallback (first by name, then id), never the input array order.
    - The helper also exposes the same total ordering used by Home so removing the selected hero leaves a deterministic rail; it never mutates the query-cache array supplied by the caller.
  </behavior>
  <action>
    Create apps/mobile/lib/select-next-festival.ts exporting a pure `selectNextFestival(festivals:
    Festival[], today: Date): Festival | undefined`. Parse each `startDate` (`YYYY-MM-DD`) date-only into
    a LOCAL date (reuse 05-03's y/m/d parsing approach — never `new Date(str)`). Partition into
    "upcoming" (parsed startDate >= start-of-`today`, date-only) and the rest; treat null/empty/invalid
    startDate as NOT upcoming. If any upcoming: return the one with the earliest startDate, breaking ties
    by `name` (localeCompare). Else (no upcoming): return the deterministic fallback — the festival with
    the smallest `name` by localeCompare (so the hero is stable regardless of the API's unordered
    `listMyFestivals` rows). Return undefined only for empty input. Define "today" as the caller-supplied
    `Date` reduced to its local Y/M/D (the screen passes `new Date()`; the test passes a fixed date).
    Create apps/mobile/lib/__tests__/select-next-festival.test.ts mirroring otp-error.test.ts, covering
    the selection behaviors above (empty, single, upcoming-earliest, tie-break, exact-today,
    all-past/null fallback).
    Execution clarification: also export
    `orderFestivalsForHome(festivals: readonly Festival[], today: Date): Festival[]`, and implement
    `selectNextFestival` as its first-item projection. The ordering helper sorts a copy, never the
    caller/query-cache array. Validate an exact `YYYY-MM-DD` shape and round-trip the constructed local
    year/month/day so impossible normalized dates such as `2026-02-31` are non-upcoming. Use a total
    comparator: upcoming rows first by date, then non-upcoming rows; resolve every remaining tie by name
    with an explicit locale/options and finally immutable festival id. This clarification supersedes the
    name-only tie/fallback wording above and supplies the deterministic rail order Task 2 requires.
    Extend tests with duplicate-name/id tie-breaking, malformed/impossible dates, reversed-input
    equivalence, full returned ordering, and proof that the input array is not mutated.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile test && pnpm --filter @festipal/mobile typecheck</automated>
  </verify>
  <done>selectNextFestival is a pure, deterministic helper covered by passing unit tests for empty/single/upcoming/tie/exact-today/fallback; mobile test + typecheck are green.</done>
  <acceptance_criteria>
    - `selectNextFestival([], today)` is `undefined`; non-empty input always returns a festival.
    - Upcoming selection uses validated date-only local parsing (exact-today counts as upcoming); ties break by name then id.
    - The no-upcoming fallback and the rail order are deterministic by name/id, not input order.
    - Tests cover empty/single/upcoming-earliest/tie/exact-today/all-past-or-null/malformed dates and immutability; `pnpm --filter @festipal/mobile test` green.
    - Duplicate names tie-break by id; malformed calendar dates are non-upcoming; the full Home ordering is input-order-independent and does not mutate its input.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lean Home overview — hero + Meine-Festivals rail + empty state</name>
  <files>apps/mobile/app/(tabs)/home.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/home.tsx (the 05-05 placeholder to replace)
    - .planning/phases/05-festival-selection-home/05-PATTERNS.md ((tabs)/home.tsx section: composition shape — hero = earliest upcoming by startDate else first-saved; rest -> horizontal rail; empty -> shared empty-state block; reuse festivals loading/error text)
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract (Home hero variant), ## Copywriting Contract (eyebrow "Dein nächstes Festival", hero CTA "Festival öffnen", rail title "Meine Festivals", "Alle" see-all, Home empty-state rows), ## Typography (micro eyebrow uppercase variant, title2 hero name, title2 SectionHead), ## Visual Hierarchy (hero is the sole focal point; rail secondary), ## UI Considerations home-hero/home-rail
    - apps/mobile/components/FestivalCard.tsx (flat + hero variants; pass saved={true} for rail) and apps/mobile/lib/date-range.ts + lib/i18n.ts (locale) + lib/active-festival-storage.ts (persist on enter)
    - apps/mobile/lib/select-next-festival.ts (Task 1 — the hero selection helper) and apps/mobile/lib/festival-queries.ts (festivalKeys.mine)
    - expo-router `useRouter().push` with the typed `/festivals?segment=all` target (the shared segment contract 05-06 consumes)
  </read_first>
  <action>
    Rewrite (tabs)/home.tsx to fetch `listMyFestivals` (`queryKey: festivalKeys.mine` i.e.
    `['me','festivals']`, guard the non-200 shape). Compute the hero via `selectNextFestival(saved,
    new Date())` (Task 1) — NOT an inline earliest-by-startDate with an API-order fallback. Render the
    hero with the FestivalCard hero variant (`saved={true}`) beneath a "Dein nächstes Festival" eyebrow
    (the uppercase `micro` variant per UI-SPEC), with the "Festival öffnen" CTA entering gate-lessly
    (`saveActiveFestivalSlug` + push `/f/:slug`). Render the remaining saved festivals (all EXCEPT the
    hero, in `selectNextFestival`-consistent deterministic order) in a "Meine Festivals" horizontal rail
    (SectionHead title + an "Alle" see-all action that navigates to `/festivals?segment=all`); rail rows
    are flat FestivalCards with `saved={true}` and NO Save affordance, tap-to-enter, no CTA of their own
    — secondary per Visual Hierarchy (REVIEW 05-07 MEDIUM). When zero saved festivals: replace hero+rail
    with ONE shared empty-state block ("Noch kein Festival gespeichert" + body + an "Alle Festivals
    ansehen" CTA that navigates to `/festivals?segment=all` — the segment param is REQUIRED so the tab
    opens on Alle, not Meine, REVIEW 05-07 HIGH) — omit the rail (no duplicate empty UI). Reuse the
    existing festivals loading-text + network-error+retry patterns (backstop — no new skeleton). No
    Artists/News/recommendations/social sections (D-04). All copy via Lingui; keep `scrollBottomPad`
    clearance for the floating nav. Pass the active UI locale into `formatDateRange` for card captions.
    Treat the ts-rest result as four explicit states: pending, transport error, resolved non-200, and
    resolved 200. Never turn a non-200 result into an empty array (that would falsely render the
    zero-saved state); reuse the localized network-error + Retry UI for both error forms and have Retry
    call `refetch`. For a 200 body, compute one `today` per render, call `orderFestivalsForHome(saved,
    today)`, take `ordered[0]` as the hero and `ordered.slice(1)` as the rail. Do not filter the hero by
    object identity or sort the cached response in place. Omit the rail and its SectionHead when exactly
    one festival is saved; key multiple rail items by festival id.

    Centralize entry in one hero/rail handler. Persist first, then use the Expo Router typed
    pathname/params form for `/f/[festivalSlug]` if generated route types reject string interpolation;
    catch a synchronous MMKV persistence failure and still navigate so gate-less entry cannot become a
    dead tap. Use a vertically scrollable screen plus a horizontal rail that remains usable for overflow,
    and put at least `layout.scrollBottomPad` on the vertical content container so the floating nav does
    not cover the final content. Use the same typed navigation form for the two
    `/festivals?segment=all` actions if required by route types.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
    <manual>On device: after login with ≥1 saved festival, the Home tab shows the "Dein nächstes Festival" hero (name/dates/place + "Festival öffnen") and a "Meine Festivals" rail; tapping "Festival öffnen" or a rail card enters that festival's home (HOME-01). With 0 saved festivals the Home shows the empty-state block whose CTA opens the Festivals tab on Alle.</manual>
  </verify>
  <done>The Home tab renders a single next-festival hero + Meine-Festivals rail from existing data (or a shared empty state at zero), opens festivals gate-lessly, and reuses the existing loading/error patterns; typecheck + lint pass.</done>
  <acceptance_criteria>
    - Exactly one hero festival chosen by `selectNextFestival(saved, new Date())`; the rest in the rail in the same deterministic order.
    - Rail cards pass `saved={true}` and have no Save affordance.
    - Hero/rail enter persists the slug and pushes `/f/:slug` (HOME-01).
    - Zero saved -> single shared empty-state block whose CTA pushes `/festivals?segment=all` (opens Alle); rail omitted.
    - The rail "Alle" see-all also pushes `/festivals?segment=all`.
    - No Artists/News/recommendations/social/ticket sections; no new loading skeleton/error copy.
    - Pending, transport-error, resolved non-200, and resolved 200 are distinct; non-200 never renders the empty state, and Retry refetches.
    - One saved festival omits the empty rail; multiple rail items use the shared copied ordering and remain horizontally scrollable above the floating-nav clearance.
    - An MMKV persistence exception does not block gate-less festival navigation.
    - typecheck + lint pass (no literal-string violations).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | `listMyFestivals` (caller-scoped read) drives the Home overview |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Home rail/hero (D-08 fields) | high | mitigate | Inherits the 05-01 cross-tenant isolation proof: the Home reads only the caller-scoped `listMyFestivals` (visitorId-scoped server-side, SEC-02); no new server surface is added, only a client composition |
| T-05-13 | Tampering | hero selection by startDate | low | accept | Hero/ordering is a pure client-side presentation choice over already-scoped data; a malformed date only affects local ordering, never data exposure |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint` (Task 2)
- On-device: hero + rail render and enter a festival; zero-saved shows the empty-state CTA
</verification>

<success_criteria>
- The Home tab is a lean, in-scope overview that lands the visitor and opens any saved festival in one tap.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-07-SUMMARY.md` when done.
</output>
