---
phase: 05-festival-selection-home
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/deep-link.ts
  - apps/mobile/lib/__tests__/deep-link.test.ts
  - apps/mobile/app/_layout.tsx
autonomous: true
gap_closure: true
gap_ids: [G-05-7, G-05-7b]
requirements: [FEST-04]
estimate:
  tokens: 50000
  raw_tokens: 27000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A custom-scheme deep link festipal://f/:slug (standard double-slash form) is captured with the FULL f/:slug route and, after auth, opens that festival's home (G-05-7)."
    - "festipal:///f/:slug (triple-slash) and https://<domain>/f/:slug both resolve to the same f/:slug route; the https domain hostname is never prepended onto the route (G-05-7)."
    - "A deep link fired while the user is ALREADY authenticated is honored and opens the linked festival, taking precedence over the persisted active-festival slug (G-05-7b)."
    - "A logged-out deep link still wins over the persisted slug after auth (existing behavior — must not regress)."
  artifacts:
    - apps/mobile/lib/deep-link.ts
    - apps/mobile/lib/__tests__/deep-link.test.ts
  key_links:
    - "Linking.parse(url) -> reconstructDeepLinkRoute({scheme,hostname,path}, APP_SCHEME) -> AUTH_FLOW_PATHS guard -> capturePendingDestination"
    - "capture effect ungated by auth status -> redirect effect consumePendingDestination() runs before getActiveFestivalSlug() (deep link precedence in BOTH auth paths)"
---

<objective>
Close two UAT deep-link gaps in the mobile root layout:

- G-05-7 (major): the custom-scheme deep link festipal://f/:slug is broken — the capture effect keeps only Linking.parse().path and drops hostname, so the standard double-slash form (which parses 'f' as the URL authority/hostname) loses the 'f/' segment and replays to an Unmatched Route.
- G-05-7b (major): a deep link fired while already authenticated is ignored (capture is gated to the unauthenticated state), and the cold-start redirect then opens the persisted active-festival slug instead of the deep-link target.

Purpose: Make festipal://f/:slug — the app's only real deep-link form (app.json configures the custom scheme only) — open the correct festival regardless of auth status, mirroring the already-correct logged-out precedence.
Output: A pure, unit-tested route reconstructor (lib/deep-link.ts) and a corrected, auth-agnostic capture effect in app/_layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-UAT.md

# Files being changed / directly relevant (read before editing)
@apps/mobile/app/_layout.tsx
@apps/mobile/lib/pending-destination.ts
@apps/mobile/app.json

# Test-style reference (node-env vitest, pure lib)
@apps/mobile/lib/__tests__/select-next-festival.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-05-7): pure deep-link route reconstructor + unit test over the three URL forms</name>
  <files>apps/mobile/lib/deep-link.ts, apps/mobile/lib/__tests__/deep-link.test.ts</files>
  <behavior>
    reconstructDeepLinkRoute(parsed, appScheme) — parsed mirrors Linking.parse output { scheme, hostname, path } (values documented below are exactly what expo-linking@57's new URL()-based parse yields for each form):
    - custom double-slash festipal://f/nova-sound-2026 -> { scheme: 'festipal', hostname: 'f', path: 'nova-sound-2026' } -> returns 'f/nova-sound-2026' (hostname rejoined because scheme === appScheme).
    - custom triple-slash festipal:///f/nova-sound-2026 -> { scheme: 'festipal', hostname: null, path: 'f/nova-sound-2026' } -> returns 'f/nova-sound-2026'.
    - https https://festipal.app/f/nova-sound-2026 -> { scheme: 'https', hostname: 'festipal.app', path: 'f/nova-sound-2026' } -> returns 'f/nova-sound-2026' (hostname NOT prepended — it is the domain).
    - custom single-segment auth path festipal://email -> { scheme:'festipal', hostname:'email', path:null } -> returns 'email'; festipal:///email -> { hostname:null, path:'email' } -> returns 'email' (both normalize identically so the AUTH_FLOW_PATHS guard still matches).
    - root festipal:/// -> { scheme:'festipal', hostname:null, path:null } -> returns null.
    - a different custom scheme value (appScheme param) still only rejoins hostname for that exact scheme.
  </behavior>
  <action>
    Root cause (05-UAT.md G-05-7): app/_layout.tsx destructures only { path } from Linking.parse(linkingUrl) and ignores hostname; new URL() parses festipal://f/:slug with 'f' as the authority (hostname='f', path=':slug'), so keeping only path drops the 'f/' segment and the replay hits an unmatched route.

    Create apps/mobile/lib/deep-link.ts, framework-free (no React/RN import) so it is unit-testable in the node-env vitest runner (mirror the pure-fn shape of lib/select-next-festival.ts). Export reconstructDeepLinkRoute(parsed: { scheme?: string | null; hostname?: string | null; path?: string | null }, appScheme: string): string | null:
    - Determine isCustomScheme = parsed.scheme === appScheme. Only for the app's OWN custom scheme, rejoin the authority: build segments from [parsed.hostname, parsed.path]; otherwise (https / anything else) use [parsed.path] alone — the hostname is a real domain and must never be prepended.
    - Normalize: strip leading/trailing slashes from each segment, drop empty segments, join with '/'. Return the joined string, or null when nothing remains (root link).
    Document WHY the hostname is only rejoined for the custom scheme (G-05-7), citing that app.json configures the custom scheme only (no Android App Links / iOS Universal Links this phase).

    Create apps/mobile/lib/__tests__/deep-link.test.ts covering every case in <behavior> above. Feed the parsed component objects directly (do NOT import expo-linking — it is not node-importable; it pulls in expo-modules-core native code). Add a comment on each fixture noting it mirrors expo-linking@57 Linking.parse output for that URL form, so the test locks the parse contract the fix depends on.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile exec vitest run lib/__tests__/deep-link.test.ts</automated>
  </verify>
  <done>reconstructDeepLinkRoute returns 'f/nova-sound-2026' for all three /f/:slug forms, 'email' for both custom-scheme forms of an auth path, and null for the root link; the spec passes.</done>
</task>

<task type="auto">
  <name>Task 2 (G-05-7 + G-05-7b): wire the reconstructor into an auth-agnostic capture effect</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <action>
    Two coordinated changes to the deep-link capture effect (the first useEffect, currently lines ~111-118), keeping the existing redirect effect (~206-227) unchanged — it already consumes the pending destination before reading the persisted slug, so fixing capture is sufficient for precedence in both auth paths.

    1. G-05-7 (parse fix): replace `const { path } = Linking.parse(linkingUrl)` + the path-only normalization with a call to reconstructDeepLinkRoute. Resolve the app scheme from expo-constants: read Constants.expoConfig?.scheme (import Constants from 'expo-constants'); it may be string | string[] | undefined, so normalize to a single string with a 'festipal' fallback (matches app.json "scheme": "festipal"). Pass Linking.parse(linkingUrl) and that scheme to reconstructDeepLinkRoute. If it returns null, return early. Keep the existing AUTH_FLOW_PATHS.has(route) guard (route is now the full normalized path without a leading slash — single-segment auth paths like 'email'/'verify'/'complete-profile' still match). capturePendingDestination(`/${route}`) as before. Import reconstructDeepLinkRoute from ../lib/deep-link.

    2. G-05-7b (auth-agnostic capture): change the effect guard from `if (!linkingUrl || authState.status !== 'unauthenticated') return;` to only bail when there is no linkingUrl (`if (!linkingUrl) return;`), so an incoming deep link is captured regardless of auth status. This makes an ALREADY-authenticated cold start capture the link too; the existing redirect effect (guarded to run on 'authenticated') then consumePendingDestination() FIRST and replays it, winning over getActiveFestivalSlug() — mirroring the already-correct logged-out precedence. Update the effect's comment: the content-leak boundary (threat T-4-06-E) is preserved NOT by the capture gate but by the redirect effect replaying only AFTER the guard has independently reached 'authenticated', plus the AUTH_FLOW_PATHS guard here — state this explicitly so the security rationale is not lost. Keep authState.status in the dependency array (capture is re-evaluated across state transitions; the effect is declared before the redirect effect, so on the authenticated transition capture runs first and the redirect consumes what it stored).

    Do NOT change the redirect effect, the coldStartRedirectRef one-shot, or the persisted-slug fallback — the logged-out deep-link precedence is CONFIRMED WORKING on device (05-UAT.md) and must not regress.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck</automated>
    <human-check>On device (second festival nova-sound-2026 is seeded): (a) logged out, session revoked, slug A=frequency-2026 persisted, open festipal://f/nova-sound-2026 (double-slash) -> auth gate shown, then nova-sound-2026 opens (G-05-7). (b) Already authenticated, close app, fire festipal:///f/nova-sound-2026 -> nova-sound-2026 opens, NOT the persisted frequency-2026 (G-05-7b). (05-UAT.md test 7)</human-check>
  </verify>
  <done>The capture effect reconstructs the full route via reconstructDeepLinkRoute for the custom scheme and captures regardless of auth status; typecheck passes; the logged-out precedence path is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external deep link → app navigation | an OS-delivered URL drives a captured/replayed route |
| unauthenticated → authenticated replay | a link captured pre-auth is replayed only post-auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-E | Elevation of privilege / content leak | deep-link capture→replay (T-4-06-E lineage) | high | mitigate | Replay happens ONLY in the redirect effect after the guard independently reaches 'authenticated'; the capture effect never stores an (auth)/(profile-setup) path (AUTH_FLOW_PATHS guard). Removing the capture-time auth gate does not weaken this — the boundary is enforced at replay, not capture. |
| T-05-10-T | Tampering | crafted deep-link path | medium | mitigate | reconstructDeepLinkRoute rejoins hostname only for the app's OWN custom scheme; the reconstructed route is resolved by expo-router (an unknown route lands on Unmatched Route, granting no privilege), and it only ever feeds the gate-less getFestival(slug) browse endpoint. |
| T-05-10-S | Spoofing (https link hostname) | https deep link | low | accept | https hostname is treated as the domain and never prepended to the route; no https App Links are configured this phase (custom scheme only). |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile test` — full mobile lib suite green (includes the new deep-link spec covering all three URL forms).
- `pnpm --filter @festipal/mobile typecheck` — clean (expo-constants scheme narrowing, reconstructDeepLinkRoute import, Href cast unchanged).
- `pnpm --filter @festipal/mobile lint` — clean.
- On-device UAT re-run of 05-UAT.md test 7 (both logged-out double-slash and already-authenticated cold-start deep links) — tracked for the /gsd-verify-work resume.
</verification>

<success_criteria>
- festipal://f/:slug, festipal:///f/:slug, and https://<domain>/f/:slug all reconstruct to the same f/:slug route (G-05-7).
- An authenticated cold-start deep link opens the linked festival and beats the persisted slug; the logged-out precedence still works (G-05-7b, no regression).
- typecheck, lint, and the mobile test suite are green.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-10-SUMMARY.md` when done.
</output>
