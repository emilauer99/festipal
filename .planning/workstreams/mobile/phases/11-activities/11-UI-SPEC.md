---
phase: 11
slug: activities
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-15
reviewed_at: 2026-08-15
---

# Phase 11 — UI Design Contract: Activities

> Visual and interaction contract for `apps/mobile` — the Aktivitäten tab becomes real
> (create/discover/join/leave/clone/route-handoff, ACT-01…ACT-06). Generated by
> gsd-ui-researcher from `11-CONTEXT.md` (locked decisions D-01…D-16), verified by
> gsd-ui-checker. This is a React Native / Expo phase, not a web/shadcn one — the "Design
> System" and "Registry Safety" sections below are adapted accordingly (see notes inline).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — this is a React Native app; **no third-party UI component library** (ADR-022). shadcn's init/registry gate does not apply here. |
| Preset | not applicable |
| Component library | owned RN primitives styled exclusively via shared tokens (`packages/ui/src/tokens.ts`, consumed through `apps/mobile/lib/theme.ts` / `theme-context.tsx`) |
| Icon library | Lucide (`lucide-react-native` 0.470), 2px stroke, outline only — CI v1.0 §5. Sizes: 12–14 inline, 15–18 in rows, 21–22 for section/action icons (matches existing `PersonRow`/`ListRow`/`RelationAction` usage) |
| Font | Outfit (display/title roles) · Plus Jakarta Sans (body/label/micro roles) · JetBrains Mono (mono/countdown roles) — resolved per role via `fontFamilyForRole(role, fontsReady)` (`lib/fonts.ts`). Outfit-tracked roles (`title2`, `title3` is NOT tracked — see Typography) **must** set `letterSpacing` alongside `fontSize` or the `type-tracking.test.ts` suite fails. |

**shadcn gate:** skipped — `components.json` does not apply to `apps/mobile`. `apps/admin` is a separate workstream/phase with its own shadcn contract; this phase touches only `apps/mobile`.

---

## Spacing Scale

This project's binding token file (`packages/ui/src/tokens.ts`) carries an explicit warning:
*"do not 'fix' [the spacing ramp] toward a generic 4/8/16 heuristic — it is the real ported
brand ramp."* Phase 11 uses the existing `spacingScale`/`layout`/`radiiScale` tokens as-is —
no new spacing values are introduced.

| Token | Value | Usage in this phase |
|-------|-------|----------------------|
| `spacingScale['sp-1']` | 2px | hairline gaps inside compact chip clusters |
| `spacingScale['sp-2']` | 4px | icon-to-label gap inside chips/badges |
| `spacingScale['sp-3']` | 6px | eyebrow-icon gap, badge internal gap |
| `spacingScale['sp-4']` | 8px | row gap inside a card (title↔meta↔participants), chip wrap `gap` |
| `spacingScale['sp-5']` | 12px | form field internal padding, stepper block padding |
| `spacingScale['sp-6']` | 16px | card padding (`ActivityCard`, form section blocks) — same as `PersonRow`/`FestivalCard` |
| `spacingScale['sp-8']` | 24px | button horizontal padding (matches every existing pill button) |
| `spacingScale['sp-9']` | 32px | gap between form sections |
| `layout.screenPad` | 18px | screen-edge padding on every new screen |
| `layout.sectionGap` | 28px | gap between "Deine Aktivitäten" and "Wer kommt mit?" sections |
| `layout.hitMin` | 44px | minimum tap target — every Pressable in this phase (chips, stepper buttons, chip-remove, join/leave/dissolve) |
| `layout.scrollBottomPad` | 104px | bottom padding on both push-screen ScrollViews (clears `FloatingNav`/home-indicator on the tab; push screens still use it for consistency with every other scroll screen) |
| `radiiScale['r-card']` | 22px | `ActivityCard`, permission callouts, confirm-style blocks |
| `radiiScale['r-md']` | 16px | form field blocks, `CapacityField` card |
| `radiiScale['r-pill']` | 999px | every chip/pill/button, `Chip` component |

**Exceptions:**
- Stepper increment/decrement circle: **36px**, reusing `AppHeader`'s `LEFT_BUTTON_CIRCLE_SIZE` constant precedent (not on the `sp-*` ramp, but an existing sized-circle convention — do not invent a second circle size).
- Everything else in this phase resolves from the table above. None.

---

## Typography

Canonical 4-row summary (template shape):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 15px | 400 | 1.45 |
| Label | 13.5px | 700 | 1.3 |
| Heading | 26px (`title2`) | 700 | 1.2 |
| Display | 44px (`display2`) | 800 | 1.06 |

`display2` is **not used** in this phase (no hero surface here) — declared only because the
project scale carries it.

**Full role usage for Phase 11** (all roles already exist in `tokens.ts` — none new):

| Element | Role | Size/Weight | Notes |
|---|---|---|---|
| Section headings ("Deine Aktivitäten", "Wer kommt mit?"), form screen headings | `title2` | 26/700, tracked | Outfit-tracked — set `letterSpacing: typeRoles.title2.letterSpacing` |
| `ActivityCard` title | `bodyStrong` | 15/600 | 1 line, truncate |
| `ActivityCard` meta line (day · time), detail screen's start-time line | `countdown` | 13.5/500 mono | compact mono meta, same role `friend-detail`'s handle-adjacent mono elements use |
| Detail screen's larger standalone mono figures (e.g. seat count "3/8") | `mono` | 15/500 mono | reserve for the ONE prominent mono figure on the detail screen; list cards use `countdown` |
| Participants row text ("5 sind dabei", spots hint) | `bodySm` | 13.5/400 | `textSecondary`/`textMuted` per side (see Color) |
| "Gestartet" fact-chip, tag chip, "Dabei" badge | `micro` | 10.5/700 caps | matches `ListRow`'s badge / `FestivalCard`'s "Saved" badge |
| Form section eyebrows ("Treffpunkt", "Wann", "Kapazität") | `label` | 13.5/700 | matches CI "Label" role exactly |
| Form field values / description textarea / free-text location input | `body` | 15/400 | |
| Primary button text (all pills: "Aktivität starten", "Los, posten", "Beitreten", "Route öffnen", "Einstellungen öffnen") | `title3` | 19/700 | same role every existing pill button (`RelationAction`, `calloutPrimaryButton`, `heroCta`, `retryButton`) already uses — do not introduce a second button-text size |
| "Verlassen" / "Klonen" secondary action text | `title3` | 19/700 | same size as primary buttons, distinguished by color/fill only (see Color) |
| "Auflösen" destructive action text | `title3` | 19/700, `dangerText` | matches `friend-detail.tsx`'s `dangerRow` exactly |
| Empty-state heading/body (sectional, not full-screen) | `bodyStrong` / `bodySm` | — | sectional empties are NOT `PlaceholderScreen` (that's full-screen only) — see Copywriting Contract |
| Participant list rows | `bodyStrong` (name) / `bodySm` (handle) | — | via reused `PersonRow`, unchanged |
| Permission callout title/body (location denied) | `bodyStrong` / `bodySm` | — | matches `CameraScanPanel`'s callout exactly |

---

## Color

Hell-first (light is default per CI v1.0 §6). All values below are **role names**, never raw
hex — components resolve through `useTheme().colors` per render.

| Role | Token | Usage |
|------|-------|-------|
| Dominant (60%) | `colors.bgApp` (Papier `#F7F5F2` light / Ink `#0C0E13` dark) | screen background — tab and both push screens |
| Secondary (30%) | `colors.surfaceCard` (`#FFFFFF` light / white 6% dark) | `ActivityCard`, `CapacityField` block, permission callouts (via `fillInfoQuiet`, see Info below), form field wrappers |
| Accent (10%) | `colors.primary` (Beere `#E8559F`) | see explicit reserved-for list below |
| Destructive | `colors.dangerText` (`#FF4D5E` dark / `#C93D4A` light-AA) | "Auflösen" action + its confirm dialog + mutation failure text |

**Accent reserved for — explicit list, nothing else:**

1. Primary CTA solid pills: "Aktivität starten" (top of tab + sectional empty-state CTA), "Los,
   posten" (create-form submit), "Beitreten" (join, when enabled) — solid `colors.primary` fill,
   `colors.textOnPrimary` text (matches `RelationAction`/`calloutPrimaryButton`/`heroCta`).
2. "Dabei" badge — on both the list card and the detail screen — `colors.fillBrandQuiet`
   background + `colors.primary` text (matches `FestivalCard`'s "Saved" badge exactly).
3. Selected state of: the single-select tag chip, the day chip, the "Standort angeheftet" chip
   once captured — `colors.fillBrandQuiet` fill + `colors.primary` border/text (mirrors
   `SegmentedControl.itemSelected`).
4. "Route öffnen" pill in the detail meeting-point block — a **secondary-accent** treatment
   (`colors.fillBrandQuiet` background, `colors.borderBrand` border, `colors.primary` text/icon)
   — deliberately NOT a second solid-primary pill on the same screen as "Beitreten"/"Verlassen".
5. `@handle` text inside every `PersonRow` (participant list) — existing, unchanged rule.
6. `AvatarTile` tint + border ring on every participant avatar — existing, unchanged rule.
7. "Einstellungen öffnen" button inside the location-permission-denied callout — matches the
   existing camera-denied callout precedent (`CameraScanPanel.tsx`'s `calloutPrimaryButton`)
   exactly; do not invent a different treatment for the second native-permission callout.

**Never accent:** tag-chip UNSELECTED state, "Gestartet" fact-chip, "Verlassen" button,
"Klonen" action, section headings, form field borders. Tag-chip color is a deliberate
deviation from the Figma-style design reference (which used an Amber-quiet fill) — this
project's token set has no quiet-Amber role, and this phase does not add one (out of the
phase's own file-touch scope). Unselected/neutral chips use `colors.fillQuiet` background +
`colors.textSecondary` text instead (same neutral-chip treatment `ListRow`'s badge and
`PersonRow`'s compact chrome already establish) — "design is direction, not wording" (house
rule, Phase 6 D-14).

**Info (non-accent semantic):** `colors.fillInfoQuiet` / `colors.borderInfo` / `colors.infoText`
— reserved for the location-permission-denied callout only, identical structure to
`CameraScanPanel`'s camera-denied callout (D-13's explicit modeling instruction).

**Destructive (danger):** `colors.dangerText` for the "Auflösen" row (icon + label, matches
`friend-detail.tsx`'s `dangerRow`/`dangerLabel` verbatim) and its native `Alert.alert`
destructive button; also every generic mutation-failure inline text in this phase
(`colors.dangerText`, `bodySm`, reused "Couldn't save — try again." pattern).

---

## Copywriting Contract

Response language for this project is German (Du-form, Satzcase, no exclamation stacks, "ein
Freund mit Plan" voice — CI v1.0 §1). Every row below carries the German catalog string plus
its English counterpart; both must exist in `apps/mobile/locales/{de,en}/messages.po`.
**User-generated content (activity title, subtitle, description, free-text location) is NEVER
wrapped in a Lingui macro** (ADR-012/020, same rule as `username`/`displayName`).

| Element | DE | EN |
|---|---|---|
| Section heading — mine | Deine Aktivitäten | Your activities |
| Section heading — public | Wer kommt mit? | Who's coming? |
| Primary CTA (tab top + empty-state) | Aktivität starten | Start an activity |
| Empty — "Deine Aktivitäten" (nothing created/joined) | Noch nichts Eigenes — starte eine Aktivität oder werde bei einer dabei. | Nothing of your own yet — start an activity or join one below. |
| Empty — "Wer kommt mit?" (festival has zero activities) | Noch keine Aktivitäten in diesem Festival. Sei die erste Person, die eine startet. | No activities in this festival yet. Be the first to start one. |
| Loading (list/detail) | *(reuse existing msgid)* "Wird geladen…" | "Loading…" |
| Transport error (list/detail) | *(reuse existing msgid)* "Server nicht erreichbar — stelle sicher, dass dein Gerät im selben WLAN wie die Dev-API ist." | "Can't reach the server — make sure your device is on the same Wi-Fi as the dev API." |
| Generic mutation failure | *(reuse existing msgid)* "Konnte nicht gespeichert werden — versuch's nochmal." | "Couldn't save — try again." |
| Create-form screen title (AppHeader push title) | Aktivität erstellen | Create activity |
| Detail screen title (AppHeader push title, static — see Screens Contract) | Aktivität | Activity |
| Title field label | Was habt ihr vor? | What's the plan? |
| Title field placeholder | z. B. Beerpong am Pavillon | e.g. beer pong by the pavilion |
| Title validation error (neither tag nor title on submit) | Gib einen Titel ein oder wähle ein Tag. | Enter a title or choose a tag. |
| Subtitle field label (visible only once a tag is selected, D-05) | Untertitel · optional | Subtitle · optional |
| Description field label | Beschreibung · optional | Description · optional |
| Description placeholder | Kurz, was euch erwartet | Briefly, what to expect |
| Location eyebrow | Treffpunkt | Meeting point |
| Location free-text placeholder | Genauer, z. B. Reihe 3 · optional | More specific, e.g. row 3 · optional |
| "Standort anheften" button (before capture) | Standort anheften | Pin location |
| Removable geo chip (after capture) | Standort angeheftet | Location pinned |
| Location permission — pending | *(reuse existing msgid)* "Wird geladen…" | "Loading…" |
| Location permission — denied, callout title | Standort nicht verfügbar | Location unavailable |
| Location permission — denied, callout body | quiks nutzt deinen Standort nur einmalig, um einen Treffpunkt zu setzen — kein Tracking, keine Standortverfolgung. Erlaube den Zugriff in den Einstellungen, oder gib den Ort einfach als Text ein. | quiks uses your location once to pin a meeting point — no tracking, no location watching. Allow access in Settings, or just type the place instead. |
| Location permission — denied, primary action | Einstellungen öffnen | Open Settings |
| "Wann" eyebrow | Wann | When |
| Day-chip validation error (no day selected on submit) | Wähle einen Tag. | Choose a day. |
| Time field validation error (no time selected on submit) | Wähle eine Uhrzeit. | Choose a time. |
| "Kapazität" eyebrow | Kapazität | Capacity |
| Capacity — inactive/default state | Ohne Limit | No limit |
| Capacity — activate stepper (tap target) | Begrenzen | Limit it |
| Capacity — deactivate stepper (tap target, once active) | Limit entfernen | Remove limit |
| Create-form submit | Los, posten | Post it |
| List card — "Gestartet" fact-chip (own activities, past `startTime`) | Gestartet | Started |
| List card — "Dabei" badge | Dabei | In |
| List card — participants line | {n} sind dabei | {n} are in |
| List card — spots hint (hidden entirely when `capacity` is `null`) | {remaining} Plätze frei | {remaining} spots left |
| List card — spots hint, zero left but not yet full-flagged edge case | Letzter Platz | Last spot |
| Detail — seat line, limited | {participantCount}/{capacity} Plätze belegt | {participantCount}/{capacity} spots filled |
| Detail — seat line, unlimited | {participantCount} sind dabei · ohne Limit | {participantCount} are in · no limit |
| Join button (enabled) | Beitreten | Join |
| Join button, disabled — full (D-10, exact reason shown before submit) | Voll — {joined}/{capacity} Plätze | Full — {joined}/{capacity} spots |
| Join button, disabled — already started (D-10) | Bereits gestartet | Already started |
| Join-race fallback (409, D-10) | Der letzte Platz ist gerade weg — die Liste wurde aktualisiert. | The last spot just went — the list has been refreshed. |
| Leave button (joined, non-creator) | Verlassen | Leave |
| Dissolve action (creator only, D-11) | Auflösen | Dissolve |
| Dissolve confirm dialog — title | Aktivität auflösen? | Dissolve this activity? |
| Dissolve confirm dialog — body | Alle {participantCount} Teilnehmenden werden mit entfernt. Das kann nicht rückgängig gemacht werden. | All {participantCount} participants will be removed too. This can't be undone. |
| Dissolve confirm dialog — cancel | *(reuse existing msgid)* "Abbrechen" | "Cancel" |
| Dissolve confirm dialog — confirm (destructive) | Auflösen | Dissolve |
| Clone action (every visible activity, D-12) | Klonen | Clone |
| Route-open action (only when a geo point exists, D-16) | Route öffnen | Open route |
| No participants yet (edge case, only creator seated) | Noch niemand außer dir. | Nobody else yet. |
| Empty description on detail (no description was set) | *(element simply omitted — no placeholder text; same "absent field renders nothing" rule `friend-detail.tsx`'s `identityLine` uses)* | *(same)* |

**Destructive confirmation summary** (template row): Dissolve: "Aktivität auflösen?" / "Alle N
Teilnehmenden werden mit entfernt. Das kann nicht rückgängig gemacht werden." — native
`Alert.alert`, destructive-styled confirm button, exact `friend-detail.tsx` pattern.

---

## Component Inventory

**Reused verbatim, no changes:**

| Component | Used for |
|---|---|
| `PersonRow` | Detail screen's participant list (`profile` is already the exact `visitorProfileForeign` shape `activityParticipantSchema.profile` carries — VIS-02, no second projection) |
| `AvatarTile` | Via `PersonRow`; no direct standalone use |
| `PlaceholderScreen` | NOT used for sectional empties (too heavy — full-screen only); reserved for the unreachable "transport error on first paint" case only if ever needed |
| `CameraScanPanel`'s three-substate permission pattern | Modeled (not imported) for the new location-capture block — see Native Capability Contract |
| Native `Alert.alert` (Phase 6 pattern) | "Auflösen" confirm dialog |
| `useHeaderClearance` / `AppHeader` push state | Both new push screens |
| `SegmentedControl`'s selected-state visual language | Modeled (not imported) for `Chip`'s selected variant |
| `@react-native-community/datetimepicker` (already installed, `app.json` plugin list) | Time picker in the create form — **no new native package needed for time** |

**New components** (owned RN primitives on shared tokens, ADR-022):

| Component | File | Purpose |
|---|---|---|
| `ActivityCard` | `apps/mobile/components/ActivityCard.tsx` | The one shared list-row card for both "Deine Aktivitäten" and "Wer kommt mit?" (D-01) — title (`bodyStrong`) + meta line (`countdown`, day·time) + tag chip + "Gestartet" fact-chip (own-section only) + participants line (`n sind dabei` / spots hint) + "Dabei" badge. `r-card` radius, `surfaceCard` background, `borderSubtle` border — same card shell language as `PersonRow`/`FestivalCard`. Never renders a photo block (D-06 — photo is deferred, this component carries no `hasPhoto` prop at all). |
| `Chip` | `apps/mobile/components/Chip.tsx` | One generic pill primitive reused three times: single-select tag, single-select day, removable geo chip. Props: `label`, `selected?`, `onPress?`, `onRemove?` (renders a trailing `X` when set). `r-pill` radius, `layout.hitMin` height floor. Selected state uses the accent treatment (item 3 above); unselected uses `fillQuiet`/`textSecondary`. |
| `CapacityField` | `apps/mobile/components/CapacityField.tsx` | The "Kapazität" block (D-08): default renders a single row ("Ohne Limit" + "Begrenzen" tap target); tapping swaps to a Max-only stepper (−/+, 36px circles, `mono` value, minimum 1) plus a "Limit entfernen" text-link back to unlimited. No Min stepper exists (D-06 — `minParticipants` deferred). |
| `LocationCaptureBlock` | `apps/mobile/components/LocationCaptureBlock.tsx` | The "Treffpunkt" geo capture (D-13): free-text `Input` (always present) + "Standort anheften" button OR removable `Chip` once captured, modeled on `CameraScanPanel`'s three permission sub-states (`pending`/`granted`/`denied`) — a single `getCurrentPosition()` call per tap, no watcher (ADR-017 §2). |
| `DayTimeField` | `apps/mobile/components/DayTimeField.tsx` | The "Wann" block (D-07): day `Chip` row derived from `useFestivalContext()`'s date range + one `@react-native-community/datetimepicker` time field, composed together. |
| `Input` (generic text field) | `apps/mobile/components/Input.tsx` (new, if not already generalized elsewhere) | Label + text input + optional helper/error line — used for title/subtitle/description/location text fields. Owned primitive, no third-party form library. |

**New screens:**

| Screen | File | Route |
|---|---|---|
| Activity create/clone | `apps/mobile/app/activity-create.tsx` | `/activity-create` (optional `cloneFromId` param) |
| Activity detail | `apps/mobile/app/activity-detail.tsx` | `/activity-detail` (`activityId` param) |
| Activities tab (replaces Phase 9 placeholder) | `apps/mobile/app/(festival)/f/[festivalSlug]/activities.tsx` | unchanged route, new content |

**New `lib/` modules** (pure functions, node-env Vitest testable per house pattern — no RN
component harness exists):

| Module | Purpose |
|---|---|
| `lib/activity-queries.ts` | `activityKeys` factory (mirrors `friendKeys`/`festivalKeys`) |
| `lib/use-activity-mutations.ts` | `useActivityMutations` (mirrors `useFriendMutations` exactly — shared `pendingTargetId`/`failedTargetId`/`failedTargetStatus`, `onSettled` invalidates `activityKeys.all(festivalId)`) |
| `lib/activity-form.ts` | Pure: `canSubmitActivity(form)` (D-05 tag-or-title rule), `resolveJoinability(activity, now)` (D-10's three-way disabled reason), `buildClonePrefill(source)` (D-12/D-15 — drops `startTime` and `geo`, keeps everything else), `resolveTitleOnTagChange(args)` (G-11-3 — writes the selected tag's label into the title field as real, editable text without ever clobbering user-typed text), `resolveSubmittedTitle(title, selectedTag)` (G-11-3 — nulls the submitted title when it still exactly equals the selected tag's label, so the server's per-locale auto-title resolution is preserved) |
| `lib/geo-link.ts` | Pure: `buildRouteUri(geo, platform)` — `geo:{lat},{lng}` (Android) / `https://maps.apple.com/?ll={lat},{lng}` (iOS), per D-14 |

---

## Screens & Navigation Contract

- Both new screens are **root-level Stack siblings** of `(tabs)`, exactly like
  `cashless.tsx`/`friend-detail.tsx`/`friends-qr.tsx` — registered explicitly in
  `app/_layout.tsx`'s authenticated `Stack.Protected` block (an unregistered sibling
  dead-ends on Expo Router's Unmatched Route screen, the same failure class as the resolved
  `first-login-unmatched-route` bug).
- `apps/mobile/lib/app-chrome.ts`'s `PUSH_SCREEN_ROUTES` set and `PushScreenRoute` type gain
  two members: `'activity-create'` and `'activity-detail'`. `AppHeader`'s `pushScreenTitle`
  map gets two new **static** entries (DE/EN per the Copywriting Contract above) — the
  detail screen's title stays the fixed "Aktivität"/"Activity", not the activity's own name;
  the activity's own title renders as an in-content heading below the header (same split
  `friend-detail.tsx` uses: fixed header title, dynamic content heading).
- Both screens use `useHeaderClearance()` + `SafeAreaView edges={['bottom']}`, identical to
  `cashless.tsx`.
- **Clone is a pure UI action, no endpoint** (ADR-017): navigating `/activity-create` with a
  `cloneFromId` param, the create screen reads the source `ActivityDetail` out of the
  `activityKeys.detail(...)` React Query cache — same idiom `findCachedFriend`/
  `findCachedFestivalBySlug` already establish. If the cache entry is missing (cold process
  restart before the detail screen was ever visited), the clone param is silently ignored and
  the form opens empty — never a broken/crashing screen (same "cache miss closes gracefully"
  precedent as `friend-detail.tsx`).
- Navigation after actions (Claude's Discretion in `11-CONTEXT.md`, resolved here):
  - **Create success** → `router.replace({ pathname: '/activity-detail', params: { activityId: created.id } })` (replace, not push — back from the fresh detail screen returns to the tab, not to the empty form).
  - **Join/Leave** → no navigation; the detail screen re-renders in place once the shared mutation hook settles.
  - **Dissolve success** → `router.canGoBack() ? router.back() : router.replace to the festival's Activities tab` — same pattern `friend-detail.tsx`'s unfriend-success navigation uses, silently (no toast — matches unfriend's precedent of no confirmation toast either).
  - **Clone tap (from detail)** → `router.push` (not replace) to `/activity-create?cloneFromId=...` — back returns to the source detail screen.
- Participant list ordering: rendered in `participants` array order (`joinedAt` ascending, per
  the contract) with no extra sort — this naturally puts the creator first (they join in the
  same transaction the activity is created in, `10-CONTEXT.md` D-07) without needing a
  separate "creator" badge or a second sort key.

---

## Query Key & Cache Contract

```ts
// lib/activity-queries.ts — mirrors friendKeys/festivalKeys exactly
export const activityKeys = {
  all: (festivalId: string) => ['activities', festivalId] as const,
  tags: (festivalId: string) => ['activities', festivalId, 'tags'] as const,
  list: (festivalId: string) => ['activities', festivalId, 'list'] as const,
  mine: (festivalId: string) => ['activities', festivalId, 'mine'] as const,
  detail: (festivalId: string, activityId: string) =>
    ['activities', festivalId, 'detail', activityId] as const,
};
```

- `activityKeys.all(festivalId)` is the shared prefix. A single
  `queryClient.invalidateQueries({ queryKey: activityKeys.all(festivalId) })` in every
  mutation's `onSettled` (create, join, leave, dissolve) refreshes tags/list/mine/every open
  detail at once — same one-call invalidation shape `friendKeys.all` already uses.
- **"Feels immediate" contract (Success Criterion 2), reconciled with the phase's own
  online-only rule** (`REQUIREMENTS.md` Out of Scope: "activity create/join and chat send are
  online-only — no offline mutation queue"): this phase does **not** introduce a
  `setQueryData`-based optimistic cache write. It follows the existing `useFriendMutations`
  idiom exactly — `pendingTargetId`/`failedTargetId` local state disables the tapped
  control immediately (opacity `PENDING_OPACITY` pattern) while the mutation is in flight, and
  `onSettled` invalidation repaints the real numbers once the (fast, local-network) response
  lands. If the device is offline, the mutation rejects, the pending state clears, and the
  existing generic "Couldn't save — try again." inline copy applies — no queue, no silent
  retry.
- "Survives an app restart" (Success Criterion 2) means: on cold start, the persisted
  TanStack Query cache paints the last-known seat count/joined-state instantly, and the
  screen's own `useQuery` immediately issues a fresh network fetch that reconciles it — the
  same project-wide offline-first read pattern every other list in the app already follows.
  No new persistence code is needed in this phase.

---

## Native Capability Contract — Standort (D-13)

- **New native dependency: `expo-location`.** This is a native module addition — same class
  of change as `expo-camera` (Phase 8) and `react-native-webview` (Phase 09-06) — and MUST go
  through the manual package-legitimacy gate (publisher, repo, exact package name, dependency
  tree) **before** the install command runs, recorded as a `T-11-SC` entry in the phase's
  security register, exactly like the `T-08-SC` entries for `qrcode-generator`/`expo-camera`.
- One `Location.getCurrentPositionAsync()` call per "Standort anheften" tap — never
  `watchPositionAsync`, never a background task. This is the literal ADR-017 §2 boundary:
  the geo point is a one-off, opt-in capture for route hand-off, explicitly **not** presence
  tracking (contrast with ADR-014's ban on any "who is here" GPS signal).
- Permission flow: three sub-states, modeled 1:1 on `CameraScanPanel.tsx`'s
  `PermissionSubState` (`pending`/`granted`/`denied`) — request exactly once per mount via a
  `useRef` guard, never re-prompt on every render.
  - `pending` → the standard "Wird geladen…" text.
  - `granted` → "Standort anheften" button is live; a tap fires the one-shot position read and
    swaps the button for the removable "Standort angeheftet" chip on success.
  - `denied` → the info-toned callout (`fillInfoQuiet`/`borderInfo`/`infoText`, `r-card`
    radius) exactly matching `CameraScanPanel`'s camera-denied callout structure: icon + title
    + body + "Einstellungen öffnen" (`Linking.openSettings()`) button. **Denial is fully
    graceful — the activity is completely functional without a geo point** (free-text location
    still works, per ACT-05); nothing in the form is blocked by a denied permission.
- Requires a rationale string in `app.json`'s `expo-location` plugin config (same pattern as
  the existing `expo-camera` entry): e.g. *"quiks nutzt deinen Standort einmalig, um beim
  Erstellen einer Aktivität einen Treffpunkt zu setzen — kein Tracking."* Requires a native
  rebuild (`npx expo run:android` from `apps/mobile`, never the repo root) before device
  testing — a human checkpoint, not an autonomous executor step (same as Phase 8's camera
  rebuild).

---

## Route/Maps Handoff Contract (D-14, ACT-05)

- `Linking.openURL` with a platform-native URI — **no new package**, no in-app map/sheet:
  - Android: `geo:{lat},{lng}` (hands off to the device's default maps app or its own app
    chooser).
  - iOS: `https://maps.apple.com/?ll={lat},{lng}`.
- Only the activity's OWN validated `geo` object (from `activityDetailSchema.geo`, already
  `z.number().min/max`-bounded by the contract) is ever interpolated into this URI — **never**
  user-entered free text, and never a value read back out of a WebView or deep link. This is
  the phase's one security-relevant construction point (per `11-CONTEXT.md` Specific Ideas) —
  build the string from the two numeric fields only.
- Visible **only** on the detail screen's meeting-point block, and **only** when `geo` is
  non-null (D-16) — no route affordance anywhere in `ActivityCard`'s list row, no second
  entry point. When `geo` is null, the meeting-point block renders the free-text `location`
  only (or nothing, if that too is empty — never a placeholder claiming there's a location
  when there isn't).

---

## Auto-Title & Clone Prefill Contract

- **The server already resolves the display title** (`activitySchema.title` is always the
  final, resolved string — explicit title if set, else the localized tag title,
  `10-CONTEXT.md`). The create form only *previews* the rule client-side (D-05) — it never
  reconstructs a resolved title for display anywhere else. `canSubmitActivity(form)`
  (`lib/activity-form.ts`) is the one place the "tagId OR non-empty title" rule lives
  client-side, mirroring (never duplicating logic from) the contract's own
  `createActivityBodySchema.refine`.
- Live rule (D-05, amended by G-11-3 — see 11-CONTEXT.md's dated change note): selecting a tag
  writes that tag's label into the title field as a real, editable VALUE (via
  `resolveTitleOnTagChange`) whenever the field is empty or still holds exactly the previously
  selected tag's label — a user-typed title is never overwritten. Selecting a tag also makes the
  title optional and reveals the Subtitle field; deselecting the tag clears an unchanged label
  back out and makes the title required again, but leaves a user-typed title standing. If the
  title still equals the selected tag's label at submit time, it rides as `null` in the request
  body (`resolveSubmittedTitle`) so the server's per-locale auto-title resolution is preserved
  (10-04/ADR-012); once the user edits it, it rides as the explicit, trimmed title. Validation
  surfaces at submit-tap time, not on blur — inline error text below the title field, never a
  toast.
- Clone prefill (D-12/D-15): `buildClonePrefill(source: ActivityDetail)` copies
  `tag`/`title`/`subtitle`/`description`/`location`/`capacity` verbatim and **explicitly
  drops** `startTime` and `geo` — both must be re-entered. This is a deliberate, user-chosen
  deviation from the recommended default (`11-CONTEXT.md` D-15: "gegen die Empfehlung"): the
  cost (whoever clones is usually not standing at the original meeting point right now) was
  named and accepted in exchange for never surfacing a stale pin. Do not "fix" this back to
  carrying the geo point over.
- The create screen carries **no visual indication that it's in clone mode beyond the
  already-filled fields** — no banner, no "Cloned from…" copy. The prefilled state IS the
  communication (`11-CONTEXT.md` phrases it as "Zeit geleert" being sufficient signal); adding
  banner copy is out of scope for this phase.

---

## UI Considerations

Derived by the ui-consideration-probe over the 9 surfaces this phase introduces, then resolved
against the contracts above. Element kinds were confirmed with the user (no `media` surface exists
this phase — the photo block is deferred per D-06; participant avatars ride `PersonRow`, already
covered by Phase 8).

**Coverage: 66 applicable — 53 resolved (explicit) · 6 resolved (backstop) · 7 dismissed · 0 unresolved.**

Empty-state and error-state COPY is not restated here — it lives in `## Copywriting Contract` and is
referenced by row. This section covers shape-rooted STATE coverage.

### E1 — Activities tab (list screen) · list-collection, nav, interactive-control

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | Each section carries its own sectional empty copy (Copywriting rows "Empty — Deine Aktivitäten" / "Empty — Wer kommt mit?") — never `PlaceholderScreen` (full-screen only, too heavy for a sub-section), never a bare gap. The "Aktivität starten" CTA stays visible above both sections in every state. |
| loading | ✅ resolved (explicit) | Reused "Wird geladen…" msgid; no skeleton introduced this phase (matches every other list screen). On cold start the persisted TanStack cache paints last-known content first, so this text is only reached on a genuinely first-ever fetch. |
| error | ✅ resolved (explicit) | Reused transport-error msgid, which itself names the fix. The CTA stays tappable — a failed list read never blocks creating an activity. |
| populated | ✅ resolved (explicit) | Two ordered sections of `ActivityCard` rows, "Deine Aktivitäten" first, "Wer kommt mit?" second (D-01); full field set per Component Inventory. |
| partial | ✅ resolved (explicit) | The two sections resolve independently: `mine` present while `list` is still loading or failed paints the section that has data plus the state copy for the one that doesn't. No all-or-nothing screen state. |
| overflow | ✅ resolved (explicit) | Plain vertical scroll, no pagination (`REQUIREMENTS.md` Out of Scope). `useHeaderClearance()` + `SafeAreaView edges={['bottom']}` keep the first and last row clear of the chrome. |
| zero-one-many | 🧪 resolved (backstop) | "Deine Aktivitäten" at 10+ joined activities is an unbounded scroll with no pagination — acceptable for the expected per-visitor volume, but not device-verified at volume. *verification: backstop* |
| long-text | ✅ resolved (explicit) | Section headings are static catalog strings; every variable-length string on this screen lives inside `ActivityCard` and follows E2's truncation rule. |

### E2 — `ActivityCard` (shared list row) · list-collection, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ⛔ dismissed | A card only exists for an activity that exists. The zero-activity case is E1's sectional empty state, not a card state. |
| loading | ⛔ dismissed | The card has no fetch of its own — it renders from the parent list query. In-flight join/leave is a per-control pending state on E3, not a card-level loading state. |
| error | ⛔ dismissed | Same reason: no own fetch. Transport failure is handled once, at E1. |
| populated | ✅ resolved (explicit) | Title (`bodyStrong`) + meta line (`countdown`, day·time) + tag chip + "Gestartet" fact-chip (own section only) + participants line + "Dabei" badge, on the `r-card`/`surfaceCard`/`borderSubtle` shell shared with `PersonRow`/`FestivalCard`. |
| partial | ✅ resolved (explicit) | `capacity: null` omits the spots-hint half of the participants line entirely (D-03) — never "∞", never a dash. An absent subtitle or tag omits its element outright (`friend-detail.tsx` `identityLine` rule). |
| overflow | ✅ resolved (explicit) | `numberOfLines={1}` truncation on both the title and the free-text location within the card. |
| zero-one-many | ✅ resolved (explicit) | "{n} sind dabei" is a plural-safe ICU pattern (same shape as the existing `{friendCount, plural, …}` msgids). The zero-remaining edge has its own copy row ("Letzter Platz"). |
| long-text | ✅ resolved (explicit) | Covered by the same 1-line truncate. The UGC title is never Lingui-wrapped (ADR-012/020) and never reflows to a second line on the card. |

### E3 — Activity detail screen · list-collection, interactive-control, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | Absent optional fields (subtitle, description, free-text location) are omitted entirely — no dash, no "nicht gesetzt" placeholder. The only-creator-seated case renders "Noch niemand außer dir." rather than a bare empty list. |
| loading | ✅ resolved (explicit) | Reused "Wird geladen…". On cold start the persisted cache paints the last-known seat count and joined-state instantly, then the screen's `useQuery` reconciles — Success Criterion 2's "survives an app restart", no new persistence code. |
| error | ✅ resolved (explicit) | Reused transport-error copy for the detail fetch; reused generic mutation-failure copy for join/leave/dissolve; the 409 join race has its own specific row ("Der letzte Platz ist gerade weg…"). |
| populated | ✅ resolved (explicit) | Content heading (UGC title) + optional subtitle/description + day·time + meeting-point block + exactly one prominent `mono` seat figure + `PersonRow` participant list in `joinedAt` order. |
| partial | ✅ resolved (explicit) | `geo: null` → the meeting-point block renders the free-text location only, with no route affordance (D-16); both absent → the block renders nothing rather than claiming a location that isn't there. `capacity: null` → the unlimited seat-line variant. |
| overflow | ✅ resolved (explicit) | The content heading wraps unrestricted (`title2` heading precedent — `PlaceholderScreen` deliberately never truncates its heading either); the participant list scrolls with the screen. |
| zero-one-many | ✅ resolved (explicit) | Participants render in `participants` array order (`joinedAt` ascending), which puts the creator first by construction — no second sort key, no "creator" badge. The 1-participant case has its dedicated copy row; the seat line is plural-safe. |
| long-text | ✅ resolved (explicit) | A long UGC description wraps freely on the detail screen — no clamp and no "mehr anzeigen" affordance this phase. The free-text location wraps here rather than truncating (the inverse of its card treatment). |

### E4 — Activity create / clone form · form, list-collection, interactive-control, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | The unfilled form is the default state: labels + placeholders per the Copywriting Contract, submit ("Los, posten") disabled until `canSubmitActivity(form)` passes. A clone whose cached source is missing falls back to exactly this empty form — never a broken or crashing screen. |
| loading | ✅ resolved (explicit) | The tag row's fetch shows the reused "Wird geladen…"; on submit the button takes the existing pending treatment (`PENDING_OPACITY`, control disabled) per the `useFriendMutations` idiom. No optimistic cache write — this phase is online-only for mutations (`REQUIREMENTS.md` Out of Scope). |
| error | ✅ resolved (explicit) | Validation errors surface inline below their own field at submit-tap time, never on blur and never as a toast ("Gib einen Titel ein oder wähle ein Tag." / "Wähle einen Tag." / "Wähle eine Uhrzeit."). A failed POST shows the reused generic mutation-failure copy and clears the pending state — entered values are never discarded. |
| populated | ✅ resolved (explicit) | Clone mode prefills `tag`/`title`/`subtitle`/`description`/`location`/`capacity` verbatim and deliberately drops `startTime` + `geo` (D-12/D-15). The prefilled state IS the clone signal — no banner, no "Cloned from…" copy (explicit scope decision, do not "fix" it back). |
| partial | ✅ resolved (explicit) | The tag-or-title rule is the one partial-completeness gate: a selected tag makes the title optional and reveals the Subtitle field; clearing the tag makes the title required again. `canSubmitActivity` in `lib/activity-form.ts` is the single client-side home of that rule, mirroring (never duplicating) the contract's `createActivityBodySchema.refine`. |
| overflow | ✅ resolved (explicit) | The form scrolls; the description textarea grows/scrolls inside its own field rather than pushing the submit button off-screen. |
| zero-one-many | ✅ resolved (explicit) | Delegated — the form's only collections are the day chips (E7) and tag chips (E8), each covered there. |
| long-text | 🧪 resolved (backstop) | The description accepts up to the contract's `max(2000)` with no client-side character counter this phase; the server 400s past it. No ACT requirement demands a counter — held out for UAT to flag if it reads as a trap. *verification: backstop* |

### E5 — `LocationCaptureBlock` (Treffpunkt / geo capture, D-13) · form, interactive-control, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | The default state — no point captured, no text — renders the free-text input with its placeholder plus the "Standort anheften" button. A geo point is never required for a valid activity (ACT-05). |
| loading | ✅ resolved (explicit) | The `pending` permission sub-state shows the standard "Wird geladen…". Permission is requested exactly once per mount via a `useRef` guard (the `CameraScanPanel` pattern) — never re-prompted on every render. |
| error | 🧪 resolved (backstop) | A `getCurrentPositionAsync()` that rejects for a non-permission reason (no fix, timeout, location services off at OS level) is not separately specified: the block must fall back to the un-captured state and leave the free-text input fully usable rather than leaving a dead spinner. Verify on device at UAT. *verification: backstop* |
| partial | ✅ resolved (explicit) | Free-text location without a geo point is a fully valid, fully supported state — it simply yields no route affordance on the detail screen. Denial is completely graceful; nothing in the form is blocked by it. |
| overflow | ✅ resolved (explicit) | The captured-point chip carries the static "Standort angeheftet" label, never raw coordinates, so its width is bounded by catalog copy; the free-text input scrolls horizontally within its own field. |
| long-text | ✅ resolved (explicit) | The denied-permission callout body is a deliberately long no-tracking sentence — it wraps fully inside its `r-card` callout and is never truncated. The ADR-017 §2 reassurance ("kein Tracking, keine Standortverfolgung") is the point of that state; clipping it would defeat it. |

### E6 — `CapacityField` (D-08) · form, list-collection, interactive-control, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | The default is the "Ohne Limit" row plus a "Begrenzen" tap target — the field is never blank and never opens in the stepper state. |
| loading | ⛔ dismissed | Purely local component state; no fetch exists to be in flight. |
| error | ⛔ dismissed | The stepper's floor of 1 makes an invalid value unreachable — there is no error state to render. |
| populated | ✅ resolved (explicit) | Stepper active = −/+ 36px circles around a `mono` value plus a "Limit entfernen" text link back to unlimited. |
| partial | ✅ resolved (explicit) | Only a maximum exists. There is deliberately no minimum-participants stepper (D-06 — `minParticipants` is deferred), so a "max set, min unset" partial state cannot occur. |
| overflow | ✅ resolved (explicit) | Fixed single-row layout in both states; the value is a small integer in `mono` with no growth path. |
| zero-one-many | ✅ resolved (explicit) | The floor is 1, so 0 is unreachable. The `null` case is the "Ohne Limit" default, not a zero value. |
| long-text | ⛔ dismissed | All copy is static catalog strings plus a small integer — no variable-length text passes through this component. |

### E7 — `DayTimeField` (D-07) · form, list-collection, interactive-control

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | ✅ resolved (explicit) | Neither day nor time is preselected; each surfaces its own submit-time inline error. Clone mode deliberately arrives in exactly this empty state (D-15 — `startTime` is dropped on purpose, "Zeit geleert" is the intended signal). |
| loading | ⛔ dismissed | The day range comes from `useFestivalContext()`, already resident in memory — no fetch of its own. |
| error | ✅ resolved (explicit) | The two validation errors ("Wähle einen Tag." / "Wähle eine Uhrzeit."), inline below their own block at submit-tap time. |
| populated | ✅ resolved (explicit) | One selected day chip (single-select) plus a chosen time in the already-installed `@react-native-community/datetimepicker` field — no new native package is needed for time. |
| partial | ✅ resolved (explicit) | Day-without-time and time-without-day are both reachable intermediate states, and each blocks submit with its own specific message rather than one generic one. |
| overflow | ✅ resolved (explicit) | The day chip row wraps onto further lines (no horizontal scroll), so a long festival's chips reflow rather than hide. |
| zero-one-many | 🧪 resolved (backstop) | A single-day festival yields exactly one chip; a multi-week one yields many. The wrap must read correctly at both ends — not device-verified until UAT. *verification: backstop* |
| long-text | ✅ resolved (explicit) | Day chip labels are `Intl`-formatted short dates, bounded by locale — no UGC reaches this component. |

### E8 — Tag chip row (create form) · form, list-collection, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| empty | 🧪 resolved (backstop) | `listActivityTags` may legitimately return `[]` (SEC-03 default). The row must then render as an empty wrap without breaking the form's layout and without inventing "keine Tags" copy — leaving the title-required path (D-05) as the only route. Not device-verified until UAT. *verification: backstop* |
| loading | ✅ resolved (explicit) | Reused "Wird geladen…" while the tags query is in flight; the rest of the form stays interactive throughout. |
| error | ✅ resolved (explicit) | A failed tags fetch never blocks the form: the row is simply absent and the title-required path applies, identical to the zero-tags case. No new error vocabulary is introduced for it. |
| populated | ✅ resolved (explicit) | Single-select `Chip` pills — selected uses the accent treatment, unselected `fillQuiet`/`textSecondary`; `r-pill` radius, `layout.hitMin` height floor. |
| partial | ✅ resolved (explicit) | Selecting a tag is optional by design — the tag-or-title rule (D-05) makes "no tag selected" a fully valid submit state, not an incomplete one. |
| overflow | ✅ resolved (explicit) | Wrapping row, same as the day chips: many tags reflow onto further lines rather than scrolling horizontally out of sight. |
| zero-one-many | ✅ resolved (explicit) | Zero is the backstop row above; one and many share the same wrap layout with no special-casing. |
| long-text | 🧪 resolved (backstop) | Tag labels are localized DB content of unbounded length — a long label must not blow out the pill or clip mid-word. No per-chip truncation rule is specified this phase; verify at UAT against the seeded tag set. *verification: backstop* |

### E9 — Push-screen chrome (`activity-create`, `activity-detail`) · nav, static-content

| Category | Status | Resolution / Reason |
|---|---|---|
| loading | ✅ resolved (explicit) | The header title is a static catalog string, available immediately — it never waits on the activity fetch. That is precisely why the detail header title is the fixed "Aktivität" and the UGC title renders as an in-content heading below it (the `friend-detail.tsx` split). |
| error | ✅ resolved (explicit) | Both routes are registered explicitly in `app/_layout.tsx`'s authenticated `Stack.Protected` block. An unregistered sibling dead-ends on Expo Router's Unmatched Route screen — the same failure class as the resolved `first-login-unmatched-route` bug; explicit registration is the mitigation, and it is a plan-level acceptance item. |
| overflow | ✅ resolved (explicit) | Both header titles are short static strings; the growing UGC title lives below the header where it wraps freely (E3). |
| long-text | ✅ resolved (explicit) | No UGC ever reaches `AppHeader`'s `pushScreenTitle` map — it holds two static DE/EN entries only. |

---

## Registry Safety

Not applicable in the shadcn sense — this is a React Native phase with no component registry.
The equivalent supply-chain gate for this phase is the **native package-legitimacy check**
covered under Native Capability Contract above (`expo-location`, one new dependency, `T-11-SC`
evidence required before install — same manual gate `expo-camera`/`react-native-webview`
already went through).

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not applicable to `apps/mobile` | not required |
| npm (native module) | `expo-location` | manual package-legitimacy gate (publisher/repo/name/dependency-tree), evidence recorded as `T-11-SC` in the phase's `11-SECURITY.md`, **before** `npx expo install expo-location` runs |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: FLAG (non-blocking)
- [x] Dimension 5 Spacing: FLAG (non-blocking)
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED — gsd-ui-checker, 2026-08-15

**Non-blocking recommendations carried into planning:**

- *Typography (D4):* the usage table spans 5 sizes (10.5/13.5/15/19/26) and 4 weights
  (400/500/600/700). Accepted because every one is an existing `typeRoles` entry from the binding
  CI v1.0 token set — no new role is declared. The executor must not add a further role to this
  phase's surfaces.
- *Spacing (D5):* values 2/6/12/18/22/28/36/104 sit off the generic 4-multiple grid, but all resolve
  from the brand ramp in `packages/ui/src/tokens.ts`, which carries an explicit "do not normalize
  toward 4/8/16" warning (ADR-023). Zero new values are introduced. Keep the 36px stepper circle as
  the single off-ramp value and introduce no ad-hoc pixel values.
