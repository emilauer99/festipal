---
phase: 08-friends
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/mobile/app/(tabs)/friends.tsx
  - apps/mobile/locales/en/messages.po
  - apps/mobile/locales/de/messages.po
  - apps/mobile/locales/en/messages.js
  - apps/mobile/locales/de/messages.js
autonomous: true
requirements: [FRND-05]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Ein Visitor sieht auf `(tabs)/friends` beide Anfragerichtungen gleichzeitig in EINER Sektion `Anfragen` mit den beiden beschrifteten Untergruppen `An dich` (eingehend) und `Von dir` (ausgehend); keine der beiden Richtungen liegt hinter einem Umschalter (D-07/FRND-05)."
    - "Annehmen, Ablehnen und Zurueckziehen sind ausfuehrbar und die betroffene Zeile verschwindet danach ohne manuellen Refresh aus ihrer Untergruppe; alle drei kommen aus `useFriendMutations()` in `lib/use-friend-mutations.ts` — derselben EINEN Definition, die auch die Trefferzeile aus 08-01 benutzt (D-04-Konsequenz)."
    - "Die Ueberschrift `Anfragen` traegt ein Zaehler-Badge mit der Anzahl der EINGEHENDEN Anfragen; bei 0 eingehenden existiert gar kein Badge-Element (D-08)."
    - "Die Blockreihenfolge ist fest: Suchfeld, quiks-Code-Karte, Anfragen, Deine Crew. Der Anfragen-Block sortiert sich zur Laufzeit nie nach oben (D-08)."
    - "Der Chats-Block und der Vorschlagsblock der Phase-6-Fassung sind aus `(tabs)/friends.tsx` entfernt — ersatzlos, nicht gedaempft, ohne SoonToast-Nachfolger (D-05/D-06, ADR-020)."
    - "Wird dieselbe Anfrage waehrenddessen auf einem anderen Geraet beantwortet, hinterlaesst der lokale Versuch keine Geisterzeile: jede der drei Mutationen invalidiert `friendKeys.all`, und ein 404 aus `acceptFriendRequest` erscheint als Inline-Fehlerzeile unter der betroffenen Zeile, waehrend die Liste sich neu laedt (Edge FRND-05/concurrency)."
    - "E3 (Anfragen-Sektion) deckt die UI-Considerations 15,16,17,18,19,20,21,22 ab: empty -> je Untergruppe eigene Voraussetzungs-Copy, loading -> projektweites `Loading...` an der einen `listFriendRequests`-Query, error -> woertliche Transport-Fehlercopy + Retry, populated -> eingehend `Annehmen`+`Ablehnen` / ausgehend Chip `Angefragt`+`Zurueckziehen`, partial -> beide Untergruppen sind unabhaengig (0 eingehend + N ausgehend rendert korrekt), overflow -> Rendern in der bestehenden ScrollView, zero-one-many -> Badge bei 0 abwesend, sonst blanke Zahl ohne Plural-Copy, long-text -> `numberOfLines={1}` auf beiden Zeilen der `PersonRow`."
    - "Weder Ablehnen noch Zurueckziehen oeffnen einen Bestaetigungsdialog (Phase-7-D-11/D-12: idempotent, keine Historie, kein Cooldown)."
  artifacts:
    - path: "apps/mobile/app/(tabs)/friends.tsx"
      provides: "Anfragen-Sektion mit beiden Richtungen, Zaehler-Badge, drei Lifecycle-Mutationen; Chats- und Vorschlagsblock entfernt"
      contains: "listFriendRequests"
  key_links:
    - from: "apps/mobile/app/(tabs)/friends.tsx"
      to: "apps/mobile/lib/api-client.ts"
      via: "useQuery mit queryKey friendKeys.requests ruft apiClient.listFriendRequests()"
      pattern: "listFriendRequests"
    - from: "apps/mobile/app/(tabs)/friends.tsx"
      to: "apps/mobile/lib/friend-queries.ts"
      via: "jede der drei Mutationen invalidiert friendKeys.all in onSettled"
      pattern: "friendKeys\\.all"
  prohibitions:
    - requirement_id: FRND-05
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT der Gegenseite sichtbar machen, dass eine Anfrage abgelehnt oder zurueckgezogen wurde — kein `abgelehnt`-Zustand, keine Historienzeile, kein Hinweis, kein Zaehler, der die Differenz verraet. Die API ist bewusst beweisfrei (T-07-15); die UI darf den Beweis nicht wiederherstellen."
    - requirement_id: FRND-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT Druck- oder Dringlichkeitsrahmung fuer offene Anfragen benutzen — kein `wartet seit X Tagen`, kein Countdown, keine Alarmfarbe, keine Formulierung, die das Ignorieren einer Anfrage als Versaeumnis darstellt. Das Badge bleibt eine blanke Zahl."
---

<objective>
Die Anfragen-Sektion wird echt: beide Richtungen in EINER Sektion, mit den drei
Lifecycle-Aktionen aus Phase 7 — und die beiden Bloecke, die nie ein Backing bekommen, werden
ersatzlos entfernt.

Purpose: Phase 6 hat den Screen bewusst „sichtbar tot statt weggelassen" gebaut. Mit echten Daten
kehrt sich diese Haltung um (D-05/D-06): ein Ausblick auf 1:1-Chat, den ADR-020 dauerhaft
ausschliesst und den Phase 07-05 mechanisch verriegelt hat, ist die schlimmste Form des
unehrlichen Leerzustands. Was kein Backing hat und keins bekommt, verschwindet.

Output: Anfragen-Sektion mit `An dich` / `Von dir`, Zaehler-Badge, Annehmen/Ablehnen/Zurueckziehen,
und ein Screen mit vier statt sechs Bloecken.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/08-friends/08-CONTEXT.md
@.planning/workstreams/mobile/phases/08-friends/08-UI-SPEC.md
@.planning/workstreams/mobile/phases/08-friends/08-PATTERNS.md
@.planning/workstreams/mobile/phases/08-friends/08-01-SUMMARY.md
@packages/contracts/src/schemas.ts
</context>

## Artifacts this phase produces

Dieser Plan (08-02) erzeugt keine neue Datei, sondern erweitert `apps/mobile/app/(tabs)/friends.tsx`
um:

| Symbol | Art |
|---|---|
| `listFriendRequests`-Query unter `friendKeys.requests` | Query |
| `RequestsViewState` + `computeRequestsState()` | lokale Ableitung nach dem Screen-Muster |
| Decline- und Withdraw-Mutation (Accept teilt sich die Instanz aus 08-01) | Mutationen |
| Zaehler-Badge an der Ueberschrift `Anfragen` | UI-Element |
| Lingui-msgids: Untergruppen-Beschriftungen, zwei Leerzustands-Texte, `Decline`, `Withdraw`, Mutations-Fehlercopy | Katalogeintraege |

Entfernt werden zwei Bloecke der Phase-6-Fassung samt ihrer `section`-Wrapper und ihrer msgids.
Die uebrigen Symbole der Phase entstehen in 08-01, 08-03, 08-04 und 08-05.

<tasks>

<task type="auto">
  <name>Task 1: Tote Bloecke entfernen und die Anfragen-Sektion mit beiden Untergruppen aufbauen</name>
  <files>apps/mobile/app/(tabs)/friends.tsx</files>
  <read_first>
    - `apps/mobile/app/(tabs)/friends.tsx` — Stand nach 08-01; besonders die `section`/`sectionHead`/`emptyBody`-Styles, der `badge`/`badgeText`-Style und die Bloecke ab dem `Requests`-Abschnitt
    - `.planning/workstreams/mobile/phases/08-friends/08-CONTEXT.md` §D-05, §D-06, §D-07, §D-08
    - `.planning/workstreams/mobile/phases/08-friends/08-UI-SPEC.md` §Requests Section Contract, §Copywriting Contract (Zeilen `Requests — …`), §Color Punkt 5
    - `packages/contracts/src/schemas.ts` — `friendRequestListsSchema`, `friendRequestItemSchema` (kein `relation`, dafuer `requestedAt`)
    - `apps/mobile/components/PersonRow.tsx` — der `trailing`-Slot
    - `apps/mobile/components/SoonToast.tsx` — zum Nachweis, dass der Mechanismus unangetastet bleibt und nur zwei seiner Aufrufer verschwinden
  </read_first>
  <action>
    (1) Entferne die beiden Bloecke, die in dieser Phase ersatzlos wegfallen: den Block mit der
    Ueberschrift `Chats` (D-05, ADR-020 schliesst 1:1-Nachrichten dauerhaft aus) und den
    Vorschlagsblock darunter (D-06, es gibt weder Endpunkt noch Requirement dafuer) — jeweils
    inklusive `section`-Wrapper, Ueberschrift und Leerzustands-Text. Kein `SoonToast`-Ersatz, keine
    gedaempfte Restfassung, kein Kommentar-Torso. `useSoonToast`/`ToastProvider` selbst bleiben
    unveraendert im Projekt; wenn nach dem Entfernen kein Aufrufer mehr in dieser Datei uebrig ist,
    faellt auch der Import hier weg (`pnpm lint` erzwingt das).

    (2) Baue die Anfragen-Sektion. Eine Query: `useQuery` mit `queryKey: friendKeys.requests`,
    `queryFn: () => apiClient.listFriendRequests()`. Zustandsableitung wieder ueber die
    `...ViewState`-Union + `compute...State()`-Schablone des Screens, inklusive der Verzweigung
    „ein non-200 ts-rest-Ergebnis ist ein ERFOLGREICHES React-Query-Ergebnis".

    Struktur: EINE `section` mit der Ueberschrift `Anfragen` (Rolle `title2`, wie die bestehenden
    `sectionHead`), darunter zwei beschriftete Untergruppen — zuerst `An dich` aus
    `body.incoming`, darunter `Von dir` aus `body.outgoing`. Die Untergruppen-Beschriftungen nutzen
    laut UI-SPEC dieselbe Rolle `title2`, aber `colors.textSecondary` statt `textPrimary` — keine
    kleinere Groesse. Kein `SegmentedControl` zwischen den Richtungen (D-07): eine zurueckgezogene
    Anfrage muss auffallen koennen.

    Jede Zeile ist eine `PersonRow` mit `profile={item.profile}`, ohne `onPress` (nur Crew-Zeilen
    sind tappbar, D-09), mit einem aus Lingui gefuehrten `accessibilityLabel` und einem
    `trailing`-Slot, der in diesem Task noch nur die Darstellung traegt: eingehend ein aktiver
    Primaer-Pill plus ein Klartext-Link, ausgehend ein statischer Chip plus ein Klartext-Link. Die
    Verdrahtung der drei Mutationen folgt in Task 2.

    Beide Untergruppen bekommen ihre EIGENE Leerzustands-Copy aus dem UI-SPEC-Copywriting-Contract
    (die Voraussetzung nennen, nicht die Abwesenheit — D-11-Regel aus Phase 6). Eine Untergruppe mit
    Zeilen und eine ohne muessen gleichzeitig korrekt rendern.

    (3) Zaehler-Badge an der Ueberschrift (D-08): Anzahl von `body.incoming`. Bei `0` wird gar kein
    Badge-Element gerendert — keine `0`. Das Badge ist bewusst NICHT der neutrale
    `fillQuiet`/`textMuted`-Badge des Screens, sondern ein solider `colors.primary`-Fill mit
    `colors.textOnPrimary` in der Rolle `micro`, `radiiScale['r-pill']` (UI-SPEC §Color Punkt 5):
    eine echte, handlungsrelevante Zahl liest sich anders als ein dekoratives Platzhalter-Badge.

    (4) Reihenfolge festschreiben (D-08): Suchfeld, quiks-Code-Karte, Anfragen, Deine Crew. Der
    Anfragen-Block darf nie zur Laufzeit umsortiert werden. Der D-03-Modus aus 08-01 bleibt die
    einzige Bedingung, unter der er ueberhaupt nicht rendert.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm exec lingui compile --strict &amp;&amp; ! grep -q 'Chats unlock' "app/(tabs)/friends.tsx" &amp;&amp; ! grep -q 'People you may know' "app/(tabs)/friends.tsx"</automated>
    <human-check>Am Geraet mit einem Account, der eine eingehende und eine ausgehende Anfrage hat: die Sektion `Anfragen` zeigt beide Untergruppen untereinander, die Ueberschrift traegt ein Badge mit `1`. Mit einem Account ohne eingehende Anfragen: kein Badge-Element an der Ueberschrift, und unter `An dich` steht der Voraussetzungstext direkt ueber echten `Von dir`-Zeilen. Auf dem gesamten Screen ist weder ein Chat-Ausblick noch ein Vorschlagsblock sichtbar.</human-check>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile &amp;&amp; pnpm typecheck` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm lint` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm exec lingui compile --strict` endet mit Exit-Code 0.
    - `grep -q 'Chats unlock' "apps/mobile/app/(tabs)/friends.tsx"` findet nichts (Exit-Code 1) — der Block ist entfernt, nicht auskommentiert.
    - `grep -q 'People you may know' "apps/mobile/app/(tabs)/friends.tsx"` findet nichts (Exit-Code 1).
    - `apps/mobile/components/SoonToast.tsx` ist unveraendert (der Mechanismus bleibt fuer andere Screens verfuegbar; nur seine beiden Aufrufer auf diesem Screen fallen weg).
    - Geraetepruefung: bei 0 eingehenden Anfragen existiert an der Ueberschrift kein Badge-Element (nicht: ein Badge mit dem Text `0`).
    - Geraetepruefung: eine Untergruppe mit Zeilen und eine ohne rendern gleichzeitig korrekt, jede mit ihrer eigenen Copy.
  </acceptance_criteria>
  <done>Der Screen hat vier Bloecke statt sechs, und die Anfragen-Sektion zeigt beide Richtungen mit eigenen Leerzustaenden und dem Zaehler-Badge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Annehmen, Ablehnen, Zurueckziehen verdrahten inklusive Fehler- und Rennfall</name>
  <files>apps/mobile/app/(tabs)/friends.tsx, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - `apps/mobile/app/(tabs)/friends.tsx` — Stand nach Task 1
    - `apps/mobile/lib/use-friend-mutations.ts` — die in 08-01 angelegte EINE Definition aller fuenf Mutationen samt `pendingTargetId`/`failedTargetId`
    - `packages/contracts/src/router.ts` Z. 96–120 — `acceptFriendRequest`, `declineFriendRequest`, `withdrawFriendRequest` samt der Doc-Kommentare zu `404` bei accept und zur immer-200-Antwort bei decline/withdraw
    - `.planning/workstreams/mobile/phases/08-friends/08-UI-SPEC.md` §Requests Section Contract, §Copywriting Contract (`Requests — mutation failure`)
    - `apps/mobile/lib/festival-queries.ts` — `unwrapOk`/`ApiResponseError` fuer die Mutations-Rejection
  </read_first>
  <behavior>
    - Tap auf `Annehmen` (eingehend): ruft `apiClient.acceptFriendRequest({ params: { accountId }, body: {} })` ueber die EINE gemeinsame Mutation aus 08-01; nach Erfolg verschwindet die Zeile aus `An dich` und die Person taucht in `Deine Crew` auf (Crew-Block landet in 08-03).
    - Tap auf `Ablehnen` (eingehend): ruft `declineFriendRequest`; die Zeile verschwindet; kein Bestaetigungsdialog.
    - Tap auf `Zurueckziehen` (ausgehend): ruft `withdrawFriendRequest`; die Zeile verschwindet; kein Bestaetigungsdialog.
    - Eine bereits anderswo beantwortete Anfrage: `acceptFriendRequest` antwortet 404; die Inline-Fehlercopy erscheint unter genau dieser Zeile und die Liste laedt neu, sodass die Geisterzeile verschwindet.
    - Waehrend eine Mutation fuer eine Zeile laeuft, sind die Aktionen genau dieser Zeile deaktiviert.
  </behavior>
  <action>
    Verdrahte die drei Aktionen ueber `useFriendMutations()`: `acceptRequest`, `declineRequest` und
    `withdrawRequest`. Es entsteht KEINE neue Mutationsdefinition in diesem Screen — 08-01 hat alle
    fuenf bereits an einer Stelle definiert, inklusive `unwrapOk` und der Invalidierung von
    `friendKeys.all` in `onSettled`. Genau diese `onSettled`-Invalidierung ist die Aufloesung des
    Rennfalls „auf einem anderen Geraet schon beantwortet": auch der Fehlerfall laedt die Listen neu,
    der Server bleibt die Wahrheit und der Client behaelt keine Geisterzeile.

    Fehlerdarstellung: `failedTargetId` aus dem Hook nennt die `accountId`, deren letzte Mutation
    fehlgeschlagen ist; unter genau dieser Zeile rendert die Fehlercopy aus dem
    UI-SPEC-Copywriting-Contract in `colors.dangerText`, Rolle `bodySm`. Kein neues
    Toast-/Snackbar-Bauteil.

    Sperren: `pendingTargetId` aus dem Hook deaktiviert die Aktionen genau der betroffenen Zeile,
    solange deren Mutation laeuft — nicht die ganze Liste.

    Ausdruecklich KEIN `Alert.alert` fuer Ablehnen und Zurueckziehen: Phase-7-D-11/D-12 lassen weder
    Historie noch Cooldown zurueck, eine Rueckfrage wuerde den Einsatz ueberzeichnen. Der einzige
    Bestaetigungsdialog dieser Phase gehoert dem Entfreunden (08-03).

    Neue Strings ueber `cd apps/mobile && pnpm exec lingui extract` ziehen; Quelltext-Literale
    englisch (`sourceLocale: 'en'`), deutschen Katalog mit dem UI-SPEC-Wortlaut fuellen.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm exec lingui compile --strict</automated>
    <human-check>Zwei Geraete/Accounts. (a) B fragt A an; A tippt `Annehmen` — die Zeile verschwindet aus `An dich`, ohne dass A den Screen verlaesst. (b) C fragt A an; A tippt `Ablehnen` — kein Dialog, Zeile weg. (c) A fragt D an; A tippt `Zurueckziehen` — kein Dialog, Zeile weg. (d) Rennfall: B fragt A an; A laesst den Screen offen, B zieht die Anfrage auf seinem Geraet zurueck; A tippt danach `Annehmen`. Erwartet: unter der Zeile erscheint die Fehlercopy und die Liste laedt neu, sodass die Zeile verschwindet — kein Absturz, keine dauerhaft stehende Geisterzeile.</human-check>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile &amp;&amp; pnpm typecheck` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm lint` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm exec lingui compile --strict` endet mit Exit-Code 0.
    - `apps/mobile/app/(tabs)/friends.tsx` ruft keinen der drei Lifecycle-Endpunkte direkt ueber `apiClient` auf; alle drei kommen aus `useFriendMutations()`, und `lib/use-friend-mutations.ts` bleibt die einzige Datei mit diesen `apiClient`-Aufrufen.
    - Alle drei Lifecycle-Mutationen invalidieren `friendKeys.all` in `onSettled` (nicht ausschliesslich in `onSuccess`).
    - Geraetepruefung (d): der Rennfall endet in Fehlercopy + neu geladener Liste, nicht in einer stehenbleibenden Zeile.
    - Geraetepruefung: weder `Ablehnen` noch `Zurueckziehen` oeffnen einen Systemdialog.
  </acceptance_criteria>
  <done>Alle drei Lifecycle-Aktionen funktionieren am Geraet, die Liste stimmt danach ohne manuellen Refresh, und der Rennfall hinterlaesst keine Geisterzeile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anfragenliste -> `POST /me/friend-requests/:accountId/{accept,decline,withdraw}` | ein Tap veraendert eine Beziehung zu einer dritten Person |
| API-Antwort -> Anfragezeile | fremde Profildaten einer Person, mit der noch keine Freundschaft besteht, werden gerendert |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Anfragezeile | medium | mitigate | Die Zeile rendert dieselbe `PersonRow` ueber dieselbe `visitorProfileForeignSchema`-Shape wie Suche und Crew; `friendRequestItemSchema` traegt bewusst kein zusaetzliches Feld. VIS-02 ist die Grundlage (Phase-7 `projection-uniqueness.spec.ts`). |
| T-08-07 | Repudiation | Ablehnen/Zurueckziehen | low | accept | Es entsteht bewusst keine Historie: Phase-7-D-12 loescht die Zeile ohne Statusspalte. Akzeptiert als Produktentscheid — die Abwesenheit eines Nachweises ist hier das Schutzziel, nicht die Luecke (siehe Prohibition). |
| T-08-08 | Elevation of Privilege | `accountId` im Pfad der drei Mutationen | medium | mitigate | `:accountId` benennt in allen drei Routen die Gegenseite, nie den Handelnden; der Handelnde kommt serverseitig aus der Session (T-07-12/T-07-19). Der Client uebergibt ausschliesslich `item.profile.accountId` aus einer gerenderten Zeile. |
| T-08-09 | Denial of Service | Mehrfach-Taps auf Zeilenaktionen | low | mitigate | Die Aktionen der betroffenen Zeile sind waehrend der laufenden Mutation deaktiviert; alle drei Endpunkte sind serverseitig idempotent und antworten beweisfrei. |

Kein Threat dieser Tabelle ist `high` — der Blocking-Schwellwert wird nicht erreicht.
</threat_model>

<verification>
- `cd apps/mobile && pnpm typecheck` — 0
- `cd apps/mobile && pnpm lint` — 0
- `cd apps/mobile && pnpm exec lingui compile --strict` — 0
- `cd apps/mobile && grep -q 'Chats unlock' "app/(tabs)/friends.tsx"` — Exit-Code 1
- `cd apps/mobile && grep -q 'People you may know' "app/(tabs)/friends.tsx"` — Exit-Code 1
- Geraete-UAT laut den beiden `human-check`-Bloecken
</verification>

<success_criteria>
- FRND-05 ist erfuellt: beide Richtungen sichtbar, alle drei Aktionen ausfuehrbar, Liste ohne manuellen Refresh korrekt.
- Der Screen hat vier Bloecke; kein Ausblick auf ein Feature, das es nie geben wird.
- Jeder Leerzustand nennt seine Voraussetzung, auch wenn die jeweils andere Untergruppe Daten hat.
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/08-friends/08-02-SUMMARY.md` when done
</output>
