---
phase: 09-festival-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/home.tsx
  - apps/mobile/app/(tabs)/start.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/components/FloatingNav.tsx
  - apps/mobile/lib/festival-navigation.ts
  - apps/mobile/lib/cold-start-redirect.ts
  - apps/mobile/lib/__tests__/cold-start-redirect.test.ts
  - apps/mobile/lib/__tests__/root-redirect.test.ts
  - apps/mobile/locales/de/messages.po
  - apps/mobile/locales/en/messages.po
  - apps/mobile/locales/de/messages.js
  - apps/mobile/locales/en/messages.js
autonomous: false
requirements: [NAV-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Die erste globale Tab-Route heisst `start`: die Datei ist `apps/mobile/app/(tabs)/start.tsx`, `(tabs)/_layout.tsx` registriert `<Tabs.Screen name=\"start\" />` und setzt `initialRouteName=\"start\"` (D-19/NAV-03)."
    - "`FloatingNav` fuehrt den ersten Tab unter dem Routennamen `start`: Union-Typ, Icon-Tabelle, Typ-Guard und Fallback-Routenname sind alle auf denselben Wert umgestellt, das Haus-Glyph bleibt unveraendert (UI-SPEC § Start Rename Contract)."
    - "Das Label des ersten Tabs ist in beiden Katalogen `Start` — der Quelltext ruft `t` mit dem englischen Quellstring `Start` auf, der DE-Katalog uebersetzt ihn unveraendert mit `Start`, der EN-Katalog ebenfalls mit `Start` (D-20)."
    - "Keine Alias-Route: nach dem Rename existiert in `apps/mobile/app/` genau EINE Datei, die die erste globale Tab-Route bedient, und `apps/mobile/lib` erzeugt keinen Pfad mehr auf die alte Route (D-19)."
    - "`leaveFestival(router)` behaelt seine Nicht-Sackgassen-Logik unveraendert (canGoBack -> back, sonst replace) und zielt im Fallback auf die umbenannte Route (D-02-Mechanismus, nur das Ziel-Literal wandert)."
    - "`resolveColdStartRedirect` und `coldStartRedirectHref` behalten ihre drei Praezedenzstufen (Deep Link > persistierter Festival-Slug > Leerzustand) und liefern fuer den Leerzustand die umbenannte Route; beide Testdateien belegen das ohne Geraet."
    - "`apps/mobile/lib/deep-link.ts` und der Deep-Link-Capture-Block in `app/_layout.tsx` bleiben in diesem Plan unveraendert — `AUTH_FLOW_PATHS`, `isIgnorableDeepLinkRoute` und `reconstructDeepLinkRoute` werden nicht angefasst."
    - statement: "Ein Deep Link und ein Cold Start landen nach dem Rename weiterhin auf einer echten Route und nie auf Expo Routers Unmatched-Route-Screen; belegt am Geraet mit `npx expo start -c` aus `apps/mobile`, nicht im node-env-Runner (ROADMAP-SC-4, Lehre aus `first-login-unmatched-route`)."
      verification: backstop
    - "E9 (Start-Rename) deckt die UI-Considerations 58 und 59 ab: error -> ein Deep Link auf die App darf nach dem Rename nie den Unmatched-Route-Screen zeigen (Geraetepruefung mit `expo start -c`), overflow -> das umbenannte Label landet im selben Label-Slot der `FloatingNav` und erbt deren `numberOfLines`/Tail-Ellipsize unveraendert. Die Zeilen 57 (loading) und 60 (long-text) sind dismissed."
  artifacts:
    - path: "apps/mobile/app/(tabs)/start.tsx"
      provides: "die umbenannte erste globale Tab-Route, Inhalt unveraendert aus `home.tsx`"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Registrierung und Default-Tab unter dem neuen Routennamen"
      contains: "start"
    - path: "apps/mobile/components/FloatingNav.tsx"
      provides: "Routennamen-Union, Icon-Tabelle, Typ-Guard und Label-Record des ersten Tabs unter dem neuen Namen"
    - path: "apps/mobile/lib/cold-start-redirect.ts"
      provides: "Cold-Start-Entscheidung und Href-Mapping auf die umbenannte Route"
      exports: ["ColdStartRedirect", "resolveColdStartRedirect", "coldStartRedirectHref"]
  key_links:
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/app/(tabs)/start.tsx"
      via: "Tabs.Screen name + initialRouteName zeigen auf den Dateinamen"
      pattern: "start"
    - from: "apps/mobile/lib/cold-start-redirect.ts"
      to: "apps/mobile/app/(tabs)/start.tsx"
      via: "coldStartRedirectHref liefert den Pfad, den app/index.tsx per <Redirect> abspielt"
      pattern: "/start"
    - from: "apps/mobile/lib/festival-navigation.ts"
      to: "apps/mobile/app/(tabs)/start.tsx"
      via: "leaveFestival ersetzt den Stack durch den ersten globalen Tab, wenn keine Historie existiert"
      pattern: "/start"
  prohibitions:
    - requirement_id: NAV-03
      category: safety
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT eine Alias-Route auf die alte erste Tab-Route stehen lassen oder neu anlegen. Zwei Dateien, die denselben Screen bedienen, sind genau die Konstellation, aus der in Phase 5 der Unmatched-Route-Bug entstand: Expo Router baut seine Linking-Map statisch und guard-agnostisch, der statische Gewinner kann also ein Screen sein, der unter dem aktiven Guard herausgefiltert ist. Der Rename ist hart (D-19)."
---

<objective>
Der erste globale Tab heisst ueberall `start` — in der Route, im Katalog und in der UI. Das ist der
erste Plan der Phase, weil D-21 es so festlegt: der Fuenf-Tab-Navigator wird gegen die bereits
umbenannte Flaeche gebaut, nicht daneben.

Purpose: NAV-03 ist der kleinste Posten dieser Phase und zugleich der einzige, der den
Deep-Link-Capture-Pfad beruehrt — dieselbe Flaeche, die in Phase 5 vier Debug-Runden gekostet hat.
Er laeuft deshalb allein, vor allem anderen, und wird am Geraet abgenommen, nicht im node-env-Runner.

Output: `app/(tabs)/start.tsx`, angepasste Navigator-Registrierung, `FloatingNav`, die beiden
`lib/`-Module mit ihren Tests und beide Lingui-Kataloge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-CONTEXT.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-UI-SPEC.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-PATTERNS.md
</context>

## Artifacts this phase produces

Dieser Plan (09-01) erzeugt bzw. benennt um:

| Symbol / Pfad | Art |
|---|---|
| `apps/mobile/app/(tabs)/start.tsx` | neue Datei (Umbenennung von `home.tsx`, Inhalt unveraendert) |
| `apps/mobile/app/(tabs)/home.tsx` | geloescht |
| Routenname `start` in `(tabs)/_layout.tsx` (`Tabs.Screen name`, `initialRouteName`) | Registrierung |
| `LiveRouteName`-Mitglied `start`, `LIVE_TAB_ICON`-Schluessel `start`, Fallback `start` in `FloatingNav.tsx` | geaenderte Exporte/Interna |
| `ColdStartRedirect`-Diskriminante `start` in `lib/cold-start-redirect.ts` | geaenderter Typ |
| Lingui-msgid `Start` (ersetzt `Home`) in beiden Katalogen | Katalogeintrag |

<tasks>

<task type="auto">
  <name>Task 1: Route, Navigator, FloatingNav und die beiden lib-Module auf `start` umstellen</name>
  <files>apps/mobile/app/(tabs)/home.tsx, apps/mobile/app/(tabs)/start.tsx, apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/components/FloatingNav.tsx, apps/mobile/lib/festival-navigation.ts, apps/mobile/lib/cold-start-redirect.ts, apps/mobile/lib/__tests__/cold-start-redirect.test.ts, apps/mobile/lib/__tests__/root-redirect.test.ts</files>
  <read_first>
    - `apps/mobile/app/(tabs)/home.tsx` — die umzubenennende Datei im Ganzen; ihr Inhalt bleibt unveraendert
    - `apps/mobile/app/(tabs)/_layout.tsx` — `initialRouteName` und die vier `Tabs.Screen`-Zeilen
    - `apps/mobile/components/FloatingNav.tsx` — Z. 28 (`LiveRouteName`), Z. 37–46 (`LIVE_TAB_ICON`, `isLiveRouteName`), Z. 92–97 (`liveTabLabel`), Z. 125 (Fallback-Routenname)
    - `apps/mobile/lib/festival-navigation.ts` — `leaveFestival` samt Begruendungskommentar
    - `apps/mobile/lib/cold-start-redirect.ts` — `ColdStartRedirect`-Union, `resolveColdStartRedirect`, `coldStartRedirectHref`
    - `apps/mobile/lib/__tests__/cold-start-redirect.test.ts` und `apps/mobile/lib/__tests__/root-redirect.test.ts` — die Tests, die die Literale festhalten
    - `apps/mobile/lib/root-redirect.ts` — liest den Cold-Start-Href durch, ohne ihn selbst zu bilden (nicht zu aendern)
    - `apps/mobile/lib/deep-link.ts` — bewusst NICHT zu aendern; hier steht die Phase-5-Root-Cause-Erklaerung
    - `.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-PATTERNS.md` § `apps/mobile/app/(tabs)/start.tsx`
  </read_first>
  <action>
    (1) Datei umbenennen: `apps/mobile/app/(tabs)/home.tsx` -> `apps/mobile/app/(tabs)/start.tsx`,
    per `git mv`, damit die Historie erhalten bleibt. Der Dateiinhalt bleibt Zeile fuer Zeile
    unveraendert — dieselbe Default-Export-Funktion, dieselben Queries, dieselben Styles. Der
    Funktionsname `HomeScreen` darf bleiben oder zu `StartScreen` werden; wenn er umbenannt wird,
    dann konsistent im ganzen File. Es entsteht KEINE zweite Datei, die dieselbe Route bedient.

    (2) `apps/mobile/app/(tabs)/_layout.tsx`: `initialRouteName` und der erste `Tabs.Screen`
    tragen beide den Routennamen `start`. Die Deklarationsreihenfolge der vier `Tabs.Screen`
    bleibt unveraendert (sie IST die Leistenreihenfolge). Die drei uebrigen Screens
    (`festivals`, `friends`, `mehr`) samt ihrer `headerShown`/`title`-Optionen bleiben in diesem
    Plan unangetastet — die raeumt erst 09-04 mit dem `AppHeader` auf. Der Erklaerkommentar oberhalb
    der Komponente wird auf den neuen Namen nachgezogen.

    (3) `apps/mobile/components/FloatingNav.tsx` — vier Stellen, alle auf denselben Wert:
    das erste Mitglied des `LiveRouteName`-Union-Typs, der erste Schluessel von `LIVE_TAB_ICON`
    (das zugeordnete `Home`-Glyph bleibt unveraendert — nur der Schluessel wird umbenannt), der
    erste Vergleich in `isLiveRouteName`, und der Fallback-Routenname in der `state.routes`-Map.
    Im `liveTabLabel`-Record wird der Schluessel `start` und der Quellstring des `t`-Aufrufs
    `Start` (D-20). `t` bleibt ein Babel-Makro und muss textuell an der Aufrufstelle stehen —
    keine vorbereitete Map ausserhalb des Renders.

    (4) `apps/mobile/lib/festival-navigation.ts`: das Ziel-Literal des `replace`-Zweigs wird
    `/start`. Die `canGoBack()`-Verzweigung bleibt exakt wie sie ist — sie ist der D-02-Mechanismus,
    den 09-04 spaeter in den `AppHeader` verlagert. Der JSDoc-Block ueber der Funktion wird
    auf den neuen Zielnamen nachgezogen.

    (5) `apps/mobile/lib/cold-start-redirect.ts`: die dritte Diskriminante der `ColdStartRedirect`-
    Union heisst `start` statt `home`, `resolveColdStartRedirect` gibt im Leerzustand
    `{ kind: 'start' }` zurueck, und `coldStartRedirectHref` mappt diesen Fall auf `/start`.
    (Das war ein von `09-CONTEXT.md` ausdruecklich an den Planer delegierter Punkt; entschieden
    wird fuer die Mit-Umbenennung, weil D-19 einen harten Rename ohne zweite Benennung derselben
    Sache verlangt.) Die beiden anderen Zweige (`deep-link`, `active-festival`) und die
    Praezedenzreihenfolge bleiben unveraendert.

    (6) Beide Testdateien nachziehen: `lib/__tests__/cold-start-redirect.test.ts` (jede Assertion
    auf die Diskriminante und auf das Href-Literal) und `lib/__tests__/root-redirect.test.ts`
    (der durchgereichte Cold-Start-Href). Die Testfaelle selbst bleiben inhaltlich gleich —
    insbesondere der Regressionstest „frischer Account ohne Deep Link und ohne Slug landet auf
    einer echten Route" bleibt erhalten und traegt weiterhin seinen Begruendungskommentar.

    (7) `apps/mobile/lib/__tests__/qr-payload.test.ts` NICHT anfassen: der dort geprueft
    verworfene Payload ist eine quiks-Code-Nutzlast, keine App-Route — er belegt genau, dass ein
    fremder Code abgewiesen wird, und haengt nicht am Tab-Namen.

    (8) `apps/mobile/lib/deep-link.ts` und der Deep-Link-Capture-Block in `app/_layout.tsx`
    bleiben unveraendert. Der Rename fuegt kein Deep-Link-Ziel hinzu und entfernt keines.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; pnpm exec vitest run &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile &amp;&amp; pnpm exec vitest run` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm typecheck` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm lint` endet mit Exit-Code 0.
    - `apps/mobile/app/(tabs)/start.tsx` existiert und `apps/mobile/app/(tabs)/home.tsx` existiert nicht mehr (`git status` zeigt die Umbenennung als Rename, nicht als Add+Delete).
    - `apps/mobile/app/(tabs)/_layout.tsx` enthaelt `initialRouteName="start"` und `<Tabs.Screen name="start" />`.
    - In `apps/mobile/app/` und `apps/mobile/lib/` (Kommentare ausgenommen) erzeugt kein Modul mehr den Pfad der alten Route: `grep -rn "'/home'" apps/mobile/app apps/mobile/lib --include=*.ts --include=*.tsx | grep -v '^\s*\*'` liefert keine Treffer.
    - `apps/mobile/components/FloatingNav.tsx` fuehrt den ersten Tab unter dem Routennamen `start` in allen vier Stellen (Union, Icon-Tabelle, Typ-Guard, Fallback) und ruft fuer sein Label `t` mit dem Quellstring `Start` auf.
    - `apps/mobile/lib/deep-link.ts` ist in `git diff` dieses Tasks unveraendert.
  </acceptance_criteria>
  <done>Die Route, ihre Registrierung, die Leiste und die beiden reinen Module tragen denselben Namen; die node-env-Suite belegt die Href-Literale.</done>
</task>

<task type="auto">
  <name>Task 2: Lingui-Katalog `Home` -> `Start` und die Geraeteabnahme des Renames</name>
  <reversibility rating="costly">Der Rename selbst ist eine Umbenennung ohne externen Konsumenten (`lib/pending-destination.ts` ist ein In-Memory-Singleton, persistiert wird nur der Festival-Slug, die App erzeugt selbst nie einen Link auf die alte Route — D-19). Teuer ist nicht der Rueckbau des Codes, sondern die Geraeteverifikation, die er erneut kostet: der Deep-Link-Capture-Pfad ist genau die Flaeche, an der Phase 5 vier Debug-Runden verloren hat. Deshalb `costly` und nicht `reversible`.</reversibility>
  <files>apps/mobile/locales/de/messages.po, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.js, apps/mobile/locales/en/messages.js</files>
  <read_first>
    - `apps/mobile/locales/de/messages.po` und `apps/mobile/locales/en/messages.po` — der bestehende Eintrag mit dem msgid des ersten Tabs (DE-Wert steht bereits auf dem Zielwort, EN nicht)
    - `apps/mobile/lingui.config.ts` — `sourceLocale`, Katalogpfade
    - `apps/mobile/components/FloatingNav.tsx` — Stand nach Task 1, die neue Aufrufstelle
    - `.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-UI-SPEC.md` § Start Rename Contract und § Copywriting Contract
    - `.planning/debug/resolved/` § `first-login-unmatched-route` — die Root Cause und warum der node-env-Runner hier nicht genuegt
  </read_first>
  <action>
    (1) `cd apps/mobile && pnpm exec lingui extract` ausfuehren. Weil sich der Quellstring in
    `FloatingNav.tsx` geaendert hat, entsteht ein NEUER msgid; der alte wird von Lingui als obsolet
    markiert.

    (2) Beide Kataloge von Hand nachziehen: der neue Eintrag bekommt im DE-Katalog den msgstr
    `Start` (das war schon der bisherige DE-Wert, er aendert sich fuer den Nutzer nicht) und im
    EN-Katalog ebenfalls `Start` (D-20: englische Nutzer sehen ab jetzt `Start`, weil ein Eintrag
    mit dem msgid `Start` und dem englischen Wert `Home` beim naechsten Lesen wie ein Fehler
    aussaehe). Den obsolet markierten Alt-Eintrag in BEIDEN Katalogen entfernen, damit kein msgid
    stehen bleibt, auf den keine Aufrufstelle mehr zeigt.

    (3) `cd apps/mobile && pnpm exec lingui compile --strict` ausfuehren, damit beide
    `messages.js` neu erzeugt werden. `--strict` schlaegt fehl, wenn eine Uebersetzung leer bleibt —
    das ist hier das Gate.

    (4) Geraeteabnahme vorbereiten: Metro/Expo vorher stoppen (unter Windows laesst ein laufender
    Watcher `pnpm install` und Cache-Operationen scheitern), dann `npx expo start -c` **aus
    `apps/mobile`**, nie aus dem Repo-Root. Der `-c`-Flag leert den Metro-Cache; ohne ihn misst die
    Pruefung eine alte Route-Map und traegt.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; pnpm exec lingui compile --strict &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm exec vitest run</automated>
    <human-check>Am Geraet, nach `npx expo start -c` aus `apps/mobile`:
      1. **Label:** der erste Tab der unteren Leiste heisst `Start`. Geraetesprache auf Englisch stellen und die App neu starten: er heisst weiterhin `Start`, nicht `Home`.
      2. **Cold Start ohne gespeichertes Festival:** App vollstaendig beenden (Force-Quit) und neu starten. Sie landet auf dem ersten Tab und zeigt NIE Expo Routers Unmatched-Route-Screen.
      3. **Cold Start mit gespeichertem Festival:** ein Festival speichern, Force-Quit, neu starten. Die App landet im Festival, nicht auf dem ersten Tab.
      4. **Deep Link:** einen Link auf ein Festival (`quiks://f/&lt;slug&gt;`) aus einer anderen App heraus oeffnen (z. B. aus einer Notiz-App antippen). Er loest weiterhin auf die Festival-Route auf — kein Unmatched-Route-Screen.
      5. **Dev-Client-Start:** die App mehrfach ueber den Dev-Client starten. Der Unmatched-Route-Screen darf bei keinem Start erscheinen (das ist die Phase-5-Regression, gefiltert in `isIgnorableDeepLinkRoute`).
      6. **Nicht-Sackgassen-Rueckweg:** aus dem per Cold Start geoeffneten Festival zurueckgehen — die App landet auf dem ersten Tab und beendet sich nicht.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile &amp;&amp; pnpm exec lingui compile --strict` endet mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm typecheck` und `pnpm lint` enden mit Exit-Code 0.
    - `cd apps/mobile &amp;&amp; pnpm exec vitest run` endet mit Exit-Code 0.
    - Beide `.po`-Kataloge enthalten den Eintrag `msgid "Start"` mit einem nicht-leeren `msgstr` und keinen obsoleten Eintrag fuer das alte msgid mehr.
    - Der EN-Katalog uebersetzt `Start` mit `Start` (nicht mit dem alten Wort) — D-20.
    - Geraetepruefung 1: das Tab-Label lautet in beiden Geraetesprachen `Start`.
    - Geraetepruefung 2, 3 und 6: Cold Start ohne Festival, Cold Start mit Festival und der Rueckweg aus dem Festival landen jeweils auf einer echten Route.
    - Geraetepruefung 4 und 5: weder ein Festival-Deep-Link noch ein Dev-Client-Start zeigt den Unmatched-Route-Screen.
  </acceptance_criteria>
  <done>Der Rename ist in Route, msgid und UI vollzogen und am Geraet abgenommen — die naechsten Plaene bauen gegen die bereits umbenannte Flaeche (D-21).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| externer Deep Link -> Expo-Router-Linking-Map | eine von aussen gesetzte URL entscheidet, welcher Screen mountet |
| Expo-Dev-Client-Launch-URL -> Deep-Link-Capture | Tooling-interne URLs sehen wie App-Routen aus |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Denial of Service | Expo-Router-Linking-Map nach dem Rename | high | mitigate | Harter Rename ohne Alias (D-19): es bleibt genau EINE Datei pro Route, also kann der statische, guard-agnostische Linking-Match keinen Screen waehlen, der unter dem aktiven Guard herausgefiltert ist. Belegt durch die node-env-Tests (Href-Literale) UND die sechs Geraetepruefungen in Task 2 — der node-env-Runner allein ist hier nachweislich nicht ausreichend. |
| T-09-02 | Tampering | Deep-Link-Capture-Block in `app/_layout.tsx` | medium | mitigate | Der Block wird in diesem Plan nicht angefasst; `AUTH_FLOW_PATHS` und `isIgnorableDeepLinkRoute` bleiben unveraendert, also veraendert der Rename weder die Content-Leak-Grenze noch den Dev-Client-Filter. Als Akzeptanzkriterium („`lib/deep-link.ts` ist unveraendert") pruefbar hinterlegt. |
| T-09-03 | Information Disclosure | Lingui-Katalog | low | accept | Der geaenderte Eintrag ist ein Navigationslabel ohne Nutzerdaten. Kein Bezug zu Konto- oder Mandantendaten. |
</threat_model>

## Flagged Assumptions

1. **Die `ColdStartRedirect`-Diskriminante wird mit umbenannt.** `09-CONTEXT.md` § Claude's
   Discretion laesst offen, ob der Union-Member-Name mitwandert. Entschieden wird dafuer: D-19
   verlangt einen harten Rename ohne zweite Benennung derselben Sache, und ein Union-Member, der
   anders heisst als die Route, die er erzeugt, ist genau so eine zweite Benennung. Wird das
   abgelehnt, bleibt allein der Member-Name stehen — das Href-Literal wandert in jedem Fall.

<verification>
- `cd apps/mobile && pnpm exec vitest run` — 0
- `cd apps/mobile && pnpm typecheck` — 0
- `cd apps/mobile && pnpm lint` — 0
- `cd apps/mobile && pnpm exec lingui compile --strict` — 0
- Geraete-UAT nach dem `human-check`-Block von Task 2 (sechs Pruefungen, `expo start -c` aus `apps/mobile`)
</verification>

<success_criteria>
- NAV-03 ist erfuellt: die erste globale Tab-Route heisst `start` in Route, msgid und UI.
- Es gibt keine Alias-Route und keinen zweiten Besitzer desselben Screens.
- Bestehende Deep Links und der Cold Start loesen weiterhin auf echte Routen auf — am Geraet belegt.
- Die Folgeplaene dieser Phase bauen gegen die bereits umbenannte Flaeche (D-21).
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-01-SUMMARY.md` when done
</output>
