---
phase: 09-festival-navigation-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/router.ts
  - apps/api/src/friendship/friendship.service.ts
  - apps/api/src/friendship/friendship.controller.ts
  - apps/api/test/festival-friends-isolation.spec.ts
autonomous: false
requirements: [FRND-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`GET /api/v1/festivals/:festivalId/friends` existiert als Contract-Eintrag in `packages/contracts/src/router.ts` mit `pathParams: z.object({ festivalId: z.string().uuid() })` und `responses: { 200: z.array(friendSchema) }` — dieselbe Antwortform wie `listFriends`, kein neues Schema (D-18, UI-SPEC § Festival Friends Tab Contract)."
    - "Der Endpunkt antwortet ausschliesslich mit Freunden des AUTHENTIFIZIERTEN Besuchers, die genau dieses Festival gespeichert haben — die Schnittmenge `meine Freunde ∩ my_festival(festivalId)`. Es gibt keinen Pfad-, Query- oder Body-Wert, mit dem ein Client nach den Freunden einer anderen Person fragen koennte (SEC-02, T-07-19-Muster)."
    - "Die Abfrage liest die Fremd-View ausschliesslich ueber `foreignProfileColumns` und formt sie ausschliesslich ueber `pickForeignProfile` aus `apps/api/src/friendship/visitor-projection.ts` — es entsteht keine zweite Spaltenliste und kein zweiter Former (VIS-02, `projection-uniqueness.spec.ts` bleibt gruen)."
    - "Weder `festivalId` noch `visitorId` noch ein Zeitstempel aus `my_festival` erscheint im Response-Body; die Antwort traegt pro Eintrag genau `profile` (sechs Fremd-View-Felder) und `friendsSince` (das Datum der Freundschaft, nicht des Speicherns)."
    - "Ein zweiter Besucher, der dieselben Freunde hat, aber ein anderes Festival gespeichert hat, bekommt fuer sein Festival eine leere Liste — und derselbe Aufrufer bekommt fuer ein Festival, das keiner seiner Freunde gespeichert hat, `[]`, nie `null` und nie einen Fehler."
    - "`packages/db` wird NICHT angefasst: `my_festival` und `friendship` existieren beide bereits, es entsteht keine neue Tabelle und keine Migration (09-CONTEXT.md § Integration Points)."
    - "`apps/api/test/festival-friends-isolation.spec.ts` belegt die Isolation am laufenden System auf HTTP-Ebene: gleiche Freunde, zwei Festivals, zwei Aufrufer — jede Sicht enthaelt nur die eigene Schnittmenge, und der Test ist nicht vakuum (mindestens ein Fall liefert eine nicht-leere Liste)."
    - "Diese Phase schreibt genau EINEN Endpunkt. Ein zweiter (Fremdprofil-Detail, separater Zaehl-Endpunkt, News) wuerde D-07, D-18 oder Phase-7-D-04 widersprechen und ist nicht Teil dieses Plans."
  artifacts:
    - path: "packages/contracts/src/router.ts"
      provides: "der Contract-Eintrag des festival-gescopten Freundeslisten-Endpunkts"
      contains: "/festivals/:festivalId/friends"
    - path: "apps/api/src/friendship/friendship.service.ts"
      provides: "die Join-Abfrage ueber visitor_profile x friendship x my_festival, gescoped auf callerId UND festivalId"
    - path: "apps/api/src/friendship/friendship.controller.ts"
      provides: "der ts-rest-Handler, der callerId ausschliesslich aus der Session zieht"
    - path: "apps/api/test/festival-friends-isolation.spec.ts"
      provides: "der SEC-02-Cross-Tenant-Beleg fuer den neuen Join"
  key_links:
    - from: "apps/api/src/friendship/friendship.controller.ts"
      to: "apps/api/src/friendship/friendship.service.ts"
      via: "der Handler reicht session.user.id und params.festivalId weiter, nie einen Client-Wert als Aufrufer"
      pattern: "session.user.id"
    - from: "apps/api/src/friendship/friendship.service.ts"
      to: "apps/api/src/friendship/visitor-projection.ts"
      via: "foreignProfileColumns im select, pickForeignProfile beim Formen"
      pattern: "pickForeignProfile"
    - from: "packages/contracts/src/router.ts"
      to: "apps/api/src/friendship/friendship.controller.ts"
      via: "TsRestHandler bindet den Handler an den Contract-Eintrag"
      pattern: "friendsInFestival"
  prohibitions:
    - requirement_id: FRND-07
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT irgendein Feld ausliefern, aus dem sich ein Aufenthaltsort, eine Anwesenheit, eine Distanz oder ein „gerade am Gelaende\" ableiten liesse — ADR-014 schliesst Praesenz dauerhaft aus. Konkret: der Zeitstempel aus `my_festival` (wann jemand das Festival gespeichert hat) gehoert NICHT in die Antwort, denn er waere ein schwaches Ankunftssignal; ebensowenig `festivalId`, `visitorId`, Koordinaten oder ein Online-/Zuletzt-gesehen-Wert. Die Antwort ist die reine Schnittmenge in der bestehenden `friendSchema`-Form."
---

<objective>
Der eine Endpunkt dieser Phase entsteht: `GET /festivals/:festivalId/friends` liefert die
Fremd-View-Profile derjenigen eigenen Freunde, die dieses Festival gespeichert haben. Contract,
Service, Handler und der SEC-02-Cross-Tenant-Beleg landen zusammen.

Purpose: Das ist die einzige Backend-Flaeche der Phase und zugleich ihre einzige echte
Sicherheitsflaeche. Der Join beruehrt die Mandantengrenze (`my_festival`) UND die Fremd-View
(`visitor_profile`) — beide haben in Phase 7 eigene Invarianten bekommen, und beide muessen hier
gelten, ohne dass eine neue Tabelle entsteht. Der Plan laeuft in Welle 1 parallel zum Rename, weil
er keine einzige Datei mit ihm teilt.

Output: ein Contract-Eintrag, eine Service-Methode, ein Handler und ein HTTP-Level-Isolationstest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-CONTEXT.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-PATTERNS.md
@.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-UI-SPEC.md
</context>

## Artifacts this phase produces

Dieser Plan (09-02) erzeugt:

| Symbol / Pfad | Art |
|---|---|
| Contract-Route-Key `friendsInFestival` (`GET /festivals/:festivalId/friends`) in `packages/contracts/src/router.ts` | neuer Contract-Eintrag |
| `FriendshipService.listFriendsInFestival(callerId, festivalId)` | neue Service-Methode |
| `FriendshipController.friendsInFestival` | neuer ts-rest-Handler |
| `apps/api/test/festival-friends-isolation.spec.ts` | neue Testdatei |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Einbahntuer — Veroeffentlichung des Contract-Eintrags (D-18)</name>
  <decision>Der Contract-Eintrag `GET /festivals/:festivalId/friends` wird veroeffentlicht — eine Einbahntuer.</decision>
  <context>
    `09-CONTEXT.md` D-18 stuft diese Entscheidung selbst als **one-way** ein: sobald der Eintrag in
    `packages/contracts` steht, ist jede spaetere Aenderung an Pfad, Parameter oder Antwortform ein
    Breaking Change am Contract plus Client-Release — dasselbe Muster wie `gender` in Phase 07-01,
    das der User damals mit `confirm-both` bewusst geschlossen hat. Zusaetzlich ist
    `packages/contracts` die **Kollisionszone mit dem `admin`-Workstream**: solange dieser Plan
    laeuft, darf der andere Stream das Paket nicht anfassen.

    Drei Festlegungen haengen daran und sind danach teuer:
    - **Pfadparameter `:festivalId` (UUID), nicht `:slug`.** Die App navigiert per Slug, aber
      `saveFestival` und `listTags` nehmen beide bereits `:festivalId` als UUID — Konsistenz mit dem
      bestehenden Router entscheidet (09-CONTEXT.md § Claude's Discretion). Die App hat die
      `festival.id` nach der Gate-Abfrage ohnehin in der Hand.
    - **Antwortform `z.array(friendSchema)`**, wortgleich wiederverwendet. Kein neues Schema, weil
      `PersonRow` und `sortFriendsByDisplayName` genau diese Struktur bereits konsumieren.
    - **EIN Endpunkt fuer Liste und Zaehler.** Die Dashboard-Kachel liest die Laenge derselben
      Antwort; ein separater Zaehl-Endpunkt koennte eine andere Zahl nennen als die Liste darunter
      (dasselbe Argument wie Phase-7-VIS-02).
  </context>
  <options>
    <option id="publish-as-specified">
      <name>Veroeffentlichen wie in D-18 beschrieben</name>
      <pros>Erfuellt FRND-07 vollstaendig; eine Wahrheit, eine Invalidierung; keine neue Schema-Flaeche; konsistent mit `saveFestival`/`listTags`.</pros>
      <cons>Pfad und Antwortform sind danach nur noch mit Breaking Change aenderbar; `packages/contracts` ist waehrenddessen fuer den `admin`-Stream gesperrt.</cons>
    </option>
    <option id="slug-instead-of-id">
      <name>Pfad auf `:slug` statt `:festivalId` legen</name>
      <pros>Die App navigiert ohnehin per Slug und koennte die Gate-Abfrage sparen.</pros>
      <cons>Bricht mit `saveFestival`/`listTags`, die beide `:festivalId` als UUID fuehren; ein Slug ist aenderbar, eine UUID nicht — ein umbenanntes Festival wuerde Query-Keys und Caches entwerten.</cons>
    </option>
    <option id="defer">
      <name>Endpunkt vertagen, Friends-Tab dieser Phase leer lassen</name>
      <pros>Contract bleibt unangetastet, keine Serialisierung mit `admin` noetig.</pros>
      <cons>FRND-07 faellt aus dieser Phase heraus und ROADMAP-Erfolgskriterium 3 waere nicht erfuellt; der Friends-Tab waere ein vierter Platzhalter statt eines echten Tabs.</cons>
    </option>
  </options>
  <resume-signal>Waehle: publish-as-specified, slug-instead-of-id oder defer. Bestaetige zugleich, dass der `admin`-Stream `packages/contracts` waehrend dieses Plans nicht anfasst.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Contract-Eintrag, Service-Join und ts-rest-Handler</name>
  <precondition>Der `admin`-Workstream aendert `packages/contracts` waehrend dieser Ausfuehrung nicht (geteilte Kollisionszone, `.claude/CLAUDE.md` § Parallel Workstreams).</precondition>
  <precondition>Die lokale Docker-Postgres laeuft und `apps/api/.env` sowie `packages/db/.env` zeigen auf sie (localhost:5432, nicht Neon) — die Spec in Task 2 laeuft gegen die echte Datenbank.</precondition>
  <reversibility rating="one-way">Ein veroeffentlichter Eintrag in `packages/contracts` ist danach nur noch mit Breaking Change plus Client-Release aenderbar (D-18). Das Checkpoint oben ist die Tuer davor.</reversibility>
  <files>packages/contracts/src/router.ts, apps/api/src/friendship/friendship.service.ts, apps/api/src/friendship/friendship.controller.ts</files>
  <read_first>
    - `packages/contracts/src/router.ts` — `listTags` (Z. 44–51, Pfadparameter-Muster `/festivals/:festivalId/...` mit `z.string().uuid()`), `listFriends` (Z. 126–132, Antwortform), `listMyFestivals` (Z. 170–175, die SEC-02-Konvention im `summary`)
    - `packages/contracts/src/schemas.ts` Z. 74–90 und Z. 186–190 — `visitorProfileForeignSchema`, `friendSchema`, `Friend`
    - `apps/api/src/friendship/friendship.service.ts` — `listFriends` samt JSDoc (die Counterpart-aufloesende Join-Bedingung, die hier erweitert wird) und der Import-Kopf
    - `apps/api/src/friendship/visitor-projection.ts` — `foreignProfileColumns`, `pickForeignProfile`, `toIsoString` und die Begruendung, warum es genau diese zwei Orte gibt
    - `apps/api/src/friendship/friendship.controller.ts` — `listFriends` (die beiden parameterlosen Listen-Handler und ihr Kommentar zur Client-Supplied-Scope-Regel)
    - `apps/api/src/festival/festival.controller.ts` — `listTags` und `saveFestival`, das Muster fuer einen Handler MIT Pfadparameter
    - `packages/db/src/schema/my-festival.ts` — Spalten und Primaerschluessel von `my_festival` (`visitorId` ist `text` und FKt auf `visitor_profile.accountId`, `festivalId` ist `uuid`)
    - `apps/api/test/projection-uniqueness.spec.ts` — die Invariante, die eine zweite Projektion sofort rot faerbt
  </read_first>
  <behavior>
    `FriendshipService.listFriendsInFestival(callerId, festivalId)`:
    - Aufrufer hat zwei Freunde, beide haben das Festival gespeichert -> beide Eintraege, aufsteigend nach `username`.
    - Aufrufer hat zwei Freunde, nur einer hat das Festival gespeichert -> genau ein Eintrag.
    - Aufrufer hat Freunde, keiner hat das Festival gespeichert -> `[]`.
    - Aufrufer hat keine Freunde -> `[]`, unabhaengig davon, wie viele fremde Besucher das Festival gespeichert haben.
    - Ein Besucher, der das Festival gespeichert hat, aber NICHT mit dem Aufrufer befreundet ist, erscheint nie.
    - Der Aufrufer selbst erscheint nie in seiner eigenen Liste, auch wenn er das Festival gespeichert hat.
    - Dieselbe Freundschaftszeile wird von beiden Seiten aufgeloest (Symmetrie aus D-14): A sieht B und B sieht A, ohne dass eine Spiegelzeile existiert.
    - Jeder Eintrag traegt genau `profile` (die sechs Fremd-View-Felder) und `friendsSince` als ISO-String.
    - Zwei verschiedene `festivalId`-Werte mit derselben Freundesmenge liefern verschiedene Ergebnisse.
  </behavior>
  <action>
    (1) `packages/contracts/src/router.ts`: neuer Route-Key `friendsInFestival` mit
    `method: 'GET'`, `path: '/festivals/:festivalId/friends'`,
    `pathParams: z.object({ festivalId: z.string().uuid() })` (identisch zu `listTags`) und
    `responses: { 200: z.array(friendSchema) }` (identisch zu `listFriends`). `friendSchema` ist
    bereits importiert. Der `summary` folgt der Haus-Konvention fuer mandantengescopte Endpunkte und
    nennt SEC-02 ausdruecklich: der Aufrufer kommt allein aus der Session, `festivalId` allein aus
    dem Pfad, und die Antwort ist die Schnittmenge — kein Praesenzsignal. Platziere den Eintrag
    neben den anderen `/festivals/...`-Routen, damit die Datei nach Ressourcen gruppiert bleibt.
    Kein neues Schema in `schemas.ts`.

    (2) `apps/api/src/friendship/friendship.service.ts`: neue Methode
    `listFriendsInFestival(callerId: string, festivalId: string): Promise<Friend[]>`. Sie nimmt die
    bestehende `listFriends`-Abfrage als Ausgangspunkt — `select({ ...foreignProfileColumns,
    friendsSince: friendship.createdAt })` von `visitorProfile`, `innerJoin` auf `friendship` mit der
    `or(and(...), and(...))`-Bedingung, die den Aufrufer in der jeweils anderen Paarspalte findet —
    und ergaenzt genau EINEN weiteren `innerJoin` auf `myFestival` mit
    `and(eq(myFestival.visitorId, visitorProfile.accountId), eq(myFestival.festivalId, festivalId))`.
    `orderBy(asc(visitorProfile.username))` bleibt unveraendert, damit die Reihenfolge dieselbe ist
    wie in `listFriends` und der Client sie nicht zweimal lernen muss. Die Rueckgabe formt jede
    Zeile ueber `pickForeignProfile(row)` und `toIsoString(row.friendsSince)`.

    Zwei Dinge sind hier load-bearing und gehoeren als Kommentar an die Methode:
    - Beide Scopes stehen INNERHALB der Join-Bedingung, nicht in einem nachgelagerten Filter:
      `callerId` aus der Session und `festivalId` aus dem Pfad. Es gibt keinen dritten Parameter,
      also gibt es keinen Wert, mit dem ein Client die Sicht einer anderen Person anfordern koennte
      (dasselbe Muster wie `sendRequest`/`unfriend`: „der Aufrufer liefert eine der beiden Haelften").
    - Aus `my_festival` wird KEINE Spalte selektiert. Die Tabelle dient allein als Filter. Ihr
      Speicher-Zeitstempel waere ein schwaches Ankunftssignal und faellt damit unter das
      ADR-014-Verbot (siehe `must_haves.prohibitions`).

    `myFestival` ist aus `@quiks/db` zu importieren (der Import-Kopf fuehrt bereits `friendship`,
    `visitorProfile` und `friendRequest`).

    (3) `apps/api/src/friendship/friendship.controller.ts`: neuer Handler `friendsInFestival` nach
    dem Muster der beiden bestehenden Listen-Handler, aber mit Pfadparameter:
    `@TsRestHandler(contract.friendsInFestival)` und
    `tsRestHandler(contract.friendsInFestival, async ({ params }) => ...)`, der
    `this.friendship.listFriendsInFestival(session.user.id, params.festivalId)` aufruft und
    `{ status: 200, body: friends }` zurueckgibt. Kein `@AllowAnonymous` — der globale AuthGuard
    (SEC-01) bleibt die Tuer. Kein 404-Zweig: eine leere Schnittmenge ist ein normaler Zustand der
    Ressource, und ein 404 fuer eine unbekannte `festivalId` waere zugleich ein
    Festival-Existenz-Orakel ohne Nutzen fuer den Client.

    Der Handler gehoert bewusst in den `FriendshipController` und nicht in den
    `FestivalController`: ts-rest bindet ueber den Contract-Key, nicht ueber ein
    Controller-Praefix, und die Projektionsregel (VIS-02) lebt im `friendship`-Modul. Ein Handler im
    Festival-Modul muesste `FriendshipService` injizieren oder die Spaltenliste erneut aufzaehlen —
    genau das, was `visitor-projection.ts` verhindert. Schreibe diese Begruendung als Kommentar dazu.
  </action>
  <verify>
    <automated>pnpm --filter @quiks/contracts build &amp;&amp; cd apps/api &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm exec vitest run test/projection-uniqueness.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @quiks/contracts build` endet mit Exit-Code 0.
    - `cd apps/api &amp;&amp; pnpm typecheck` und `pnpm lint` enden mit Exit-Code 0.
    - `cd apps/api &amp;&amp; pnpm exec vitest run test/projection-uniqueness.spec.ts` endet mit Exit-Code 0 — die VIS-02-Invariante (genau ein `foreignProfileColumns`, genau ein `pickForeignProfile`) bleibt unverletzt.
    - `packages/contracts/src/router.ts` fuehrt den Pfad `/festivals/:festivalId/friends` mit `pathParams` als UUID und `responses[200]` als Array von `friendSchema`; `packages/contracts/src/schemas.ts` ist in `git diff` unveraendert.
    - `packages/db/` ist in `git diff` dieses Plans unveraendert — es entsteht keine Migration.
    - Die neue Service-Methode selektiert keine Spalte aus `my_festival`: ihr `select`-Objekt besteht aus `...foreignProfileColumns` und `friendsSince`, nichts sonst.
    - Der Handler zieht den Aufrufer ausschliesslich aus `session.user.id`; die Methodensignatur nimmt neben `callerId` genau einen weiteren Parameter (`festivalId`).
  </acceptance_criteria>
  <done>Der Endpunkt existiert vom Contract bis zur Abfrage, ist typgleich mit `listFriends` und liest die Fremd-View durch die eine zugelassene Projektion.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-02-Cross-Tenant-Beleg auf HTTP-Ebene</name>
  <files>apps/api/test/festival-friends-isolation.spec.ts</files>
  <read_first>
    - `apps/api/test/friendship-isolation.spec.ts` — die naechste Vorlage: HTTP-Level-Spec mit OTP-Login, `createTestApp`/`createTestDatabase`, Fixtures inklusive `myFestival`, Aufraeumen in `afterAll`; besonders die Faelle 7, 8 und 9 (Festival-Unabhaengigkeit der Freundschaft)
    - `apps/api/test/festival-isolation.spec.ts` — die SEC-02-Baseline fuer festival-gescopte Reads und ihr Fixture-Aufbau (`festival`, `myFestival`)
    - `apps/api/test/setup.ts` — `createTestApp`, `createTestDatabase`
    - `apps/api/test/account-ids.ts` — `testAccountId`, gemischte Gross-/Kleinschreibung wie better-auth sie vergibt
    - `apps/api/src/friendship/friendship.service.ts` — Stand nach Task 1
    - `.planning/workstreams/mobile/STATE.md` § Blockers/Concerns — „SEC-02 ist eine vererbte Pflicht" und der Testkommando-Fallstrick
  </read_first>
  <action>
    (1) `apps/api/test/festival-friends-isolation.spec.ts` neu anlegen, strukturell nach
    `friendship-isolation.spec.ts`: OTP-Login ueber die Capture-Datei, Cookie-Header, `supertest`
    gegen die laufende Nest-App, Fixtures direkt ueber Drizzle, vollstaendiges Aufraeumen in
    `afterAll` (auch `myFestival`, sonst verschmutzt der Lauf die Dev-Datenbank).

    (2) Fixture: zwei Festivals (`festivalA`, `festivalB`) und drei Besucher — `caller`, `friendA`
    (befreundet mit `caller`), `stranger` (nicht befreundet). `friendA` und `stranger` speichern
    beide `festivalA`; `caller` speichert `festivalA` ebenfalls. Zusaetzlich ein `friendB`,
    befreundet mit `caller`, der nur `festivalB` speichert. Die Freundschaftszeilen werden ueber
    `canonicalPair` in kanonischer Ordnung eingefuegt, damit der CHECK sie annimmt.

    (3) Die Faelle, jeder als eigenes `it`:
    - **Schnittmenge:** `GET /api/v1/festivals/{festivalA.id}/friends` als `caller` liefert genau
      `friendA` — nicht `stranger` (kein Freund) und nicht `friendB` (anderes Festival). Der Fall
      ist damit nicht vakuum: die Liste ist nicht leer.
    - **Cross-Tenant:** derselbe Aufrufer, `festivalB` -> genau `friendB`. Dieselbe Freundesmenge,
      andere `festivalId`, anderes Ergebnis — das ist der eigentliche SEC-02-Beweis.
    - **Fremde Sicht:** `stranger` fragt `festivalA` ab und bekommt `[]` (er hat keine Freunde),
      obwohl `caller` und `friendA` beide dasselbe Festival gespeichert haben. Ein Client kann die
      Schnittmenge eines anderen nicht anfordern.
    - **Leere Schnittmenge:** `caller` fragt ein drittes Festival ab, das keiner seiner Freunde
      gespeichert hat -> Status 200 mit `[]`, nicht 404 und nicht `null`.
    - **Selbstausschluss:** `caller` erscheint nie in seiner eigenen Liste.
    - **Feld-Absenz:** der serialisierte Body wird auf ABWESENHEIT geprueft, nicht nur auf die
      erlaubten Felder (dasselbe Argument wie `foreign-projection.spec.ts`): jeder Eintrag hat genau
      die Schluessel `profile` und `friendsSince`, `profile` genau die sechs Fremd-View-Schluessel,
      und weder Geburtsdatum noch E-Mail noch ein aus `my_festival` stammender Wert (Speicherdatum,
      `festivalId`, `visitorId`, `camp`) taucht irgendwo im Body auf. Diese Assertion ist der
      pruefbare Teil der ADR-014-Prohibition.
    - **Unbekanntes Festival:** eine syntaktisch gueltige, aber nicht existierende UUID liefert 200
      mit `[]` — der Endpunkt ist kein Existenz-Orakel.
    - **Ohne Session:** derselbe Aufruf ohne Cookie liefert 401 (globaler AuthGuard, SEC-01).

    (4) Kommentiere oben in der Datei, warum dieser Test trotz fehlender neuer Tabelle noetig ist:
    SEC-02 ist eine vererbte Pflicht und gilt fuer jeden neuen festival-gescopten Read; hier joint
    die Abfrage zwei bestehende Tabellen ueber die Mandantengrenze, und genau dieser Join ist die
    Stelle, an der eine vergessene Bedingung die Isolation aufheben wuerde.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm exec vitest run test/festival-friends-isolation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api &amp;&amp; pnpm exec vitest run test/festival-friends-isolation.spec.ts` endet mit Exit-Code 0 (das ist das korrekte Kommando — `pnpm --filter @quiks/api test -- &lt;name&gt;` filtert NICHT, siehe STATE.md).
    - `cd apps/api &amp;&amp; pnpm exec vitest run` endet mit Exit-Code 0 (die Gesamtsuite bleibt gruen, insbesondere `projection-uniqueness.spec.ts`, `foreign-projection.spec.ts` und `festival-isolation.spec.ts`).
    - Der Cross-Tenant-Fall ist nicht vakuum: mindestens ein `it` erwartet eine NICHT-leere Liste, und mindestens ein `it` erwartet fuer dieselbe Freundesmenge bei anderer `festivalId` ein anderes Ergebnis.
    - Ein `it` prueft die ABWESENHEIT von Feldern am serialisierten Body, nicht nur die Anwesenheit der erlaubten.
    - Ein `it` belegt 401 ohne Session.
    - `afterAll` loescht die angelegten Zeilen aus `myFestival`, `friendship`, `visitorProfile`, `user` und `festival`.
  </acceptance_criteria>
  <done>Die Isolation des neuen Joins ist am laufenden System belegt, nicht nur behauptet — SEC-02 ist fuer diesen Read gezogen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Client -> `:festivalId`-Pfadparameter | untrusted Eingabe bestimmt den Mandantenscope |
| Session-Cookie -> `session.user.id` | die einzige zugelassene Quelle der Aufruferidentitaet |
| `visitor_profile` -> Response-Body | Owner-Felder duerfen die Fremd-View nie verlassen |
| `my_festival` -> Response-Body | die Mandanten-Beziehungstabelle darf keinen ihrer Werte ausliefern |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `listFriendsInFestival` Join-Bedingung | high | mitigate | Beide Scopes stehen INNERHALB der Join-Bedingung: `callerId` kommt ausschliesslich aus `session.user.id`, `festivalId` ausschliesslich aus dem Pfad. Es existiert kein dritter Parameter, ueber den ein Client eine fremde Sicht anfordern koennte. Belegt durch den Cross-Tenant- und den Fremde-Sicht-Fall in `festival-friends-isolation.spec.ts`. |
| T-09-05 | Information Disclosure | Fremd-View-Projektion | high | mitigate | Der `select` liest ausschliesslich `foreignProfileColumns`, die Formung laeuft ausschliesslich ueber `pickForeignProfile`. Eine zweite Projektion faerbt `projection-uniqueness.spec.ts` rot; die Feld-Absenz-Assertion im neuen Spec belegt zusaetzlich am serialisierten Body, dass Geburtsdatum und E-Mail fehlen. |
| T-09-06 | Information Disclosure | `my_festival`-Zeitstempel als Ankunftssignal | medium | mitigate | Aus `my_festival` wird keine Spalte selektiert — die Tabelle ist reiner Filter. Als Prohibition (ADR-014) und als Feld-Absenz-Assertion im Spec pruefbar hinterlegt. |
| T-09-07 | Spoofing | Enumeration ueber `:festivalId` | low | accept | Ein unbekanntes Festival liefert `[]` statt 404, der Endpunkt ist also kein Existenz-Orakel. Selbst bei geratenen UUIDs enthaelt die Antwort ausschliesslich EIGENE Freunde des Aufrufers — der Informationsgewinn beschraenkt sich auf „welches meiner Festivals hat welcher meiner Freunde gespeichert", und genau das ist die von ADR-014 gewollte Funktion. Speichern ist ohnehin gate-less. |
| T-09-08 | Elevation of Privilege | fehlende Authentifizierung | high | mitigate | Kein `@AllowAnonymous`: der Handler erbt den globalen AuthGuard (SEC-01). Ein `it` im Spec belegt 401 ohne Session. |
</threat_model>

## Flagged Assumptions

1. **Der Handler lebt im `FriendshipController`, nicht im `FestivalController`** — obwohl der Pfad
   mit `/festivals/` beginnt. `09-PATTERNS.md` liess beides offen. Entschieden wird fuer
   `friendship`, weil ts-rest ueber den Contract-Key bindet (das Controller-Praefix ist leer) und
   weil die VIS-02-Projektionsregel dort lebt: ein Handler im Festival-Modul muesste entweder
   `FriendshipService` injizieren oder die Spaltenliste erneut aufzaehlen. Wird das abgelehnt, ist
   die Alternative, `FestivalController` den `FriendshipService` injizieren zu lassen — nicht, die
   Projektion zu duplizieren.

2. **Kein 404-Zweig fuer eine unbekannte `festivalId`.** `getFestival` und `saveFestival` haben
   einen; dieser Endpunkt bekommt bewusst keinen (siehe T-09-07). Wird ein 404 gewuenscht, kostet
   das eine zusaetzliche Existenzabfrage pro Aufruf und macht den Endpunkt zum Existenz-Orakel.

<verification>
- `pnpm --filter @quiks/contracts build` — 0
- `cd apps/api && pnpm typecheck` — 0
- `cd apps/api && pnpm lint` — 0
- `cd apps/api && pnpm exec vitest run` — 0 (Gesamtsuite, inkl. `festival-friends-isolation.spec.ts`)
- `git diff` zeigt keine Aenderung unter `packages/db/`
</verification>

<success_criteria>
- Der eine Endpunkt dieser Phase existiert, ist typgleich mit `listFriends` und liefert die reine Schnittmenge.
- SEC-02 ist fuer den neuen festival-gescopten Read gezogen und nicht vakuum.
- VIS-02 bleibt unverletzt: genau eine Fremd-View-Spaltenliste, genau ein Former.
- Es entsteht keine neue Tabelle und keine Migration; `packages/db` bleibt unangetastet.
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/09-festival-navigation-shell/09-02-SUMMARY.md` when done
</output>
