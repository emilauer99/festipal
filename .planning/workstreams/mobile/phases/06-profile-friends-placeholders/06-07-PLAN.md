---
phase: 06-profile-friends-placeholders
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - apps/mobile/components/AvatarSunsetRing.tsx
  - docs/DEVELOPMENT_DECISIONS.md
  - docs/brand/quiks-ci-v1.md
  - CLAUDE.md
  - apps/mobile/app/profil.tsx
autonomous: true
requirements: [PROF-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Der Profil-Avatar trägt einen kreisförmigen Sunset-Ring, und die zugrunde liegende Marken-Regel ist in BEIDEN maßgeblichen Dokumenten nachgezogen, statt einen Einzelfall auszunehmen (D-07)."
    - "Kein Dokument des Repos formuliert die Sunset-Regel nach der Änderung noch in ihrer alten, engeren Fassung — auch die Kurzfassung in der Wurzel-CLAUDE.md §Brand & Design nicht, die sonst der geänderten ADR-023 widerspräche."
    - "Kopfbereich und Konto-Sektion tragen echte Werte aus GET /me, view-only; Adden-Code-Karte, Socials, Vibe und die drei Stat-Kacheln werden gerendert, aber gedämpft und als kommt-bald markiert (D-02)."
    - "Die Konto-Zeilen Name, Handle und E-Mail behalten ihr Chevron und volle Deckkraft; ein Tap zeigt den gemeinsamen kommt-bald-Hinweis statt einer Bearbeitungsfläche (D-03)."
    - "Die Meta-Zeile lautet '{n} Festivals · {n} Friends · seit {Jahr} dabei'; die Festivals-Zahl kommt aus der bereits gecachten eigenen-Festivals-Abfrage ohne zweiten Netzabruf, die Friends-Zahl ist konstant 0 und das Jahr stammt aus Account.createdAt (D-04)."
    - "Die Identitätszeile zeigt nur die nicht-leeren der drei optionalen Felder in der Reihenfolge Pronomen, Alter, Geschlecht; sind alle leer, wird sie gar nicht gerendert (D-12)."
    - "Das gezeigte Alter ist eine Ableitung aus dem Geburtsdatum und wird nirgends gespeichert (D-12a)."
    - "Der Avatar bleibt gerätelokal und kommentarlos: lokales Foto, sonst Initialen, kein Upload-Hinweis, kein Sicherungshinweis (D-05)."
    - "Der Vibe-Block wird gedämpft gerendert; es besteht keine Spotify-Anbindung (D-06)."
    - "UI-SPEC #9: Ohne Avatar greift die bestehende Initialen-Ableitung; die Identitätszeile entfällt vollständig, wenn alle drei Felder leer sind; die Meta-Zeile rendert immer, notfalls mit Null-Werten."
    - "UI-SPEC #12: Sunset-geringter Avatar, Anzeigename in der Anzeigerolle, Handle in Markenfarbe in der Mono-Rolle, gedämpfte Meta-Zeile — an das Design gebunden."
    - "UI-SPEC #13: Der Kopfbereich ist ein fester vertikaler Stapel ohne eigenen Scrollcontainer."
    - "UI-SPEC #14: Anzeigename und Handle sind einzeilig mit Abschneiden am Ende — app-weite Regel, keine Ausnahme pro Fläche."
    - "UI-SPEC #15: Eine Konto-Zeile ohne Wert rendert Label und Chevron und lässt den Wert-Slot weg — nie ein Platzhalterstrich."
    - "UI-SPEC #18: Konto-Zeilen laufen in Muster B: volle Deckkraft, echter rechtsbündiger Wert, Chevron vorhanden, Tap öffnet den Hinweis."
    - "UI-SPEC #19: Die Zeilen rendern unabhängig voneinander; eine fehlende E-Mail leert nicht die Namenszeile."
    - "UI-SPEC #20: Eine feste Dreiergruppe im Scrollcontainer des Screens, jede Zeile mindestens auf Mindest-Tapgröße."
    - "UI-SPEC #22: Der Wert ist einzeilig mit Abschneiden am Ende; das Label behält Layoutpriorität und der Wert schrumpft zuerst."
    - "UI-SPEC #57: Die Ausblick-Blöcke sind strukturell leer BY DESIGN und tragen Dämpfung plus statisches Bald-Badge, damit die Leere absichtsvoll statt kaputt liest."
    - "UI-SPEC #62: Die Stat-Kacheln bilden eine dreispaltige Reihe, die Vibe-Chips umbrechen, alles innerhalb des Scrollcontainers."
    - statement: "Vibe-Chip-Reihe und Stat-Kachel-Raster verhalten sich beim längsten unterstützten Katalogwert unverändert lesbar und brechen die Spaltenaufteilung nicht."
      verification: backstop
  prohibitions:
    - "Die gedämpften Ausblick-Blöcke — Adden-Code-Karte, Socials, Vibe-Chips und Stat-Kacheln — dürfen nicht als echte, hinterlegte Daten gelesen werden können: keine erfundenen Zahlenwerte, die wie Messwerte wirken, keine Beispiel-Social-Konten und keine Chips, die eine bestehende Musik-Verknüpfung suggerieren."
    - "Der gerätelokale Avatar darf durch keine Copy und kein Symbol den Eindruck erwecken, er sei mit dem Konto gesichert oder auf andere Geräte übertragbar — nach einer Neuinstallation sieht der Visitor nur Initialen, und darüber darf die Oberfläche nicht hinwegtäuschen."
  artifacts:
    - path: "apps/mobile/components/AvatarSunsetRing.tsx"
      provides: "Kreisförmiger Sunset-Gradientring um den bestehenden Avatar-Baustein"
      min_lines: 40
    - path: "docs/DEVELOPMENT_DECISIONS.md"
      provides: "Nachgezogene Sunset-Verwendungsregel in ADR-023, erweitert um Avatar- und Identitätsflächen"
    - path: "docs/brand/quiks-ci-v1.md"
      provides: "Dieselbe Erweiterung an allen Stellen, die die Sunset-Regel formulieren"
    - path: "CLAUDE.md"
      provides: "Nachgezogene Kurzfassung der Sunset-Regel in §Brand & Design, damit die Projektanweisung der geänderten ADR-023 nicht widerspricht"
    - path: "apps/mobile/app/profil.tsx"
      provides: "Vollständiger Profil-Screen mit Kopfbereich, Konto-Sektion und den gedämpften Ausblick-Blöcken"
      min_lines: 220
  key_links:
    - from: "apps/mobile/app/profil.tsx"
      to: "apps/mobile/components/AvatarSunsetRing.tsx"
      via: "der Kopfbereich umschließt den bestehenden Avatar-Baustein mit dem Ring"
      pattern: "AvatarSunsetRing"
    - from: "apps/mobile/app/profil.tsx"
      to: "apps/mobile/lib/profile-meta-line.ts"
      via: "Identitätszeile und Meta-Zeile entstehen aus den reinen Bausteinen"
      pattern: "build(IdentityLine|ProfileMetaLine)"
    - from: "apps/mobile/app/profil.tsx"
      to: "apps/mobile/lib/festival-queries.ts"
      via: "die Festivals-Zahl kommt aus dem bestehenden Cache der eigenen Festivals, nicht aus einem zweiten Abruf"
      pattern: "festivalKeys"
    - from: "apps/mobile/components/AvatarSunsetRing.tsx"
      to: "packages/ui/src/tokens.ts"
      via: "der Ring liest den Sunset-Gradient-Token aus dem Theme"
      pattern: "gradientSunset"
---

<objective>
Der Profil-Screen im vollen Aufbau des Designs, plus die Markenregel, die ihn erst erlaubt.

Umgesetzte Entscheidungen: **D-02** (Identitätskarte echt, Ausblick-Blöcke sichtbar deaktiviert),
**D-03** (Konto-Zeilen bleiben tappbar mit Rückmeldung), **D-04** (vollständige Meta-Zeile),
**D-05** (Avatar bleibt gerätelokal, kommentarlos), **D-06** (Vibe-Block tot),
**D-07** (ADR-023 wird nachgezogen statt umgangen — die Sunset-Regel wird generell um Avatar- und
Identitätsflächen erweitert), Anzeige der in **D-12/D-12a** erfassten Identitätsfelder.

Purpose: Das Design gibt dem Profil-Avatar einen Sunset-Ring, den die heutige Markenregel nur für
Zeichen und Hero erlaubt. Statt einer stillen Einzelfall-Ausnahme wird die Regel geändert — und
zwar in einem ausdrücklichen eigenen Schritt, weil sie sonst beim Komponentenbau nebenbei
vergessen wird und Code und Doku auseinanderlaufen.
Output: Ein neuer Ring-Baustein, drei nachgezogene Dokumente (ADR-023, Markendokument,
Wurzel-`CLAUDE.md`) und ein vollständiger Profil-Screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-CONTEXT.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-PATTERNS.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-02-SUMMARY.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-03-SUMMARY.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-04-SUMMARY.md
</context>

## Artifacts this phase produces

Neue Symbole, die dieser Plan erzeugt (existieren im Repo noch NICHT):

- Datei und Komponente: `apps/mobile/components/AvatarSunsetRing.tsx` / `AvatarSunsetRing`
- Eine erweiterte Formulierung der Sunset-Verwendungsregel in ADR-023, in
  `docs/brand/quiks-ci-v1.md` und in der Kurzfassung der Wurzel-`CLAUDE.md` §„Brand & Design"

<tasks>

<task type="auto">
  <name>Task 1: Sunset-Ring als Baustein und die Markenregel in beiden Dokumenten nachziehen</name>
  <reversibility rating="costly">D-07 ändert eine veröffentlichte Markenregel; ein Rückzieher betrifft jede Fläche, die die Erweiterung inzwischen genutzt hat, und muss in beiden Dokumenten zugleich zurückgenommen werden.</reversibility>
  <read_first>
    - `apps/mobile/components/FestivalCard.tsx` — die bestehende Sunset-Schicht: die Gradient-Konstruktion mit `Defs`, `LinearGradient` und `Stop` aus `react-native-svg`, die Vektor- und Stop-Werte aus dem Token und die Import-Zeile
    - `apps/mobile/components/AvatarTile.tsx` — die Avatar-Hülle, ihre feste Größe und ihre Props
    - `packages/ui/src/tokens.ts` — der Sunset-Gradient-Token mit Vektor und Stops
    - `apps/mobile/lib/__tests__/theme.test.ts` — der bestehende Testblock über die Sunset-Token-Definition: er prüft WERTE, nicht Verwendungsorte, und darf unverändert grün bleiben
    - `docs/DEVELOPMENT_DECISIONS.md` — ADR-023, insbesondere die Regel, dass Sunset der einzige erlaubte Verlauf ist und nur für Zeichen und Hero gilt
    - `docs/brand/quiks-ci-v1.md` — ALLE Stellen, die diese Regel formulieren: die Farbtabelle, die Regel-Aufzählung und die Do-and-Dont-Liste
    - `CLAUDE.md` (Repo-Wurzel) — §„Brand & Design", die Aufzählungszeile, die dieselbe Regel in Kurzfassung wiederholt
    - `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md` — §Avatar Sunset Ring Contract
  </read_first>
  <files>apps/mobile/components/AvatarSunsetRing.tsx, docs/DEVELOPMENT_DECISIONS.md, docs/brand/quiks-ci-v1.md, CLAUDE.md</files>
  <action>
Beide Hälften — Code und Regel — gehören in denselben Schritt. Es gibt für D-07 KEINEN
automatisierten Blocker: der einzige gradientbezogene Test prüft die Token-Definition, nicht ihre
Verwendungsorte. Genau deshalb ist die Doku-Änderung hier ein ausdrücklicher Auftrag und keine
Fußnote.

1. `apps/mobile/components/AvatarSunsetRing.tsx` NEU. Die Gradient-Konstruktion wird strukturell
   aus der bestehenden Sunset-Schicht der Festival-Karte übernommen — dieselbe Token-Quelle,
   dieselben `Defs`/`LinearGradient`/`Stop`-Elemente, dieselben Vektor- und Stop-Werte aus dem
   Theme. Ersetzt wird allein das gefüllte Rechteck durch einen Kreis. Die Komponente umschließt
   den bestehenden Avatar-Baustein und reicht dessen Props unverändert durch:
   - Ringdurchmesser gleich Avatargröße plus acht Pixel, also vier Pixel Versatz auf jeder Seite,
     passend zum Design.
   - Zwischen Ring und Avatar liegt ein drei Pixel breiter Spalt in der App-Hintergrundfarbe. Genau
     dieser Spalt macht die Fläche als RING lesbar statt als Hintergrundwäsche — er ist keine
     Feinheit, sondern der Punkt.
   - Der Ring gilt in beiden Farbmodi unverändert, weil Sunset modusinvariant ist.
   - Der Ring ist rein dekorativ: er fängt keine Tap-Ereignisse ab und trägt keine
     Barrierefreiheits-Rolle. Die Zugänglichkeit des Avatars bleibt beim umschlossenen Baustein.

2. `docs/DEVELOPMENT_DECISIONS.md`, ADR-023: Die Regelformulierung, die Sunset auf Zeichen und
   Hero begrenzt, um Avatar- und Identitätsflächen erweitern. Die Erweiterung gilt generell und
   app-weit, ausdrücklich auch für spätere Freundes-Avatare — sie ist keine Einzelfallausnahme
   für diesen einen Screen. Der Grund der Änderung wird in einem Satz mit Verweis auf Phase 6 und
   D-07 festgehalten, damit die Historie des ADR nachvollziehbar bleibt. Der Rest von ADR-023,
   insbesondere dass Sunset der EINZIGE erlaubte Verlauf bleibt, wird nicht angefasst.

3. `docs/brand/quiks-ci-v1.md`: Dieselbe Erweiterung an JEDER Stelle, die die Regel formuliert.
   Das Dokument nennt sie mehrfach — in der Farbtabelle, in der Regel-Aufzählung und in der
   Do-and-Dont-Liste. Bleibt eine Stelle stehen, widersprechen sich die Absätze desselben
   Dokuments. Alle Fundstellen sind vorher zu erheben und danach gegenzuprüfen.

4. `CLAUDE.md` in der Repo-Wurzel, §„Brand & Design": Dort steht dieselbe Regel als
   Aufzählungszeile in Kurzfassung. Sie zählt heute nur Marken- und Hero-Flächen auf und würde der
   geänderten ADR-023 nach dem Merge widersprechen — die Projektanweisung ist die Fassung, die
   jede spätere Session zuerst liest. Die Aufzählung der zulässigen Flächen in dieser einen Zeile
   um Avatar- und Identitätsflächen erweitern, sonst nichts an der Zeile ändern: Sunset bleibt
   ausdrücklich der EINZIGE erlaubte Verlauf. Kein weiterer Abschnitt der Datei wird angefasst.

5. Bestehende Token- und Gradient-Guard-Tests prüfen: Der Testblock über die Sunset-Definition
   prüft Werte, nicht Verwendungsorte, und muss unverändert grün bleiben. Es existiert heute kein
   Test, der die Verwendung auf Zeichen und Hero einschränkt — es ist also keiner zu lockern. Der
   Befund ist im Summary festzuhalten, damit ein späterer Reviewer nicht nach einem geänderten
   Gate sucht.
  </action>
  <acceptance_criteria>
    - `apps/mobile/components/AvatarSunsetRing.tsx` liest den Gradienten aus dem Theme-Token und definiert keine eigenen Farbwerte — `grep -q 'gradientSunset' apps/mobile/components/AvatarSunsetRing.tsx` exits 0 und `grep -nE '#[0-9a-fA-F]{3,8}' apps/mobile/components/AvatarSunsetRing.tsx` findet nichts
    - Der Ring ist ein Kreis, kein Rechteck — die Datei nutzt das Kreis-Primitiv von `react-native-svg`
    - Der Ring fängt keine Taps ab — `grep -q 'pointerEvents="none"' apps/mobile/components/AvatarSunsetRing.tsx` exits 0
    - In `docs/DEVELOPMENT_DECISIONS.md` nennt ADR-023 Avatar- beziehungsweise Identitätsflächen als zulässige Sunset-Fläche — `grep -niE 'avatar' docs/DEVELOPMENT_DECISIONS.md` liefert mindestens einen Treffer im ADR-023-Abschnitt
    - In `docs/brand/quiks-ci-v1.md` ist KEINE Fundstelle mehr übrig, die Sunset ausschließlich auf Zeichen und Hero begrenzt: die Anzahl der Zeilen, die die Regel formulieren, stimmt mit der Anzahl der Zeilen überein, die Avatar- beziehungsweise Identitätsflächen mitnennen — vor und nach der Änderung erhoben und im Summary dokumentiert
    - Die Kurzfassung in der Wurzel-`CLAUDE.md` widerspricht der geänderten ADR-023 nicht mehr — `[ -z "$(grep -i 'sunset' CLAUDE.md | grep -vi 'avatar')" ]` exits 0: jede Zeile der Datei, die Sunset nennt, nennt auch die Avatar-/Identitätsflächen. (`.claude/CLAUDE.md` bleibt ausdrücklich unangetastet — es behauptet nur die Einzigkeit des Verlaufs und keine Flächenbeschränkung, ist also weiterhin zutreffend.)
    - `pnpm --filter @quiks/mobile test -- theme` exits 0, und der Testblock über die Sunset-Definition ist im Diff unverändert
    - `pnpm --filter @quiks/mobile lint` exits 0, `typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @quiks/mobile lint &amp;&amp; pnpm --filter @quiks/mobile typecheck &amp;&amp; pnpm --filter @quiks/mobile test &amp;&amp; grep -q 'gradientSunset' apps/mobile/components/AvatarSunsetRing.tsx &amp;&amp; grep -niq 'avatar' docs/brand/quiks-ci-v1.md &amp;&amp; [ -z "$(grep -i 'sunset' CLAUDE.md | grep -vi 'avatar')" ]</automated>
  </verify>
  <done>Der Ring-Baustein existiert und liest den Sunset-Token; ADR-023, das Markendokument und die Kurzfassung in der Wurzel-`CLAUDE.md` nennen Avatar- und Identitätsflächen durchgängig als zulässig; kein bestehender Guard-Test wurde gelockert; ein Commit.</done>
</task>

<task type="auto">
  <name>Task 2: Profil-Screen — Kopfbereich, Konto-Sektion und die gedämpften Ausblick-Blöcke</name>
  <read_first>
    - `apps/mobile/app/profil.tsx` — der in `06-01` gebaute Screen mit `/me`-Abfrage, Loading-, Error- und Retry-Zweig
    - `apps/mobile/components/AvatarSunsetRing.tsx` — der Ring aus Task 1
    - `apps/mobile/components/AvatarTile.tsx` — Avatar-Baustein und die Initialen-Ableitung
    - `apps/mobile/lib/avatar-storage.ts` — der gerätelokale Avatar-URI (D-05)
    - `apps/mobile/lib/profile-meta-line.ts` und `apps/mobile/lib/profile-age.ts` — die reinen Bausteine aus `06-03` samt ihrer Signaturen und ihres Nullverhaltens
    - `apps/mobile/lib/festival-queries.ts` — der Query-Key der eigenen Festivals und die Auspack-Hilfe; die Zahl kommt aus diesem Cache, nicht aus einem zweiten Abruf
    - `apps/mobile/components/ListRow.tsx` und `apps/mobile/components/ComingSoonTile.tsx` — Muster B beziehungsweise Muster A
    - `apps/mobile/components/SoonToast.tsx` — `useSoonToast()`
    - `apps/mobile/components/FestivalCard.tsx` — das Auslass-bei-leer-Muster für die optionale Ortsangabe
    - `docs/concept/designs/quiks-v2/quiks-screens.template.html` — Screen `11 Profil`, Zeilen 1773–1819
    - `docs/concept/designs/quiks-v2/11-profil.png` — der gerenderte Screen für Farbe, Gewichtung und Abstände
    - `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md` — §Meta Line Contract, §Placeholder Pattern Contract, §Component Inventory, §Typography
  </read_first>
  <files>apps/mobile/app/profil.tsx</files>
  <action>
Den in `06-01` gebauten Screen auf den vollen Aufbau des Designs erweitern. Der Ladezustand, der
Fehlerzustand und die Retry-Fläche aus dem Tracer bleiben unverändert — sie decken den gesamten
Screen ab, nicht einzelne Zeilen.

1. **Kopfbereich**, in genau dieser Reihenfolge:
   - Avatar in der bestehenden Größe, umschlossen vom Ring aus Task 1. Bild aus dem gerätelokalen
     Speicher, sonst Initialen aus der bestehenden Ableitung. Kein Upload-Hinweis, kein
     Sicherungshinweis, kein Symbol, das eine Serverkopie suggeriert (D-05).
   - Die Identitätszeile aus den drei optionalen Feldern. Sie entsteht über den reinen Baustein
     aus `06-03`: nur die nicht-leeren Werte, feste Reihenfolge Pronomen, Alter, Geschlecht, mit
     einem Mittelpunkt verbunden. Das Alter kommt aus der Altersableitung über das Geburtsdatum
     der Antwort. Liefert der Baustein einen Nullwert, wird die Zeile GAR NICHT gerendert — keine
     reservierte Höhe, kein hängender Trenner, kein Platzhalterstrich.
   - Der Anzeigename in der Rolle `title2`. Weil `title2` eine getrackte Rolle ist, MUSS an
     derselben Stelle auch der Tracking-Wert derselben Rolle gesetzt werden — sonst schlägt der
     Tracking-Guard-Test fehl. Der Name wird nie übersetzt, ist einzeilig und wird am Ende
     abgeschnitten.
   - Der Handle mit vorangestelltem Klammeraffen in der Mono-Rolle `countdown` in Markenfarbe,
     ebenfalls nie übersetzt, einzeilig, am Ende abgeschnitten.
   - Die Meta-Zeile in `bodySm` gedämpft: „{n} Festivals · {n} Friends · seit {Jahr} dabei". Die
     Festivals-Zahl stammt aus dem bereits gecachten Ergebnis der eigenen-Festivals-Abfrage über
     deren bestehenden Query-Key — es wird KEIN zweiter Netzabruf ausgelöst. Die Friends-Zahl ist
     in dieser Phase konstant null, läuft aber trotzdem durch das Lingui-Pluralmakro, weil null
     eine echte Pluralkategorie ist; dasselbe gilt für die Festivals-Zahl. Das Wort „Friends"
     bleibt in beiden Sprachen das unübersetzte Markenwort des Designs, wird aber dennoch über
     Lingui geführt. Das Jahr kommt aus dem Jahresanteil von `createdAt` über den Baustein aus
     `06-03`; eine bloße Jahreszahl braucht keine locale-abhängige Formatierung.
     Sollte das Pluralmakro von Lingui unter diesem Projekt-Build nicht auflösen, ist der
     Importpfad an der offiziellen Lingui-Dokumentation zu prüfen und zu korrigieren — nicht durch
     einen handgebauten Zahl-plus-Wort-String zu ersetzen.
   - Der Kopfbereich ist ein fester vertikaler Stapel ohne eigenen Scrollcontainer.

2. **Konto-Sektion** in Muster B: drei ListRow-Zeilen für Name, Handle und E-Mail mit vollen
   Deckkraftwerten, echten rechtsbündigen Werten und vorhandenem Chevron. Ein Tap öffnet den
   gemeinsamen kommt-bald-Hinweis mit dem Satz, dass Bearbeiten bald kommt — echtes Bearbeiten ist
   ausdrücklich vertagt. Fehlt ein Wert, rendert die Zeile Label und Chevron und lässt den
   Wert-Slot weg statt eines Platzhalterstrichs; die Zeilen rendern unabhängig voneinander. Werte
   sind einzeilig und schrumpfen zuerst, Labels behalten Layoutpriorität. Jede Zeile hält
   mindestens die Mindest-Tapgröße.

3. **Ausblick-Blöcke in Muster A** — gerendert, aber gedämpft auf Deckkraft 0,45 und mit einem
   statischen Bald-Badge versehen, sodass die Leere absichtsvoll statt kaputt liest:
   - die Adden-Code-Karte mit QR-Fläche und dem Knopf zum Teilen des Handles,
   - die Socials-Sektion. Es werden KEINE Beispielkonten gerendert.
   - der Vibe-Block „von Spotify gesynct" (D-06). Es besteht keine Spotify-Anbindung und es werden
     KEINE Beispiel-Chips mit erfundenen Künstlernamen gerendert.
   - die drei Stat-Kacheln als dreispaltige Reihe über den bestehenden Platzhalter-Kachelbaustein,
     dessen Spaltenbreite von zwei auf drei Spalten angepasst wird; sonst unverändert. Die
     Kachelwerte dürfen nicht wie echte Messwerte wirken.
   Ein ganzer deaktivierter Block braucht keinen Tap-Hinweis, weil er sein eigenes Badge trägt;
   die Adden-Code-Karte und der Handle-teilen-Knopf dürfen einen Hinweis öffnen.

4. Screen-Rahmen wie im Rest der App: Scrollcontainer mit Bildschirmrand und unterem
   Scrollabstand, Farben pro Render über `useTheme()`, Styles in einem `useMemo` über
   `createStyles(colors)`, Schriftfamilien über `fontFamilyForRole`, kein numerisches
   Schriftgewicht. Jeder nutzersichtbare String und jedes `accessibilityLabel` läuft über Lingui;
   die Katalogdateien fasst dieser Plan NICHT an.
  </action>
  <acceptance_criteria>
    - Der Kopfbereich nutzt den Ring — `grep -q 'AvatarSunsetRing' apps/mobile/app/profil.tsx` exits 0
    - Identitätszeile und Meta-Zeile entstehen aus den reinen Bausteinen, nicht inline — `grep -q 'buildIdentityLine' apps/mobile/app/profil.tsx` und `grep -q 'deriveAge' apps/mobile/app/profil.tsx` exits 0
    - Die Identitätszeile hängt an einer Wahrheitsprüfung des Bausteinergebnisses und rendert bei einem Nullwert kein Element — im Diff gibt es keinen Fallback-String und keinen Platzhalterstrich für diese Zeile
    - Die Festivals-Zahl kommt aus dem bestehenden Cache — `grep -q 'festivalKeys' apps/mobile/app/profil.tsx` exits 0, und es existiert kein zweiter Abruf der eigenen Festivals in dieser Datei
    - Die Zählangaben laufen durch das Lingui-Pluralmakro — `grep -q 'plural(' apps/mobile/app/profil.tsx` exits 0
    - Wo der Screen die Größe der getrackten Anzeigerolle setzt, setzt er auch deren Tracking — bewiesen durch `pnpm --filter @quiks/mobile test -- type-tracking`
    - Anzeigename und Handle sind einzeilig mit Abschneiden am Ende — `grep -c 'numberOfLines={1}' apps/mobile/app/profil.tsx` ergibt mindestens 2
    - Es gibt keine erfundenen Beispieldaten: der Screen enthält keine Beispiel-Künstlernamen, keine Beispiel-Social-Handles und kein Array mit Platzhalterpersonen
    - Die vier Ausblick-Blöcke tragen jeweils ein Bald-Badge und eine reduzierte Deckkraft
    - Kein rohes Farbliteral, kein numerisches `fontWeight` — `grep -nE '#[0-9a-fA-F]{3,8}|fontWeight: *[0-9]' apps/mobile/app/profil.tsx` findet nichts
    - `pnpm --filter @quiks/mobile lint` exits 0, `typecheck` exits 0, `test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @quiks/mobile lint &amp;&amp; pnpm --filter @quiks/mobile typecheck &amp;&amp; pnpm --filter @quiks/mobile test &amp;&amp; grep -q 'AvatarSunsetRing' apps/mobile/app/profil.tsx &amp;&amp; grep -q 'festivalKeys' apps/mobile/app/profil.tsx &amp;&amp; ! grep -nE '#[0-9a-fA-F]{3,8}|fontWeight: *[0-9]' apps/mobile/app/profil.tsx</automated>
  </verify>
  <done>Kopfbereich und Konto-Sektion tragen echte Werte view-only; die vier Ausblick-Blöcke sind gerendert, gedämpft und badge-markiert; die Meta-Zeile ist vollständig und löst keinen zweiten Netzabruf aus; ein Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session → `GET /api/v1/me` | Der Screen zeigt personenbezogene Daten des eigenen Kontos, darunter das Geburtsdatum als abgeleitetes Alter und das Geschlecht. |
| Gerät → gerätelokaler Avatar-Speicher | Ein lokal gewähltes Foto wird gerendert; es verlässt das Gerät nicht. |
| Screen → Nutzendenerwartung | Vier gedämpfte Blöcke, die vorgeben können, hinterlegte Daten zu zeigen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-28 | Information Disclosure | Anzeige von abgeleitetem Alter und Geschlecht im Profilkopf | high | mitigate | Die Werte stammen ausschließlich aus der sessiongebundenen `/me`-Antwort des EIGENEN Kontos; der Screen ist nur hinter der authentifizierten Stack-Grenze erreichbar und es gibt in dieser Phase keinen Pfad, der ein fremdes Profil rendert. Die fehlende Sichtbarkeits-Policy ist als bewusst getragenes Restrisiko in Plan `06-02` (T-06-06) verzeichnet. |
| T-06-29 | Information Disclosure | Gerätelokaler Avatar | low | accept | Das Foto bleibt im bestehenden MMKV-Speicher aus Phase 4 und wird weder hochgeladen noch synchronisiert (D-05). Restrisiko entspricht dem eines beliebigen lokalen Fotos. |
| T-06-30 | Spoofing | Gedämpfte Ausblick-Blöcke wirken wie hinterlegte Daten | medium | mitigate | Jeder Block trägt Dämpfung plus statisches Bald-Badge und rendert keine erfundenen Werte, Namen oder Konten; die Akzeptanzkriterien prüfen ausdrücklich auf das Fehlen von Beispieldaten. |
| T-06-31 | Repudiation | Konto-Zeilen wirken bearbeitbar | low | mitigate | Ein Tap öffnet ausschließlich den gemeinsamen Hinweis; es existiert kein schreibender Pfad vom Screen aus, und keine Eingabefläche wird gerendert (PROF-01 fordert view-only). |
| T-06-32 | Tampering | Nutzergenerierter Anzeigename oder Handle im Layout | low | mitigate | Beide sind einzeilig mit Abschneiden am Ende; ein überlanger Wert kann das Layout nicht sprengen und keine anderen Elemente verdrängen. Beide werden als reiner Text gerendert, nie als Markup. |
</threat_model>

<verification>
- `pnpm --filter @quiks/mobile lint`, `typecheck` und `test` grün, inklusive des Tracking-Guards
- ADR-023, das Markendokument und die Wurzel-`CLAUDE.md` nennen Avatar- und Identitätsflächen durchgängig
- Der Screen löst genau eine `/me`-Abfrage aus und keinen zweiten Abruf der eigenen Festivals
- Keine erfundenen Beispieldaten in den gedämpften Blöcken
</verification>

<success_criteria>
- Kopfbereich mit Sunset-Ring, Identitätszeile mit sauberem Auslassverhalten, vollständige Meta-Zeile
- Konto-Sektion in Muster B mit echten Werten und Hinweis-Tap statt Bearbeitung
- Vier Ausblick-Blöcke gerendert, gedämpft und ehrlich als kommt-bald gekennzeichnet
- Die Markenregel ist nachgezogen, nicht umgangen
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-07-SUMMARY.md` when done
</output>
