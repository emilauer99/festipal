---
phase: 6
slug: profile-friends-placeholders
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-11
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for Friends, Mehr and Profil (view-only) plus the tab-bar
> reconciliation (D-01). Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **North-star source:** `docs/concept/designs/quiks-v2/quiks-screens.template.html` — `03 Friends`
> (L1435–1491), `04 Mehr` (L1493–1528), `11 Profil` (L1773–1819), `FloatingNav` (L1849). All line
> refs below point into that file. `06-CONTEXT.md` (D-01…D-15) is authoritative for scope; this
> document transcribes it into pixel/token/copy contracts and resolves the "Claude's Discretion"
> items it left open.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — ADR-022 forbids a third-party RN UI kit; every element below is an owned RN-primitive component styled exclusively through `packages/ui/src/tokens.ts` semantic roles (no raw hex/px in component code) |
| Preset | not applicable |
| Component library | none (owned components in `apps/mobile/components/`) |
| Icon library | `lucide-react-native` 0.470, stroke width 2 (2.4 for the active nav icon), CI §5 |
| Font | Outfit (display/brand) · Plus Jakarta Sans (body) · JetBrains Mono (numerics/handles) — `packages/ui/src/tokens.ts` `fontFamilies`, resolved per-weight via `apps/mobile/lib/fonts.ts` `fontFamilyForRole()` (05.1 D-10: never pair a role-resolved family with a numeric `fontWeight`) |

This is not greenfield. Every value below is either an EXISTING token name from `packages/ui/src/tokens.ts` / `apps/mobile/lib/theme.ts`, or is explicitly flagged **NEW TOKEN** with its derivation shown. Do not invent parallel values.

---

## Spacing & Layout Scale

The project's real ported CI ramp (`spacingScale`/`layout`/`radiiScale` in `packages/ui/src/tokens.ts`) — **unchanged this phase**, per the same precedent as Phases 4/5/5.1. Do not round these toward a generic 4/8/16/24/32/48/64 scale; they are the binding brand ramp.

| Token | Value | Usage this phase |
|-------|-------|-------------------|
| `spacingScale['sp-2']` | 4px | icon↔badge gaps |
| `spacingScale['sp-3']` | 6px | tight inline gaps (chat unread dot, badge padding) |
| `spacingScale['sp-4']` | 8px | row internal gaps, ListRow icon↔label gap |
| `spacingScale['sp-5']` | 12px | block-internal stacks (empty-state text blocks, section row gaps) |
| `spacingScale['sp-6']` | 16px | card padding, ListRow group gap, default section-internal gap |
| `spacingScale['sp-8']` | 24px | CTA horizontal padding |
| `layout.screenPad` | 18px | screen horizontal padding (all 3 screens, matches Home/Festivals) |
| `layout.sectionGap` | 28px | gap between Mehr's/Friends'/Profil's top-level sections (`Konto`, `Darstellung`, …) |
| `layout.hitMin` | 44px | minimum tap target — every ListRow, Switch row, toast-dismiss area, disabled row |
| `layout.scrollBottomPad` | 104px | ScrollView bottom padding so the last row clears the floating nav (Friends, Mehr) |
| `radiiScale['r-card']` | 22px | cards (quiks-code card, request/suggestion rows, empty-state cards) |
| `radiiScale['r-md']` | 16px | ListRow group container, Switch row, SafeNow callout, toast |
| `radiiScale['r-pill']` | 999px | badges, avatar, CTA pills, active-tab pill |

Exceptions: none.

---

## Typography

Mandatory summary (per template):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 15px | 400 | 1.45 |
| Label | 13.5px | 700 | 1.3 |
| Heading | 26px | 700 | 1.2 |
| Display | 44px | 800 | 1.06 |

Full role-to-usage map for this phase (existing `typeRoles` from `packages/ui/src/tokens.ts` unless flagged **NEW**):

| Design element | Design line(s) | `typeRoles` key | Size/Weight/Family | Note |
|---|---|---|---|---|
| Section eyebrows ("Konto", "Darstellung", "Standort & Sicherheit", "App") | 1495, 1500, 1503, 1509, 1521 | `micro` | 10.5/700 caps, body font | uppercase via `textTransform`, matches `.fp-eyebrow`; identical usage to `home.tsx`'s `eyebrow` style |
| Block headings ("Anfragen", "Chats", "Deine Crew", "Vielleicht kennst du") | 1443, 1455, 1470, 1478 | `title2` | 26/700, display font | matches `home.tsx` `sectionHead` |
| Row/card primary label (ListRow label, "Dein quiks-Code" title, chat name, request name) | 1439, 1448, 1461, 1497 | `bodyStrong` | 15/600, body font | |
| Row/card secondary text (ListRow value, descriptions, chat preview) | 1439, 1448, 1462 | `bodySm` | 13.5/400, body font | |
| Section body copy ("Freunde deiner Freunde…", SafeNow body) | 1479, 1516 | `bodySm` | 13.5/400, body font | |
| Handle / mono identifiers (`@feli.quiks`) | 1439, 1779 | `countdown` **(reused)** | 13.5/500, mono font | reuses the existing 13.5/500-mono role verbatim instead of adding a new one for a single extra usage — the role's *name* is historical, its *shape* is an exact match |
| Profile header display name ("Feli Auer") | 1778 | `title1` **NEW TOKEN** | 24/800, display font, lineHeight 1.1, letterSpacing −0.96 (24 × −4%, Outfit tracking rule) | Gap between `title2` (26/700) and `display2` (44/800) that no existing role covers; single, deliberate addition mirroring how `title3`/`bodyStrong`/`bodySm`/`otpDigit`/`countdown` were added in 05.1-03 for the same reason. **Additive, mobile-only `packages/ui/src/tokens.ts` change — sequence with the admin stream per `CLAUDE.md` §Parallel Workstreams before landing.** If the planner judges a new token not worth the coordination cost for one usage, the sanctioned fallback is `title2` unchanged (26/700) — flag the substitution in the plan, don't silently pick either. |
| Meta line ("3 Festivals · 6 Friends · seit 2025 dabei") | 1781 | `bodySm` | 13.5/400, body font, `textMuted` | matches `--text-body-sm` in the design 1:1 |
| Toast / "kommt bald" text | n/a (new component) | `bodySm` | 13.5/400, body font | |
| Alert.alert (native, logout confirm) | n/a | native OS text | — | not themeable — copy only, see Copywriting Contract |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `colors.bgApp` (Papier `#F7F5F2` light / Ink `#0C0E13` dark) | Screen background — Friends, Mehr, Profil |
| Secondary (30%) | `colors.surfaceCard` (`#FFFFFF` light / 6% white dark) | Cards (quiks-code, request/chat/suggestion rows), ListRow group containers, Switch rows |
| Accent (10%) | `colors.primary` (Beere `#E8559F`, mode-invariant) + `colors.gradientSunset` (mode-invariant) | see reserved-for list below |
| Destructive | `colors.dangerText` (text/icon/border only — never the bare `colors.danger` fill as text, per the existing status-hue rule in `tokens.ts`) | "Abmelden" row label + icon; logout confirm is native `Alert.alert` (no color contract) |

**Accent reserved for** (explicit — never "all interactive elements"):

1. `FloatingNav` active tab pill background (`fillBrandQuiet`) + active icon/label (`primary`) — unchanged from Phase 5.1.
2. `AvatarTile` border + initials text (`primary`) — existing component, reused verbatim in the Profil header.
3. **New:** the Sunset gradient ring behind the Profil header avatar (`colors.gradientSunset`, D-07 — see § Avatar Sunset Ring Contract below).
4. Handle/@username mono text wherever it appears (Profil header, quiks-code card) — reuses the established "link/brand text" precedent already shipped in `home.tsx`'s `seeAll` style (`color: colors.primary` on a `label`-sized text run).
5. Dark-Mode `Switch` track/thumb fill when the switch is ON (Mehr → Darstellung) — the only functionally "live" accent-colored control this phase.
6. Chat unread dot (only if the Chats section ever renders a non-empty row again after FRND-02 — not reachable this phase since Chats always renders its empty state).

Everything else — icons, section headings, body text, disabled rows, the quiks-code card's static QR placeholder — resolves through neutral roles (`textPrimary`/`textSecondary`/`textMuted`/`fillQuiet`/`borderSubtle`).

**New tokens required** (mirrors the existing `fillDangerQuiet`/`fillDangerSubtle` pattern — status fills are mode-invariant per `tokens.ts`'s own convention):

| Token | Value | Derivation | Usage |
|---|---|---|---|
| `fillInfoQuiet` **NEW** | `rgba(95,180,255,.12)` | `info` (#5FB4FF) at 12%, matches the design's SafeNow card background literally (L1512) | SafeNow callout background |
| `borderInfo` **NEW** | `rgba(95,180,255,.35)` | `info` at 35%, matches the design's SafeNow border literally | SafeNow callout border |

SafeNow's icon and heading text resolve through **`colors.infoText`**, not the bare `info` fill — same existing rule as `danger`/`dangerText` (status hue used as text/icon/border always takes the `*Text` variant; `info` alone measures 2.04:1 on Papier and fails AA).

Both new tokens are **additive, mode-invariant, mobile-only** `packages/ui/src/tokens.ts` changes — same collision-zone sequencing note as `title1` above.

---

## Copywriting Contract

Design copy (D-14) is **direction, not wording** — the German tone below may be adjusted for line-wrap/length at implementation time; English is a sense-for-sense translation, not a literal one. `username`/`displayName` are never translated (ADR-012/020).

| Element | Copy |
|---------|------|
| Primary CTA (Mehr → Profil) | DE "Profil" / EN "Profile" — `ListRow` label, navigates to the push screen |
| Primary CTA (Friends → quiks-code) | DE "QR zeigen" / EN "Show QR" — dampened, tap opens the "kommt bald" toast (D-11, FRND-02 deferred) |
| Empty state — Friends "Anfragen" | DE "Noch keine Anfragen. Sobald dich jemand addet, taucht es hier auf." / EN "No requests yet. Once someone adds you, it'll show up here." |
| Empty state — Friends "Chats" | DE "Chats gibt's, sobald ihr euch addet und schreiben könnt — kommt bald." / EN "Chats unlock once you've added someone and can message them — coming soon." (deliberately names the *precondition*, not just "no messages", per D-11's own risk note: this section must not read as a working inbox with zero threads) |
| Empty state — Friends "Deine Crew" | DE "Noch niemand in deiner Crew. Sobald ihr euch addet, seht ihr euch hier." / EN "No one in your crew yet. Once you've added each other, you'll show up here." |
| Empty state — Friends "Vielleicht kennst du" | DE "Sobald du erste Freunde hast, schlagen wir dir hier weitere vor." / EN "Once you have your first friends, we'll suggest more here." |
| quiks-code card body | DE "Zeigen, scannen, fertig — so addet ihr euch." (D-14 literal direction) / EN "Show it, scan it, done." |
| Search input placeholder | DE "Name oder @handle" / EN "Name or @handle" |
| Search input static badge | DE "Bald" / EN "Soon" |
| Dark-Mode switch label + description | DE "Dunkler Modus" / "Die Nachtschicht für deine Augen" (D-14 literal direction) / EN "Dark mode" / "The night shift for your eyes." |
| SafeNow card | DE "Wir stehen in keiner Verbindung zu SafeNow — finden die App aber richtig gut. Nutz sie am Gelände." (D-14 literal direction, distancing sentence MUST survive any rewording) / EN "We have no connection to SafeNow — we just think the app is genuinely good. Use it on-site." + link "safenow.app öffnen" / "Open safenow.app" |
| Toast / "kommt bald" (generic pattern) | DE "{Feature} kommt bald." / EN "{Feature} is coming soon." — e.g. "Bearbeiten kommt bald.", "Sprachumschalter kommt bald.", "Zahlungsmittel kommen bald.", "QR-Adden kommt bald." |
| Error state (any GET /me failure) | Reuses the existing verbatim project pattern: DE "Kann den Server nicht erreichen — stell sicher, dass dein Gerät im selben WLAN wie die Dev-API ist." / EN "Can't reach the server — make sure your device is on the same Wi-Fi as the dev API." (same copy as `festivals.tsx`/`home.tsx`, do not invent new wording) |
| Destructive confirmation — Logout | Native `Alert.alert`: title DE "Abmelden?" / EN "Log out?"; body DE "Du kannst dich jederzeit wieder per E-Mail-Code anmelden." / EN "You can always sign back in with an email code."; buttons: Cancel (DE "Abbrechen" / EN "Cancel") + destructive confirm (DE "Abmelden" / EN "Log out") |
| Konto row tap feedback (Name/Handle/E-Mail/Sprache) | Toast: DE "Bearbeiten kommt bald." / EN "Editing is coming soon." (Sprache row: DE "Sprachumschalter kommt bald." / EN "Language switching is coming soon.") |
| Identity meta line (optional fields, empty) | Whole line omitted — no "—" placeholders, no empty dots (see § Meta Line Contract) |

---

## Screens & Navigation Contract (D-01)

`FloatingNav`'s tab set changes from `Home · Festivals` (+ 2 decorative items) to **4 real tabs**: `Start · Festivals · Friends · Mehr`. "Start" naming is **out of scope** — the existing route/label stays `home`/"Home" (per `06-CONTEXT.md` § Specifics: renaming is a later phase's decision, only flag at hand-off).

| Route | Kind | Header | Notes |
|---|---|---|---|
| `(tabs)/home.tsx` | `Tabs.Screen` (existing) | none | unchanged |
| `(tabs)/festivals.tsx` | `Tabs.Screen` (existing) | native, title "Festivals" | **logout button removed from `headerRight`** (D-09 — moves to Mehr) |
| `(tabs)/friends.tsx` **NEW** | `Tabs.Screen` | native, title "Friends" (Claude's Discretion, resolved: match the existing Festivals precedent rather than a bespoke top bar — cheapest, most consistent option) | `Users` icon, replaces the decorative Friends item |
| `(tabs)/mehr.tsx` **NEW** | `Tabs.Screen` | native, title "Mehr" | `UserRound`/`Menu` icon (Claude's Discretion — pick whichever `lucide-react-native` glyph the executor finds least confusable with the Profil push-screen's own icon; do not reuse the same icon for both), replaces the decorative Profil item |
| `app/profil.tsx` **NEW** | root-level `Stack.Screen`, pushed from Mehr | native, title "Profil", default back chevron | **Route-structure discretion, resolved:** lives OUTSIDE `(tabs)` at root level, mirroring the existing `(festival)/f/[festivalSlug]` precedent for "a full-screen destination that hides the tab bar" — pushing it therefore automatically removes `FloatingNav` from view without new visibility-gating logic. A single file is enough; no new route group is needed for one screen. |

`FloatingNav.tsx` changes: both `DisabledNavItem` instances are replaced by real routed items using the SAME `LIVE_TAB_ICON` map pattern already used for `home`/`festivals` (extend `LiveRouteName` to `'home' | 'festivals' | 'friends' | 'mehr'`, icons `Home`/`Tent`/`Users`/`UserRound`(or chosen Mehr icon)). No new visual variant is needed — the 4 tabs render through the exact same `Pressable`/`activePill`/`label` styling already in place; only the decorative branch is deleted.

---

## Placeholder Pattern Contract

Two DISTINCT "not real yet" treatments are used this phase — do not blend them (`06-CONTEXT.md` D-02/D-03 draw this line explicitly):

| Pattern | Visual | Interaction | Used for |
|---|---|---|---|
| **A — Dampened static** | Full block/row at reduced opacity (`0.45`, matches `FloatingNav`'s existing `itemDisabled: { opacity: 0.4 }` precedent, bumped slightly for readability of denser content) + a small static "Bald"/"Soon" badge (`micro` role, `fillQuiet` pill — mirrors `ComingSoonTile`'s `badge` style verbatim) | Non-interactive OR tap opens the toast (component's choice per row — a whole disabled block like the Stat-Tiles grid needs no tap-to-toast, since it carries its own static badge) | Profil: quiks-code card, Socials rows, Vibe chips, Stat-Tile grid. Mehr: "Zahlungsmittel" row, all 3 notification switches, "Standort mit der Crew teilen" switch, "quiks empfehlen" row. Friends: nothing (all 6 Friends blocks use real, honest empty-state COPY instead — the risk D-11 names is specifically that dampening alone reads as broken; the mitigation is the empty-state copy in the Copywriting Contract above, not visual dimming) |
| **B — Live data, disabled edit** | FULL opacity, real data rendered, chevron kept | Tappable, `onPress` opens the shared toast (D-13) instead of a real edit flow | Profil: Konto rows (Name/Handle/E-Mail — D-03). Mehr: "Sprache" row (shows the resolved UI locale as its value, e.g. "Deutsch"/"German") |

The Friends screen's 6 blocks are NOT visually dampened at all (per D-11's resolution) — they render at full weight with the honest, section-specific empty-state copy from the Copywriting Contract. The quiks-code card is the one Friends exception: it shows REAL data (the visitor's own `@username` from `GET /me`) with a Pattern-A-dampened CTA (`QR zeigen`).

---

## Component Inventory

New owned components (ADR-022, `apps/mobile/components/`):

| Component | Contract |
|---|---|
| `ListRow` **NEW** | `{ icon: LucideIcon, label: string, value?: string, danger?: boolean, disabled?: boolean, onPress?: () => void }`. Row: icon (22px, `textMuted` or `dangerText` if `danger`) + `bodyStrong` label + optional right-aligned `bodySm` `value` (muted) + trailing `ChevronRight` (only if `onPress` set). `minHeight: layout.hitMin`. `disabled` → Pattern A (opacity 0.45, badge). Used by both Mehr and Profil — build once, reuse. |
| `SettingsSwitch` **NEW** | `{ label: string, description?: string, value: boolean, onValueChange?: (v: boolean) => void, disabled?: boolean }`. Row: `bodyStrong` label + `bodySm` muted description (2-line max) + RN `Switch` (`trackColor` off `fillQuiet` / on `primary`, `thumbColor` white in both states). `disabled` → Pattern A. Backs both the real Dark-Mode switch and the 4 dead notification/location switches. |
| `SoonToast` **NEW** (context + component) | `ToastProvider` at the app shell root (in `app/_layout.tsx`, alongside the existing `ThemeProvider`) exposing `useSoonToast(): (message: string) => void`. Single-slot (a new call replaces any visible toast instantly — D-13 "ein einziger Mechanismus"). Renders as a pill (`r-md`, `surfaceInset`/`bgAppDeep`-ish neutral fill, NOT brand-colored — see § Color reserved-for list item 5's exclusion), `bodySm` text, centered, positioned `bottom: layout.navHeight + layout.navInset + insets.bottom + spacingScale['sp-4']` so it always floats clear of `FloatingNav` (Claude's Discretion, resolved). Auto-dismiss after ~2400ms; no manual dismiss control needed (short enough not to require one). |
| `AvatarSunsetRing` **NEW** (or an optional prop on `AvatarTile`) | Wraps `AvatarTile` with a circular Sunset gradient ring behind it — see § Avatar Sunset Ring Contract. Used ONLY in the Profil header this phase (not on the smaller `FloatingNav`/list-row avatars — D-07 widens the RULE app-wide but this phase only has one avatar surface to apply it to). |

Reused unchanged: `AvatarTile` + `deriveInitials` (Phase 4), `ComingSoonTile` (Phase 5, reused as the 3-tile Stat-Tile placeholder grid — adjust `flexBasis` from `48%` to `~31%` for a 3-column row, everything else identical), `FloatingNav` (extended per § Screens & Navigation Contract), `KeyboardScreen` (complete-profile screen's new optional fields).

---

## Avatar Sunset Ring Contract (D-07)

The Profil header avatar gets a circular Sunset gradient ring (design L1776: an absolutely-positioned `inset:-4px` circle behind a `border:3px solid var(--bg-app)`-ringed `AvatarTile`).

- Implementation mirrors `FestivalCard.tsx`'s existing `SunsetLayer` pattern (same `gradientSunset` token, same `react-native-svg` `LinearGradient`/`Defs`/`Stop` construction) but with a `Circle` primitive instead of a rounded `Rect`.
- Ring diameter = avatar size (88px, `AvatarTile`'s existing `AVATAR_SIZE`) + 8px (4px inset on each side, matching the design's `-4px` offset).
- A 3px `colors.bgApp` gap sits between the ring and the avatar itself (matches the design's `border:3px solid var(--bg-app)`) so the ring never touches the photo/initials edge-to-edge — this is what makes it read as a RING, not a background wash.
- Applies in BOTH color modes unchanged (Sunset is mode-invariant per CI §6/§3).
- **Documentation follow-up (not this agent's job, flagged for the plan):** D-07 also requires editing `docs/DEVELOPMENT_DECISIONS.md` (ADR-023) and `docs/brand/quiks-ci-v1.md` to widen the Sunset-usage rule from "mark and hero surfaces only" to "…and avatar/identity surfaces". That is a docs-authoring task for `gsd-plan-phase`/`gsd-executor`, not a UI-visual decision — this UI-SPEC only fixes what the ring looks like.

---

## Meta Line Contract (D-04, D-12)

Profil header renders, in order, on a single `bodySm`/`textMuted` line:

```
{identityLine}      ← D-12, OMITTED ENTIRELY if pronoun/age/gender are all empty
{displayName}        ← title1 (or title2 fallback), never translated
@{username}           ← countdown role, colors.primary, never translated
{metaLine}            ← "{n} Festivals · {n} Friends · seit {year} dabei"
```

- **Identity line** (`sie/ihr · 23 · weiblich` in the design): join only the NON-EMPTY optional fields with ` · `. Zero, one, two or three present values are all valid — the line is entirely absent (not rendered, no reserved height) when all three are empty, per the same "omit-if-empty" precedent `FestivalCard.tsx` already uses for the optional `place` field. Order is fixed: pronoun, age, gender.
- **Meta line**: `{n} Festivals · {n} Friends · seit {year} dabei`. `n Festivals` count sources the ALREADY-CACHED `festivalKeys.mine` query (`apps/mobile/lib/festival-queries.ts`) — no second network call (Claude's Discretion, resolved per `06-CONTEXT.md`'s own note). `n Friends` is the constant `0` this phase (FRND-02 not built) — still routed through a Lingui `plural` macro for correctness, not a bare string, since 0 is a real plural category. `{year}` is `Account.createdAt`'s year only (`new Date(createdAt).getFullYear()` — no full-date `Intl` formatting needed, a bare year has no locale-variant form). Recommend a pure, testable helper (`lib/profile-meta-line.ts`, mirroring `lib/date-range.ts`'s pattern) so this logic lands in the node-env Vitest runner rather than screen code.
- "Friends" is used as a literal, untranslated brand-voice word in BOTH DE and EN copy per the design source (the design's own German copy says "Friends", not "Freunde", consistently across the Friends tab, meta line and stat tile) — still routed through Lingui `t`/`plural` for I18N-01/lint compliance even though the DE and EN catalog values are expected to be identical.

**Open item for `gsd-plan-phase` (not resolved here — visual-only spec, this is a data-model decision):** whether `age` is stored raw or derived from a `birthDate` column is explicitly left open by `06-CONTEXT.md` D-12 ("Der Planner muss das explizit vorlegen, nicht still entscheiden"). This UI-SPEC only fixes the DISPLAY: age renders as a plain localized integer (e.g. `23`), however it is sourced.

---

## complete-profile.tsx Additions (D-12)

Three new OPTIONAL fields, appended after the existing `displayName` field, before the submit CTA — mirrors the existing avatar section's "Optional. You can add this later." helper-text convention (no red-badge "Required" marker, unlike `displayName`):

| Field | Input type | Constraints (client soft-cap, server is the real gate) | Label / helper |
|---|---|---|---|
| Pronoun | free-text `TextInput` | max ~20 chars | DE "Pronomen" · helper "Optional" |
| Age | numeric `TextInput` (`keyboardType="number-pad"`) | 1–3 digits, soft-bounds hint only (not enforced client-side beyond digit-only input) | DE "Alter" · helper "Optional" |
| Gender | free-text `TextInput` | max ~30 chars | DE "Geschlecht" · helper "Optional" |

**Free-text over a fixed selector** for all three (deliberate, resolved discretion): the design implies a value like "weiblich" but building/choosing a taxonomy is explicitly the piece IDN-02 defers to Birgit's later concept (`06-CONTEXT.md` § Deferred). A free-text field lets a visitor self-describe without the app pre-committing to an enum it may need to change once that policy lands, and needs no new `Select`/`Picker` primitive this phase (none exists in `packages/ui` yet — ADR-022 scope discipline). Same `styles.field`/`styles.label`/`styles.input` styling as the existing `username`/`displayName` fields (reuse the screen's existing `createStyles`, no new visual language).

---

## Theme Override Contract (D-08a)

Storage shape (mirrors `active-festival-storage.ts`'s MMKV idiom — own storage id, lazy `require`, error-swallowing):

- `ThemeOverride = 'system' | 'light' | 'dark'` — THREE stored states, satisfying the CONTEXT's "dritter Zustand" requirement even though the Mehr-screen `SettingsSwitch` UI only ever WRITES `'system'` (off) or `'dark'` (on) this phase; `'light'` exists in the type for forward-compatibility (a future "force light against a dark device" control) but has no writer yet.
- Effective mode: `override === 'dark' ? 'dark' : override === 'light' ? 'light' : resolveThemeMode(deviceScheme)` — the `'system'` branch is the UNCHANGED 05.1 `resolveThemeMode` call, so the hell-first invariant (only the exact device value `'dark'` yields night mode when following system) is preserved byte-for-byte.
- Default (no stored value / first launch): `'system'`.
- The `Switch` in Mehr → Darstellung reflects the CURRENT EFFECTIVE mode (checked = dark, whichever state produced it), not the raw override value — toggling it always writes an EXPLICIT `'dark'` or `'system'` (never `'light'`).

Visual: identical `SettingsSwitch` component as the dead notification switches, just `disabled={false}` and wired to this state instead of a no-op.

---

## UI Considerations

Applicable state considerations resolved: 9 covered, 3 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|---|---|---|---|
| empty | Friends: Anfragen, Chats, Deine Crew, Vielleicht kennst du | ✅ covered | Each section renders its own honest empty-state copy from the Copywriting Contract — never a shared generic string, never a blank/missing block |
| empty | Profil meta line — 0 saved festivals | ✅ covered | Renders "0 Festivals" via the Lingui `plural` macro (0 is a valid category), never omitted or dashed |
| empty | Profil identity line — all three D-12 fields unset | ✅ covered | Line is omitted entirely (§ Meta Line Contract) — no dangling `·` separators |
| zero-one-many | Profil meta line — Festivals count | ✅ covered | `plural` macro handles 0/1/n in both DE and EN |
| long-text | Profil header `displayName` | 🧪 backstop | `numberOfLines={1}` + ellipsis on the header name (same guard `FestivalCard.tsx` already applies to festival names) — held-out UI-state test, no explicit truncation-width proof captured here |
| long-text | Friends chat preview / row `bodySm` values | ✅ covered | `numberOfLines={1}` + `ellipsizeMode`, matches the design's own `text-overflow:ellipsis` (L1462) |
| loading | Profil / GET /me | ✅ covered | Reuses the existing project-wide loading copy pattern ("Loading…" text, no skeleton) — same backstop level as `festivals.tsx`/`home.tsx`, not a new pattern |
| error | Profil / GET /me failure | ✅ covered | Reuses the EXACT existing transport-error copy + Retry button pattern from `festivals.tsx`/`home.tsx` (Copywriting Contract row "Error state") |
| overflow | Friends "Vielleicht kennst du" / "Deine Crew" row lists (once FRND-02 ships) | ⚠ unresolved | Out of this phase's scope (always empty this phase) — planner should treat "how a long friends list scrolls/paginates" as FRND-02's own assumption, not Phase 6's |
| partial | Mehr — mixed live (Profil, Dark Mode, Sprache, Abmelden) + dead rows in the SAME section list | ✅ covered | § Placeholder Pattern Contract draws the exact Pattern A/B line per row — no section is "half-styled" by accident |
| populated | Profil Konto rows with real `GET /me` data | ✅ covered | Full-opacity, tappable-with-toast (Pattern B) |
| loading | Theme-override first read (MMKV) | 🧪 backstop | Falls back to `'system'` synchronously (no async gate) — same "never paint transparent on first frame" guarantee as `useTheme()`'s existing default; held-out, no separate loading-state test authored here |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — not applicable (ADR-022, mobile has no shadcn/registry surface) | not required |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
