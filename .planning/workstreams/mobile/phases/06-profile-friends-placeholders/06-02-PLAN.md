---
phase: 06-profile-friends-placeholders
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/visitor-profile.ts
  - packages/db/drizzle/
  - packages/contracts/src/schemas.ts
  - packages/ui/src/tokens.ts
  - apps/api/src/me/me.service.ts
  - apps/api/src/me/me.controller.ts
  - apps/api/test/me-endpoints.spec.ts
autonomous: false
requirements: [PROF-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /api/v1/me liefert zusätzlich createdAt als ISO-String (nicht als Date-Objekt) und die drei optionalen Identitätsfelder pronoun, birthDate, gender (D-04 + D-12)."
    - "birth_date wird als nullable date-Spalte gespeichert und im Contract als YYYY-MM-DD-String transportiert; ein Alter wird NIE als eigener Wert persistiert (D-12a)."
    - "Die Erweiterung ist rein additiv: keine bestehende Spalte und kein bestehendes Contract-Feld ändert Typ, Nullability oder Namen — Bedingung der Serialisierung mit dem Admin-Workstream."
    - "Die drei neuen Spalten sind nullable und im Insert-Schema optional; ein complete-profile-Request ohne diese Felder bleibt gültig."
    - "pronoun und gender tragen serverseitige Längenobergrenzen im drizzle-zod .extend() — der Client-Cap der UI-SPEC ist nur UX, der Server ist das echte Gate."
    - "Eine neue, nummerierte Migrationsdatei liegt unter packages/db/drizzle/ und ist committet; das Schema wurde NICHT per db:push angewendet."
    - "Die laufende lokale Docker-Postgres-Instanz trägt die drei neuen Spalten, bewiesen durch einen gegen die echte DB laufenden apps/api-Test."
  artifacts:
    - path: "packages/db/src/schema/visitor-profile.ts"
      provides: "Nullable Spalten pronoun, birthDate, gender plus passende drizzle-zod .extend()-Overrides"
      contains: "birthDate"
    - path: "packages/contracts/src/schemas.ts"
      provides: "meSchema.createdAt sowie die drei Identitätsfelder in visitorProfilePublicSchema und completeProfileBodySchema"
      contains: "createdAt"
    - path: "packages/ui/src/tokens.ts"
      provides: "Statusfüllung fillInfoQuiet und Statusrand borderInfo für die SafeNow-Karte"
      contains: "fillInfoQuiet"
    - path: "apps/api/src/me/me.controller.ts"
      provides: "Explizite ISO-Serialisierung von session.user.createdAt im getMe-Response-Body"
      contains: "toISOString"
    - path: "apps/api/src/me/me.service.ts"
      provides: "Erweiterte select- und returning-Projektionen um die drei Identitätsfelder"
      contains: "birthDate"
  key_links:
    - from: "packages/db/src/schema/visitor-profile.ts"
      to: "packages/contracts/src/schemas.ts"
      via: "visitorProfileSelectSchema/visitorProfileInsertSchema werden per .pick() weiterverwendet, nie handgeschrieben nachgebaut"
      pattern: "visitorProfile(Select|Insert)Schema"
    - from: "apps/api/src/me/me.controller.ts"
      to: "packages/contracts/src/schemas.ts"
      via: "tsRestHandler(contract.getMe) validiert den Response-Body gegen meSchema"
      pattern: "contract\\.getMe"
    - from: "apps/api/src/me/me.service.ts"
      to: "packages/db/src/schema/visitor-profile.ts"
      via: "db.select({...}).from(visitorProfile) liest die drei neuen Spalten"
      pattern: "visitorProfile\\.(pronoun|birthDate|gender)"
---

<objective>
Der koordinierte Schnitt durch die geteilten Pakete: `packages/db` → `packages/contracts` →
`apps/api`, plus die zwei Status-Token für die SafeNow-Karte. Danach trägt `GET /api/v1/me`
alles, was die drei Screens dieser Phase anzeigen.

Umgesetzte Entscheidungen: **D-04** (`Account.createdAt` additiv über `meSchema`, für „seit … dabei"),
**D-12** (Pronomen, Alter und Geschlecht werden optionale Profilfelder, ohne Sichtbarkeits-Policy,
Altersgrenze oder Signup-Disclaimer), **D-12a** (Geburtsdatum speichern, Alter ableiten).

Purpose: Alle Schema- und Contract-Änderungen dieser Phase landen in EINEM Plan und EINER Welle,
weil `packages/contracts`, `packages/db` und `packages/ui` die Kollisionszone mit dem parallel
geplanten Admin-Workstream sind.
Output: Drei neue nullable Spalten, ein erweitertes `meSchema`, eine committete Migrationsdatei,
zwei neue UI-Token, eine erweiterte API-Testsuite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/mobile/ROADMAP.md
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-CONTEXT.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-RESEARCH.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-PATTERNS.md
@.claude/CLAUDE.md
</context>

## Koordinationshinweis (Workstreams — vor Ausführung lesen)

`packages/contracts`, `packages/db` und `packages/ui` sind laut `.claude/CLAUDE.md`
§„Parallel Workstreams" die Kollisionszone mit dem gleichzeitig geplanten Admin-Stream: **nur ein
Stream ändert sie gleichzeitig**, und die Schema-Arbeit des Admin-Streams muss additiv bleiben.
Dieser Plan ist der EINZIGE Plan der Phase 6, der diese drei Pakete anfasst. Vor der Ausführung
ist zu bestätigen, dass der Admin-Stream aktuell keine Änderung an diesen Paketen offen hat;
`drizzle-zod` propagiert eine Schemaänderung in jede App und bricht sonst dessen Typecheck.

## Artifacts this phase produces

Neue Symbole, die dieser Plan erzeugt (existieren im Repo noch NICHT):

- Spalten in `visitor_profile`: `pronoun`, `birth_date`, `gender`
- Felder auf `visitorProfileSelectSchema` / `visitorProfileInsertSchema`:
  `pronoun`, `birthDate`, `gender`
- Felder auf `visitorProfilePublicSchema` und `completeProfileBodySchema`:
  `pronoun`, `birthDate`, `gender`
- Feld auf `meSchema`: `createdAt`
- Token in `packages/ui/src/tokens.ts`: `fillInfoQuiet`, `borderInfo`
- Eine neue Migrationsdatei `packages/db/drizzle/0004_*.sql`

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>D-12 / D-12a umsetzen: eine nullable `birth_date`-Spalte auf `visitor_profile` plus drei neue Felder auf der veröffentlichten Contract-Fläche `packages/contracts`.</decision>
  <context>
Diese Entscheidung ist als **one-way** eingestuft und in `06-CONTEXT.md` bereits vom User
getroffen — der Checkpoint fragt sie nicht neu, er stellt die Tür vor dem Durchschreiten sichtbar.

Was der Rückzieher kosten würde:
- Eine Rückwärts-Migration auf `visitor_profile`, die bereits erfasste **personenbezogene Daten**
  (Geburtsdatum, Geschlecht) löscht — nicht nur ein Refactor.
- Eine Änderung an `packages/contracts`, die der Admin-Workstream mitliest; dessen Typecheck bricht
  bei jeder nicht abgestimmten Änderung.
- `PROJECT.md` führt IDN-02 als „pending Birgit's concept — out of this milestone", weil daran
  Jugendschutz, Flinta-Filter und der Sicherheits-Disclaimer hängen. D-12 weicht davon bewusst ab
  und erfasst die Daten **ohne** Sichtbarkeits-Policy, **ohne** Altersgrenze und **ohne**
  Signup-Disclaimer. Der Einwand wurde im Discuss benannt und vom User überstimmt.
- Der Designtext verspricht „du entscheidest, was Freunde sehen" — eine Visibility-Policy dafür
  existiert nicht und ist ausdrücklich nicht Teil dieser Phase.
  </context>
  <options>
    <option id="proceed-as-locked">
      <name>Wie in D-12 / D-12a festgelegt umsetzen</name>
      <pros>Setzt die user-gelockte Entscheidung um. Geburtsdatum statt Alter heißt: der Wert veraltet nicht, und eine spätere Altersgrenze aus Birgits Konzept ist ohne Migration möglich.</pros>
      <cons>Personenbezogene Daten liegen ab jetzt ohne Sichtbarkeits-Policy im Schema. Rückzieher nur per Migration.</cons>
    </option>
    <option id="defer-identity-fields">
      <name>Nur `Account.createdAt` (D-04) jetzt, die drei Identitätsfelder auf IDN-02 vertagen</name>
      <pros>Keine Erfassung personenbezogener Daten vor Birgits Konzept; `packages/db` bleibt unangetastet.</pros>
      <cons>Widerspricht der user-gelockten Entscheidung D-12. Die Identitätszeile im Profilkopf und die drei Felder im complete-profile-Screen entfallen; die Pläne 06-07 und 06-08 verlieren Umfang.</cons>
    </option>
  </options>
  <resume-signal>Auswählen: `proceed-as-locked` oder `defer-identity-fields`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Additive Erweiterung von Schema, Contract, UI-Token und /me-Endpunkt</name>
  <reversibility rating="one-way">Eine nullable `birth_date`-Spalte auf `visitor_profile` plus drei neue Felder auf der von zwei Workstreams gelesenen Contract-Fläche; der Rückzieher braucht eine Migration und betrifft bereits erfasste personenbezogene Daten.</reversibility>
  <read_first>
    - `packages/db/src/schema/visitor-profile.ts` — die vollständige Tabelle, der Kommentarblock über die drizzle-zod-Inferenzlücke und die bestehenden `.extend()`-Overrides für die `text()`-Spalten
    - `packages/db/src/schema/festival.ts` — das exakte Vorbild für `date({ mode: 'string' })` inklusive der Pflicht, für Select UND Insert je einen `.extend()`-Override zu setzen
    - `packages/db/src/schema/auth.ts` — die `user`-Tabelle mit `createdAt`, aus der better-auths Session-Objekt gespeist wird
    - `packages/contracts/src/schemas.ts` — `visitorProfilePublicSchema`, `meSchema`, `completeProfileBodySchema`, `festivalSchema` (Vorbild für String-statt-Date-Transport)
    - `packages/ui/src/tokens.ts` — die bestehenden Statusfüllungen `fillDangerQuiet`/`fillDangerSubtle` und die `*Text`-Varianten-Konvention
    - `apps/api/src/me/me.controller.ts` — der `getMe`-Handler und die `UserSession`-Injektion
    - `apps/api/src/me/me.service.ts` — `getProfile`s `.select({...})` und `completeProfile`s `.returning({...})`
    - `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md` — §Color (Herleitung der beiden neuen Token)
  </read_first>
  <files>packages/db/src/schema/visitor-profile.ts, packages/contracts/src/schemas.ts, packages/ui/src/tokens.ts, apps/api/src/me/me.service.ts, apps/api/src/me/me.controller.ts</files>
  <action>
Alle fünf Dateien landen in EINEM Commit — `meSchema` mit einem Pflichtfeld `createdAt` zu erweitern
bricht den Typecheck von `apps/api`, bis der Controller es liefert. Ein Zwischenstand mit rotem
Typecheck ist nicht zulässig.

1. `packages/db/src/schema/visitor-profile.ts`: Drei neue Spalten in die `pgTable`-Definition —
   `pronoun: text()`, `birthDate: date({ mode: 'string' })`, `gender: text()`. Alle drei ohne
   `.notNull()` und ohne Default, also nullable. `date` muss zum bestehenden Import aus
   `drizzle-orm/pg-core` hinzugefügt werden. Danach die Overrides ergänzen:
   auf `visitorProfileSelectSchema` je `z.string().nullable()`, auf `visitorProfileInsertSchema`
   je `z.string().nullable().optional()`. Der `date({ mode: 'string' })`-Override ist NICHT
   optional — der Kommentarblock der Datei und `festival.ts` dokumentieren, dass drizzle-zod diesen
   Spaltentyp sonst als Buffer-Typ inferiert. Serverseitige Längenobergrenzen auf den beiden
   Freitextfeldern setzen, im selben Stil wie die vorhandenen Caps auf `username` und
   `displayName`: `pronoun` maximal 20 Zeichen, `gender` maximal 30 Zeichen (deckt sich mit den
   Client-Caps der UI-SPEC; der Server ist das echte Gate). `birthDate` bekommt KEINE
   Format-Refinement im Zod-Schema — der Postgres-`date`-Typ validiert beim Insert selbst.

2. `packages/contracts/src/schemas.ts`:
   - `visitorProfilePublicSchema.pick({...})` um `pronoun: true, birthDate: true, gender: true`
     erweitern.
   - `completeProfileBodySchema.pick({...})` um dieselben drei erweitern.
   - `meSchema` um ein NEUES Top-Level-Feld `createdAt: z.string()` erweitern — ausdrücklich NICHT
     `z.date()` und ausdrücklich NICHT über `visitorProfilePublicSchema`, weil `createdAt` von
     `Account`/`user` stammt und nicht von `visitor_profile`. Das folgt der Konvention, die
     `festivalSchema` für `startDate`/`endDate` bereits setzt: Der Contract transportiert Daten
     immer als String.
   - Keine Handschrift: nur `.pick()`/`.extend()` auf den drizzle-zod-Basen, nie ein
     `z.object({...})`-Duplikat.

3. `packages/ui/src/tokens.ts`: Zwei neue Statuswerte im Stil der bestehenden
   `fillDangerQuiet`/`fillDangerSubtle`-Reihe ergänzen — `fillInfoQuiet` mit
   `rgba(95,180,255,.12)` (die Info-Hue bei 12 %) und `borderInfo` mit `rgba(95,180,255,.35)`
   (dieselbe Hue bei 35 %). Beide sind modusinvariant wie die übrigen Statusfüllungen. Die
   bestehende `infoText`-Variante NICHT anfassen — sie bleibt die Text-/Icon-Farbe, weil die bare
   Info-Hue auf Papier an AA scheitert.

4. `apps/api/src/me/me.service.ts`: `getProfile`s `.select({...})` um
   `pronoun: visitorProfile.pronoun`, `birthDate: visitorProfile.birthDate`,
   `gender: visitorProfile.gender` erweitern. `completeProfile`s `.returning({...})` identisch
   erweitern. Der `.insert(visitorProfile).values({ accountId, ...input })`-Aufruf braucht keine
   Änderung — sobald der Contract-Typ die drei optionalen Felder trägt, fließen sie durch.

5. `apps/api/src/me/me.controller.ts`: Den `getMe`-Response-Body um
   `createdAt: session.user.createdAt.toISOString()` ergänzen. `session.user.createdAt` ist zur
   Laufzeit ein Date-Objekt aus better-auths Session — es braucht KEINE zusätzliche DB-Query, aber
   die Umwandlung in den ISO-String muss explizit im Code stehen und darf nicht der
   JSON-Serialisierung überlassen werden, sonst lügt der TypeScript-Typ über die Form nach der
   Netzwerkübertragung. Sollte sich beim Ausführen zeigen, dass `UserSession` das Feld nicht
   mitführt (RESEARCH Assumptions Log A4), ist der Fallback eine zusätzliche `db.select` auf die
   `user`-Tabelle im Service — nicht das Weglassen des Feldes.

Der Typecheck von `apps/api` ist der schärfste Nachweis, dass Schema, Contract und Endpunkt
zueinander passen: `meSchema` verlangt `createdAt` als String, `VisitorProfilePublic` verlangt die
drei neuen Schlüssel im Service-Rückgabewert.
  </action>
  <acceptance_criteria>
    - `grep -q "birthDate: date({ mode: 'string' })" packages/db/src/schema/visitor-profile.ts` exits 0
    - `grep -c 'birthDate' packages/db/src/schema/visitor-profile.ts` ergibt mindestens 3 (Spalte + Select-Override + Insert-Override)
    - `grep -q 'createdAt: z.string()' packages/contracts/src/schemas.ts` exits 0
    - `grep -c 'z.date()' packages/contracts/src/schemas.ts` ergibt 0
    - `grep -q 'toISOString' apps/api/src/me/me.controller.ts` exits 0
    - `grep -q 'fillInfoQuiet' packages/ui/src/tokens.ts` exits 0 und `grep -q 'borderInfo' packages/ui/src/tokens.ts` exits 0
    - Additivitätsnachweis: `git diff -- packages/db/src/schema/visitor-profile.ts packages/contracts/src/schemas.ts packages/ui/src/tokens.ts | grep '^-' | grep -v '^---'` enthält keine Zeile, die eine bestehende Spalten- oder Felddefinition entfernt oder umtypisiert — nur Zeilen, die durch eine erweiterte Version derselben Definition ersetzt werden
    - Keine der drei neuen Spalten trägt `.notNull()` — `grep -E '(pronoun|birthDate|gender).*notNull' packages/db/src/schema/visitor-profile.ts` findet nichts
    - `pnpm --filter @quiks/db typecheck && pnpm --filter @quiks/contracts typecheck && pnpm --filter @quiks/ui typecheck && pnpm --filter @quiks/api typecheck` exits 0
    - `pnpm --filter @quiks/mobile typecheck` exits 0 (Beweis, dass die Änderung für den Mobile-Client additiv ist)
  </acceptance_criteria>
  <verify>
    <automated>pnpm build --filter @quiks/db --filter @quiks/contracts --filter @quiks/ui &amp;&amp; pnpm --filter @quiks/api typecheck &amp;&amp; pnpm --filter @quiks/mobile typecheck &amp;&amp; grep -q 'createdAt: z.string()' packages/contracts/src/schemas.ts &amp;&amp; grep -q 'toISOString' apps/api/src/me/me.controller.ts</automated>
  </verify>
  <done>Schema, Contract, Token und `/me`-Endpunkt tragen die neuen Felder; jeder Paket-Typecheck ist grün; kein bestehendes Feld wurde geändert; ein Commit.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Migration erzeugen, gegen die lokale Docker-DB anwenden und end-to-end beweisen</name>
  <precondition>Die lokale Docker-Postgres-Instanz ist erreichbar (`docker compose ps` zeigt den Dienst des Projekts `quiks` als laufend) und `packages/db/.env` enthält eine auf `localhost:5432` zeigende `DATABASE_URL` bzw. `DATABASE_URL_UNPOOLED`.</precondition>
  <read_first>
    - `packages/db/drizzle.config.ts` — die Auflösung `DATABASE_URL_UNPOOLED ?? DATABASE_URL` und das Migrationsverzeichnis
    - `packages/db/package.json` — die Skripte `db:generate`, `db:migrate`, `db:push`
    - `docker-compose.yml` — der explizite Projektname `quiks`, Port und Zugangsdaten der lokalen Instanz
    - `packages/db/drizzle/` — die vier bereits committeten Migrationsdateien `0000_*` bis `0003_*` als Beleg für den generate+migrate-Workflow
    - `apps/api/test/me-endpoints.spec.ts` — der bestehende OTP-Round-Trip, die `createTestApp`/`createTestDatabase`-Helfer und die vorhandenen `/me`-Assertions
    - `apps/api/test/setup.ts` — wie die Testdatenbank aufgesetzt wird
  </read_first>
  <files>packages/db/drizzle/, apps/api/test/me-endpoints.spec.ts</files>
  <action>
**Dieser Task ist blockierend.** `pnpm typecheck` und `pnpm build` laufen ohne die Migration grün,
weil die Typen aus der Schemaquelle stammen und nicht aus der laufenden Datenbank — ohne diesen
Task entsteht ein falsch-positiver Verifikationszustand, in dem alles kompiliert, aber jede echte
Abfrage auf die drei neuen Spalten fehlschlägt.

1. `pnpm --filter @quiks/db db:generate` ausführen. Das Ergebnis ist eine neue nummerierte
   SQL-Datei unter `packages/db/drizzle/` (erwartet: `0004_*.sql`) mit drei
   `ALTER TABLE "visitor_profile" ADD COLUMN`-Anweisungen. Die Datei MUSS committet werden — die
   Migrationshistorie ist in diesem Repo die Quelle der Wahrheit.
2. `pnpm --filter @quiks/db db:migrate` ausführen, um sie auf die laufende lokale Instanz
   anzuwenden.
3. **`db:push` ist verboten.** Push und generate/migrate divergieren; das Repo führt vier
   committete Migrationsdateien und dieser Workflow wird fortgesetzt.
4. Die Zieldatenbank ist die lokale Docker-Postgres-Instanz auf `localhost:5432` (Projektname
   `quiks` in `docker-compose.yml`), **nicht Neon**. Die Zugangsdaten kommen aus `packages/db/.env`
   (für die Migration) beziehungsweise `apps/api/.env` (für den API-Test); `drizzle.config.ts`
   bevorzugt `DATABASE_URL_UNPOOLED`, wenn gesetzt. Ist die Datenbank nicht erreichbar oder zeigt
   die aufgelöste Verbindungszeichenfolge auf einen anderen Host, ist der Task mit einer klaren
   Fehlermeldung abzubrechen — niemals still weiterlaufen und „erfolgreich" melden.
5. Falls `drizzle-kit generate` eine interaktive Rückfrage stellt, die sich nicht unterdrücken
   lässt, ist die Frage im Klartext an den User zu geben statt zu raten.
6. `apps/api/test/me-endpoints.spec.ts` erweitern — dieser Test läuft gegen die echte Datenbank und
   ist damit der Beweis, dass die Migration wirklich angewendet wurde:
   - Eine Assertion, dass die `GET /me`-Antwort ein Feld `createdAt` trägt, dass dessen Typ
     `string` ist und dass `new Date(...)` darauf eine gültige Zeit ergibt. Zusätzlich eine
     negative Assertion, dass der Wert kein Date-Objekt ist.
   - Ein Round-Trip: `POST /me/complete-profile` mit gesetzten Werten für Pronomen, Geburtsdatum
     im Format `YYYY-MM-DD` und Geschlecht; danach `GET /me` und Vergleich, dass genau diese drei
     Werte unverändert zurückkommen und das Geburtsdatum weiterhin ein String im Format
     `YYYY-MM-DD` ist (kein Zeitstempel, keine Zeitzonenverschiebung).
   - Ein zweiter Round-Trip OHNE die drei Felder: der Request bleibt gültig, und `GET /me` liefert
     für alle drei `null`.
   - Eine Grenzwert-Assertion: ein Pronomen-Wert über der serverseitigen Obergrenze wird mit einem
     4xx abgewiesen, nicht gespeichert.
  </action>
  <acceptance_criteria>
    - Eine neue Datei `packages/db/drizzle/0004_*.sql` existiert und ist in git verzeichnet — `ls packages/db/drizzle/0004_*.sql` exits 0 und `git status --porcelain packages/db/drizzle/ | grep -q '^??'` schlägt fehl (die Datei ist getrackt, nicht untracked)
    - Die Migrationsdatei enthält drei ADD-COLUMN-Anweisungen auf `visitor_profile` — `grep -ci 'add column' packages/db/drizzle/0004_*.sql` ergibt 3
    - Die Migrationsdatei enthält keine DROP- oder ALTER-COLUMN-TYPE-Anweisung — `grep -Ei 'drop column|alter column .* type' packages/db/drizzle/0004_*.sql` findet nichts
    - Kein `db:push`-Aufruf im Ausführungsprotokoll dieses Tasks
    - `pnpm --filter @quiks/api test -- me-endpoints` exits 0 und die Suite enthält die vier neuen Assertionsgruppen
    - Die gesamte `apps/api`-Suite bleibt grün: `pnpm --filter @quiks/api test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>ls packages/db/drizzle/0004_*.sql &amp;&amp; ! grep -Ei 'drop column|alter column .* type' packages/db/drizzle/0004_*.sql &amp;&amp; pnpm --filter @quiks/api test</automated>
  </verify>
  <done>Die Migrationsdatei ist erzeugt, committet und auf die lokale Docker-Instanz angewendet; die erweiterte `me-endpoints`-Suite beweist gegen die echte Datenbank, dass `createdAt` als ISO-String und die drei Identitätsfelder als Round-Trip funktionieren; ein Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `POST /api/v1/me/complete-profile` | Unvertrauter Freitext (Pronomen, Geschlecht) und ein Datum überqueren hier die Grenze in die Datenbank. |
| Session → `GET /api/v1/me` | Die zurückgegebene Profilprojektion ist an die Session gebunden; die Account-Identität stammt allein aus `session.user.id`. |
| `packages/contracts` → Admin-Workstream | Eine veröffentlichte Typfläche, die ein zweiter, gleichzeitig arbeitender Stream mitliest. |
| Entwicklungsrechner → lokale Docker-Postgres-Instanz | Die Migration schreibt Schemaänderungen; ein falsch aufgelöster Connection-String zielt auf die falsche Datenbank. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | `visitorProfilePublicSchema` trägt ab jetzt `birthDate` und `gender` | high | mitigate | Der EINZIGE Erzeuger dieser Projektion ist `MeService.getProfile(accountId)`, und der einzige Aufrufer übergibt `session.user.id` — nie eine Account-ID aus Pfad, Query oder Body. In Phase 6 entsteht kein Endpunkt, der ein fremdes Profil liest. Akzeptanzkriterium: jede `getProfile(`-Aufrufstelle in `apps/api` übergibt `session.user.id`. |
| T-06-06 | Information Disclosure | Ein künftiger „Freunde sehen mein Profil"-Endpunkt (FRND-02/PROF-02) würde dieselbe Projektion wiederverwenden | medium | accept | Bewusst getragenes Restrisiko: D-12 legt ausdrücklich fest, dass es in dieser Phase keine Sichtbarkeits-Policy gibt, und IDN-02 vertagt sie auf Birgits Konzept. Vor dem ersten Endpunkt, der ein FREMDES Profil ausliefert, muss die Projektion in eine Eigentümer-Sicht und eine Freundes-Sicht getrennt werden. Als Blocker in `STATE.md` §Blockers/Concerns zu vermerken. |
| T-06-07 | Denial of Service | Freitextfelder `pronoun` und `gender` | medium | mitigate | Serverseitige Längenobergrenzen im drizzle-zod `.extend()` (Pronomen 20, Geschlecht 30 Zeichen), nach dem Vorbild der bestehenden Caps auf `username` und `displayName`. Die Client-Caps der UI-SPEC sind nur UX und werden nicht als Gate gewertet. Bewiesen durch die Grenzwert-Assertion in Task 3. |
| T-06-08 | Tampering | Migration gegen die falsche Datenbank | medium | mitigate | Der Task benennt die Zielinstanz (lokales Docker-Postgres, Projektname `quiks`, Port 5432) und die Env-Datei ausdrücklich und bricht bei Unerreichbarkeit laut ab, statt still zu „gelingen". `db:push` ist ausgeschlossen, damit Migrationshistorie und Live-Schema nicht divergieren. |
| T-06-09 | Tampering | Ungültiges Datumsformat in `birthDate` | low | mitigate | Der Postgres-`date`-Typ parst und validiert beim Insert; ein ungültiges Format wird als Datenbankfehler abgewiesen statt gespeichert. Zusätzlich verhindert der native Picker in Plan `06-08` clientseitig jede Freitexteingabe. |
| T-06-10 | Repudiation | `createdAt` als vom Client manipulierbarer Wert | low | mitigate | `createdAt` wird serverseitig aus `session.user.createdAt` gelesen und ist an keiner Stelle Teil eines Request-Bodys; der Client kann ihn nicht setzen. |
</threat_model>

<verification>
- Jeder Paket-Typecheck grün (`@quiks/db`, `@quiks/contracts`, `@quiks/ui`, `@quiks/api`, `@quiks/mobile`)
- `pnpm --filter @quiks/api test` grün, inklusive der vier neuen Assertionsgruppen
- Genau eine neue Migrationsdatei unter `packages/db/drizzle/`, committet, ohne DROP/ALTER-TYPE
- Der Diff über die drei geteilten Pakete ist additiv
</verification>

<success_criteria>
- `GET /api/v1/me` liefert `createdAt` als ISO-String sowie `pronoun`, `birthDate`, `gender`
- Ein complete-profile-Round-Trip erhält ein `YYYY-MM-DD`-Geburtsdatum unverändert
- Ein complete-profile-Request ohne die drei Felder bleibt gültig
- Die lokale Datenbank trägt die drei neuen Spalten, bewiesen durch einen Test gegen die echte DB
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-02-SUMMARY.md` when done
</output>
