---
phase: 04-visitor-auth-profile-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/tokens.ts
  - packages/db/src/schema/visitor-profile.ts
  - apps/api/test/username-race.spec.ts
files_touched_readonly:
  - packages/contracts/src/schemas.ts
autonomous: true
requirements: [IDN-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "packages/ui/src/tokens.ts holds the REAL festipal brand values (green-500 #74CC1F primary, ink-900 #0C0E13 bg-app, ink-1000 #07080B surface-inset, ink-050 #E9ECF2 text-primary, red-500 #FF4D5E danger, green-400 #8FDA3B success) sourced from docs/concept/designs/auth/source/festipal-tokens.css — not the placeholder #4f46e5 set — and exposes dark + light semantic roles used by every restyled screen."
    - "Server-side name caps are enforced by extending the EXISTING `.extend()` z.string() values in packages/db visitor-profile.ts: username `.min(3).max(20).regex(/^[a-z0-9_.]+$/)`, displayName `.min(1).max(40)` — NOT via createInsertSchema's refinement callback (Pitfall 4 re-triggers the text()→unknown inference bug)."
    - "A username of length 2 or 21, an uppercase/space/special-char username, and a displayName of 41 chars are all rejected at the Zod parse layer (400-level) and never reach the DB (IDN-01 boundary + empty edges); length 3 / 20 / 40 and charset a-z 0-9 _ . are accepted."
    - "CompleteProfileBody and VisitorProfilePublic in @festipal/contracts still resolve to concrete types (not `unknown`) after the cap edit — the documented text()→unknown regression symptom does not reappear (Pitfall 4 warning sign)."
    - statement: "No DATABASE schema push (db:push) is required for D-03: the change is drizzle-zod validation refinement only (.min/.max/.regex on existing z.string() values); the avatar text() and username_lower unique index columns already exist from Phase 1, so no column/constraint DDL changes."
      verification: backstop
  prohibitions:
    - "MUST NOT add the caps via createInsertSchema's second-argument refinement callback — use the existing .extend() z.string() values (Pitfall 4: the callback re-triggers the text()-column type-inference collapse the file's own comment warns about)."
    - "MUST NOT hand-redeclare completeProfileBodySchema / visitorProfilePublicSchema shapes in packages/contracts — the caps must flow up automatically from the drizzle-zod base via the existing .pick() composition (Pitfall 6)."
  artifacts:
    - "packages/ui/src/tokens.ts (real brand colors + dark/light semantic roles + fonts/radii/spacing extended)"
    - "packages/db visitor-profile.ts .extend() with D-03 min/max/regex on username + displayName"
    - "apps/api/test/username-race.spec.ts new D-03 rejection cases"
---

<objective>
Land the two package-level foundations the screens build on, both independent of the mobile app:
(1) replace `packages/ui/src/tokens.ts`'s placeholder values with the REAL festipal brand tokens
(UI-SPEC binding, ADR-015) so every restyled screen — and the already-shipped Festivals list —
resolves the real palette; (2) close the D-03 server-side name-cap gap the UI-SPEC flagged, by
tightening the EXISTING drizzle-zod `.extend()` values and proving the rejections with tests.

Purpose: the tokens file gets its first real values this phase and Phases 5/6 build on it — there
must be exactly one token set, not a competing third. The name caps are the authoritative half of
D-03 (the client soft-cap in 04-03/04-05 is UX sugar only; the server is the source of truth).
Output: real tokens, capped Zod refinements in `packages/db`, extended `username-race.spec.ts`.
</objective>

## Artifacts this plan produces

- **packages/ui/src/tokens.ts:** real `colors` (brand green `#74CC1F` / press `#63B117`, secondary
  violet `#5A4DFF`, `bgApp #0C0E13`, `bgAppDeep #07080B`, `surfaceCard #14161D`, `surfaceInset #07080B`,
  `textPrimary #E9ECF2`, `textSecondary #9FA6B6`, `textMuted #7C8394`, `success #8FDA3B`,
  `warning #FFC53D`, `danger #FF4D5E`, `info #5FB4FF`, `textOnPrimary #07080B`), dark + light role
  maps, extended `radii` (`control 12`, `md 16`, `lg 22`, `pill 999`), `spacing` (sp-scale + `screenPad 18`,
  `hitMin 44`, `sectionGap 28`), `fontFamilies` (`display 'Outfit'`, `body 'Plus Jakarta Sans'`,
  `mono 'JetBrains Mono'`), and the UI-SPEC type-role sizes.
- **packages/db/src/schema/visitor-profile.ts:** D-03 `.min()/.max()/.regex()` on the existing
  `.extend()` `username`/`displayName` values (insert + select schemas).
- **apps/api/test/username-race.spec.ts:** new rejection cases (username &lt;3, &gt;20, bad charset;
  displayName &gt;40; boundary accepts at 3/20/40).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visitor-auth-profile-completion/04-CONTEXT.md
@.planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md
@.planning/phases/04-visitor-auth-profile-completion/04-PATTERNS.md
@docs/concept/designs/auth/source/festipal-tokens.css
@packages/ui/src/tokens.ts
@packages/db/src/schema/visitor-profile.ts
@packages/contracts/src/schemas.ts
@apps/api/test/username-race.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the real festipal brand tokens into packages/ui/src/tokens.ts</name>
  <files>packages/ui/src/tokens.ts</files>
  <read_first>
    - packages/ui/src/tokens.ts (self — placeholder export shape to extend, NOT throw away)
    - docs/concept/designs/auth/source/festipal-tokens.css (the binding token source: `--green-500` etc., dark `:root` + `[data-theme="light"]` scope)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Spacing Scale, ## Typography, ## Color — the transcribed binding values)
    - .planning/phases/04-visitor-auth-profile-completion/04-PATTERNS.md (`packages/ui/src/tokens.ts` — "Key source values to port")
  </read_first>
  <action>
Replace the placeholder color values (`#4f46e5`, `#16a34a`, `#f4f4f5`, etc.) with the real festipal
palette from `festipal-tokens.css`, keeping the existing exported shape (`spacing`, `radii`,
`fontSizes`, `fontWeights`, `colors`, `tokens`) and EXTENDING it — do not invent a competing third
set. Add dark-first semantic role aliases the screens need: `bgApp #0C0E13`, `bgAppDeep #07080B`,
`surfaceCard #14161D`, `surfaceInset #07080B`, brand `primary #74CC1F` / `primaryPress #63B117`,
`secondary #5A4DFF`, `textPrimary #E9ECF2` / `textSecondary #9FA6B6` / `textMuted #7C8394`,
`textOnPrimary #07080B`, status `success #8FDA3B` / `warning #FFC53D` / `danger #FF4D5E` / `info #5FB4FF`.
Provide a `light` variant map (from the CSS `[data-theme="light"]` scope: `bgApp #FFFFFF`,
`surfaceInset #E9ECF2`, `textPrimary #07080B`, `textSecondary #363B49`, `textMuted #565C6E`) so screens
can resolve by `useColorScheme()` (UI-SPEC Scope note #8 — brand hues green/success/danger stay the
same across modes). Extend `radii` with `control 12`, `md 16`, `lg 22`, `pill 999`; extend `spacing`
with the `sp-*` ramp (2/4/6/8/12/16/20/24/32/40/56/72) plus layout constants `screenPad 18`,
`hitMin 44`, `sectionGap 28`, `stackGap 12`, `topbar 56`. Add `fontFamilies` (`display 'Outfit'`,
`body 'Plus Jakarta Sans'`, `mono 'JetBrains Mono'`) and the UI-SPEC type-role sizes (wordmark 56,
display2 34, title2 21, title3 17, body 15, bodySm 13.5, label 12, micro 10.5, otpDigit 27) and
weights (400/500/600/700/800). Update the file header comment to drop "PLACEHOLDER" (these are now the
real values). Since the Festivals list already imports `tokens`, it inherits the new palette — verify
no crash/regression.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/ui build && pnpm --filter @festipal/ui typecheck</automated>
    <human-check>Cold-start the Festivals list on the dev build; confirm it renders on the new dark palette with no broken/undefined color (the pre-existing screen inherits the new token values).</human-check>
  </verify>
  <acceptance_criteria>tokens.ts exports real brand values (no `#4f46e5`); `colors.primary === '#74CC1F'`; dark + light role maps exist; `pnpm --filter @festipal/ui build` succeeds; the Festivals list still renders.</acceptance_criteria>
  <done>Real, single, brand token set is in place; header no longer says placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03 server-side username/displayName caps + rejection tests</name>
  <files>packages/db/src/schema/visitor-profile.ts, apps/api/test/username-race.spec.ts</files>
  <read_first>
    - packages/db/src/schema/visitor-profile.ts (self — the EXISTING `.extend()` block lines 64-75 + its verbatim text()→unknown warning comment)
    - packages/contracts/src/schemas.ts (completeProfileBodySchema/.pick() composition — confirm caps flow up, NO edit needed here)
    - apps/api/test/username-race.spec.ts (existing TOCTOU race test — the pattern to extend)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (Pitfall 4 — the exact edit target + the wrong mechanism to avoid)
  </read_first>
  <behavior>
    - username length 2 → rejected (Zod .min(3), 400-level, never hits DB)
    - username length 21 → rejected (.max(20))
    - username "Ab_1" (uppercase) / "a b" (space) / "a!b" (special) → rejected (.regex /^[a-z0-9_.]+$/)
    - username "abc" (3) and 20-char lowercase-alnum-dot-underscore → accepted
    - displayName length 41 → rejected (.max(40)); length 1 and 40 → accepted; empty "" → rejected (.min(1))
    - CompleteProfileBody / VisitorProfilePublic still infer concrete field types, not `unknown`
  </behavior>
  <action>
Edit the EXISTING `.extend()` values in `visitorProfileInsertSchema` (and mirror on
`visitorProfileSelectSchema`): change `username: z.string()` to
`z.string().min(3).max(20).regex(/^[a-z0-9_.]+$/, 'lowercase letters, numbers, _ and . only')` and
`displayName: z.string()` to `z.string().min(1).max(40)`; leave `accountId`/`avatar` untouched.
CRITICAL (Pitfall 4): add these to the existing `.extend()` `z.string()` values — do NOT use
`createInsertSchema`'s refinement-callback mechanism, which re-triggers the documented
text()→`unknown` inference collapse. Do NOT touch `packages/contracts/src/schemas.ts`: its
`.pick()` composition already re-derives `completeProfileBodySchema` from this base, so the caps
propagate automatically (Pitfall 6). Extend `apps/api/test/username-race.spec.ts` with the rejection
+ boundary cases from `<behavior>`. Record in the SUMMARY the schema-push decision: **NO db:push
required** — this is validation-refinement only, the `avatar`/`username_lower` columns already exist
(Phase 1), so build/types/DB are unaffected by the refinement (this is the visible, reasoned
schema-push decision the planner delegated).
  </action>
  <verify>
    <automated>pnpm --filter @festipal/db typecheck && pnpm --filter @festipal/contracts typecheck && pnpm --filter @festipal/api test -- username-race</automated>
  </verify>
  <acceptance_criteria>The new min/max/regex live on the existing `.extend()` values; `username-race.spec.ts` proves 2/21/uppercase/space/special usernames and 41-char displayName reject while 3/20/40 accept; contracts typecheck stays green (no `unknown` regression); the SUMMARY records "D-03: no db:push required (validation-only, columns pre-exist)".</acceptance_criteria>
  <done>Server rejects out-of-range names; tests prove it; contract types stay concrete; schema-push decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`POST /me/complete-profile`) | Untrusted username/displayName strings cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-02-T | Tampering | username/displayName input (V5 Input Validation) | medium | mitigate | D-03 `.min/.max/.regex` server-side Zod caps reject malformed/oversized names at parse time before they reach the DB or downstream avatar-initials derivation (closes the previously-bare-`z.string()` gap) |
| T-4-02-I | Information Disclosure | username charset | low | accept | charset restricted to `a-z 0-9 _ .` (no mixed-case/unicode) reduces homograph/impersonation surface; full anti-impersonation is out of scope this phase |
</threat_model>

<verification>
- `pnpm --filter @festipal/ui build` + `pnpm --filter @festipal/ui typecheck` green.
- `pnpm --filter @festipal/db typecheck` + `pnpm --filter @festipal/contracts typecheck` green (no `unknown` regression).
- `pnpm --filter @festipal/api test -- username-race` proves the D-03 rejections + boundaries.
- Festivals list renders on the new palette (human-check).
</verification>

<success_criteria>
Real single brand-token set in `packages/ui`; D-03 caps enforced server-side via the correct
`.extend()` mechanism, proven by tests; contract types stay concrete; schema-push decision (no push)
recorded.
</success_criteria>

<output>
Create `.planning/phases/04-visitor-auth-profile-completion/04-02-SUMMARY.md` when done.
</output>
