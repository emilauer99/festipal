---
phase: 04-visitor-auth-profile-completion
plan: 04
type: execute
wave: 3
depends_on: ["04-03", "04-01"]
files_modified:
  - apps/mobile/app/(auth)/verify.tsx
  - apps/mobile/components/OtpBoxes.tsx
  - apps/mobile/components/ResendCountdown.tsx
  - apps/mobile/lib/otp-error.ts
  - apps/mobile/lib/__tests__/otp-error.test.ts
  - apps/mobile/locales/en/messages.po
  - apps/mobile/locales/de/messages.po
autonomous: true
requirements: [AUTH-05]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The OTP screen renders six fixed-width digit boxes reading off ONE captured TextInput (maxLength=6, keyboardType='number-pad', autoComplete='one-time-code', near-zero opacity), the active box carrying the accent border — preserving Phase 3's overflow-impossible reasoning while matching the mockup (UI-SPEC Scope note #4)."
    - "Verification auto-submits: the verify mutation fires from OtpBoxes onComplete the instant the 6th digit is entered — there is NO Verify button (UI-SPEC Scope note #5); while the call is in flight all six boxes go to a muted non-interactive state (loading, no new copy)."
    - "A wrong OR expired code shows ONE unified error box: EN 'That code is wrong or has expired.' / DE 'Der Code ist falsch oder abgelaufen.' with body 'Get a new code.' and a 'Send new code' retry CTA — mapOtpError still DISTINGUISHES OTP_EXPIRED / INVALID_OTP / TOO_MANY_ATTEMPTS|429 codes internally, but the wrong/expired return strings converge to this one copy (Copywriting Contract)."
    - "A rate-limited response (TOO_MANY_ATTEMPTS or bare 429) shows the reused 'Too many attempts — try again in a few minutes.' copy; a network failure shows the reused server-unreachable copy (AUTH-05)."
    - "The resend affordance is a disabled 'Resend in {mm:ss}' countdown starting at 60s (JetBrains Mono timer) that becomes a tappable 'Resend code' link at exactly 0:00 (UI-SPEC Scope note #6); resend reuses the same code server-side (resendStrategy:'reuse', Phase 2) and a 'Change email' link returns to the Email route."
    - "mapOtpError is extracted to a pure lib function and unit-tested: OTP_EXPIRED and INVALID_OTP both map to the unified wrong/expired copy, TOO_MANY_ATTEMPTS and status 429 map to the rate-limit copy, and an unknown/networkless error maps to the server-unreachable copy (AUTH-05 boundary — closes the VALIDATION Wave-0 untested-mapOtpError gap)."
    - statement: "The resend control is guarded against re-entrancy: a double-tap during an in-flight resend cannot fire two sends (Claude's-discretion backstop, UI-SPEC loading/logout-control reasoning applied to resend)."
      verification: backstop
    - "AUTH-05 precision (rounding / overflow / tie-breaking) is N/A for this phase — flagged, not fabricated: the OTP flow performs no numeric/monetary computation; the only numeric display is the mm:ss countdown, which shows whole integer seconds with no rounding ambiguity."
  prohibitions:
    - "MUST NOT change WHICH better-auth error codes mapOtpError distinguishes (OTP_EXPIRED / INVALID_OTP / TOO_MANY_ATTEMPTS / 429) — only converge the wrong/expired RETURN STRINGS to the unified copy (Deprecated note, RESEARCH State of the Art)."
    - "MUST NOT build a client-side OTP expiry timer or client-side rate-limit counter — the server is the sole source of OTP validity/rate-limits; the 60s resend cooldown is a purely cosmetic client timer, not OTP expiry (Don't Hand-Roll)."
    - "MUST NOT reintroduce a Verify button or the 'Verify'/'Verifying…' strings — they are superseded by auto-submit (Scope note #5)."
  artifacts:
    - "apps/mobile/components/OtpBoxes.tsx (6-box display driven by one hidden TextInput, auto-submit onComplete)"
    - "apps/mobile/components/ResendCountdown.tsx (60s countdown → tappable resend link, re-entrancy guarded)"
    - "apps/mobile/lib/otp-error.ts (extracted pure mapOtpError)"
    - "apps/mobile/lib/__tests__/otp-error.test.ts"
    - "unified error-box + change-email DE/EN msgids"
---

<objective>
Expand the tracer's still-plain OTP screen into the designed 6-box auto-submitting code entry with a
unified error box, a 60-second resend countdown, and a change-email link — the AUTH-05 surface. Extract
`mapOtpError` into a pure, unit-tested lib function (the VALIDATION Wave-0 gap: it exists but is
untested), converging the wrong/expired return strings onto the mockup's single combined error copy
while keeping its internal code-distinguishing logic.

Purpose: AUTH-05 (OTP errors + edge cases shown clearly and localized) is the phase's error-handling
requirement; this plan owns it. The OTP round-trip itself is unchanged from Phase 3 — this is a visual
+ error-copy + resend-UX expansion on the already-proven verify flow (tracer 04-03).
Output: `OtpBoxes` + `ResendCountdown` components, the restyled `verify.tsx`, the extracted+tested
`mapOtpError`, and the unified DE/EN error/resend/change-email copy.
</objective>

## Artifacts this plan produces

- **apps/mobile/components/OtpBoxes.tsx** — 6 box Views reading one hidden `TextInput`; `onComplete`
  fires at 6 digits; accepts a `disabled`/muted loading state; active-box accent border + caret.
- **apps/mobile/components/ResendCountdown.tsx** — disabled "Resend in {mm:ss}" from 60s → tappable
  "Resend code"; re-entrancy-guarded resend handler.
- **apps/mobile/lib/otp-error.ts** — pure `mapOtpError(err)` (moved out of verify.tsx).
- **apps/mobile/lib/__tests__/otp-error.test.ts** — Vitest coverage of every branch.
- **apps/mobile/app/(auth)/verify.tsx** — restyled: OtpBoxes + auto-submit + unified error box +
  ResendCountdown + Change-email link; no Verify button.
- **DE/EN msgids** — unified error headline/body/retry, resend countdown/link, change-email.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md
@.planning/phases/04-visitor-auth-profile-completion/04-PATTERNS.md
@.planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md
@apps/mobile/app/(auth)/verify.tsx
@apps/mobile/lib/auth-client.ts
@packages/ui/src/tokens.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract mapOtpError to a pure lib + unit test (unified copy convergence)</name>
  <files>apps/mobile/lib/otp-error.ts, apps/mobile/lib/__tests__/otp-error.test.ts, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - apps/mobile/app/(auth)/verify.tsx (self — the existing mapOtpError branch logic lines 32-43 to move + converge)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Copywriting Contract Code error rows — unified wrong/expired headline/body/retry, rate-limit, network)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (State of the Art "Deprecated" — converge wrong/expired strings, keep code-distinguishing logic; AUTH-05 support row)
    - .planning/phases/04-visitor-auth-profile-completion/04-VALIDATION.md (Wave-0: mapOtpError untested)
  </read_first>
  <behavior>
    - err.code === 'OTP_EXPIRED' → unified wrong/expired headline copy
    - err.code === 'INVALID_OTP' → the SAME unified wrong/expired copy (convergence)
    - err.code === 'TOO_MANY_ATTEMPTS' → rate-limit copy
    - err.status === 429 (no code) → rate-limit copy
    - anything else / undefined → server-unreachable copy
  </behavior>
  <action>
Move `mapOtpError` out of `verify.tsx` into a pure `apps/mobile/lib/otp-error.ts` that takes the
error object and the Lingui `t` (or returns a stable msgid key the screen maps) so it is unit-testable
with no RN runtime. Converge the OTP_EXPIRED and INVALID_OTP branches to return the SINGLE unified
copy (EN "That code is wrong or has expired.") — but KEEP the code-distinguishing branch structure
(do not collapse the code checks, only the return strings; Deprecated note). Keep TOO_MANY_ATTEMPTS |
429 → rate-limit copy and the fallthrough → server-unreachable copy. Add
`lib/__tests__/otp-error.test.ts` covering every branch from `<behavior>`. Add the unified
error-box msgids (headline/body/retry CTA "Send new code") + fill DE verbatim ("Der Code ist falsch
oder abgelaufen." / "Hol dir einen neuen Code." / "Neuen Code senden"); run extract.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test -- otp-error && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`mapOtpError` lives in `lib/otp-error.ts`, is imported by verify.tsx, and its test proves OTP_EXPIRED+INVALID_OTP converge to the unified copy while 429/TOO_MANY_ATTEMPTS and the fallthrough map correctly; DE unified copy matches the contract; test + typecheck pass.</acceptance_criteria>
  <done>The OTP error mapping is a tested pure function with converged wrong/expired copy.</done>
</task>

<task type="auto">
  <name>Task 2: OtpBoxes + ResendCountdown components (ADR-022 primitives on tokens)</name>
  <files>apps/mobile/components/OtpBoxes.tsx, apps/mobile/components/ResendCountdown.tsx, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - apps/mobile/app/(auth)/verify.tsx (self — the single-TextInput sanitize idiom `value.replace(/[^0-9]/g,'').slice(0,6)` OtpBoxes extracts; the handleResend call ResendCountdown wraps)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Copywriting Contract resend rows; ## Color OTP active-digit accent; ## Typography Mono OTP digit 27 / countdown 13.5; Scope notes #4/#5/#6)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (Pattern 1 hidden-TextInput 6-box)
    - packages/ui/src/tokens.ts (accent/danger/mono-font tokens)
  </read_first>
  <action>
Build `components/OtpBoxes.tsx` per RESEARCH Pattern 1: six fixed-width box `View`s reading off one
real `TextInput` (maxLength 6, number-pad, autoComplete one-time-code, near-zero opacity, focused via
a Pressable overlay), digits in JetBrains Mono 27px; the active box (index === value.length) carries
the `colors.primary` border + a blinking caret; accepts `value`, `onChangeText`, `onComplete` (fires
at length 6), and a `disabled` prop that renders all boxes muted/non-interactive (the in-flight
loading state) plus a `hasError` prop that applies the danger border. Build
`components/ResendCountdown.tsx`: a 60-second countdown rendering the disabled "Resend in {mm:ss}"
(JetBrains Mono) with a timer icon (Lucide), transitioning at 0:00 to a tappable "Resend code" link
that calls the passed `onResend`; the handler MUST be guarded against re-entrancy (a double-tap while
a resend is in flight cannot fire twice) and restarts the 60s countdown on a successful resend. Both
are custom RN primitives on `packages/ui` tokens — no third-party UI kit; icons via `lucide-react-native`.
Add resend/change-email msgids + DE verbatim ("Erneut senden in {mm:ss}" / "Code erneut senden" /
"E-Mail ändern"); run extract.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`OtpBoxes` renders 6 boxes off one hidden TextInput with an `onComplete` at 6 digits + disabled/error states; `ResendCountdown` counts 60→0 then exposes a re-entrancy-guarded tappable resend; both use tokens + Lucide, no hard-coded hex; lint (no-literal-string) + typecheck pass.</acceptance_criteria>
  <done>The two OTP components exist, are token-styled, and encode the auto-submit + countdown + re-entrancy contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle verify.tsx — auto-submit, unified error box, resend, change-email</name>
  <files>apps/mobile/app/(auth)/verify.tsx, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - apps/mobile/app/(auth)/verify.tsx (self — the verify round-trip + resend to preserve; move the trigger from a button onPress to OtpBoxes onComplete)
    - apps/mobile/components/OtpBoxes.tsx (Task 2)
    - apps/mobile/components/ResendCountdown.tsx (Task 2)
    - apps/mobile/lib/otp-error.ts (Task 1)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Visual Hierarchy OTP focal point; ## UI Considerations otp loading/error/overflow rows; Scope notes #4/#5/#6/#7)
  </read_first>
  <action>
Rewire `verify.tsx` to render `OtpBoxes` (auto-submit: call the existing
`authClient.signIn.emailOtp({ email, otp })` round-trip from `onComplete`, NOT a button) with the
boxes going muted (`disabled`) while `verifying`. On error, feed the error through the extracted
`mapOtpError` and render the unified danger-tinted error box (headline + body + "Send new code" retry
CTA that re-sends and clears the field; danger applied as a ~16% tint behind the border per UI-SPEC
Color). Remove the Verify button and the "Verify"/"Verifying…" strings entirely (superseded). Replace
the old always-tappable resend link with `ResendCountdown` (60s, wrapping the existing `handleResend`
via `authClient.emailOtp.sendVerificationOtp`). Add a "Change email" link that routes back to
`/email`. Use headerShown:false + in-body H1 (`t\`Enter code\``) + the subtitle
`t\`Six digits sent to {email}.\`` with the email bold-emphasized (Scope note #7). Keep the
no-manual-nav-on-success contract (root guard routes forward). Style with real tokens; run extract for
any remaining new msgids.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm lint && pnpm typecheck && pnpm test</automated>
    <human-check>On the dev build with Mailpit: enter a correct code → auto-submits at the 6th digit (no button), boxes mute during the call, advances. Enter a wrong code → single unified error box + "Send new code". Let the resend countdown run to 0:00 → link becomes tappable; double-tap it fast → only one send. Tap "E-Mail ändern" → back to Email. Confirm DE copy matches the mockup.</human-check>
  </verify>
  <acceptance_criteria>verify.tsx uses OtpBoxes with auto-submit (no Verify button), the unified error box via mapOtpError, ResendCountdown (60s→link), and a Change-email link; loading mutes the boxes; DE copy verbatim; lint + typecheck + test green.</acceptance_criteria>
  <done>The OTP screen matches the mockup interaction contract and shows clear localized errors for every edge (AUTH-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → auth (OTP verify/resend) | Untrusted OTP + resend requests cross to better-auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-04-S | Spoofing | OTP brute force | high | mitigate | Server-side better-auth rate limiter + attempt counter (Phase 2, unchanged); client surfaces TOO_MANY_ATTEMPTS/429 copy, adds no bypass; no client-side validity check |
| T-4-04-D | Denial of Service | resend spam | medium | mitigate | 60s client cooldown + re-entrancy-guarded resend reduce accidental floods; server rate limiter is the authoritative control |
| T-4-04-I | Information Disclosure | error copy | low | accept | Unified wrong/expired copy avoids leaking whether a code was wrong vs expired to the user surface, while mapOtpError still logs the distinct code path internally |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile test` (otp-error branches) + `lint` (no-literal-string) + `typecheck` green.
- Manual: auto-submit, unified error box, 60s resend → link, double-tap-safe resend, change-email — recorded in SUMMARY.
</verification>

<success_criteria>
The OTP screen matches the mockup (6-box auto-submit, no Verify button), shows one unified localized
error for wrong/expired plus reused rate-limit/network copy, and offers a 60s resend countdown +
change-email — with mapOtpError extracted and fully unit-tested (AUTH-05).
</success_criteria>

<output>
Create `.planning/phases/04-visitor-auth-profile-completion/04-04-SUMMARY.md` when done.
</output>
