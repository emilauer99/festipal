---
phase: 04-visitor-auth-profile-completion
plan: 07
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/auth/auth.instance.ts
  - apps/api/test/signout-origin.spec.ts
  - apps/mobile/lib/fonts-context.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(auth)/index.tsx
  - apps/mobile/app/(auth)/email.tsx
  - apps/mobile/app/(auth)/verify.tsx
  - apps/mobile/app/(profile-setup)/complete-profile.tsx
  - apps/mobile/app/festivals/index.tsx
  - apps/mobile/components/OtpBoxes.tsx
  - apps/mobile/components/ResendCountdown.tsx
  - apps/mobile/components/AvatarTile.tsx
autonomous: true
requirements: [AUTH-04]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "AUTH-04 (server revocation): a real OTP-authenticated session's sign-out request — carrying the session cookie plus an `expo-origin` header and NO standard `origin` header (exactly what apps/mobile's expoClient sends) — is ACCEPTED by the server (no 403 MISSING_OR_NULL_ORIGIN) AND the session is genuinely revoked server-side (a subsequent GET /api/v1/me with the same cookie returns 401), proven by a headless integration test."
    - "The `@better-auth/expo` server `expo()` plugin is installed in apps/api/src/auth/auth.instance.ts's `plugins` array (alongside `emailOTP(...)`) and `@better-auth/expo` is a declared dependency of apps/api's package.json (pinned to 1.6.25 to match better-auth and the existing apps/mobile dependency)."
    - "Design-fidelity (ADR-015 / 04-UI-SPEC typography contract, IDN-01 screens): all eight restyled Phase-4 screens/components apply the registered weight-specific font keys (FONT_DISPLAY / FONT_BODY / FONT_MONO via `resolveFontFamily`, gated on a shared fonts-ready signal) so the three brand fonts actually render; the generic-token-name family pattern no longer appears in any of those eight files, and mobile lint + typecheck + the existing 19-test suite stay green."
    - statement: "AUTH-04 device UAT (WINDOWS #11): on a real device, tapping logout returns to Welcome AND the server session is no longer valid (the same session cannot re-authorize a protected call), including the airplane-mode and fast-double-tap paths."
      verification: backstop
    - statement: "Font visual UAT (WINDOWS #15): on a real device the three brand fonts (Outfit / Plus Jakarta Sans / JetBrains Mono) visibly render on the Welcome / Email / Verify / complete-profile / Festivals screens and the OTP / countdown / avatar components — not the system-font fallback."
      verification: backstop
  prohibitions:
    - "MUST NOT change the `/me/complete-profile` contract body — it stays `{username, displayName}` only, `visitor_profile.avatar` stays null, no avatar field (D-01). This plan touches no contract/schema."
    - "MUST NOT weaken origin/CSRF checking or `trustedOrigins` beyond adding the `expo()` server plugin — no wildcard origins, no disabling the origin-check middleware. The plugin's job is only to translate `expo-origin` -> `origin`; the existing `trustedOrigins` (festipal://, exp://, exp://**) stay as-is (D-05)."
    - "MUST NOT add a useFonts()-based splash-hide / `return null` gate — `bootstrapped` (locale + auth) stays the SOLE splash gate; font readiness only gates which `fontFamily` a style resolves (Pitfall 5 / D-04). Do not regress _layout.tsx's existing non-blocking splash contract."
    - "MUST NOT persist the deep-link destination or the avatar to any NEW durable storage — the in-memory pending-destination (D-02) and the MMKV-only device-local avatar (D-01) contracts are unchanged by this plan."
    - "MUST NOT loosen the D-03 server-side name caps or any `festivalId` tenant scoping."
  artifacts:
    - "apps/api/src/auth/auth.instance.ts (expo() added to the betterAuth plugins array)"
    - "apps/api/package.json (@better-auth/expo@1.6.25 dependency)"
    - "apps/api/test/signout-origin.spec.ts (new headless expo-origin sign-out + server-revocation integration spec)"
    - "apps/mobile/lib/fonts-context.tsx (new FontsReadyProvider + useFontsReady shared readiness hook)"
    - "apps/mobile/app/_layout.tsx (wraps the routed tree in FontsReadyProvider fed by the existing useAppFonts() fontsLoaded)"
    - "the eight restyled screens/components applying resolveFontFamily(FONT_DISPLAY|FONT_BODY|FONT_MONO, fontsReady)"
---

<objective>
Close the two gaps 04-VERIFICATION.md found after Phase 4 executed. GAP 1 (BLOCKER, AUTH-04): the
server-side better-auth instance is missing the `@better-auth/expo` `expo()` plugin, so the mobile
client's `expo-origin` header is never translated to a standard `origin` header and any
cookie-bearing better-auth call — including `authClient.signOut()` — is rejected 403
(MISSING_OR_NULL_ORIGIN) before the session is revoked; the session then stays valid server-side until
its 90-day expiry even though the client visually reaches Welcome. GAP 2 (WARNING, design-fidelity
ADR-015): every restyled Phase-4 screen sets `fontFamily` to the GENERIC token name (`'Outfit'` etc.)
which does not match the WEIGHT-SPECIFIC key the fonts are registered under (`'Outfit_700Bold'`), so RN
silently falls back to the system font even though the fonts are loaded.

Purpose: make AUTH-04's server-side session revocation genuinely work (the phase's one failed truth),
and honour the binding 04-UI-SPEC typography contract on the screens that were supposed to use it.
Output: the `expo()` server plugin + a headless sign-out integration test proving no-403 + real
revocation; and a shared fonts-ready hook plus the eight screens applying the registered font keys.
This is a gap-closure pass — it does not re-plan or re-touch 04-01..04-06.

Resolves WINDOWS.md id 2 (server expo() plugin) and id 15 (font-family application). The executor
marks both fixed via `gsd-tools windows fixed 2` / `... fixed 15` after acceptance passes; this plan
does not edit WINDOWS.md directly.
</objective>

## Artifacts this plan produces

- **apps/api/package.json** — adds `@better-auth/expo` (pin `1.6.25`, matching `better-auth` and the
  existing `apps/mobile` dependency) to `dependencies`.
- **apps/api/src/auth/auth.instance.ts** — `import { expo } from '@better-auth/expo'` and `expo()`
  appended to the `betterAuth({ plugins: [...] })` array alongside `emailOTP(...)`.
- **apps/api/test/signout-origin.spec.ts** — new headless integration spec: real OTP sign-in (via the
  dev-OTP capture transport) -> POST sign-out with `expo-origin` + cookie (no `origin`) -> asserts
  non-403 + real server-side revocation (GET /me then returns 401); plus a negative control proving
  origin-check is active on cookie-bearing requests.
- **apps/mobile/lib/fonts-context.tsx** — `FontsReadyProvider` + `useFontsReady()` — a React context
  exposing the already-computed `fontsLoaded` boolean to any screen (no re-registration of fonts).
- **apps/mobile/app/_layout.tsx** — wraps the routed provider tree in `FontsReadyProvider` fed by the
  existing root `useAppFonts()` `fontsLoaded` (SplashView keeps its own prop; splash gate unchanged).
- **eight restyled screens/components** — apply `resolveFontFamily(FONT_DISPLAY|FONT_BODY|FONT_MONO,
  fontsReady)` inline (mirroring _layout.tsx's SplashView), replacing the generic-token family usages.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visitor-auth-profile-completion/04-CONTEXT.md
@.planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md
@.planning/phases/04-visitor-auth-profile-completion/04-VERIFICATION.md
@apps/api/src/auth/auth.instance.ts
@apps/api/test/me-endpoints.spec.ts
@apps/mobile/lib/fonts.ts
@apps/mobile/app/_layout.tsx
@packages/ui/src/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the @better-auth/expo server plugin + prove sign-out is accepted and genuinely revokes (AUTH-04)</name>
  <files>apps/api/package.json, apps/api/src/auth/auth.instance.ts, apps/api/test/signout-origin.spec.ts</files>
  <reversibility rating="reversible">Adding the `expo()` server plugin touches the live auth request surface, but it only translates the `expo-origin` header into a standard `origin` before the existing origin-check runs — it adds no new trust and can be removed by deleting one import + one array entry. No schema/contract/session-shape change.</reversibility>
  <precondition>apps/api integration tests require a reachable Neon DB (DATABASE_URL / DATABASE_URL_UNPOOLED) and the dev-OTP capture transport, exactly as the existing me-endpoints.spec.ts already relies on — the 42/42 API suite already runs in this environment.</precondition>
  <read_first>
    - apps/api/src/auth/auth.instance.ts (self — the `betterAuth({ plugins: [emailOTP(...)] })` call at lines 19-55 and the existing `trustedOrigins` block; append `expo()`, do not alter trustedOrigins/session/emailOTP config)
    - apps/api/package.json (self — dependencies block; `better-auth` is pinned `1.6.25` — pin `@better-auth/expo` to the same)
    - apps/api/test/me-endpoints.spec.ts (mirror `signInWithOtp`, `readCapturedOtp`, `cookieHeaderFromSetCookie`, and the `ORIGIN`/dev-OTP-capture pattern for a real headless session cookie)
    - apps/api/test/auth-guard.spec.ts (mirror the supertest + createTestApp lifecycle; the anonymous-401 pattern for the post-revocation GET /me assertion)
    - apps/api/test/setup.ts (createTestApp / createTestDatabase helpers)
    - apps/mobile/lib/auth-client.ts (confirms the client is `@better-auth/expo/client`'s expoClient sending `expo-origin` — the header the server plugin must translate; scheme `festipal`)
    - .planning/phases/04-visitor-auth-profile-completion/04-VERIFICATION.md (frontmatter `gaps:` + Additional Cross-Cutting Findings #1 — the authoritative defect trace)
    - .planning/WINDOWS.md (id 2 — the pre-flagged missing server plugin this task resolves)
  </read_first>
  <action>
Add `@better-auth/expo` at version `1.6.25` to apps/api/package.json `dependencies` (same version as
`better-auth` and the existing apps/mobile dependency, so the workspace resolves one copy). Run
`pnpm install` from the repo root (stop any running Expo/Metro watcher first — the Windows Metro x pnpm
install race, MEMORY). In apps/api/src/auth/auth.instance.ts add `import { expo } from
'@better-auth/expo';` and append `expo()` to the existing `plugins` array so it reads
`plugins: [emailOTP({ ... }), expo()]`. Leave `trustedOrigins`, `session`, `secret`, the drizzle
adapter, and the `emailOTP(...)` config exactly as-is (the plugin only translates the `expo-origin`
header into a standard `origin` in its onRequest hook — the existing festipal:// / exp:// entries
already cover the origin it emits; confirm the installed `@better-auth/expo` export name is `expo`
against node_modules at implementation time).

Create apps/api/test/signout-origin.spec.ts as a self-contained supertest integration spec (replicate
the tiny `signInWithOtp` / `readCapturedOtp` / `cookieHeaderFromSetCookie` helpers from
me-endpoints.spec.ts rather than editing that file — keep the gap-closure diff minimal; you MAY instead
factor them into a shared test helper module if you keep me-endpoints.spec.ts green). The spec:
  1. Boots the app via `createTestApp()`; signs in a throwaway `@festipal.dev` email via the real OTP
     round-trip to obtain a valid session cookie.
  2. POSTs `/api/auth/sign-out` with that session cookie AND an `expo-origin: festipal://` header and
     NO standard `origin` header (reproducing the real device request that `apps/mobile`'s expoClient
     makes) — asserts the response status is NOT 403 (and specifically not the
     MISSING_OR_NULL_ORIGIN/FORBIDDEN origin error). This is the direct proof the `expo()` plugin
     translated the header.
  3. Asserts GENUINE server-side revocation: a subsequent `GET /api/v1/me` with the SAME cookie now
     returns 401 (the session row was actually invalidated, not just the client cache).
  4. Negative control (proves the mechanism, on a SEPARATE fresh sign-in so it does not perturb the
     revocation assertion): POST `/api/auth/sign-out` with a session cookie but NEITHER an `origin` NOR
     an `expo-origin` header -> asserts 403, confirming origin-check is active on cookie-bearing
     requests and that the `expo-origin` header is exactly what makes request (2) pass.
Use throwaway `randomUUID()` emails and follow the existing specs' cleanup discipline (better-auth owns
the user/session rows; the randomUUID email keeps runs isolated).
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api typecheck && pnpm --filter @festipal/api lint && pnpm --filter @festipal/api test</automated>
    <human-check>WINDOWS #11 (device UAT): on a real device, tap the Festivals logout icon -> Welcome; confirm the just-revoked session can no longer authorize a protected call; airplane-mode logout still reaches Welcome; fast double-tap does not double-fire or crash.</human-check>
  </verify>
  <acceptance_criteria>
- `@better-auth/expo` is a declared dependency of apps/api (grep apps/api/package.json) and `expo()`
  appears in the plugins array of apps/api/src/auth/auth.instance.ts alongside `emailOTP`.
- apps/api/test/signout-origin.spec.ts exists and, under `pnpm --filter @festipal/api test`: the
  `expo-origin`+cookie sign-out returns a non-403 status; the post-sign-out GET /me returns 401; the
  no-origin negative-control sign-out returns 403.
- The full apps/api suite (previously 42 tests) plus the new spec pass; typecheck + lint stay clean.
  </acceptance_criteria>
  <done>authClient.signOut() is accepted by the server (no 403) and genuinely revokes the session server-side, proven headlessly; AUTH-04's failed truth passes (WINDOWS id 2 resolved).</done>
</task>

<task type="auto">
  <name>Task 2: Apply the registered weight-specific font keys across the eight restyled screens (ADR-015 design-fidelity)</name>
  <files>apps/mobile/lib/fonts-context.tsx, apps/mobile/app/_layout.tsx, apps/mobile/app/(auth)/index.tsx, apps/mobile/app/(auth)/email.tsx, apps/mobile/app/(auth)/verify.tsx, apps/mobile/app/(profile-setup)/complete-profile.tsx, apps/mobile/app/festivals/index.tsx, apps/mobile/components/OtpBoxes.tsx, apps/mobile/components/ResendCountdown.tsx, apps/mobile/components/AvatarTile.tsx</files>
  <read_first>
    - apps/mobile/lib/fonts.ts (self-adjacent — the exact `resolveFontFamily(family, fontsLoaded)` signature and the `FONT_DISPLAY` / `FONT_BODY` / `FONT_MONO` registered keys; the NON-BLOCKING CONTRACT comment)
    - apps/mobile/app/_layout.tsx (self — how the root already calls `useAppFonts()` and passes `fontsLoaded` to SplashView via `resolveFontFamily(FONT_DISPLAY, fontsLoaded)`; SplashView at lines 233-262 is the exact inline-style pattern to mirror; the routed provider tree at lines 206-223 is where FontsReadyProvider wraps)
    - packages/ui/src/tokens.ts (the `typeRoles` table — each role's `family` is one of `fontFamilies.display` / `.body` / `.mono`; use this to map each role to the correct FONT_* key)
    - apps/mobile/app/(auth)/index.tsx (representative screen — shows StyleSheet.create holding the generic family entry that must move inline)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Typography — the binding 11-role type contract, ADR-015)
    - .planning/phases/04-visitor-auth-profile-completion/04-VERIFICATION.md (Additional Cross-Cutting Findings #2 — the exact defect + prescribed fix + the eight affected files)
    - .planning/WINDOWS.md (id 15 — the tracked follow-up this task resolves)
  </read_first>
  <action>
Create apps/mobile/lib/fonts-context.tsx exposing a React context that carries the already-computed
font-readiness boolean: `FontsReadyProvider` (props `{ ready: boolean; children }`) and a
`useFontsReady(): boolean` hook (defaulting to `false` outside a provider). This shares the single
root `useAppFonts()` result so no screen re-registers the fonts. In apps/mobile/app/_layout.tsx, wrap
the routed provider tree (the QueryClientProvider/I18nProvider/Stack returned when `bootstrapped`) in
`<FontsReadyProvider ready={fontsLoaded}>`; do NOT touch the `bootstrapped` splash gate, the SplashView
(it keeps its own `fontsLoaded` prop), or any auth/deep-link/timeout effect (Pitfall 5 / D-04 — the
splash contract is unchanged).

Across all EIGHT files, replace every style entry of the shape `fontFamily` set to a generic
`typeRoles.<role>.family` value with `fontFamily: resolveFontFamily(<FONT_KEY>, fontsReady)`, where
`<FONT_KEY>` is chosen by the role's family category (read from tokens.ts):
  - roles whose family is the display family (wordmark, display2, title2) -> `FONT_DISPLAY`
  - roles whose family is the body family (title3, bodyStrong, body, bodySm, label, micro) -> `FONT_BODY`
  - roles whose family is the mono family (otpDigit, countdown) -> `FONT_MONO`
Because `fontsReady` is a runtime value it cannot live in the module-level `StyleSheet.create` object:
move the `fontFamily` out of the StyleSheet entry and apply it inline at each `<Text>` (or equivalent)
via a style array — `style={[styles.x, { fontFamily: resolveFontFamily(FONT_BODY, fontsReady) }]}` —
exactly as _layout.tsx's SplashView already does. Read `const fontsReady = useFontsReady()` once per
component. Keep the role's `size` / `weight` / `lineHeight` entries in the StyleSheet unchanged (only
the family moves). Import `FONT_DISPLAY` / `FONT_BODY` / `FONT_MONO` / `resolveFontFamily` from
`../lib/fonts` (adjust relative depth per file) and `useFontsReady` from the new context module. Add no
new user-facing strings (no Lingui impact). Documented tradeoff to record in the SUMMARY: the three
FONT_* constants carry one weight per family, so semibold display roles resolve to the bold display
file — this matches the verification-prescribed fix; a future weight-granular pass can register the
additional semibold/medium keys already loaded in useAppFonts if pixel-exact weight is later required.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint && pnpm --filter @festipal/mobile test && ( ! grep -REn 'typeRoles\.[A-Za-z0-9]+\.family' apps/mobile/app apps/mobile/components )</automated>
    <human-check>WINDOWS #15 (device UAT): on a real device the three brand fonts visibly render on Welcome / Email / Verify / complete-profile / Festivals and the OTP-box / countdown / avatar components — not the system-font fallback.</human-check>
  </verify>
  <acceptance_criteria>
- A repo-wide search for the generic family pattern (regex `typeRoles\.[A-Za-z0-9]+\.family`) over
  `apps/mobile/app` + `apps/mobile/components` returns ZERO matches (the final `( ! grep ... )` clause
  in the automated command succeeds only when there are none).
- apps/mobile/lib/fonts-context.tsx exports `FontsReadyProvider` + `useFontsReady`; _layout.tsx wraps
  the routed tree in `FontsReadyProvider ready={fontsLoaded}` and leaves the `bootstrapped` splash gate
  and every existing effect untouched (no new useFonts()-based splash gate).
- Each of the eight screens/components reads `useFontsReady()` and applies
  `resolveFontFamily(FONT_DISPLAY|FONT_BODY|FONT_MONO, fontsReady)` inline for its text families.
- `pnpm --filter @festipal/mobile typecheck` + `lint` + `test` (the existing 19-test suite) all pass;
  no new user-facing string was added.
  </acceptance_criteria>
  <done>The eight restyled screens apply the registered weight-specific font keys so the brand fonts actually render; the generic-name pattern is gone; lint/typecheck/tests green (WINDOWS id 15 resolved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → better-auth (sign-out) | Cookie-bearing, state-changing session-revocation request crossing from the Expo app (sends `expo-origin`, not `origin`) into the server auth surface |
| external origin → origin-check middleware | The CSRF/origin trust decision on every cookie-bearing better-auth call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07-S | Spoofing | origin translation via expo() plugin | medium | mitigate | The `expo()` plugin only maps `expo-origin` -> `origin`; the existing origin-check still validates the translated origin against the unchanged `trustedOrigins` allowlist (festipal:// / exp://), so a forged origin outside the allowlist is still rejected — no new trust is introduced |
| T-4-07-E | Elevation of Privilege | session not revoked server-side (current defect) | high | mitigate | Installing the server plugin makes sign-out actually reach the revoke path (proven by the headless test asserting a subsequent GET /me returns 401), closing the window where a still-valid session token survived to its 90-day expiry after a visual logout |
| T-4-07-I | Information Disclosure | origin-check disabled/loosened by mistake | high | accept-with-guard | Prohibition: MUST NOT weaken origin-check or wildcard trustedOrigins; the negative-control test asserts a no-origin cookie request still 403s, so an accidental loosening would fail CI |
| T-4-07-SC | Tampering | new npm install (@better-auth/expo) | high | mitigate | Package is the official `@better-auth/expo`, already an approved apps/mobile dependency (Phase 4 package-legitimacy audit) pinned to the same 1.6.25 as `better-auth`; no new/unaudited package enters the tree |
</threat_model>

<verification>
- `pnpm --filter @festipal/api typecheck && lint && test` green, including the new
  apps/api/test/signout-origin.spec.ts (non-403 sign-out + post-sign-out GET /me == 401 + no-origin
  negative control == 403).
- `pnpm --filter @festipal/mobile typecheck && lint && test` green; the generic-family regex over
  apps/mobile/app + apps/mobile/components returns zero matches.
- WINDOWS ids 2 and 15 marked fixed by the executor (`gsd-tools windows fixed 2` / `fixed 15`) after
  acceptance passes.
- Manual (device backstops): WINDOWS #11 (logout revokes + offline + double-tap) and #15 (fonts
  visibly render) — recorded in the SUMMARY, not headlessly runnable.
</verification>

<success_criteria>
AUTH-04's server-side session revocation genuinely works (sign-out accepted, session invalidated),
proven by a headless integration test; and the eight restyled Phase-4 screens apply the registered
weight-specific font keys so the brand typography renders per the binding 04-UI-SPEC (ADR-015). No
regression of D-01/D-02/D-03/D-04 or the non-blocking splash contract.
</success_criteria>

<output>
Create `.planning/phases/04-visitor-auth-profile-completion/04-07-SUMMARY.md` when done.
</output>
