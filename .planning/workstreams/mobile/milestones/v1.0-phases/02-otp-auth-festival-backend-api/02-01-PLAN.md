---
phase: 02-otp-auth-festival-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/vitest.config.ts
  - apps/api/test/setup.ts
  - apps/api/src/config/env.ts
  - apps/api/.env.example
  - packages/contracts/src/schemas.ts
  - packages/contracts/src/router.ts
  - packages/db/package.json
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup:
  - service: neon-postgres
    why: "Integration tests + seed run against a live Postgres (Phase 1 already provisioned it)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard -> Connection Details -> Pooled connection string"
      - name: DATABASE_URL_UNPOOLED
        source: "Neon Dashboard -> Connection Details -> Direct (unpooled) connection string"
      - name: BETTER_AUTH_SECRET
        source: "Generate a strong random value: `openssl rand -base64 32` (env-only, never commit)"

must_haves:
  truths:
    - "The full ts-rest contract surface (getMe, completeProfile, usernameAvailability, listFestivals, saveFestival, listMyFestivals) exists in packages/contracts under the single c.router({...}, { pathPrefix: '/api/v1' }) call (SC-6)"
    - "Every new contract request/response schema composes on a drizzle-zod base (.pick/.omit/.extend) from @festipal/db — none is a hand-redeclared z.object of a DB shape (SC-6, Pitfall 6)"
    - "better-auth's own OTP routes are absent from the contract (SC-6)"
    - "`env` is a single memoized module-level export from config/env.ts; BETTER_AUTH_SECRET is required, RESEND_API_KEY is optional (SC-5, no third raw loadEnv call site)"
    - "vitest + supertest + @nestjs/testing are installed and `pnpm --filter @festipal/api exec vitest run` executes (Wave 0 harness for every later spec)"
  artifacts:
    - apps/api/vitest.config.ts
    - apps/api/test/setup.ts
    - packages/contracts/src/router.ts
    - apps/api/src/config/env.ts
    - apps/api/.env.example
  key_links:
    - "packages/contracts schemas import drizzle-zod bases (visitorProfileInsertSchema/visitorProfileSelectSchema/myFestivalSelectSchema) from @festipal/db/schema"
    - "auth.instance.ts (Plan 02) and every service consume the memoized `env` singleton, not loadEnv()"
  prohibitions:
    - "No committed secret: BETTER_AUTH_SECRET / RESEND_API_KEY appear in .env.example as empty placeholders only, never with a real value"
---

<objective>
Lay the Phase 2 foundation with zero behavior yet: install the auth + email + test
dependencies, harden the env loader, stand up the vitest integration harness every
later plan's `<automated>` verify depends on, and publish the complete `/api/v1`
contract surface (interface-first) so the Wave 2/3 expansion plans implement handlers
against a frozen, drizzle-zod-derived shape without re-touching the shared contract files.

Purpose: front-load the shared, high-contention artifacts (contract router/schemas,
env, test harness) so expansion plans stay parallel and conflict-free.
Output: installed deps, `env` singleton, vitest harness, full ts-rest contract.
</objective>

<artifacts_produced>
- apps/api runtime deps: `better-auth@1.6.25`, `@thallesp/nestjs-better-auth@^2.7.0`, `resend@^6.18.1`
- apps/api dev deps + harness: `vitest`, `supertest`, `@types/supertest`, `@nestjs/testing`; `apps/api/vitest.config.ts`, `apps/api/test/setup.ts`; `test` script
- packages/db dev dep `tsx` + `db:seed` script entry (script file itself lands in Plan 04)
- `apps/api/src/config/env.ts`: memoized `env` export; `BETTER_AUTH_SECRET` required; `RESEND_API_KEY`, `DATABASE_URL_UNPOOLED`, `OTP_EMAIL_TRANSPORT` added
- `apps/api/.env.example`: placeholders for the new vars
- `packages/contracts` contract endpoints: `getMe`, `completeProfile`, `usernameAvailability`, `listFestivals`, `saveFestival`, `listMyFestivals`
- `packages/contracts` schemas: `meSchema`, `completeProfileBodySchema`, `usernameAvailabilitySchema`, plus reuse of `festivalSchema` / `visitorProfilePublicSchema`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-otp-auth-festival-backend-api/02-CONTEXT.md
@.planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md
@.planning/phases/02-otp-auth-festival-backend-api/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install auth/email/test deps + stand up the vitest integration harness</name>
  <read_first>
    - apps/api/package.json (current deps; add runtime + dev deps)
    - packages/db/package.json (add `tsx` devDependency + `db:seed` script entry)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md ("Validation Architecture" Wave 0 Gaps + Installation + version-verification block; note better-auth is already pinned at 1.6.25 in packages/db)
    - packages/db/src/client.ts (createDatabase signature the test setup reuses for a test connection)
  </read_first>
  <action>
    Install into apps/api runtime deps: `better-auth@1.6.25` (match the existing packages/db pin — do NOT bump), `@thallesp/nestjs-better-auth@^2.7.0`, `resend@^6.18.1`. Install into apps/api devDependencies: `vitest`, `supertest`, `@types/supertest`, `@nestjs/testing`. Add `tsx` to packages/db devDependencies and a `"db:seed": "tsx scripts/seed.ts"` script entry (the scripts/seed.ts file is created in Plan 04). Use `pnpm --filter @festipal/api add ...` and `pnpm --filter @festipal/db add -D tsx`. Add `"test": "vitest run"` to apps/api package.json scripts. Create `apps/api/vitest.config.ts` with a Node test environment (no DOM), `globals: true`, and `setupFiles: ['./test/setup.ts']`. Create `apps/api/test/setup.ts` exporting a helper that builds a Drizzle client via `createDatabase(process.env.DATABASE_URL_UNPOOLED ?? process.env.DATABASE_URL)` for integration specs, plus a helper to build a NestJS app via `@nestjs/testing` Test.createTestingModule against AppModule (used by later specs). Note the `@thallesp/nestjs-better-auth` peer floor is Node >=22.22.1 — see precondition.
  </action>
  <precondition>Node runtime satisfies `@thallesp/nestjs-better-auth`'s engine floor (>=22.22.1); if `node -v` is lower the executor must halt and ask the user to upgrade Node before installing the wrapper (peer floor, not a soft warning).</precondition>
  <verify>
    <automated>pnpm --filter @festipal/api exec vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @festipal/api exec vitest run --passWithNoTests` exits 0 (harness boots, zero specs is not an error)
    - `apps/api/package.json` lists `better-auth`, `@thallesp/nestjs-better-auth`, `resend` in dependencies and `vitest`, `supertest`, `@nestjs/testing`, `@types/supertest` in devDependencies
    - `packages/db/package.json` has a `tsx` devDependency and a `db:seed` script
    - `apps/api/vitest.config.ts` and `apps/api/test/setup.ts` exist
  </acceptance_criteria>
  <done>vitest runs green with no specs; all deps present; harness helpers exported.</done>
</task>

<task type="auto">
  <name>Task 2: Harden env.ts into a memoized singleton with the auth/email vars</name>
  <read_first>
    - apps/api/src/config/env.ts (current schema — extend, keep one loader)
    - apps/api/src/config/config.module.ts (ENV provider that calls loadEnv — stays working)
    - apps/api/.env.example (extend placeholders; read first, do not clobber existing keys)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md ("New: loadEnv() gains a third call site this phase" — memoize to avoid a 3rd raw call site)
  </read_first>
  <action>
    Extend `envSchema` in env.ts: make `BETTER_AUTH_SECRET` required (`z.string().min(1)`, drop `.optional()`); add `RESEND_API_KEY: z.string().optional()`, `DATABASE_URL_UNPOOLED: z.string().url().optional()`, and `OTP_EMAIL_TRANSPORT: z.enum(['dev','resend']).default('dev')`. Keep `loadEnv()` as-is but add `export const env = loadEnv();` as a memoized module-level singleton so auth.instance.ts (Plan 02) imports `env` rather than adding a third raw `loadEnv()` call. Leave config.module.ts's ENV provider consuming the schema transparently. Append placeholder lines to apps/api/.env.example for `RESEND_API_KEY=`, `DATABASE_URL_UNPOOLED=`, `OTP_EMAIL_TRANSPORT=dev` and ensure `BETTER_AUTH_SECRET=` is present as an empty placeholder — never a real value.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/config/env.ts` exports both `loadEnv` and a memoized `env` constant; `Env` type includes `BETTER_AUTH_SECRET` (required), `RESEND_API_KEY`, `DATABASE_URL_UNPOOLED`, `OTP_EMAIL_TRANSPORT`
    - Typecheck passes: `pnpm --filter @festipal/api exec tsc --noEmit` exits 0
    - `apps/api/.env.example` contains empty placeholders for `BETTER_AUTH_SECRET`, `RESEND_API_KEY`, `DATABASE_URL_UNPOOLED`, `OTP_EMAIL_TRANSPORT` and no real secret values
  </acceptance_criteria>
  <done>env is a single memoized source with the auth/email vars; typecheck green; .env.example has placeholders only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish the full /api/v1 contract surface (interface-first, drizzle-zod-derived)</name>
  <behavior>
    - `contract.getMe` GET /me responds 200 with `{ accountId, email, profile }` where `profile` is `VisitorProfilePublic | null`
    - `contract.completeProfile` POST /me/complete-profile has a body derived from `visitorProfileInsertSchema` and responds 200 (profile) or 409 (username conflict)
    - `contract.usernameAvailability` GET /me/username-availability?username=... responds 200 `{ available: boolean }`
    - `contract.listFestivals` GET /festivals responds 200 with an array of the existing `festivalSchema` (D-04 minimal fields, no pagination)
    - `contract.saveFestival` POST /festivals/:festivalId/save responds 200 (idempotent) and 404 (unknown festival)
    - `contract.listMyFestivals` GET /me/festivals responds 200 with an array of `festivalSchema` (caller-scoped)
  </behavior>
  <read_first>
    - packages/contracts/src/router.ts (existing c.router shape — add to the SAME router, keep pathPrefix '/api/v1')
    - packages/contracts/src/schemas.ts (existing festivalSchema, visitorProfilePublicSchema composed on drizzle-zod base)
    - packages/db/src/schema/visitor-profile.ts (visitorProfileInsertSchema/visitorProfileSelectSchema exports)
    - packages/db/src/schema/my-festival.ts (myFestivalSelectSchema export)
    - .planning/phases/02-otp-auth-festival-backend-api/02-PATTERNS.md (router.ts + schemas.ts analog sections)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md (A4 GET /me shape assumption + Open Question 1)
  </read_first>
  <action>
    In schemas.ts add: `meSchema = z.object({ accountId: z.string(), email: z.string().email(), profile: visitorProfilePublicSchema.nullable() })` (A4 default — `profile: null` discriminates "needs profile"; document this choice inline as the sensible technical default per Open Question 1); `completeProfileBodySchema = visitorProfileInsertSchema.pick({ username: true, displayName: true, avatar: true })` (compose on the drizzle-zod base, never hand-redeclare); `usernameAvailabilitySchema = z.object({ available: z.boolean() })`. Reuse existing `festivalSchema` for browse/my-festivals responses. In router.ts add the six endpoints to the existing `c.router({...}, { pathPrefix: '/api/v1' })` — do NOT open a second router: `getMe` (GET /me, 200 meSchema), `completeProfile` (POST /me/complete-profile, body completeProfileBodySchema, 200 visitorProfilePublicSchema, 409 errorSchema), `usernameAvailability` (GET /me/username-availability, query `{ username: z.string() }`, 200 usernameAvailabilitySchema), `listFestivals` (GET /festivals, 200 z.array(festivalSchema)), `saveFestival` (POST /festivals/:festivalId/save, pathParams `{ festivalId: z.string().uuid() }`, 200 z.object({ saved: z.literal(true) }), 404 errorSchema), `listMyFestivals` (GET /me/festivals, 200 z.array(festivalSchema)). Do NOT add any /api/auth/* route — better-auth's OTP routes stay out of the contract. Export any new types via z.infer as the file already does.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/contracts exec tsc --noEmit && pnpm --filter @festipal/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @festipal/contracts exec tsc --noEmit` exits 0 and `pnpm --filter @festipal/contracts build` succeeds
    - `grep -v '^\s*//' packages/contracts/src/router.ts` shows all six endpoint keys under one `c.router` call and NO `/auth` path string
    - `completeProfileBodySchema` is derived via `visitorProfileInsertSchema.pick(...)` (source assertion: import of `visitorProfileInsertSchema` present, no hand-written `z.object` mirroring visitor_profile columns)
    - `meSchema.profile` is `.nullable()`
  </acceptance_criteria>
  <done>Full contract surface compiles and builds, derived from drizzle-zod bases, with better-auth routes excluded.</done>
  <reversibility rating="costly">The response Zod shapes (meSchema, festival list shape) are published to future clients (D-04); adding fields later is safe, renaming/removing breaks derived clients — freeze the shape now, keep additive.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → environment | Secrets (BETTER_AUTH_SECRET, RESEND_API_KEY) must live only in env, never in committed files |
| contract → clients | Published Zod response shapes become a hard-to-reverse contract for Phase 3+ clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .env.example / committed config | high | mitigate | Only empty placeholders in .env.example; BETTER_AUTH_SECRET required at runtime via env, asserted by the Zod loader (Task 2 acceptance greps for absence of real values) |
| T-02-02 | Tampering | contract shape drift | medium | mitigate | Every schema composed on drizzle-zod bases so a DB column rename is a compile error, not silent drift (Pitfall 6) |
| T-02-SC | Tampering | npm installs (better-auth, wrapper, resend) | high | mitigate | RESEARCH Package Legitimacy Audit approved all three (multi-M weekly downloads, official repos, Context7 High reputation, no postinstall); "too-new" verdict is a documented false positive — no [SLOP], no [SUS] requiring a human gate |
</threat_model>

<verification>
- `pnpm --filter @festipal/api exec vitest run --passWithNoTests` green
- `pnpm --filter @festipal/api exec tsc --noEmit` green
- `pnpm --filter @festipal/contracts exec tsc --noEmit && pnpm --filter @festipal/contracts build` green
- No secret values committed (inspect .env.example diff)
</verification>

<success_criteria>
Deps installed, vitest harness runs, env is a memoized singleton with required BETTER_AUTH_SECRET, and the complete drizzle-zod-derived /api/v1 contract compiles and builds with better-auth routes excluded.
</success_criteria>

<output>
Create `.planning/phases/02-otp-auth-festival-backend-api/02-01-SUMMARY.md` when done
</output>
