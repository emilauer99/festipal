---
phase: 02-otp-auth-festival-backend-api
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/api/src/me/me.controller.ts
  - apps/api/src/me/me.service.ts
  - apps/api/test/username-race.spec.ts
  - apps/api/test/me-endpoints.spec.ts
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "POST /api/v1/me/complete-profile inserts the visitor_profile row and returns 200 with the public profile (IDN-01 backend)"
    - "username-availability is advisory (a lower(username) SELECT) while complete-profile is the source of truth: a duplicate username insert is caught as Postgres 23505 and mapped to 409, not 500 (SC-5, Pitfall 11)"
    - "GET /api/v1/me/festivals returns only the caller's saved festivals, scoped by session.user.id — never by a request parameter (SC-3, SEC-02)"
    - statement: "Concurrent duplicate-username complete-profile calls: exactly one succeeds, the loser gets 409 (deterministic under the lower(username) unique index)"
      verification: backstop
  artifacts:
    - apps/api/src/me/me.service.ts
    - apps/api/src/me/me.controller.ts
    - apps/api/test/username-race.spec.ts
    - apps/api/test/me-endpoints.spec.ts
  key_links:
    - "me.service.completeProfile checks err.cause instanceof PostgresError && cause.code === '23505' (drizzle wraps the driver error in .cause)"
    - "me.service.listMyFestivals joins my_festival -> festival WHERE eq(myFestival.visitorId, session.user.id)"
  prohibitions:
    - "complete-profile must NOT trust the prior availability check as authoritative — it must independently catch 23505 and return 409"
    - "listMyFestivals must NOT accept a festivalId/visitorId request param for scope — scope comes only from the session"
---

<objective>
Expand the `me` module from the tracer's single GET /me into the three remaining
identity/membership reads/writes: first-login profile completion (TOCTOU-safe via the
DB unique index, not the advisory check), the advisory username-availability lookup, and
the caller-scoped "my festivals" list. Every write/read here is the pattern later content
phases copy, so the SEC-02 caller-scoping and the 23505->409 race guard must be exact.

Purpose: complete the visitor-identity surface with the race and scope invariants correct.
Output: complete-profile, username-availability, my-festivals handlers + their specs.
</objective>

<artifacts_produced>
- `me.service.ts`: `completeProfile(accountId, { username, displayName, avatar? })` (insert + 23505->conflict), `checkUsernameAvailability(username)` (lower(username) SELECT), `listMyFestivals(visitorId)` (scoped join)
- `me.controller.ts`: `@TsRestHandler` methods for `completeProfile` (200/409), `usernameAvailability` (200), `listMyFestivals` (200), each with `@Session()`
- `apps/api/test/username-race.spec.ts`, `apps/api/test/me-endpoints.spec.ts`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-otp-auth-festival-backend-api/02-CONTEXT.md
@.planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md
@.planning/phases/02-otp-auth-festival-backend-api/02-PATTERNS.md
@.claude/skills/nestjs-best-practices/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: me.service — completeProfile (23505->409), usernameAvailability, listMyFestivals</name>
  <behavior>
    - `completeProfile(accountId, input)` inserts visitor_profile and returns `{ status: 'ok', profile }`; a duplicate username returns `{ status: 'conflict' }` (never throws to a 500)
    - `checkUsernameAvailability(username)` returns `false` when a case-variant of the name already exists, `true` otherwise
    - `listMyFestivals(visitorId)` returns exactly the festivals joined via my_festival for that visitorId, in festivalSchema shape
  </behavior>
  <read_first>
    - apps/api/src/me/me.service.ts (getProfile from the tracer — extend this file)
    - apps/api/src/festival/festival.service.ts (the where(eq(...)) tenant-scoped query pattern + the festival->DTO mapping to mirror)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md (Pattern 3 verbatim: 23505 via err.cause instanceof PostgresError; "Cross-tenant denial query shape" for listMyFestivals)
    - packages/db/src/schema/visitor-profile.ts (visitorProfile table, lower(username) unique index name visitor_profile_username_lower_unq)
    - packages/db/src/schema/my-festival.ts (myFestival composite PK, visitorId -> visitor_profile.accountId)
    - packages/db/src/schema/festival.ts (festival + festivalLocale for the DTO)
  </read_first>
  <action>
    Extend me.service.ts. `completeProfile(accountId, input)`: `this.db.insert(visitorProfile).values({ accountId, ...input }).returning()` inside try/catch; in catch read `const cause = (err as { cause?: unknown }).cause;` and if `cause instanceof PostgresError && cause.code === '23505'` return `{ status: 'conflict' as const }`, else rethrow; on success return `{ status: 'ok' as const, profile: <picked public fields> }`. Import `PostgresError` from `postgres`. `checkUsernameAvailability(username)`: `SELECT` from visitorProfile `WHERE sql\`lower(${visitorProfile.username}) = lower(${username})\`` limit 1, return `available: rows.length === 0`. `listMyFestivals(visitorId)`: `this.db.select({ festival }).from(myFestival).innerJoin(festival, eq(festival.id, myFestival.festivalId)).where(eq(myFestival.visitorId, visitorId))` then map each row to the festivalSchema DTO (reuse the tracer/festival service's festival->DTO mapping incl. festivalLocale supportedLocales). The visitorId is always the caller's session.user.id — the method signature takes it as an argument, never reads a request param.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api exec vitest run test/username-race.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run test/username-race.spec.ts` passes (see Task 2 for the spec) — sequential check-available -> insert A -> insert B(same, case-variant) returns 409/conflict, not a thrown 500
    - Source assertion: completeProfile's catch checks `err.cause instanceof PostgresError && cause.code === '23505'` (not `err instanceof PostgresError`)
    - Source assertion: listMyFestivals's WHERE is `eq(myFestival.visitorId, visitorId)` and the method takes visitorId as a parameter (no festivalId/visitorId read from query/params)
  </acceptance_criteria>
  <done>Service methods implement the race-safe conflict mapping and caller-scoped my-festivals read.</done>
</task>

<task type="auto">
  <name>Task 2: me.controller handlers + username-race + me-endpoints specs</name>
  <read_first>
    - apps/api/src/me/me.controller.ts (getMe from the tracer — add the three handlers)
    - apps/api/src/festival/festival.controller.ts (the {status, body} branching shape for 404/200; mirror for 409/200)
    - packages/contracts/src/router.ts (completeProfile 200/409, usernameAvailability 200, listMyFestivals 200 signatures)
    - apps/api/test/setup.ts (Nest test-app + DB helpers) + apps/api/test/auth-guard-tracer.spec.ts (session-cookie acquisition pattern to reuse)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md (Pattern 2 @Session + @TsRestHandler)
  </read_first>
  <action>
    Add three `@TsRestHandler` methods to me.controller.ts, each with `@Session() session: UserSession`: `completeProfile` -> call `me.completeProfile(session.user.id, body)`; map `{ status:'ok' }` to `{ status: 200, body: profile }` and `{ status:'conflict' }` to `{ status: 409, body: { message: 'Username already taken' } }` (never throw). `usernameAvailability` -> `{ status: 200, body: { available } }` from `me.checkUsernameAvailability(query.username)`. `listMyFestivals` -> `{ status: 200, body: await me.listMyFestivals(session.user.id) }`. Write `apps/api/test/username-race.spec.ts`: seed one account, call checkUsernameAvailability('Foo') -> true, completeProfile(accountA, { username:'foo', ... }) -> ok, completeProfile(accountB, { username:'FOO', ... }) -> conflict/409 (proves lower(username) index + 23505 mapping). Write `apps/api/test/me-endpoints.spec.ts`: with a session cookie, GET /me before profile -> profile null; POST /me/complete-profile -> 200; GET /me -> profile populated; directly insert a my_festival row for the caller via the DB helper, then GET /me/festivals -> returns exactly that festival (do NOT depend on the save endpoint from Plan 04 — insert the row directly so this spec is self-contained).
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api exec vitest run test/username-race.spec.ts test/me-endpoints.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both specs pass: POST /api/v1/me/complete-profile returns 200 then a duplicate (case-variant) username returns 409
    - GET /api/v1/me/festivals with a session returns only the caller's directly-seeded my_festival rows
    - Handlers use {status, body} tuples (no thrown exceptions for the 409/expected cases)
  </acceptance_criteria>
  <done>All three me endpoints are wired, race-safe, and covered by green specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /me/complete-profile | Untrusted username; DB unique index is the authority, not the advisory check |
| session → my-festivals read | Scope derived only from session.user.id, never client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | username-availability TOCTOU | high | mitigate | lower(username) unique index is authoritative; completeProfile catches 23505 -> 409 (Pitfall 11); username-race.spec proves it |
| T-02-08 | Elevation of Privilege / Information Disclosure | listMyFestivals scope | high | mitigate | WHERE eq(myFestival.visitorId, session.user.id); no request param feeds scope; me-endpoints.spec asserts caller-only rows (SEC-02) |
| T-02-09 | Information Disclosure | completeProfile error surface | low | mitigate | Conflict returns a clean 409 message, not a raw DB error / stack (V7) |
</threat_model>

<verification>
- `pnpm --filter @festipal/api exec vitest run test/username-race.spec.ts test/me-endpoints.spec.ts` green
- `pnpm --filter @festipal/api exec tsc --noEmit` green
</verification>

<success_criteria>
complete-profile is the TOCTOU-safe source of truth (23505->409), username-availability is advisory, and my-festivals is strictly caller-scoped — all proven by green specs.
</success_criteria>

<output>
Create `.planning/phases/02-otp-auth-festival-backend-api/02-03-SUMMARY.md` when done
</output>
