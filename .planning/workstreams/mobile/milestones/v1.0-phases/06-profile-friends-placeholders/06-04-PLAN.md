---
phase: 06-profile-friends-placeholders
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/mobile/components/ListRow.tsx
  - apps/mobile/components/SettingsSwitch.tsx
  - apps/mobile/components/SoonToast.tsx
  - apps/mobile/app/_layout.tsx
autonomous: true
requirements: [PROF-01, FRND-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Es gibt GENAU EINEN kommt-bald-Mechanismus für die ganze App: einen kurzen, sich selbst schließenden Hinweis am unteren Rand, den Profil, Mehr und Friends gemeinsam nutzen (D-13) — kein Info-Sheet, keine zweite Lösung."
    - "Der Hinweis verdeckt die FloatingNav nie: er sitzt oberhalb von Navigationshöhe plus Navigationsabstand plus unterem Safe-Area-Inset."
    - "Ein neuer Aufruf ersetzt einen sichtbaren Hinweis sofort — es stapeln sich nie zwei."
    - "Der Hinweis wird von Screenreadern angesagt, weil er als höfliche Live-Region ausgezeichnet ist; ein rein visueller Hinweis wäre für Screenreader-Nutzende unsichtbar."
    - "ListRow und SettingsSwitch tragen beide Platzhaltermuster: gedämpft-statisch mit Bald-Badge für tote Zeilen, volle Deckkraft mit Chevron und Hinweis-Tap für Zeilen mit echten Daten ohne Bearbeitungsfunktion."
    - "Jedes accessibilityLabel der drei neuen Komponenten läuft über Lingui — die no-literal-string-Regel schließt dieses Attribut aus und fängt ein hartcodiertes Label NICHT."
    - "Keine der drei Komponenten enthält einen Farbwert auf Modulebene; Farben lösen pro Render über useTheme() auf und Styles entstehen in einem useMemo über createStyles(colors)."
    - "UI-SPEC #33: In der Schalterzeile schrumpft die Spalte aus Label und Beschreibung; der Schalter selbst hat eine feste Eigenbreite und wird nie zusammengedrückt."
    - "UI-SPEC #34: Das Label der Schalterzeile ist einzeilig, die Beschreibung höchstens zweizeilig; die Zeile wächst mindestens auf die Mindest-Tapgröße."
    - "UI-SPEC #37: Die Hinweis-Pille ist auf Bildschirmbreite abzüglich zweier Bildschirmränder begrenzt und sitzt über der FloatingNav."
    - "UI-SPEC #38: Der Hinweistext ist auf zwei Zeilen begrenzt und wird danach am Ende abgeschnitten."
  prohibitions: []
  artifacts:
    - path: "apps/mobile/components/ListRow.tsx"
      provides: "Wiederverwendbare Einstellungs-/Kontozeile mit Icon, Label, optionalem Wert, Chevron, Gefahr- und Deaktiviert-Variante"
      min_lines: 60
    - path: "apps/mobile/components/SettingsSwitch.tsx"
      provides: "Schalterzeile mit Label, optionaler Beschreibung und Deaktiviert-Variante"
      min_lines: 50
    - path: "apps/mobile/components/SoonToast.tsx"
      provides: "ToastProvider, useSoonToast-Hook und die Hinweis-Pille"
      min_lines: 70
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Montage des ToastProvider innerhalb des ThemeProvider"
      contains: "ToastProvider"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/components/SoonToast.tsx"
      via: "ToastProvider umschließt die Root-Navigation innerhalb des ThemeProvider"
      pattern: "<ToastProvider"
    - from: "apps/mobile/components/SoonToast.tsx"
      to: "apps/mobile/lib/theme-context.tsx"
      via: "die Pille löst ihre Farben über useTheme() auf, weshalb sie innerhalb des ThemeProvider hängen muss"
      pattern: "useTheme\\(\\)"
---

<objective>
Die drei geteilten Bausteine, auf denen die Screens der Wellen 3 und 4 aufsetzen: eine
Einstellungs-/Kontozeile, eine Schalterzeile und der eine gemeinsame kommt-bald-Hinweis.

Umgesetzte Entscheidung: **D-13** — „Kommt bald" ist ein Toast/Snackbar und ein EINZIGER
Mechanismus, ein eigener kleiner Baustein auf RN-Primitiven (ADR-022), von Profil, Mehr und
Friends gemeinsam genutzt. Ausdrücklich NICHT das Info-Sheet des Designs und ausdrücklich nicht
sechs verschiedene Lösungen.

Purpose: Diese Phase hat sehr viele tote Zeilen. Ohne einen einzigen, vorab gebauten Baustein
erfindet jeder Screen sein eigenes Feedback, und die Platzhalter-Haltung dieser Phase zerfällt in
drei Dialekte.
Output: Drei neue Komponenten und ein Montagepunkt in der App-Wurzel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mobile/STATE.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-CONTEXT.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-PATTERNS.md
@.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-01-SUMMARY.md
</context>

## Artifacts this phase produces

Neue Symbole, die dieser Plan erzeugt (existieren im Repo noch NICHT):

- Dateien: `apps/mobile/components/ListRow.tsx`, `apps/mobile/components/SettingsSwitch.tsx`,
  `apps/mobile/components/SoonToast.tsx`
- Komponenten/Hooks: `ListRow`, `SettingsSwitch`, `SoonToast`, `ToastProvider`, `useSoonToast`

## Offener Punkt aus der UI-SPEC, hier aufgelöst

Die Spacing-Tabelle der UI-SPEC führt die Mindest-Tapgröße unter anderem für eine
„Toast-Dismiss-Fläche", während die Komponenten-Tabelle derselben Datei ausdrücklich festhält,
dass der Hinweis KEINE manuelle Schließfläche hat. Aufgelöst wie folgt, ohne Wertänderung: Der
Hinweis bekommt keine Schließfläche; die Mindest-Tapgröße gilt in dieser Phase für die
`ListRow`-Zeilen und die Schalterzeilen. Die Hinweis-Pille selbst ist nicht interaktiv.

<tasks>

<task type="auto">
  <name>Task 1: ListRow und SettingsSwitch — die zwei Zeilenbausteine für Mehr und Profil</name>
  <read_first>
    - `apps/mobile/components/ComingSoonTile.tsx` — der bestehende Präzedenzfall für „sichtbar tot": Dämpfung plus kleines Badge, inklusive der Badge-Styles und der Schriftrollenauflösung
    - `apps/mobile/app/(tabs)/festivals.tsx` — das Pressable-Idiom einer tappbaren Zeile und die Verwendung der Mindest-Tapgröße
    - `apps/mobile/components/FloatingNav.tsx` — wie ein `accessibilityLabel` korrekt über Lingui geführt wird und wie eine Komponente Styles und gedämpfte Farbe als Props statt über einen eigenen `useTheme()`-Aufruf erhält
    - `packages/ui/src/tokens.ts` — `spacingScale`, `layout`, `radiiScale`, `typeRoles`, die Statusfarben und ihre Text-Varianten
    - `apps/mobile/lib/fonts.ts` — `fontFamilyForRole`
    - `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md` — §Component Inventory, §Placeholder Pattern Contract, §Spacing &amp; Layout Scale, §Typography
  </read_first>
  <files>apps/mobile/components/ListRow.tsx, apps/mobile/components/SettingsSwitch.tsx</files>
  <action>
Beide Komponenten sind eigene RN-Primitiven-Bausteine auf geteilten Tokens (ADR-022) — kein
Fremd-UI-Kit, keine Fremdkomponente.

1. `apps/mobile/components/ListRow.tsx` NEU. Props: ein Icon aus `lucide-react-native`, ein
   Pflicht-Label, ein optionaler rechtsbündiger Wert, ein Gefahr-Flag, ein Deaktiviert-Flag und ein
   optionaler Press-Handler. Aufbau der Zeile: Icon (22 Pixel, gedämpfte Textfarbe, bei gesetztem
   Gefahr-Flag die Gefahr-TEXT-Variante — nie die bare Gefahr-Hue als Text, das ist die bestehende
   Statushue-Regel), dann das Label in der Rolle `bodyStrong`, dann optional der Wert in `bodySm`
   gedämpft, dann ein `ChevronRight` — der Chevron erscheint NUR, wenn ein Press-Handler gesetzt
   ist. Mindesthöhe ist die Mindest-Tapgröße aus den Layout-Tokens. Das Label ist einzeilig, der
   Wert ist einzeilig und schrumpft zuerst, während das Label seine Layoutpriorität behält.
   Deaktiviert-Flag heißt: Deckkraft 0,45 plus ein kleines statisches Bald-Badge in der Rolle
   `micro` mit stiller Füllung, exakt so wie `ComingSoonTile` es bereits rendert.
   `accessibilityRole` ist `button`, wenn ein Press-Handler gesetzt ist; `accessibilityState`
   spiegelt das Deaktiviert-Flag. Das `accessibilityLabel` kommt als Prop von außen und ist beim
   Aufrufer über Lingui gebildet — die Komponente selbst erfindet keinen Text.

2. `apps/mobile/components/SettingsSwitch.tsx` NEU. Props: ein Pflicht-Label, eine optionale
   Beschreibung, ein boolescher Wert, ein optionaler Änderungs-Handler und ein Deaktiviert-Flag.
   Aufbau: links eine Spalte aus Label in `bodyStrong` (einzeilig) und Beschreibung in `bodySm`
   gedämpft (höchstens zweizeilig), rechts der native RN-Schalter. Die Textspalte schrumpft, der
   Schalter behält seine Eigenbreite und wird nie zusammengedrückt. Die Schienenfarbe des
   Schalters ist im Aus-Zustand die stille Füllung und im Ein-Zustand die Markenfarbe, der
   Daumen ist in beiden Zuständen weiß — der eingeschaltete Dark-Mode-Schalter ist damit das
   einzige funktional lebendige akzentfarbene Bedienelement dieser Phase. Deaktiviert-Flag heißt
   auch hier Deckkraft 0,45 plus Bald-Badge, und der Schalter ist tatsächlich `disabled`, nicht
   nur optisch gedämpft.

3. Beide Komponenten lösen Farben pro Render über `useTheme()` auf und bauen ihre Styles in einem
   `useMemo` über eine `createStyles(colors)`-Funktion. Auf Modulebene steht KEIN Farbwert und kein
   `StyleSheet.create` mit Farben — hell ist der zuerst zu prüfende Fall. Schriftfamilien laufen
   über `fontFamilyForRole(role, fontsReady)`; ein numerisches Schriftgewicht auf einer echten
   Gewichtsdatei erzeugt Faux-Bold und ist verboten.
  </action>
  <acceptance_criteria>
    - `apps/mobile/components/ListRow.tsx` und `apps/mobile/components/SettingsSwitch.tsx` enthalten keinen rohen Farbwert — `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' apps/mobile/components/ListRow.tsx apps/mobile/components/SettingsSwitch.tsx` findet nichts außer der weißen Daumenfarbe des Schalters, die als Token oder als benannte Konstante gesetzt sein muss
    - Beide Dateien enthalten `createStyles(colors)` innerhalb eines `useMemo` — `grep -q 'useMemo(() => createStyles(colors)' apps/mobile/components/ListRow.tsx` exits 0
    - Kein `StyleSheet.create` auf Modulebene mit Farbreferenzen — `createStyles` ist die einzige Stelle, an der `colors.` in einem Stilobjekt vorkommt
    - Kein numerisches `fontWeight` in beiden Dateien — `grep -E 'fontWeight: *[0-9]' apps/mobile/components/ListRow.tsx apps/mobile/components/SettingsSwitch.tsx` findet nichts
    - Kein hartcodiertes Barrierefreiheits-Label — `grep -E 'accessibilityLabel="' apps/mobile/components/ListRow.tsx apps/mobile/components/SettingsSwitch.tsx` findet nichts
    - `pnpm --filter @quiks/mobile lint` exits 0 (deckt die no-literal-string-Regel für `components/` mit ab)
    - `pnpm --filter @quiks/mobile typecheck` exits 0
    - `pnpm --filter @quiks/mobile test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @quiks/mobile lint &amp;&amp; pnpm --filter @quiks/mobile typecheck &amp;&amp; pnpm --filter @quiks/mobile test &amp;&amp; ! grep -E 'fontWeight: *[0-9]' apps/mobile/components/ListRow.tsx apps/mobile/components/SettingsSwitch.tsx</automated>
  </verify>
  <done>Beide Zeilenbausteine existieren mit Gefahr-, Wert- und Deaktiviert-Variante, lösen Farben pro Render auf und bestehen Lint, Typecheck und Testsuite; ein Commit.</done>
</task>

<task type="auto">
  <name>Task 2: SoonToast — der eine gemeinsame kommt-bald-Hinweis samt Provider</name>
  <read_first>
    - `apps/mobile/lib/theme-context.tsx` — die Dreiteilung Context, Provider, Hook, die hier strukturell wiederholt wird, inklusive der never-throw-Semantik außerhalb eines Providers
    - `apps/mobile/app/_layout.tsx` — die Provider-Komposition um die Root-Navigation herum; der neue Provider hängt INNERHALB des ThemeProvider, damit die Pille Farben auflösen kann
    - `apps/mobile/components/FloatingNav.tsx` — die tatsächliche Höhe und Einbettung der Navigationsleiste, gegen die die Pille positioniert wird
    - `packages/ui/src/tokens.ts` — `layout.navHeight`, `layout.navInset`, `spacingScale`, `radiiScale`, die neutralen Füllungs- und Textrollen
    - `apps/mobile/components/ComingSoonTile.tsx` — Referenz für die neutrale, nicht markenfarbene Platzhalteroptik
    - `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-UI-SPEC.md` — §Component Inventory (SoonToast), §Copywriting Contract (das Muster „{Feature} kommt bald.")
  </read_first>
  <files>apps/mobile/components/SoonToast.tsx, apps/mobile/app/_layout.tsx</files>
  <action>
Im Repo existiert kein Toast- oder Snackbar-Mechanismus — das ist der eine wirklich neue
Baustein dieser Phase. Die VERDRAHTUNG hat ein exaktes Vorbild in `theme-context.tsx`; nur die
Optik und der Auto-Schließ-Timer sind neu. Niemals eine Fremdbibliothek (ADR-022).

1. `apps/mobile/components/SoonToast.tsx` NEU mit drei Exporten in genau der Struktur, die
   `theme-context.tsx` bereits vorgibt:
   - Ein Context, dessen Wert eine Funktion ist, die eine bereits lokalisierte Nachricht
     entgegennimmt.
   - `ToastProvider`: hält die aktuell sichtbare Nachricht in lokalem State, startet beim Anzeigen
     einen Timer von rund 2400 Millisekunden und blendet danach selbsttätig aus. Ein NEUER Aufruf
     ersetzt die sichtbare Nachricht sofort und startet den Timer neu — ein einziger Slot, es
     stapeln sich nie zwei Hinweise. Der laufende Timer wird beim Ersetzen und beim Unmount
     aufgeräumt, damit kein State-Update auf einer entfernten Komponente landet.
   - `useSoonToast()`: liefert die Anzeigefunktion. Außerhalb eines Providers wirft der Hook nicht,
     sondern liefert eine wirkungslose Funktion — dieselbe defensive Semantik, die `useTheme()`
     bereits hat.
   - Die Pille rendert am unteren Rand, horizontal zentriert, mit dem mittleren Radius, einer
     neutralen Füllung und Text in der Rolle `bodySm`. Ausdrücklich NICHT markenfarben — die
     Akzentfarbe ist in dieser Phase für Navigations-Pille, Avatar, Handle und den
     Dark-Mode-Schalter reserviert. Die vertikale Position ist die Summe aus Navigationshöhe,
     Navigationsabstand, unterem Safe-Area-Inset und einem kleinen Abstand aus der Spacing-Skala;
     damit liegt sie immer frei über der FloatingNav, auch auf Screens ohne Navigationsleiste.
     Die Breite ist auf Bildschirmbreite abzüglich zweier Bildschirmränder begrenzt, der Text auf
     zwei Zeilen mit Abschneiden am Ende.
   - Barrierefreiheit: Die Pille ist als höfliche Live-Region ausgezeichnet, damit Screenreader
     den Hinweis von selbst vorlesen — ein rein visueller Hinweis bliebe für sie unsichtbar. Sie
     ist nicht interaktiv und fängt keine Tap-Ereignisse ab.
   - Die Komponente enthält KEINEN Nachrichtentext. Die Formulierungen nach dem Muster
     „{Feature} kommt bald." bildet jeder Aufrufer über Lingui und übergibt sie fertig — dadurch
     bleibt der Baustein frei von Copy-Entscheidungen und die Kataloge sammeln die Sätze dort,
     wo sie inhaltlich hingehören.

2. `apps/mobile/app/_layout.tsx`: Den `ToastProvider` innerhalb des bestehenden `ThemeProvider`
   um die Root-Navigation legen — innerhalb, weil die Pille ihre Farben über `useTheme()` auflöst.
   Die bestehende Reihenfolge der übrigen Provider und die Statusleisten-Konfiguration bleiben
   unverändert. Die in Plan `06-01` ergänzte `Stack.Screen`-Registrierung für `profil` wird dabei
   nicht angerührt.
  </action>
  <acceptance_criteria>
    - `grep -q '<ToastProvider' apps/mobile/app/_layout.tsx` exits 0 und der Provider steht INNERHALB des ThemeProvider-Elements
    - Die in `06-01` ergänzte Registrierung bleibt bestehen — `grep -q 'name="profil"' apps/mobile/app/_layout.tsx` exits 0
    - `apps/mobile/components/SoonToast.tsx` enthält keinen nutzersichtbaren Textliteral — `grep -E '<Text[^>]*>[A-Za-zÄÖÜäöü]' apps/mobile/components/SoonToast.tsx` findet nichts (der Text kommt ausschließlich aus der Nachricht-Prop)
    - Die Position der Pille wird aus `layout.navHeight`, `layout.navInset` und dem unteren Safe-Area-Inset berechnet — `grep -q 'navHeight' apps/mobile/components/SoonToast.tsx` exits 0 und `grep -q 'navInset' apps/mobile/components/SoonToast.tsx` exits 0
    - Der Auto-Schließ-Timer wird aufgeräumt — die Datei enthält ein `clearTimeout` in der Aufräumfunktion des Effekts
    - Der Text ist auf zwei Zeilen begrenzt — `grep -q 'numberOfLines={2}' apps/mobile/components/SoonToast.tsx` exits 0
    - Die Pille ist als Live-Region ausgezeichnet — `grep -q 'accessibilityLiveRegion' apps/mobile/components/SoonToast.tsx` exits 0
    - `pnpm --filter @quiks/mobile lint` exits 0, `pnpm --filter @quiks/mobile typecheck` exits 0, `pnpm --filter @quiks/mobile test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @quiks/mobile lint &amp;&amp; pnpm --filter @quiks/mobile typecheck &amp;&amp; pnpm --filter @quiks/mobile test &amp;&amp; grep -q '<ToastProvider' apps/mobile/app/_layout.tsx &amp;&amp; grep -q 'accessibilityLiveRegion' apps/mobile/components/SoonToast.tsx</automated>
  </verify>
  <done>Ein einziger, app-weit montierter kommt-bald-Mechanismus mit Auto-Schließen, Einzel-Slot, Live-Region und garantiertem Freiraum über der FloatingNav; ein Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aufrufer → Hinweis-Pille | Der angezeigte Text stammt vom aufrufenden Screen; er wird als reiner Text gerendert und nie als Markup interpretiert. |
| Komponente → Nutzendenaufmerksamkeit | Ein Hinweis, der die Navigationsleiste verdeckt, blockiert die einzige globale Navigation der App. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Denial of Service | Hinweis verdeckt die FloatingNav | medium | mitigate | Die vertikale Position wird aus Navigationshöhe, Navigationsabstand und unterem Safe-Area-Inset berechnet; die Pille ist nicht interaktiv und fängt keine Taps ab. Akzeptanzkriterium prüft die Verwendung beider Layout-Token. |
| T-06-16 | Denial of Service | Ungeräumter Auto-Schließ-Timer nach Unmount oder Ersetzen | low | mitigate | Der Timer wird beim Ersetzen und beim Unmount aufgeräumt; ein Einzel-Slot verhindert, dass sich Timer stapeln. |
| T-06-17 | Information Disclosure | Beliebiger Text in der Hinweis-Pille | low | mitigate | Der Baustein enthält selbst keinen Text und rendert die übergebene Nachricht als reinen Text-Knoten; die Aufrufer übergeben ausschließlich statische, über Lingui gebildete Sätze — nie Serverdaten und nie Nutzereingaben. |
| T-06-18 | Repudiation | Screenreader-Nutzende bemerken den Hinweis nicht | low | mitigate | Die Pille ist als höfliche Live-Region ausgezeichnet, damit der Hinweis auch ohne Sicht angesagt wird. |
</threat_model>

<verification>
- `pnpm --filter @quiks/mobile lint`, `typecheck` und `test` grün
- Der ToastProvider hängt innerhalb des ThemeProvider und die Profil-Registrierung aus `06-01` ist unversehrt
- Keine rohen Farbwerte, keine numerischen Schriftgewichte, keine hartcodierten Barrierefreiheits-Labels in den drei neuen Dateien
</verification>

<success_criteria>
- Drei wiederverwendbare Bausteine, die Mehr, Profil und Friends ohne Dialektbildung teilen
- Genau ein kommt-bald-Mechanismus für die gesamte App
- Der Hinweis liegt garantiert frei über der FloatingNav und wird angesagt
</success_criteria>

<output>
Create `.planning/workstreams/mobile/phases/06-profile-friends-placeholders/06-04-SUMMARY.md` when done
</output>
