---
phase: 05-festival-selection-home
plan: 06
type: execute
wave: 4
depends_on: [05-03, 05-04, 05-05]
files_modified:
  - apps/mobile/app/(tabs)/festivals.tsx
autonomous: true
requirements: [FEST-01, FEST-02, FEST-03]
estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Festivals tab shows a Meine/Alle SegmentedControl; its initial value is driven by a typed `segment` search param — /festivals?segment=all opens on Alle, otherwise it defaults to Meine (the shared cross-tab contract the Home CTA and rail see-all use, REVIEW 05-06/05-07 HIGH); Meine lists the caller's saved festivals (GET /me/festivals), Alle lists every festival (GET /festivals), each as a FestivalCard with name/dates/place (FEST-01, FEST-02)."
    - "Saved festivals are visually distinguished in Alle via a client-derived savedIds Set computed from listMyFestivals membership (the 'Gespeichert' badge), not a server-side saved flag (FEST-02, Pattern 3)."
    - "Tapping Save on an unsaved card saves it in one tap via POST /festivals/:id/save; the mutationFn unwraps the ts-rest response with unwrapOk and THROWS on a non-200 status so React Query's onError actually fires and rolls back (REVIEW 05-06 HIGH — a raw ts-rest non-200 otherwise resolves as success). The optimistic me/festivals cache update initializes an absent/empty mine cache with a correctly-shaped { status: 200, body: [...] } response and dedupes by festival id (no double insert on rapid taps), then onSettled invalidation reconciles with the server, so the save persists server-side and survives app restart (FEST-03)."
    - "While a save is in flight the tapped card's Save affordance is disabled via the FestivalCard `saving` prop (no duplicate enqueue), and a save failure surfaces a localized error affordance (not a silent no-op) while the optimistic insert is rolled back (REVIEW 05-06 MEDIUM)."
    - "Each segment has its own empty state (Meine: 'Noch keine gespeicherten Festivals' + 'Zu Alle wechseln'; Alle: 'Noch keine Festivals' + 'Neue Festivals erscheinen hier.') per the Copywriting Contract."
    - "Tapping a card enters the festival gate-lessly (persist active slug + push /f/:slug); the logout header control stays on this screen (AUTH-04, UI-SPEC #8)."
    - { statement: "While either query is pending the screen shows the existing festivals loading-text pattern (not a new skeleton); on error it shows the existing network-error copy + retry.", verification: backstop }
    - { statement: "A rapid double-tap on Save for the same festival enqueues exactly one save and the optimistic cache contains the festival exactly once (idempotency/concurrency edge, FEST-03).", verification: backstop }
  artifacts:
    - apps/mobile/app/(tabs)/festivals.tsx (Meine/Alle segmented list + optimistic save, composed over FestivalCard + SegmentedControl)
  key_links:
    - "useLocalSearchParams segment=all -> SegmentedControl initial value 'alle' (Home CTA/rail target)"
    - "listMyFestivals -> savedIds Set -> FestivalCard saved prop"
    - "saveMutation unwrapOk(throws non-200) + optimistic setQueryData(festivalKeys.mine, dedupe) + invalidateQueries -> survives restart"
    - "in-flight festivalId -> FestivalCard saving prop -> Save disabled during flight"
    - "SegmentedControl value -> selects listMyFestivals vs listFestivals data source"
  prohibitions:
    - "Do NOT let the mutationFn return a raw ts-rest response; unwrap with unwrapOk and throw on non-200 so onError/rollback fires (REVIEW 05-06 HIGH)."
    - "Do NOT return `old` unchanged when the mine cache is absent; initialize it with a correctly-shaped { status: 200, body: [] } response before inserting (REVIEW 05-06 MEDIUM)."
    - "Do NOT append a festival to the optimistic mine cache without a dedupe-by-id guard (rapid taps / already-saved), and do NOT allow a second in-flight save for the same festival (REVIEW 05-06 MEDIUM)."
    - "Do NOT swallow a save failure silently; surface a localized error affordance after rollback (REVIEW 05-06 MEDIUM)."
    - "Do NOT add a server-side saved flag or a second scoped/unscoped browse variant; compute saved-state client-side (Pattern 3)."
    - "Do NOT track saved-state in a session-only local Set (the Phase-3 nicety); it must be server-backed via me/festivals (D-05, State of the Art)."
    - "Do NOT render ticket/countdown/wallet/Kommend-Vergangen UI (D-05)."
    - "Do NOT build an unsave action (UI-SPEC #11)."
    - "Do NOT hardcode user-facing strings; all copy via Lingui, DE catalog per the Copywriting Contract."
---

<objective>
Turn the moved Festivals screen into the real D-05 experience: a Meine/Alle segmented list (default
Meine) over the two existing endpoints, with saved festivals visually distinguished, one-tap
server-backed optimistic save, per-segment empty states, and gate-less entry — composed thinly over
the `FestivalCard` and `SegmentedControl` primitives and the existing query/error patterns.

Purpose: deliver FEST-01/02/03 as a thin client composition (no new endpoints), fixing Phase 3's
session-only save-state to a restart-surviving server-backed one.
Output: the finished `(tabs)/festivals.tsx`.
</objective>

<review_incorporation>
Cross-AI review (05-REVIEWS.md, Codex) findings addressed in THIS plan:
- **HIGH — ts-rest non-200 does not trigger onError.** INCORPORATED: Task 2's `mutationFn` calls
  `unwrapOk(response)` (from `lib/festival-queries.ts`, 05-03) which throws on `status !== 200`, so
  React Query's `onError` rollback actually runs.
- **MEDIUM — optimistic insert no-ops when the mine cache is undefined.** INCORPORATED: `onMutate`
  initializes an absent mine cache with a correctly-shaped `{ status: 200, body: [] }` ts-rest response
  before inserting.
- **MEDIUM — optimistic inserts need deduplication.** INCORPORATED: the insert dedupes by festival id
  and the Save affordance is disabled for the in-flight festival (FestivalCard `saving` prop), so rapid
  taps or an already-saved idempotent request cannot double-insert.
- **MEDIUM — no visible save-error handling.** INCORPORATED: `onError` rolls back AND surfaces a
  localized error affordance (session-expiry/404/network); the profile-required 409 is unreachable
  after the profile guard but is handled the same way.
- **HIGH (cross-plan) — cross-tab segment selection.** INCORPORATED: this screen reads a typed
  `segment` search param (`useLocalSearchParams<{ segment?: 'all' }>()`) to initialize the
  SegmentedControl (Alle when `segment=all`, else Meine) — the shared contract the 05-07 Home CTA/rail
  navigate into.
- **LOW — Vitest filter command may not match.** INCORPORATED: Task 2 verify targets the api
  `save-idempotency` spec by the workspace test script (no assumed `--` filter that the script may not
  forward); the mobile side runs typecheck + lint.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@.planning/phases/05-festival-selection-home/05-RESEARCH.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@apps/mobile/app/(tabs)/festivals.tsx
@apps/mobile/components/FestivalCard.tsx
@apps/mobile/components/SegmentedControl.tsx
@apps/mobile/lib/date-range.ts
@apps/mobile/lib/active-festival-storage.ts
@apps/mobile/lib/api-client.ts
</context>

## Artifacts this phase produces (this plan)

- `apps/mobile/app/(tabs)/festivals.tsx` — Meine/Alle segmented festival list with optimistic one-tap save, composed over `FestivalCard` + `SegmentedControl`.

<tasks>

<task type="auto">
  <name>Task 1: Meine/Alle segmented list with client-derived saved distinction</name>
  <files>apps/mobile/app/(tabs)/festivals.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/festivals.tsx (the moved screen from 05-05: existing listFestivals query, loading/error text, logout header, enter-by-slug + saveActiveFestivalSlug wiring — all to preserve)
    - .planning/phases/05-festival-selection-home/05-PATTERNS.md ((tabs)/festivals.tsx section: the listMyFestivals query addition, the savedIds Set, the FestivalCard extraction)
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Copywriting Contract (segment labels Meine/Alle, per-segment empty states) + ## UI Considerations festivals-list (per-segment empty/loading/error) + ## Visual Hierarchy (SegmentedControl as top anchor)
    - apps/mobile/components/SegmentedControl.tsx + FestivalCard.tsx (the primitives to compose; FestivalCard now takes a `saving` prop) and apps/mobile/lib/date-range.ts (locale for the caption)
    - apps/mobile/lib/festival-queries.ts (shared `festivalKeys` + `unwrapOk` from 05-03)
    - apps/mobile/lib/i18n.ts (active UI locale for formatDateRange)
    - expo-router `useLocalSearchParams` (the typed `segment` param that initializes the SegmentedControl)
  </read_first>
  <action>
    Rewrite (tabs)/festivals.tsx to render a `SegmentedControl` (options Meine/Alle, labels via Lingui)
    as the top anchor, with local state for the selected segment INITIALIZED from a typed search param:
    `const { segment } = useLocalSearchParams<{ segment?: string | string[] }>()`, normalize an array to
    its first value, then choose initial value `'alle'` when that value is exactly `'all'`, else `'meine'`
    (the shared `/festivals?segment=all` cross-tab contract the Home CTA/rail use; unknown or empty
    values fail closed to Meine,
    REVIEW 05-06/05-07 HIGH). Keep local state authoritative after mount so user segment taps still work;
    optionally sync when the param changes on re-navigation. Add a `listMyFestivals` query (`queryKey:
    festivalKeys.mine` i.e. `['me','festivals']`) alongside the existing `listFestivals` query
    (`festivalKeys.all` i.e. `['festivals']`), reusing the shared keys from `lib/festival-queries.ts`.
    Derive `savedIds = new Set((myFestivals.status === 200 ? myFestivals.body : []).map(f => f.id))`
    (guard the non-200 shape). Treat Meine as dependent only on `listMyFestivals`; treat Alle as ready
    only after BOTH `listFestivals` and `listMyFestivals` succeed, because rendering Alle before the
    caller-scoped membership query resolves would briefly mislabel saved rows as unsaved. If the mine
    query fails while Alle is selected, show the existing error+retry affordance and refetch the failed
    dependency rather than exposing incorrect Save buttons. Render the
    active segment's data as a vertical `FlatList` of `FestivalCard`s (Meine -> my-festivals data; Alle
    -> all-festivals data), passing `saved={savedIds.has(f.id)}`, `locale`, `onEnter` (persist slug +
    push /f/:slug), and `onSave`. Reuse the existing loading-text and network-error+retry patterns per
    segment (backstop — no new skeleton). Implement the two per-segment empty states from the
    Copywriting Contract: Meine empty -> title + a "Zu Alle wechseln" CTA that programmatically sets the
    segment to Alle (no navigation); Alle empty -> title + "Neue Festivals erscheinen hier." Keep the
    logout header control (Stack.Screen headerRight) exactly as-is (UI-SPEC #8). Keep
    `scrollBottomPad` clearance for the floating nav.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
    <manual>On device: Festivals tab opens on Meine by default; switch to Alle -> every festival listed, a saved one shows the 'Gespeichert' badge while unsaved ones show a Save affordance; both segments render name/dates/place.</manual>
  </verify>
  <done>The Festivals screen shows Meine/Alle (default Meine) over the two endpoints with a client-derived saved distinction and per-segment empty/loading/error states; typecheck + lint pass.</done>
  <acceptance_criteria>
    - The SegmentedControl defaults to Meine and switches the data source (me vs all).
    - `savedIds` is derived from `listMyFestivals`; no server-side saved flag is used.
    - Alle visually distinguishes saved festivals via the FestivalCard badge.
    - Both per-segment empty states render the Copywriting-Contract copy; the Meine empty CTA switches the segment without navigating.
    - typecheck + lint pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: One-tap optimistic server-backed save (FEST-03)</name>
  <files>apps/mobile/app/(tabs)/festivals.tsx</files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md Code Examples "Optimistic save + query invalidation" (onMutate/onError/onSettled shape) and 05-PATTERNS.md ((tabs)/festivals.tsx optimistic-save section)
    - apps/mobile/lib/festival-queries.ts (unwrapOk — throws on non-200; festivalKeys.mine/all)
    - apps/mobile/lib/api-client.ts (saveFestival signature: `{ params: { festivalId }, body: {} }`, idempotent server-side; note a non-200 ts-rest result RESOLVES unless unwrapped)
    - STATE.md decision "03-05: Save-state tracked client-side per session" (the nicety this plan upgrades to server-backed correctness)
  </read_first>
  <action>
    Replace any session-only save handling with a TanStack `useMutation` whose `mutationFn` calls
    `apiClient.saveFestival({ params: { festivalId }, body: {} })` and returns `unwrapOk(response)`
    (from `lib/festival-queries.ts`, 05-03) — `unwrapOk` THROWS a typed error on `status !== 200`, so a
    non-200 ts-rest result becomes a real rejection that reaches `onError` (REVIEW 05-06 HIGH; without
    this the mutation resolves as success and never rolls back). In `onMutate`: cancel
    `festivalKeys.mine` queries, snapshot the previous cache, then optimistically update — if the mine
    cache is absent, INITIALIZE it as a correctly-shaped `{ status: 200, body: [] }` ts-rest response
    (never return `old` unchanged, REVIEW 05-06 MEDIUM); look up the festival row from the
    `festivalKeys.all` cache and insert it into `body` ONLY if not already present (dedupe by `f.id`,
    REVIEW 05-06 MEDIUM). The all-list row must exist before calling `mutate`; if it cannot be resolved,
    do not send the request and surface the same localized generic save failure instead of creating a
    malformed optimistic entry. Maintain a `useRef<Set<string>>` in-flight guard: add the id synchronously
    in `handleSave` BEFORE calling `mutate`, return immediately when it is already present, mirror the set
    into render state for `FestivalCard saving`, and remove the id in `onSettled`. This synchronous guard
    is required because two native press events can arrive before React re-renders; a single mutation
    `variables` value is not sufficient and must not prevent concurrent saves of two different festivals.
    In `onError`: restore the exact snapshot; when the prior cache was absent, remove/reset the optimistic
    mine cache rather than retaining the synthesized entry. Also surface a localized generic save-error
    affordance for every transport/non-200 failure; the contract declares 404 and profile-required 409,
    while authentication middleware may also reject before the handler. Clear a prior save error on a
    new attempt or successful save. In `onSettled`: always remove the id from the in-flight guard and call
    `invalidateQueries({ queryKey: festivalKeys.mine })` so the server (source of truth for the idempotent save)
    reconciles the cache. Wire the FestivalCard `onSave` to this mutation. Because saved-state is derived
    from the mine cache, a successful save immediately flips the card to the 'Gespeichert' badge and —
    being server-backed — survives an app restart (FEST-03).
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint && pnpm --filter @festipal/api test</automated>
    <manual>On device: in Alle, tap Save on an unsaved festival -> it immediately shows 'Gespeichert' and appears under Meine; force-quit and relaunch -> it is still saved under Meine (FEST-03). A rapid double-tap enqueues one save and Meine lists it once. Simulate a failing save (dev: kill the API) -> the optimistic insert rolls back and a localized error affordance shows. Tapping Save reflects no error for an already-saved festival (idempotent).</manual>
  </verify>
  <done>One tap saves a festival optimistically and server-backed (mutationFn throws on non-200 so rollback works); it appears under Meine and survives restart; a double-tap yields one save; a failure rolls back with a visible error; the full api suite (incl. save idempotency) stays green; typecheck + lint pass.</done>
  <acceptance_criteria>
    - `mutationFn` returns `unwrapOk(response)` (throws on non-200) so `onError` fires on failure.
    - `onMutate` initializes an absent mine cache with `{ status: 200, body: [] }` and dedupes the insert by id.
    - A synchronous per-id in-flight Set prevents same-id re-entry before React re-renders while allowing different festivals to save concurrently; FestivalCard receives `saving` from it.
    - `onError` restores the exact prior cache (including the previously-absent case) and shows a localized error; `onSettled` always removes the per-id guard and invalidates mine.
    - Save uses `useMutation` with optimistic `festivalKeys.mine` update + rollback + `onSettled` invalidation.
    - A saved festival flips to the 'Gespeichert' badge and appears under Meine without a manual refetch.
    - `pnpm --filter @festipal/api test` (includes the save-idempotency spec) is green; typecheck + lint pass.
    - On-device: save survives force-quit-and-relaunch; double-tap yields one save; a failed save rolls back visibly.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | `listFestivals` (browse), `listMyFestivals` (caller-scoped), `saveFestival` (write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Information Disclosure | saved-state composition | medium | mitigate | Saved-state is computed client-side from the caller-scoped `listMyFestivals` (visitorId-scoped server-side, SEC-02) intersected with browse; no cross-caller data is introduced and no server-side saved flag widens the browse endpoint's scope |
| T-05-12 | Tampering | optimistic save cache | low | mitigate | `onSettled` always invalidates `['me','festivals']` so the server (idempotent `saveFestival`, incl. the profile-required 409) remains the source of truth; an optimistic insert that the server rejects is reconciled away |
| T-05-01 | Information Disclosure | Meine list (D-08 fields) | high | mitigate | Inherits the 05-01 cross-tenant isolation proof (`festival-isolation.spec.ts`): `listMyFestivals` only returns the caller's saved festivals' fields — no new server surface added here |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint` (both tasks)
- `pnpm --filter @festipal/api test` green (Task 2; includes `save-idempotency.spec.ts`)
- On-device: Meine/Alle default, saved distinction, one-tap save surviving restart
</verification>

<success_criteria>
- FEST-01/02/03 delivered: browse all with name/dates/place, Meine/Alle default Meine with saved distinction, one-tap server-backed save surviving restart.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-06-SUMMARY.md` when done.
</output>
