---
phase: 05.1-quiks-rename-ci-v1-0-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app.json
  - apps/api/src/auth/auth.instance.ts
  - apps/api/test/signout-origin.spec.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/lib/deep-link.ts
  - apps/mobile/lib/__tests__/deep-link.test.ts
  - apps/mobile/lib/avatar-storage.ts
  - apps/mobile/lib/active-festival-storage.ts
  - apps/mobile/app/(auth)/welcome.tsx
  - apps/mobile/eslint.config.mjs
  - package.json
  - apps/api/package.json
  - apps/mobile/package.json
  - packages/config/package.json
  - packages/contracts/package.json
  - packages/db/package.json
  - packages/i18n/package.json
  - packages/ui/package.json
  - pnpm-lock.yaml
  - apps/api/tsconfig.json
  - apps/mobile/tsconfig.json
  - packages/config/tsconfig.base.json
  - packages/contracts/tsconfig.json
  - packages/db/tsconfig.json
  - packages/i18n/tsconfig.json
  - packages/ui/tsconfig.json
  - apps/api/eslint.config.mjs
  - packages/contracts/eslint.config.mjs
  - packages/db/eslint.config.mjs
  - packages/i18n/eslint.config.mjs
  - packages/ui/eslint.config.mjs
  - apps/api/src/
  - apps/api/test/
  - apps/mobile/app/
  - apps/mobile/components/
  - apps/mobile/lib/
  - apps/mobile/metro.config.js
  - apps/mobile/plugins/with-windows-long-path-fix.js
  - packages/contracts/src/
  - packages/db/src/
  - packages/i18n/src/
  - packages/ui/src/tokens.ts
  - README.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .env.example
  - apps/api/.env.example
  - apps/mobile/.env.example
  - packages/db/.env.example
autonomous: true
requirements: [ADR-024]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Expo app's custom URL scheme and the API's better-auth trustedOrigins allowlist carry the same new scheme string after this plan, so login/sign-out keeps working and no deep link is orphaned (D-12, ADR-024)."
    - "The trustedOrigins allowlist contains exactly ONE app-scheme entry — the new one — and never both the old and new scheme simultaneously (T-05.1-02: an additive transition state is a permanently widened origin allowlist)."
    - "apps/api/test/signout-origin.spec.ts still fails a sign-out from an UNTRUSTED origin after its EXPO_ORIGIN constant moves to the new scheme, so the negative control proves the origin check rather than asserting against a dead string (RESEARCH Validation Architecture, Wave-0 gap 2)."
    - "All seven workspace packages resolve under the new npm scope and `pnpm install` regenerates a lockfile with no unresolved workspace: link (ADR-024, D-12)."
    - "The mobile eslint i18next exclusion list carries the NEW brand literal in the same commit as the new wordmark string, so the phase does not fail its own 'still passes lint' criterion (RESEARCH Pitfall 2)."
    - "The runtime scheme fallback constant in apps/mobile/app/_layout.tsx moves with app.json's scheme — it is covered by no automated test, so only an explicit edit closes it (RESEARCH Pitfall 3)."
    - "MMKV storage IDs switch to the new prefix as a hard cut with no migration shim; avatar URI and last-entered festival on existing dev installs are intentionally abandoned (D-14)."
    - "Both CLAUDE.md files describe the post-rename repo state, so no later session follows a workspace filter or a 'names intentionally stay old' instruction that is now false (RESEARCH Pitfall 8)."
    - "SplashView overflow: the new wordmark string is shorter than the one it replaces, so the single centred display-size wordmark needs no wrap or truncate handling (UI-SPEC E4/overflow)."
    - "`pnpm lint && pnpm typecheck && pnpm test` are all green at repo root after the rename, and no tracked file outside .planning/, docs/ and pnpm-lock.yaml still carries the old product name."
  artifacts:
    - apps/mobile/app.json (name/slug/scheme/ios.bundleIdentifier/android.package/expo-image-picker permission strings/extra._comment)
    - apps/api/src/auth/auth.instance.ts (trustedOrigins app-scheme entry + @quiks/db import)
    - apps/api/test/signout-origin.spec.ts (EXPO_ORIGIN constant on the new scheme)
    - apps/mobile/app/_layout.tsx (APP_SCHEME_FALLBACK + SplashView wordmark text)
    - apps/mobile/app/(auth)/welcome.tsx (wordmark text)
    - apps/mobile/eslint.config.mjs (config import + i18next words.exclude brand literal)
    - apps/mobile/lib/avatar-storage.ts + apps/mobile/lib/active-festival-storage.ts (MMKV storage IDs)
    - 7x package.json renamed to the @quiks/* scope (+ root package.json name) and every workspace:* reference updated
    - 7x tsconfig.json + 6x eslint.config.mjs pointing at @quiks/config
    - pnpm-lock.yaml regenerated
    - CLAUDE.md + .claude/CLAUDE.md + README.md + 4x .env.example updated to the new names
  key_links:
    - "apps/mobile/app.json `scheme` -> apps/api/src/auth/auth.instance.ts `trustedOrigins` -> better-auth origin check -> sign-out succeeds instead of 403 (D-12, the phase's hardest coupling)"
    - "apps/mobile/app.json `scheme` -> Constants.expoConfig.scheme -> apps/mobile/app/_layout.tsx APP_SCHEME_FALLBACK -> lib/deep-link.ts scheme comparison"
    - "package.json `name` field -> pnpm workspace: protocol resolution -> every import specifier in apps/* and packages/* -> Metro + tsup + tsc module resolution"
    - "apps/mobile/eslint.config.mjs i18next words.exclude -> the JSX wordmark literal in _layout.tsx SplashView and (auth)/welcome.tsx -> `pnpm lint` exit code"
---

<objective>
Move the entire codebase onto the quiks name in one wave: first the native-identity slice that
couples the Expo app scheme to the API's better-auth origin allowlist (the one coupling that breaks
authentication if it is split), then the full mechanical npm-scope / bundle-ID / MMKV / docs rename.

Purpose: ADR-024 forbids a partial rename because `scheme` <-> `trustedOrigins` and bundle-ID <->
native build are coupled. This plan is the "everything that carries the old NAME" plan; the CI
v1.0 colour/typography/light-mode work lives in plans 03-06 and builds on the renamed base.
Output: a repo where every tracked source, config, lockfile and instruction file names quiks, with
`pnpm lint && pnpm typecheck && pnpm test` green at root.

Requirement-ID substitution (visible, recorded): this phase has NO REQ-IDs in
`.planning/REQUIREMENTS.md`. Per the planning context, the ADR identifiers are used as the
traceability anchors instead — this plan carries `requirements: [ADR-024]`.

Spec-less probe fallback (visible, recorded skip): there is no plain `05.1-SPEC.md` for this phase,
so no `## Edge Coverage` / `## Prohibitions` lift applies and no probe predicates were generated
this run. The `05.1-UI-SPEC.md` `## UI Considerations` lift DOES apply and is carried in the
`must_haves.truths` of the plan that owns each surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-CONTEXT.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-PATTERNS.md
@docs/DEVELOPMENT_DECISIONS.md
</context>

## Artifacts this phase produces (plan 01 share)

New symbols / files created or renamed by THIS plan — the drift-verification pass must treat these
as newly created, not as pre-existing symbols that drifted:

| Kind | Symbol / path | Note |
|---|---|---|
| package name | `@quiks/api`, `@quiks/mobile`, `@quiks/config`, `@quiks/contracts`, `@quiks/db`, `@quiks/i18n`, `@quiks/ui` | replaces the 7 `@festipal/*` names |
| package name | `quiks` (root, private) | replaces the root package name |
| bundle id | `at.quiks.app` (ios.bundleIdentifier + android.package) | replaces `at.festipal.app` |
| URL scheme | `quiks` (app.json `scheme`), `quiks://` (auth trustedOrigins entry) | replaces the old scheme pair |
| constant | `APP_SCHEME_FALLBACK = 'quiks'` (apps/mobile/app/_layout.tsx) | value change on an existing constant |
| MMKV id | `quiks-avatar`, `quiks-active-festival` | hard cut, no migration shim (D-14) |
| lint literal | `'quiks'` in apps/mobile/eslint.config.mjs i18next `words.exclude` | replaces the old brand literal |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end native identity — app scheme, bundle ID and the better-auth origin allowlist move together</name>
  <reversibility rating="costly">Undoing the bundle-ID change means renaming back plus a full native rebuild and reinstall on every dev device; no store listing or EAS project exists yet, so no published contract breaks.</reversibility>
  <precondition>The local docker compose Postgres stack is running and healthy (`docker compose ps` shows the postgres service Up/healthy) — apps/api's Vitest suite connects to it via apps/api/test/setup.ts and cannot run without it.</precondition>
  <files>apps/mobile/app.json, apps/api/src/auth/auth.instance.ts, apps/api/test/signout-origin.spec.ts, apps/mobile/app/_layout.tsx, apps/mobile/lib/deep-link.ts, apps/mobile/lib/__tests__/deep-link.test.ts</files>
  <read_first>
    - apps/mobile/app.json (full file — the `extra._comment` field documents the coupling this task honours)
    - apps/api/src/auth/auth.instance.ts (full file — trustedOrigins array and the `advanced.disableOriginCheck` note)
    - apps/api/test/signout-origin.spec.ts (full file — the EXPO_ORIGIN constant and the negative control it drives)
    - apps/mobile/app/_layout.tsx lines 100-230 (APP_SCHEME_FALLBACK, the deep-link capture site, the dev-client route filter)
    - apps/mobile/lib/deep-link.ts (full file — doc-comment example URLs and isIgnorableDeepLinkRoute)
    - apps/mobile/lib/__tests__/deep-link.test.ts (existing scheme-string expectations)
    - docs/DEVELOPMENT_DECISIONS.md ADR-024 (the rename checklist and its coupling notes)
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md § Common Pitfalls 3
  </read_first>
  <action>
    Move the app's native identity and the server-side origin allowlist in ONE commit. Concrete
    target values, all lowercase, all in this task:

    - `apps/mobile/app.json`: `expo.name` -> `quiks`, `expo.slug` -> `quiks`, `expo.scheme` ->
      `quiks`, `expo.ios.bundleIdentifier` -> `at.quiks.app`, `expo.android.package` ->
      `at.quiks.app`. Rewrite both `expo-image-picker` permission sentences so they name quiks
      instead of the old product name (keep the sentences otherwise identical — they are shipped
      user-facing permission copy). Update the prose in `expo.extra._comment` so it states the new
      scheme/bundle-ID pair and keeps documenting the scheme <-> trustedOrigins coupling.
      Do NOT touch `userInterfaceStyle`, the icon paths, `adaptiveIcon.backgroundColor`, the
      `expo-build-properties` block or `_devOnlyCleartextComment` — plan 03 and plan 06 own those.
    - `apps/api/src/auth/auth.instance.ts`: replace the single app-scheme entry in `trustedOrigins`
      with `'quiks://'`. Exactly one app-scheme entry — do NOT keep the old one alongside it, and do
      NOT add a wildcard for it. The two `exp://` entries stay untouched. Update the trailing
      comment so it still points at app.json as the sync partner. Leave the `@festipal/db` import
      alone here; Task 2 does the scope rename repo-wide in one pass.
    - `apps/api/test/signout-origin.spec.ts`: move the hardcoded EXPO_ORIGIN constant to the new
      scheme. The spec's negative control (an untrusted origin must still be rejected) must keep
      asserting rejection — do not weaken it into a positive-only test.
    - `apps/mobile/app/_layout.tsx`: set `APP_SCHEME_FALLBACK` to `quiks` and update the two nearby
      comments that quote the old scheme in example URLs. Leave SplashView, the wordmark text and
      every colour reference untouched (Task 2 owns the wordmark text, plan 03 owns the colours).
    - `apps/mobile/lib/deep-link.ts`: update the doc-comment example URLs to the new scheme. The
      dev-client route filter (`expo-development-client`, `_expo`, `--`) is scheme-independent and
      must not change behaviour.
    - `apps/mobile/lib/__tests__/deep-link.test.ts`: update any test input URL that hardcodes the
      old scheme so the suite exercises the real runtime string.

    Do not run `pnpm install` in this task — the workspace names are unchanged until Task 2.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/api test 2>&1 | tail -20</automated>
    <automated>pnpm --filter ./apps/mobile test 2>&1 | tail -20</automated>
    <automated>node -e "const a=require('./apps/mobile/app.json').expo;if(a.scheme!=='quiks'||a.ios.bundleIdentifier!=='at.quiks.app'||a.android.package!=='at.quiks.app')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./apps/mobile/app.json').expo.scheme)"` prints `quiks`
    - `node -e "const a=require('./apps/mobile/app.json').expo;console.log(a.ios.bundleIdentifier,a.android.package)"` prints `at.quiks.app at.quiks.app`
    - `apps/api/src/auth/auth.instance.ts` contains the exact string `'quiks://'` inside the `trustedOrigins` array
    - `grep -c "festipal://" apps/api/src/auth/auth.instance.ts` returns 0 <!-- planner-discipline-allow: festipal:// -->
    - `grep -c "festipal://" apps/api/test/signout-origin.spec.ts` returns 0 <!-- planner-discipline-allow: festipal:// -->
    - `apps/mobile/app/_layout.tsx` contains `const APP_SCHEME_FALLBACK = 'quiks';`
    - `pnpm --filter ./apps/api test` exits 0 with signout-origin.spec.ts green (the untrusted-origin case still rejected)
    - `pnpm --filter ./apps/mobile test` exits 0 (deep-link tests included)
    - `expo.extra._comment` names the new scheme and bundle ID and still states the scheme <-> trustedOrigins coupling
  </acceptance_criteria>
  <done>The app's scheme, both bundle IDs, the runtime scheme fallback, the deep-link doc/test URLs and the server's trusted-origin entry all name quiks in one commit; the API suite (including the sign-out origin negative control) and the mobile suite are green.</done>
</task>

<task type="auto">
  <name>Task 2: Full monorepo scope rename — packages, imports, configs, MMKV IDs, wordmark string and instruction files</name>
  <reversibility rating="costly">Undoing means a second mechanical pass over ~85 files plus a lockfile regeneration; nothing is migrated or published, so no data or contract is lost.</reversibility>
  <files>package.json, apps/api/package.json, apps/mobile/package.json, packages/config/package.json, packages/contracts/package.json, packages/db/package.json, packages/i18n/package.json, packages/ui/package.json, pnpm-lock.yaml, apps/api/tsconfig.json, apps/mobile/tsconfig.json, packages/config/tsconfig.base.json, packages/contracts/tsconfig.json, packages/db/tsconfig.json, packages/i18n/tsconfig.json, packages/ui/tsconfig.json, apps/api/eslint.config.mjs, apps/mobile/eslint.config.mjs, packages/contracts/eslint.config.mjs, packages/db/eslint.config.mjs, packages/i18n/eslint.config.mjs, packages/ui/eslint.config.mjs, apps/api/src/, apps/api/test/, apps/mobile/app/, apps/mobile/components/, apps/mobile/lib/, apps/mobile/metro.config.js, apps/mobile/plugins/with-windows-long-path-fix.js, packages/contracts/src/, packages/db/src/, packages/i18n/src/, packages/ui/src/tokens.ts, README.md, CLAUDE.md, .claude/CLAUDE.md, .env.example, apps/api/.env.example, apps/mobile/.env.example, packages/db/.env.example</files>
  <read_first>
    - The output of `git grep -n -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml'` — this is the authoritative work list (89 tracked files at plan time; re-run it, do not trust the count)
    - apps/mobile/eslint.config.mjs (full file — line ~1 config import AND the i18next `words.exclude` brand literal at line ~60)
    - apps/mobile/lib/avatar-storage.ts and apps/mobile/lib/active-festival-storage.ts (the two MMKV storage-ID constants and their doc comments)
    - apps/mobile/app/_layout.tsx lines 415-457 (SplashView wordmark JSX) and apps/mobile/app/(auth)/welcome.tsx lines 25-45 (the second wordmark render)
    - apps/mobile/metro.config.js (the workspace-resolution comment naming the scope glob)
    - apps/mobile/plugins/with-windows-long-path-fix.js (the MARKER constant written into generated native files)
    - CLAUDE.md and .claude/CLAUDE.md (both contain a now-false "names intentionally stay old until the rename phase" instruction and stale `pnpm --filter` examples — RESEARCH Pitfall 8)
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-PATTERNS.md § "7x package.json" and § Shared Patterns
  </read_first>
  <action>
    One mechanical, grep-driven pass. Rename `@festipal/` -> `@quiks/` and the standalone product
    name -> `quiks` everywhere in tracked source, config and instruction files, EXCLUDING
    `.planning/`, `docs/` (ADR-024 keeps historical design assets and ADR text as history) and
    `pnpm-lock.yaml` (regenerated, not hand-edited). Case-sensitive `at.festipal.app` was already
    handled in Task 1; do not re-edit app.json here.

    Specifics that a blind search-replace would get wrong or miss:

    - Package names: the 7 workspace `package.json` `name` fields become `@quiks/api`,
      `@quiks/mobile`, `@quiks/config`, `@quiks/contracts`, `@quiks/db`, `@quiks/i18n`,
      `@quiks/ui`; the ROOT `package.json` `name` becomes `quiks` (unscoped, private). Every
      `"@festipal/X": "workspace:*"` dependency/devDependency line in every package.json moves with
      them.
    - `packages/config/tsconfig.base.json` and the 6 per-package `tsconfig.json` `extends`
      specifiers; the 6 `eslint.config.mjs` `import base from '@festipal/config/eslint'` lines.
    - `apps/mobile/eslint.config.mjs` i18next `words.exclude`: the brand literal in that list must
      become `quiks`. This MUST land in the same commit as the wordmark string change below or the
      lint rule flags the new brand literal as an untranslated string (RESEARCH Pitfall 2).
    - Wordmark string: `apps/mobile/app/_layout.tsx` SplashView renders the brand name as a JSX text
      literal followed by a separately-coloured period; `apps/mobile/app/(auth)/welcome.tsx` renders
      the same pattern. Both text literals become `quiks` (all lowercase, trailing period stays its
      own nested Text). Do NOT change any style, colour or font reference in either file — plan 03
      owns the mode-dependent splash. Update the SplashView doc comment that quotes the old
      wordmark.
    - MMKV storage IDs (D-14, hard cut, NO migration shim, no dual-read fallback):
      `apps/mobile/lib/avatar-storage.ts` -> `quiks-avatar`,
      `apps/mobile/lib/active-festival-storage.ts` -> `quiks-active-festival`. Update the doc
      comments that name the old IDs.
    - `apps/mobile/plugins/with-windows-long-path-fix.js` MARKER comment string.
    - `apps/mobile/metro.config.js` workspace-resolution comment (the scope glob it names).
    - `packages/ui/src/tokens.ts` file header: it currently cites the old product name and the old
      brand-system doc as its binding source. Rewrite the header to name quiks and cite ADR-023 +
      `docs/brand/quiks-ci-v1.md`. Do NOT change any token VALUE here — plan 03 owns the CI swap.
    - `README.md`, `CLAUDE.md`, `.claude/CLAUDE.md`: rename package/bundle references and workspace
      filter examples; DELETE the now-false instruction stating that package/bundle names
      deliberately stay on the old name until the rename phase runs, replacing it with a statement
      that ADR-024 has landed. Also update `.claude/CLAUDE.md`'s "Key Dependencies" / "Workspace
      Scripts" entries.
    - `.env.example` files (root, apps/api, apps/mobile, packages/db): update the connection strings
      and prose to the new database user/password/name. If the tool layer refuses to read or write
      `.env*` paths in this environment, do NOT skip silently: record the exact files and the exact
      intended replacement lines in the task SUMMARY and hand them to plan 05.1-02's human-action
      checkpoint, which already covers the non-tracked `.env` files.
    - Do NOT rename anything under `docs/concept/designs/` or any historical asset filename
      (ADR-024 explicitly keeps those as history), and do NOT edit `.planning/`.

    Then run `pnpm install` at the repo root so `pnpm-lock.yaml` is regenerated against the new
    package names, and commit the regenerated lockfile.

    Windows/Metro note (project memory): stop any running Expo/Metro watcher before `pnpm install`,
    or the install fails ENOENT on pnpm's temp directories.
  </action>
  <verify>
    <automated>pnpm install 2>&1 | tail -15</automated>
    <automated>pnpm lint 2>&1 | tail -20</automated>
    <automated>pnpm typecheck 2>&1 | tail -20</automated>
    <automated>pnpm test 2>&1 | tail -30</automated>
    <automated>test "$(git grep -l -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -l -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml' | wc -l` returns `0` <!-- planner-discipline-allow: festipal -->
    - `node -e "console.log(require('./package.json').name)"` prints `quiks`
    - `pnpm -r exec node -e "console.log(require('./package.json').name)"` prints only names beginning `@quiks/` (7 lines)
    - `pnpm install` exits 0 and `git status --porcelain pnpm-lock.yaml` shows the lockfile as modified and staged
    - `pnpm lint` exits 0 (this is the gate RESEARCH Pitfall 2 predicts failing if the i18next exclusion literal is missed)
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0 across all packages
    - `grep -c "quiks-avatar" apps/mobile/lib/avatar-storage.ts` returns at least 1, and `grep -rc "migrat" apps/mobile/lib/avatar-storage.ts apps/mobile/lib/active-festival-storage.ts` shows no dual-read/migration helper was added (D-14 hard cut)
    - `apps/mobile/eslint.config.mjs` `words.exclude` array contains `'quiks'`
    - Neither CLAUDE.md nor .claude/CLAUDE.md still instructs future sessions to keep the old package/bundle names
  </acceptance_criteria>
  <done>Every tracked file outside .planning/, docs/ and the lockfile names quiks; `pnpm install` regenerated the lockfile; root lint, typecheck and test are all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile app -> API `/api/auth/*` | The Expo client's requests carry the app scheme as origin; better-auth's origin/CSRF check is the gate |
| OS deep-link handler -> app router | An externally-supplied URL enters the app through the custom scheme and is replayed as a route |
| npm registry / lockfile -> developer machine | `pnpm install` resolves and executes package lifecycle scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-01 | Spoofing | `apps/mobile/app.json` scheme x `apps/api/src/auth/auth.instance.ts` trustedOrigins | high | mitigate | Both move in Task 1, one commit; `apps/api/test/signout-origin.spec.ts` keeps a negative control that an untrusted origin is rejected, and its EXPO_ORIGIN moves with the scheme so the control asserts against a live string |
| T-05.1-02 | Spoofing | `trustedOrigins` array | high | mitigate | Exactly ONE app-scheme entry after the change; acceptance criterion greps that the old scheme string is absent from auth.instance.ts, so an additive both-schemes transition state cannot ship |
| T-05.1-03 | Tampering | `apps/mobile/lib/deep-link.ts` route filter | medium | mitigate | The dev-client/underscore route filter is scheme-independent and explicitly must not change behaviour in Task 1; the mobile deep-link suite is a task gate |
| T-05.1-04 | Information Disclosure | old-bundle-ID install left on the dev device with old MMKV data | low | accept | Dev-only device, single developer; the old app is uninstalled before device acceptance (plan 05.1-07) — ADR-024 predicted the data loss and D-14 accepts it |
| T-05.1-SC | Tampering | `pnpm install` after the scope rename | high | mitigate | No new package is added in this plan; the rename only changes local workspace `name` fields, and the regenerated lockfile is reviewed as part of the commit. The only new external dependency in the phase (`@resvg/resvg-js`) is audited in 05.1-RESEARCH.md § Package Legitimacy Audit (verdict OK / Approved, no [ASSUMED]/[SUS]/[SLOP]) and installed in plan 05.1-06 |

Schema-push gate: not applicable to this plan. The `festipal` occurrences under `packages/db/src/schema/` are doc comments and import specifiers only — no column, table, index or enum definition changes — so no `db:push`/`db:generate` is required and none is planned. (Verified at plan time via `git grep -n -i festipal -- packages/db/src/schema`.)

API-coverage checkpoint: no external API integration. This phase renames identifiers and swaps
design tokens inside an existing stack; the deterministic detector returned `detected: false`.
</threat_model>

<verification>
1. `pnpm install && pnpm lint && pnpm typecheck && pnpm test` all exit 0 at repo root.
2. `git grep -l -i festipal -- . ':!.planning' ':!docs' ':!pnpm-lock.yaml'` returns nothing. <!-- planner-discipline-allow: festipal -->
3. `apps/api/test/signout-origin.spec.ts` is green with its EXPO_ORIGIN on the new scheme and its untrusted-origin case still rejected.
4. `apps/mobile/app.json` scheme, ios.bundleIdentifier and android.package all carry the new values, and `auth.instance.ts` carries exactly one matching app-scheme trustedOrigins entry.
</verification>

<success_criteria>
- The repo builds, lints, typechecks and tests green under the quiks name.
- Authentication's origin check is provably intact (negative control green) rather than assumed.
- No transitional state ships in which both the old and the new scheme are trusted.
</success_criteria>

<output>
Create `.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-01-SUMMARY.md` when done.
</output>
