---
phase: quick-260810-q31
plan: 01
subsystem: docs
tags: [branding, adr, design-system, i18n-agnostic-docs]

requires: []
provides:
  - "docs/brand/ as the binding brand source (README.md index + quiks-ci-v1.md rendition + token-mapping)"
  - "ADR-023 (quiks CI v1.0) and ADR-024 (rename festipal -> quiks) in docs/DEVELOPMENT_DECISIONS.md"
  - "Superseded pointer inside ADR-015 pointing at ADR-023"
  - "Superseded markers in docs/concept/03-design-system.md (top + §2/§6/§7) pointing at ADR-023 and docs/brand/quiks-ci-v1.md"
  - "Both CLAUDE.md files (root + .claude/) name quiks, state the four brand facts, and the do-not-rename-yet rule for @festipal/*"
affects: [design-tokens-phase, rename-phase, ui-implementation-phases]

actuals:
  tokens: 4670
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Superseded-marker pattern: new ADR does not rewrite an old ADR, it appends a one-line pointer and the old doc gets inline blockquote markers at each affected section — history stays byte-identical, current truth is one hop away"

key-files:
  created:
    - docs/brand/README.md
    - docs/brand/quiks-ci-v1.md
  modified:
    - docs/DEVELOPMENT_DECISIONS.md
    - docs/concept/03-design-system.md
    - docs/concept/README.md
    - docs/GIT_CONVENTIONS.md
    - CLAUDE.md
    - .claude/CLAUDE.md
    - docs/quiks_CI.html (newly tracked, unchanged content)

key-decisions:
  - "docs/quiks_CI.html stays at its current path (no move) and is committed as-is; docs/brand/ holds the rendered, readable rendition instead"
  - "Token-Mapping table in quiks-ci-v1.md names the REAL role keys from packages/ui/src/tokens.ts (primary, primaryPress, secondary, bgApp, surfaceCard, textPrimary, glassFill, danger, info) so the follow-up token phase has a 1:1 target list"
  - "Every derived value (press states, tints, rgba forms not literally in the CI extract) is marked 'abgeleitet — im Token-Phase zu bestätigen', per house rule 4"
  - "ADR-015 is not rewritten — it receives exactly one appended supersede pointer line before ADR-016; all its historical sentences are unchanged (D-03)"
  - "CLAUDE.md and .claude/CLAUDE.md keep their existing English prose tone (established file language, precedent already mixes English prose with German domain terms like 'Lageplan'); the German CI role names (Beere/Amber/Papier/Sunset) are kept verbatim as proper nouns rather than translated, matching how docs/brand/quiks-ci-v1.md and ADR-023 use them"

patterns-established:
  - "Pattern: new brand/design truth lives in docs/brand/, never overwrites the historical concept doc — the concept doc gets inline 'Abgelöst (ADR-0NN)' blockquotes at the point of divergence instead"

requirements-completed: [Q31-REBRAND-DOCS]

coverage:
  - id: D1
    description: "docs/brand/ created as the binding brand source (README.md index + quiks-ci-v1.md verbatim CI rendition with 10 sections incl. token-mapping and open points)"
    requirement: "Q31-REBRAND-DOCS"
    verification:
      - kind: other
        ref: "test -f docs/brand/README.md && test -f docs/brand/quiks-ci-v1.md && grep -q E8559F/FFC53D/F7F5F2/surfaceCard/lightColors/abgeleitet docs/brand/quiks-ci-v1.md (Task 1 <verify>, ran and passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "docs/quiks_CI.html tracked in git at its unchanged path as the visual original"
    requirement: "Q31-REBRAND-DOCS"
    verification:
      - kind: other
        ref: "git ls-files --error-unmatch docs/quiks_CI.html (Task 1 <verify>, ran and passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "ADR-023 (quiks CI v1.0) and ADR-024 (rename festipal -> quiks) added to docs/DEVELOPMENT_DECISIONS.md in ADR-022's format; ADR-015 gets a one-line supersede pointer, otherwise unchanged"
    requirement: "Q31-REBRAND-DOCS"
    verification:
      - kind: other
        ref: "grep -q '^### ADR-023 —' and '^### ADR-024 —', sed-scoped checks for ADR-023 mention inside ADR-015's block, lightColors inside ADR-023, trustedOrigins + festipal-active-festival inside ADR-024 (Task 2 <verify>, ran and passed)"
        status: pass
    human_judgment: false
  - id: D4
    description: "docs/concept/03-design-system.md carries a top marker plus inline markers on §2/§6/§7 pointing at ADR-023 and docs/brand/quiks-ci-v1.md; concept/README.md and GIT_CONVENTIONS.md carry minimal factual brand corrections; both CLAUDE.md files name quiks, the four brand facts, and the do-not-rename-yet rule"
    requirement: "Q31-REBRAND-DOCS"
    verification:
      - kind: other
        ref: "sed-scoped grep for ADR-023 in §2/§6/§7 blocks, head checks on 03-design-system.md/README.md/GIT_CONVENTIONS.md, grep for docs/brand/quiks-ci-v1.md + E8559F + @festipal/ in CLAUDE.md, grep for ADR-023/ADR-024 in .claude/CLAUDE.md (Task 3 <verify>, ran and passed)"
        status: pass
    human_judgment: false
  - id: D5
    description: "D-02 held mechanically for the whole plan: zero non-documentation files (apps/, packages/, docker-compose.yml, package.json, pnpm-lock.yaml) changed"
    requirement: "Q31-REBRAND-DOCS"
    verification:
      - kind: other
        ref: "test -z \"$(git status --porcelain -- apps packages docker-compose.yml package.json pnpm-lock.yaml)\" (whole-plan verification check 1, ran and passed after all three tasks)"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-08-10
status: complete
---

# Quick Task 260810-q31: Rebrand festipal → quiks CI v1.0 in Docs Summary

**Rendered the unreadable 695 KB quiks CI v1.0 Claude-Design bundle into binding German repo docs (`docs/brand/`), two new ADRs (ADR-023 CI, ADR-024 rename), superseded-markers on the old design doc, and brand facts in both `CLAUDE.md` files — documentation only, zero source/config files touched.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3/3 completed
- **Files modified:** 8 documentation files + 1 newly tracked HTML bundle (9 total, matching the plan's `files_modified` allowlist exactly)

## Accomplishments

- Created `docs/brand/README.md` (index) and `docs/brand/quiks-ci-v1.md` (verbatim CI v1.0 rendition: logo, color, typography, iconography, modes, application, plus a Token-Mapping table against the real `packages/ui/src/tokens.ts` role names and a list of open follow-up items) — `docs/quiks_CI.html` tracked unchanged as the visual original.
- Added ADR-023 (quiks CI v1.0: Beere/Amber, Sunset gradient, hell-first, `quiks.` wordmark) and ADR-024 (rename festipal → quiks incl. technical identifiers, with the full coupling-constraint checklist) to `docs/DEVELOPMENT_DECISIONS.md`; ADR-015 received exactly one appended supersede pointer, no rewrite.
- Marked `docs/concept/03-design-system.md` (top + §2 Farben/§6 Ikonografie/§7 Theming-Vertrag) as partially superseded by ADR-023, with the historical text left intact; corrected `docs/concept/README.md` and `docs/GIT_CONVENTIONS.md` titles to quiks; updated both `CLAUDE.md` files with the quiks name, the four brand facts, and the explicit do-not-rename-`@festipal/*`-yet rule.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create docs/brand/ as the binding brand source (D-02)** - `7dd2f0d` (docs)
2. **Task 2: ADR-023 (quiks CI v1.0) + ADR-024 (Rename) + supersede pointer in ADR-015 (D-01, D-03)** - `4231959` (docs)
3. **Task 3: Superseded markers + agent instructions point at the new brand source (D-01, D-02, D-03)** - `b898985` (docs)

**Plan metadata:** committed separately by the orchestrator after this SUMMARY.

## Files Created/Modified

- `docs/brand/README.md` - new index for the binding brand source, points at quiks-ci-v1.md, the HTML original, and ADR-023/024
- `docs/brand/quiks-ci-v1.md` - new verbatim binding rendition of CI v1.0 (10 sections) incl. Token-Mapping table
- `docs/quiks_CI.html` - newly tracked at its existing path (visual original, unchanged content)
- `docs/DEVELOPMENT_DECISIONS.md` - added ADR-023, ADR-024; appended one supersede-pointer line to ADR-015
- `docs/concept/03-design-system.md` - added a top blockquote and three inline "Abgelöst (ADR-023)" markers (§2, §6, §7); no historical text deleted
- `docs/concept/README.md` - retitled to "quiks — App-Konzept…" with a one-paragraph rename note
- `docs/GIT_CONVENTIONS.md` - retitled to "quiks — Git-Konventionen"
- `CLAUDE.md` - product renamed to quiks in Project Overview; new "Brand & Design" subsection (Beere/Amber/Sunset/hell-first + do-not-rename-yet note)
- `.claude/CLAUDE.md` - `## Project` paragraph renamed to quiks with a rename-pending note; `### Constraints` gained a Brand/Design bullet

## Decisions Made

- Kept `docs/quiks_CI.html` at its current path rather than moving it under `docs/brand/` — a move would churn every future reference for zero benefit; the readable rendition lives in `docs/brand/quiks-ci-v1.md` instead.
- Token-Mapping table names the real `tokens.ts` role keys (not invented names) so the later token-swap phase has a direct 1:1 target list.
- Kept `CLAUDE.md` and `.claude/CLAUDE.md` in their established English prose tone rather than translating them wholesale — only the newly added brand-facts content and the name swap were edited; the German CI role names (Beere, Amber, Papier, Sunset) are kept verbatim as proper nouns, consistent with how the file already mixes English prose with German domain terms (e.g. "site map (Lageplan)").

## Deviations from Plan

None - plan executed exactly as written. All three tasks' `<automated>` verification checks passed on first run, as did the whole-plan verification checks (D-02 empty diff for `apps`/`packages`/`docker-compose.yml`/`package.json`/`pnpm-lock.yaml`, and the scoped `git diff --name-only ae1515f7..HEAD` listing exactly the nine allowlisted paths).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `docs/brand/quiks-ci-v1.md` §Token-Mapping is ready to drive the follow-up token-swap phase (real `tokens.ts` role names, every derived value flagged).
- ADR-024's checklist is ready to drive the follow-up rename phase (npm scope, Expo scheme/bundle id, MMKV ids, docker-compose, repo/remote), including the two coupling constraints (`scheme` ↔ `trustedOrigins`, bundle-id ↔ native rebuild) that make a partial rename unsafe.
- Open point carried forward explicitly in both the brand doc and ADR-023: light mode is not wired up in `apps/mobile` today (`lightColors` exported but unused) — hell-first is genuine new work, not a re-color.
- No blockers for Phase 6 (Profile & Friends Placeholders); this quick task was documentation-only and does not touch runtime code.

---
*Phase: quick-260810-q31*
*Completed: 2026-08-10*

## Self-Check: PASSED

All 9 created/modified files confirmed present on disk (`docs/brand/README.md`, `docs/brand/quiks-ci-v1.md`, `docs/quiks_CI.html`, `docs/DEVELOPMENT_DECISIONS.md`, `docs/concept/03-design-system.md`, `docs/concept/README.md`, `docs/GIT_CONVENTIONS.md`, `CLAUDE.md`, `.claude/CLAUDE.md`). All 3 task commits confirmed present in git log (`7dd2f0d`, `4231959`, `b898985`).
