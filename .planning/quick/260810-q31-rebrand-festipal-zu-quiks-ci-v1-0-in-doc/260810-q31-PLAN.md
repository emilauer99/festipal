---
phase: quick-260810-q31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/brand/README.md
  - docs/brand/quiks-ci-v1.md
  - docs/quiks_CI.html
  - docs/DEVELOPMENT_DECISIONS.md
  - docs/concept/03-design-system.md
  - docs/concept/README.md
  - docs/GIT_CONVENTIONS.md
  - CLAUDE.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [Q31-REBRAND-DOCS]
estimate:
  tokens: 60000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A future agent opening docs/brand/ learns the binding brand facts (Beere primary, Amber secondary, Sunset 150° gradient, hell-first Papier) without ever opening the 695 KB HTML bundle."
    - "A future agent reading docs/DEVELOPMENT_DECISIONS.md finds ADR-023 (CI v1.0) and ADR-024 (rename) and can tell exactly which parts of ADR-015 still bind (D-03)."
    - "A future agent reading docs/concept/03-design-system.md §2/§6/§7 is told those sections are superseded and where the current truth lives — the historical text is still readable (D-03)."
    - "A future agent reading CLAUDE.md knows the product is quiks but that @festipal/* package names are intentionally still old until the rename phase runs (D-01, D-02)."
    - "No source, config, token, or package file changed by this task (D-02)."
  artifacts:
    - docs/brand/README.md
    - docs/brand/quiks-ci-v1.md
    - "docs/DEVELOPMENT_DECISIONS.md containing ### ADR-023 and ### ADR-024"
    - "docs/quiks_CI.html tracked in git at its current path"
  key_links:
    - "docs/brand/quiks-ci-v1.md → the semantic token role names that exist today in packages/ui/src/tokens.ts (mapping table), so the follow-up token phase has a 1:1 target list."
    - "ADR-023 → ADR-015 (supersedes-in-part pointer in BOTH directions: forward note inside ADR-015, backward list inside ADR-023)."
    - "ADR-024 → the coupling constraints it names (app.json scheme ↔ auth.instance.ts trustedOrigins, MMKV ids, docker-compose credentials, Android package ↔ native rebuild)."
---

<objective>
Write down the quiks rebrand and CI v1.0 as binding project documentation — a new `docs/brand/`
source, two new ADRs, superseded-markers on the old design doc, and updated agent instructions —
so that every later agent and phase builds against the new brand instead of the old festipal one.

Purpose: the CI document exists only as an unreadable Claude-Design HTML bundle. Until it is
rendered into repo-native German docs plus ADRs, no phase can act on it and agents will keep
treating Limette/Violett/dark-first as current.
Output: 2 new files under `docs/brand/`, ADR-023 + ADR-024, superseded notes in
`docs/concept/03-design-system.md`, brand facts in both `CLAUDE.md` files, minimal factual
corrections in `docs/concept/README.md` and `docs/GIT_CONVENTIONS.md`.

Tracer-first is N/A here: this is a documentation-only change set with no layered architecture
to prove end-to-end.
</objective>

<user_decisions>
Locked by the user this session — do not revisit, do not offer alternatives:

- **D-01 — Rename scope is full, including technical identifiers.** `festipal` → `quiks` covers the
  npm scope `@festipal/*`, the iOS/Android bundle id `at.festipal.app`, the Expo `name`/`slug`/
  `scheme`, the MMKV storage ids, the docker-compose Postgres user/password/db/volume, the repo
  directory and the git remote. Rationale: pre-release, no EAS Submit has frozen the bundle id
  (see `extra._comment` in `apps/mobile/app.json`).
- **D-02 — Docs first, code later.** THIS task writes documentation ONLY. It must not touch
  `packages/ui/src/tokens.ts`, `apps/mobile/app.json`, any `package.json`, `docker-compose.yml`,
  or any source file. The rename + token swap + light-mode work is its own later phase.
- **D-03 — A new ADR supersedes ADR-015 in part.** ADR-015 stays in place as history and only
  receives a superseded-in-part pointer; it is NOT rewritten.
</user_decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260810-q31-rebrand-festipal-zu-quiks-ci-v1-0-in-doc/260810-q31-CI-SOURCE-EXTRACT.md

Do NOT open `docs/quiks_CI.html` — it is a 695 KB self-extracting Claude-Design bundle
(gzip payload + JSON template) and reading it will blow the context window for zero gain.
The CI-SOURCE-EXTRACT file above is its verbatim, reproducible text extraction and is the
only permitted source for CI content.
</context>

<house_rules>
Binding for every task in this plan:

1. **German output.** Every produced or edited documentation line is German, in the tone and
   formatting of `docs/DEVELOPMENT_DECISIONS.md` and `docs/concept/03-design-system.md`
   (Du-Form is for product copy; ADR/doc prose stays neutral-sachlich as in the existing files).
   Backticked identifiers, hex values, and file paths stay verbatim.
2. **Documentation only (D-02).** Allowed write targets are exactly the nine paths in
   `files_modified`. Any other path — source, tokens, JSON config, compose file, lockfile — is
   out of bounds even if it looks wrong. Reading source files read-only is allowed and expected.
3. **No 1:1 CSS port.** The stylesheet embedded in the HTML bundle is the OLD festipal design
   system (it still says `--brand-primary: var(--green-500)` and light `--bg-app: #FFFFFF`).
   The CI document TEXT binds; the embedded CSS does not.
4. **Mark derivations.** Any value not literally present in the CI extract (press states, tints,
   rgba forms) is written as „abgeleitet — im Token-Phase zu bestätigen", never as if the CI
   document had specified it.
5. **Commits** follow `docs/GIT_CONVENTIONS.md` (Conventional Commits, one commit per task,
   type `docs`). No `Co-Authored-By` trailer. Branch `docs/quiks-rebrand-ci` is already checked
   out; never commit to `main`.
</house_rules>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/brand/ as the binding brand source (D-02)</name>
  <files>docs/brand/README.md, docs/brand/quiks-ci-v1.md, docs/quiks_CI.html</files>
  <read_first>
    - `.planning/quick/260810-q31-rebrand-festipal-zu-quiks-ci-v1-0-in-doc/260810-q31-CI-SOURCE-EXTRACT.md` (already in context — the only CI source)
    - `packages/ui/src/tokens.ts` READ-ONLY, the `colors` and `lightColors` blocks (~lines 115-176), to copy the exact semantic role key names into the mapping table. Reading it is required; editing it is forbidden (D-02).
    - `docs/concept/03-design-system.md` §2/§4/§6 for the house formatting style to match.
  </read_first>
  <action>
Decide and apply this file layout: `docs/quiks_CI.html` STAYS at its current path (no move — a move
would churn every future reference for no benefit) and gets committed as-is, since it is the visual
original. Create two new German files.

`docs/brand/README.md` — short index (roughly 25-40 lines). States that `docs/brand/` is the binding
brand source as of quiks CI v1.0; a pointer table with four rows: `quiks-ci-v1.md` (verbindlicher
Text + Token-Mapping), `../quiks_CI.html` (visuelle Vorlage, NICHT direkt lesen — 695 KB
Claude-Design-Bundle, Textextrakt siehe Quick-Task-Ordner), ADR-023 (Entscheidung CI v1.0),
ADR-024 (Rename). One warning paragraph: das im Bundle eingebettete Stylesheet ist das ALTE
festipal-Design-System und darf nicht 1:1 portiert werden. One line stating that
`docs/concept/03-design-system.md` bleibt als Historie gültig, ausser in den von ADR-023 abgelösten
Punkten.

`docs/brand/quiks-ci-v1.md` — the binding rendition, German, with a short front-matter-style
blockquote (Zweck, Quelle, Stand 2026-08-10, Verbindlichkeit = Prinzip 5 Design-Treue, Entscheidung =
ADR-023). Sections, in this order:

  1. „Haltung, Sprache, Look" — the Kopf table from the extract (Freund mit Plan / Deutsch, Du-Form,
     Satzcase / hell zuerst, viel Ruhe, ein warmer Akzent / Basis: bestehendes Design-System plus
     Sunset-Akzent).
  2. „Logo — Zeichen, Wortmarke, Lockup" — all eight bullets from extract §01, plus the
     Wortmarken-Spezifikation `quiks.` (Outfit 800, komplett klein, −4 % Tracking, Punkt in Beere,
     nie Versalien) and the Freiraum-Regel (mindestens Punkthöhe ×3), Mindestgrössen 1024/180/40,
     unter 24 px nur das Zeichen.
  3. „Farbe" — the role table from extract §02 verbatim (Beere `#E8559F`, Amber `#FFC53D`, Sunset
     150° `#FFC53D`→`#E8559F`, Ink `#0C0E13`, Papier `#F7F5F2`, Text hell `#E9ECF2`, Limette CI
     `#74CC1F`, Violett CI `#5A4DFF`, Live/Gefahr `#FF4D5E`, Info `#5FB4FF`) plus the two rules:
     Sunset ist der einzige erlaubte Verlauf (nur Zeichen + Hero), maximal zwei Flächentöne pro
     Screen.
  4. „Typografie" — the six type roles from extract §03 with the family rules (Outfit / Plus Jakarta
     Sans / JetBrains Mono, Tracking −4 % bis −2 %, Fliesstext 15 px Zeilenhöhe 1,45, Mono nie für
     ganze Sätze).
  5. „Ikonografie & Bausteine" — Lucide 0.470 Strichstärke 2, Grössen 12–14/15–18/21, keine Emoji,
     keine gefüllten Glyphen; Pille/Chip/Glas/Marker; Glas nur für schwebende Ebenen, nie für eine
     Karte in der Liste; Kartenradius 22, Innenblöcke 12–16.
  6. „Modi — hell ist Standard" — the prose plus the four-row Modus-Tabelle (Fläche / Karte / Text /
     Glas, hell vs. dunkel) exactly as in extract §05.
  7. „Anwendung — So ja / So nicht" — both bullet lists from extract §06 verbatim.
  8. „Token-Mapping" — THE core new content. A table `CI-Rolle | Wert | Ziel-Token in
     packages/ui/src/tokens.ts | Modus | Status`, using the REAL key names read from tokens.ts.
     Required rows at minimum: `primary` ← Beere `#E8559F`; `primaryPress` ← `#B02D74`
     (abgeleitet aus der dusk-Rampe); `primaryForeground` / `textOnPrimary` ← Weiss `#FFFFFF`
     (CI: „Text darauf weiss"); `secondary` ← Amber `#FFC53D`; `secondaryPress` ← `#A97400`
     (abgeleitet); Text auf Amber ← Ink `#0C0E13`; `bgApp` hell ← `#F7F5F2` / dunkel ← `#0C0E13`;
     `surfaceCard` hell ← `#FFFFFF` / dunkel ← Weiss 6 % (abgeleitet als `rgba(255,255,255,.06)`);
     `textPrimary` hell ← `#0C0E13` / dunkel ← `#E9ECF2`; `glassFill` hell ← Papier 78 %
     (abgeleitet) / dunkel ← Ink 62 % (abgeleitet); `danger` ← `#FF4D5E` und `info` ← `#5FB4FF`
     (unverändert); und die vier Festival-CI-Tokens `--ci-primary`/`--ci-secondary` mit Limette
     bzw. Violett als reinem Default-Fallback (ADR-015 §3). Zusätzlich eine Zeile für den
     Sunset-Verlauf mit dem Hinweis, dass es dafür heute KEIN Token gibt — neues Token nötig.
     Every derived value carries the „abgeleitet" marker per house rule 4.
  9. „Offene Punkte für die Umsetzung" — four items: (a) Light-Mode ist heute NICHT verdrahtet
     (`lightColors` wird aus `packages/ui/src/tokens.ts` exportiert, aber in `apps/mobile` nirgends
     verwendet) — hell-first ist echte Neuarbeit, kein Umfärben; (b) für den Sunset-Verlauf fehlen
     Token und RN-Umsetzungsweg (Entscheidung gehört in die Folgephase, hier nicht entschieden);
     (c) Press-/Tint-Werte für Beere und Amber sind nicht im CI belegt; (d) die Typo-Rollen
     `label` (heute 12/600) und die Titel-Skala weichen vom CI ab. Each item states that it is
     follow-up phase work, not part of this documentation task.
 10. „Was hier NICHT geändert wird" — one short paragraph naming D-02: keine Token-, App-Config-
     oder Quellcode-Änderung; Umsetzung erfolgt in einer eigenen Phase.

Then `git add docs/quiks_CI.html` so the untracked visual original is tracked at its current path.
  </action>
  <verify>
    <automated>test -f docs/brand/README.md && test -f docs/brand/quiks-ci-v1.md && grep -q 'E8559F' docs/brand/quiks-ci-v1.md && grep -q 'FFC53D' docs/brand/quiks-ci-v1.md && grep -q 'F7F5F2' docs/brand/quiks-ci-v1.md && grep -q 'surfaceCard' docs/brand/quiks-ci-v1.md && grep -q 'lightColors' docs/brand/quiks-ci-v1.md && grep -q 'abgeleitet' docs/brand/quiks-ci-v1.md && git ls-files --error-unmatch docs/quiks_CI.html && test -z "$(git status --porcelain -- apps packages docker-compose.yml package.json)"</automated>
  </verify>
  <done>
`docs/brand/README.md` and `docs/brand/quiks-ci-v1.md` exist, are German, and cover all ten sections
above; the Token-Mapping table names real `tokens.ts` role keys and marks every derived value;
`docs/quiks_CI.html` is tracked at its unchanged path; `git status` for `apps`, `packages`,
`docker-compose.yml` and `package.json` is empty. Committed as `docs(brand): quiks CI v1.0 als
verbindliche Markenquelle in docs/brand/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-023 (quiks CI v1.0) + ADR-024 (Rename) + supersede pointer in ADR-015 (D-01, D-03)</name>
  <files>docs/DEVELOPMENT_DECISIONS.md</files>
  <read_first>
    - `docs/DEVELOPMENT_DECISIONS.md` ADR-015 (lines 301-334) and ADR-022 (lines 513-541) — already in context; ADR-022 is the format template (Titelzeile, nummerierte Entscheidungsliste, **Begründung** / **Verworfen** / **Konsequenz**).
    - `.planning/quick/…/260810-q31-CI-SOURCE-EXTRACT.md` „Reibungspunkte"-Tabelle — the delta list ADR-023 must resolve.
  </read_first>
  <action>
Insert both new ADRs into `docs/DEVELOPMENT_DECISIONS.md` directly after ADR-022's last line and
before the `---` that precedes `## Tech-Stack (Kurzüberblick)`. Match ADR-022's format exactly:
`### ADR-0NN — Titel · **ENTSCHIEDEN**`, one lead-in line naming the source and date
(Nutzer-Entscheidung, 2026-08-10), a numbered decision list, then **Begründung**, **Verworfen**
(where applicable), **Konsequenz**.

**ADR-023 — quiks CI v1.0: Beere/Amber, Sunset-Verlauf, hell-first, Wortmarke `quiks.`**
Numbered decisions:
  1. Verbindliche Quelle ist `docs/brand/quiks-ci-v1.md`; `docs/quiks_CI.html` ist die visuelle
     Vorlage. Der Dokumenttext bindet — das im Bundle eingebettete Stylesheet ist das alte
     festipal-Design-System und wird ausdrücklich NICHT 1:1 portiert.
  2. Farbe: Beere `#E8559F` ist Primärfarbe (Marke, Punkt, Primäraktion; Text darauf weiss),
     Amber `#FFC53D` ist Sekundärfarbe (Verlaufsstart, Hinweise; Text darauf Ink).
  3. Sunset `150°` von `#FFC53D` nach `#E8559F` ist der EINZIGE erlaubte Verlauf und nur für das
     Zeichen und Hero-Flächen zulässig. Ersetzt die bisherige Gradient-Regel („zwei weiche radiale
     Felder, grün/violett") aus ADR-015 bzw. `03-design-system.md` §4.
  4. Hell-first: Papier `#F7F5F2` ist die Standardfläche, Dunkel ist die „Nachtschicht" — mit der
     verbindlichen Modus-Tabelle (Fläche / Karte / Text / Glas). Reines Invertieren ist unzulässig.
  5. Limette `#74CC1F` und Violett `#5A4DFF` sind keine Markenfarben mehr, sondern nur noch die
     Default-Fallback-Werte der vier Festival-CI-Tokens (`--ci-primary`, `--ci-secondary`,
     `--ci-tint`, `--ci-on-primary`) aus ADR-015 §3.
  6. Wortmarke ist `quiks.` — Outfit 800, komplett klein, −4 % Tracking, Punkt in Beere, nie in
     Versalien, keine andere Schrift, keine Effekte.
  7. Typo-Rollen laut CI (Display 44/800, Titel 26/700, Body 15/400, Label 13,5/700, Mono 15/500,
     Micro 10,5 caps); die Schriftfamilien bleiben unverändert.

Then a paragraph **„Löst ADR-015 in diesen Punkten ab"** naming exactly: Markenfarben (Limette/
Violett → Beere/Amber), Standardmodus (dark-first → hell-first inkl. Flächen-/Karten-/Glas-Werte),
Wortmarke (`festipal.` → `quiks.`), Gradient-Regel. Followed by **„Weiter in Kraft (ADR-015)"**:
Schriftfamilien und Typo-System, Layout/Abstände/Radien, Motion, Voice & Tone als bindender
Content-Style-Guide, der Festival-Theming-Vertrag als Mechanismus (nur seine Fallback-Werte ändern
sich), Lucide-Ikonografie, „nur semantische Aliase in Komponenten".

**Konsequenz** of ADR-023 must record, explicitly: (a) Light-Mode ist heute nicht implementiert —
`lightColors` wird aus `packages/ui/src/tokens.ts` exportiert, in `apps/mobile` aber nirgends
verwendet; die Umsetzung (Modus-Umschalter, Systemfolge, Karten-/Glas-Werte) ist Arbeit einer
eigenen Folgephase und NICHT Teil dieser Entscheidung; (b) der Farbwechsel hat genau eine
Angriffsfläche, `packages/ui/src/tokens.ts`, weil in `apps/mobile` keine Marken-Hex-Werte
hartkodiert sind; (c) für den Sunset-Verlauf existiert noch kein Token — offener Punkt der
Folgephase.

**ADR-024 — Rename festipal → quiks (inkl. technischer Identifier)**
Lead-in: Nutzer-Entscheidung 2026-08-10; Umsetzung ist ausdrücklich einer eigenen Phase vorbehalten,
dieses ADR hält nur Entscheidung und Umfang fest. Numbered decisions: (1) Produkt-/Markenname ist
`quiks`; (2) der Rename umfasst auch technische Identifier — Vollumfang laut Checkliste; (3) bis zur
Rename-Phase bleiben `@festipal/*` und `at.festipal.app` im Code unverändert und dürfen NICHT
punktuell „korrigiert" werden.

Then a Checkliste table `Bereich | heute | Ziel | Kopplung/Folge` with these rows:
  - npm-Scope: `@festipal/*` → `@quiks/*` (alle `package.json` names + `workspace:*`-Abhängigkeiten
    + Imports in `apps/*`)
  - Expo `name`/`slug`/`scheme` in `apps/mobile/app.json`: `festipal` → `quiks` — Kopplung: das
    `scheme` MUSS mit `trustedOrigins` (`'festipal://'`) in `apps/api/src/auth/auth.instance.ts`
    synchron bleiben, sonst bricht der better-auth-Origin-Check
  - Bundle-ID iOS `ios.bundleIdentifier` + Android `android.package`: `at.festipal.app` →
    `at.quiks.app` — Folge: voller nativer Rebuild via `npx expo run:android`, ausgeführt in
    `apps/mobile`, niemals im Repo-Root; zulässig, weil noch kein EAS Submit die Bundle-ID
    eingefroren hat (`extra._comment` in `app.json`)
  - MMKV-Storage-IDs: `festipal-avatar` (`apps/mobile/lib/avatar-storage.ts`) und
    `festipal-active-festival` (`apps/mobile/lib/active-festival-storage.ts`) → `quiks-*` — Folge:
    bestehende lokale Zustände (Avatar-URI, aktives Festival) gehen auf aktualisierten
    Dev-Installationen verloren
  - `docker-compose.yml`: `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` `festipal` → `quiks`,
    Volume `festipal-postgres-data` → `quiks-postgres-data` — Folge: lokaler DB-Reset nötig
    (Volume weg, Migration + Seed erneut) und `DATABASE_URL` in `apps/api/.env` UND
    `packages/db/.env` nachziehen
  - Repo-Verzeichnis + Git-Remote → `quiks`
  - Historische Dateinamen/Assets (`festipal-tokens.css`, `festipal-ds.js`,
    `festipal-screens.jsx` unter `docs/concept/designs/`) bleiben als Historie unverändert

**Konsequenz** of ADR-024: Ausführung als eigene Phase (Rename + Token-Swap + Light-Mode),
koordiniert mit ADR-023; ein Teil-Rename ist nicht zulässig, weil `scheme` ↔ `trustedOrigins` und
Bundle-ID ↔ nativer Build gekoppelt sind. **Nicht abgedeckt:** Store-Listings/EAS-Konfiguration
(noch kein Submit) und Domains.

Finally, add the supersede pointer INSIDE ADR-015 without rewriting it (D-03): append one clearly
marked line at the end of ADR-015 (after the „Konsequenz"-Absatz, before `### ADR-016`) in the form
„**Teilweise abgelöst (2026-08-10):** Farben, Standardmodus, Wortmarke und Gradient-Regel ersetzt
durch **ADR-023** (quiks CI v1.0); alles Übrige bleibt in Kraft." Do not delete or reword any
existing ADR-015 sentence.
  </action>
  <verify>
    <automated>grep -q '^### ADR-023 — ' docs/DEVELOPMENT_DECISIONS.md && grep -q '^### ADR-024 — ' docs/DEVELOPMENT_DECISIONS.md && sed -n '/^### ADR-015/,/^### ADR-016/p' docs/DEVELOPMENT_DECISIONS.md | grep -q 'ADR-023' && sed -n '/^### ADR-023/,/^### ADR-024/p' docs/DEVELOPMENT_DECISIONS.md | grep -q 'lightColors' && sed -n '/^### ADR-024/,/^## Tech-Stack/p' docs/DEVELOPMENT_DECISIONS.md | grep -q 'trustedOrigins' && sed -n '/^### ADR-024/,/^## Tech-Stack/p' docs/DEVELOPMENT_DECISIONS.md | grep -q 'festipal-active-festival' && test -z "$(git status --porcelain -- apps packages docker-compose.yml package.json)"</automated>
  </verify>
  <done>
ADR-023 and ADR-024 exist in `docs/DEVELOPMENT_DECISIONS.md` in ADR-022's format, in German;
ADR-023 names the superseded and the still-binding parts of ADR-015 and records the light-mode
consequence; ADR-024 carries the full identifier checklist including the four coupling constraints;
ADR-015 has exactly one appended supersede pointer and is otherwise byte-identical. Committed as
`docs(adr): ADR-023 quiks CI v1.0 und ADR-024 Rename festipal → quiks`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Superseded markers + agent instructions point at the new brand source (D-01, D-02, D-03)</name>
  <files>docs/concept/03-design-system.md, docs/concept/README.md, docs/GIT_CONVENTIONS.md, CLAUDE.md, .claude/CLAUDE.md</files>
  <read_first>
    - `docs/concept/03-design-system.md` §2 (Zeilen 27-40), §6 (88-96), §7 (98-115) — already in context.
    - `CLAUDE.md` (Project Overview + Tech Stack sections) and `.claude/CLAUDE.md` (`## Project` + `### Constraints` sections).
  </read_first>
  <action>
Add superseded markers and brand pointers. Historical content is never deleted or reworded (D-03) —
only marked.

`docs/concept/03-design-system.md`: insert a prominent blockquote directly under the existing
Zweck-blockquote at the top, stating that Farbe, Standardmodus, Wortmarke und Gradient-Regel seit
2026-08-10 durch **ADR-023** (quiks CI v1.0) abgelöst sind, dass die verbindliche Quelle jetzt
`../brand/quiks-ci-v1.md` ist, und dass alles Übrige (Typografie-Familien, Layout/Abstände/Radien,
Motion, Voice & Tone, Theming-Mechanik, Lucide) weiter gilt. Then three inline notes, each a single
indented `> **Abgelöst (ADR-023):** …` line placed immediately under the section heading:
  - §2 „Farben & Tokens": Marke ist Beere `#E8559F` / Amber `#FFC53D`; hell-first mit Papier
    `#F7F5F2`; Beere ist nicht mehr „nur Data-Viz".
  - §6 „Ikonografie & Wortmarke": die Wortmarke ist `quiks.` (Punkt in Beere); der Icon-Teil bleibt
    unverändert gültig.
  - §7 „Festival-Theming-Vertrag": der Vertrag selbst bleibt in Kraft — nur der Fallback ändert
    sich: Limette/Violett sind ab jetzt ausschliesslich die Default-Werte der vier CI-Tokens, keine
    Markenfarben. (Note the wording so a reader sees the mechanism survives, only the fallback
    semantics change.)

`docs/concept/README.md`: retitle line 1 to `# quiks — App-Konzept (aus Claude Design abgeleitet)`
and add one blockquote right beneath it: das Konzept wurde unter dem Arbeitsnamen festipal
erarbeitet; Marke und Name sind seit 2026-08-10 quiks (ADR-023/024); für Design-Fragen gilt
`../brand/quiks-ci-v1.md`. Nothing else in that file changes.

`docs/GIT_CONVENTIONS.md`: retitle line 1 to `# quiks — Git-Konventionen`. No other edit.

`CLAUDE.md` (root): update the Project Overview so the product is named quiks (keeping the same
description of features and the multi-tenant goal), and add a short „Brand & Design" subsection
under it (or immediately after the Tech Stack list) with four facts: verbindliche Markenquelle
`docs/brand/quiks-ci-v1.md` + ADR-023; Primär Beere `#E8559F`, Sekundär Amber `#FFC53D`; Sunset
150° ist der einzige erlaubte Verlauf (nur Zeichen + Hero); hell-first (Papier `#F7F5F2`), Dunkel
ist die Nachtschicht — und den expliziten Hinweis, dass Paket-/Bundle-Namen (`@festipal/*`,
`at.festipal.app`) bis zur Rename-Phase (ADR-024) absichtlich alt bleiben und NICHT nebenbei
umbenannt werden dürfen. Also add a line noting that Light-Mode heute nicht verdrahtet ist. Keep
the rest of the file (commands, structure, principles) untouched.

`.claude/CLAUDE.md`: same facts, condensed, in the two sections named in the brief — the `## Project`
paragraph (name quiks, one line that the repo/packages still carry the old name until ADR-024 runs)
and `### Constraints` (a new bullet: Marke/Design = ADR-023 + `docs/brand/quiks-ci-v1.md`; Beere/
Amber; Sunset als einziger Verlauf; hell-first; Limette/Violett nur noch CI-Token-Fallback; Rename
ist ADR-024 und läuft als eigene Phase — nicht ad hoc umbenennen). Do not touch the auto-generated
Technology-Stack / Architecture / Conventions blocks further down that file beyond the two named
sections.
  </action>
  <verify>
    <automated>sed -n '/^## 2\. Farben/,/^## 3\./p' docs/concept/03-design-system.md | grep -q 'ADR-023' && sed -n '/^## 6\. Ikonografie/,/^## 7\./p' docs/concept/03-design-system.md | grep -q 'ADR-023' && sed -n '/^## 7\. Festival-Theming/,/^## 8\./p' docs/concept/03-design-system.md | grep -q 'ADR-023' && head -12 docs/concept/03-design-system.md | grep -q 'quiks-ci-v1.md' && head -3 docs/concept/README.md | grep -q 'quiks' && head -1 docs/GIT_CONVENTIONS.md | grep -q 'quiks' && grep -q 'docs/brand/quiks-ci-v1.md' CLAUDE.md && grep -q 'E8559F' CLAUDE.md && grep -q '@festipal/' CLAUDE.md && grep -q 'ADR-024' .claude/CLAUDE.md && grep -q 'ADR-023' .claude/CLAUDE.md && test -z "$(git status --porcelain -- apps packages docker-compose.yml package.json)"</automated>
  </verify>
  <done>
`03-design-system.md` carries a top marker plus one inline marker each on §2, §6 and §7, all
pointing at ADR-023 and the new brand doc, with no historical sentence deleted; `concept/README.md`
and `GIT_CONVENTIONS.md` carry minimal factual brand corrections; both `CLAUDE.md` files name quiks,
the four brand facts, the new brand source, and the do-not-rename-yet rule for `@festipal/*`.
`git status` for `apps`, `packages`, `docker-compose.yml`, `package.json` is empty. Committed as
`docs(brand): ADR-023/024 in Konzept-Doku und CLAUDE.md nachziehen`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-Design-Bundle → Repo | `docs/quiks_CI.html` is externally generated content (695 KB, gzip payload + JSON template) that becomes a tracked repo artifact. |
| Docs → future agents | Documentation written here steers later automated code changes (rename, token swap). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q31-01 | Tampering | `docs/quiks_CI.html` | low | accept | The bundle is committed as an inert artifact only — never executed, never served, never parsed by an agent. All CI content flows through the reviewed text extract, not the bundle. |
| T-q31-02 | Information Disclosure | new docs under `docs/brand/` | low | mitigate | Only public brand facts (colors, type, copy rules) are written. No env values, connection strings, or credentials — ADR-024 names the docker-compose Postgres user/db as identifiers to rename, without quoting any real `DATABASE_URL`. |
| T-q31-03 | Elevation of Privilege | agent scope creep into source | medium | mitigate | Each task's `<verify>` asserts an empty `git status` for `apps`, `packages`, `docker-compose.yml` and `package.json` (D-02 enforced mechanically, not by prose). |

No package-manager installs in this task — the package-legitimacy gate does not apply.
</threat_model>

<verification>
Whole-plan checks after Task 3:

1. `test -z "$(git status --porcelain -- apps packages docker-compose.yml package.json pnpm-lock.yaml)"` — D-02 held for the entire task.
2. `git diff --name-only main...HEAD -- . ':(exclude).planning'` lists only the nine paths in `files_modified`.
3. `grep -rn 'ADR-023' docs/ CLAUDE.md .claude/CLAUDE.md` shows the new ADR reachable from the brand doc, the ADR log, the concept doc and both agent instruction files.
4. Spot-read `docs/brand/quiks-ci-v1.md` §Token-Mapping and confirm every listed target key actually exists in `packages/ui/src/tokens.ts` (read-only).
5. All produced prose is German.
</verification>

<success_criteria>
- `docs/brand/` exists with an index and the binding CI rendition incl. token mapping and open points.
- ADR-023 and ADR-024 are in the ADR log in house format; ADR-015 has a supersede pointer and is otherwise unchanged.
- `03-design-system.md` §2/§6/§7 plus its head are marked as superseded, historical text intact.
- Both `CLAUDE.md` files state the quiks brand facts and the deliberate `@festipal/*` freeze.
- Zero non-documentation files changed; three `docs:` commits on `docs/quiks-rebrand-ci`.
</success_criteria>

<output>
Create `.planning/quick/260810-q31-rebrand-festipal-zu-quiks-ci-v1-0-in-doc/260810-q31-SUMMARY.md` when done.
</output>
