---
phase: 03-mobile-app-shell-i18n-foundation
plan: 06
type: execute
wave: 5
depends_on: ["03-01", "03-04", "03-05"]
files_modified:
  - apps/mobile/app.json
  - apps/mobile/package.json
autonomous: false
requirements: [PLAT-02, I18N-01]
user_setup:
  - service: android-toolchain
    why: "D-09 real-Android dev build (`expo run:android`) needs the Android SDK + adb on the Windows PC — RESEARCH Environment Availability flags it as not installed."
    dashboard_config:
      - task: "Install Android Studio + Android SDK + platform-tools (adb) and set ANDROID_HOME; enable USB debugging on the test Android device"
        location: "Android Studio / developer.android.com; device Developer Options"
  - service: ios-toolchain
    why: "D-09 real-iPhone dev build (`expo run:ios`) runs on the user's Mac with free Apple-ID provisioning (7-day signature); the repo must be checked out there."
    dashboard_config:
      - task: "On the Mac: install Xcode, sign in with a free Apple ID for provisioning, check out this repo"
        location: "Xcode > Settings > Accounts"
  - service: local-infra
    why: "Device testing hits the local API over LAN with Mailpit-readable OTP codes (D-10/D-11)."
    dashboard_config:
      - task: "Start Docker Desktop, run `docker compose up -d`, run `pnpm --filter @festipal/db db:push` + `db:seed`, start the API with `pnpm --filter @festipal/api dev`, and set apps/mobile `.env` EXPO_PUBLIC_API_URL to the PC's LAN IP:8081 (phone + PC on the same Wi-Fi)"
        location: "repo root + apps/mobile/.env"
must_haves:
  truths:
    - "A dev build allows cleartext HTTP to the LAN API on Android (via `expo-build-properties` `usesCleartextTraffic`) and iOS (ATS exception), scoped to dev builds only — Pitfall G — so `EXPO_PUBLIC_API_URL=http://<lan-ip>:8081` reaches the API from a real device."
    - "On a real Android device and a real iPhone (D-09: local dev builds via `npx expo run:android` on the Windows PC and `npx expo run:ios` on the Mac with free Apple-ID provisioning — no EAS this phase), the full core-value path works: OTP login (code read from Mailpit) → the festivals list shows the real 'Frequency 2026' → Save → gate-less Enter → festival home."
    - "The session survives a full force-quit-and-relaunch (process killed, not hot-reload): the visitor reopens the app already authenticated (D-02 / Pitfall 2 kill-and-relaunch)."
    - "On cold start there is no wrong-route flash before auth state resolves (splash held; Pitfall 5)."
    - "Switching the device system language changes the UI chrome between DE and EN, and a third (non-DE/EN) device language shows the German fallback (D-06/D-07)."
  prohibitions:
    - statement: "The Android cleartext allowance and iOS ATS exception MUST stay dev-build-only and MUST NOT be carried into a release/EAS build config (out of scope this phase) — flag it as dev-only in a code comment (Pitfall G / security)."
      status: flagged
      verification: "app.json expo-build-properties block carries a dev-only comment; no release/EAS profile is configured this phase."
  artifacts:
    - apps/mobile/app.json
  key_links:
    - "expo-build-properties usesCleartextTraffic + iOS ATS -> http://<lan-ip>:8081 reachable from device -> live OTP + festivals flow."
    - "expo-secure-store session persistence -> force-quit-and-relaunch stays authenticated."
---

<objective>
Enable dev-build cleartext HTTP to the LAN API, then verify the whole phase on real devices via a
conversational UAT — the phase's success criteria are all manual/device-verified (CONTEXT
"Sichtbarer Endzustand").

Purpose: Everything before this proved build/typecheck/lint green, but the four success criteria
(D-02 real OTP + kill-and-relaunch, D-03 real festival + save→enter, SC-2 no auth-flash, SC-3 DE/EN
device locale) can only be confirmed on real hardware over LAN — D-09: the primary test targets are
a real Android device and a real iPhone, using local dev builds (`npx expo run:android` on Windows,
`npx expo run:ios`/Xcode on the Mac with free Apple-ID provisioning), no EAS this phase. Android 9+
blocks cleartext HTTP by default, so a dev-build config flag (Pitfall G) is required before the LAN
API is even reachable.

Output: the `expo-build-properties` dev-cleartext config and a signed-off on-device UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-app-shell-i18n-foundation/03-CONTEXT.md
@.planning/phases/03-mobile-app-shell-i18n-foundation/03-RESEARCH.md
@.planning/phases/03-mobile-app-shell-i18n-foundation/03-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 06 scope)

New/changed symbols introduced by this plan (exclude from cross-plan drift verification):
- `apps/mobile/app.json` — `expo-build-properties` plugin block (`android.usesCleartextTraffic`, iOS `infoPlist` ATS exception).
- `apps/mobile/package.json` dep: `expo-build-properties`.

<tasks>

<task type="auto">
  <name>Task 1: expo-build-properties — dev-only cleartext HTTP to the LAN API</name>
  <files>apps/mobile/app.json, apps/mobile/package.json</files>
  <read_first>
    - .planning/phases/03-mobile-app-shell-i18n-foundation/03-RESEARCH.md (Pitfall G: expo-build-properties usesCleartextTraffic; iOS ATS NSExceptionDomains; Assumption A3 — verify the exact ATS shape against current Expo docs)
    - .planning/phases/03-mobile-app-shell-i18n-foundation/03-CONTEXT.md (D-10 cleartext HTTP allowed only in dev builds)
    - apps/mobile/app.json (existing identity config to extend)
  </read_first>
  <action>
    Add `expo-build-properties` (dev dependency) and register it in `app.json`'s `plugins` array. Set
    `android.usesCleartextTraffic: true`. For iOS, add an ATS exception under `ios.infoPlist` for the
    LAN-HTTP case (verify the exact `NSAppTransportSecurity`/`NSExceptionDomains` shape against current
    Expo docs — RESEARCH A3; a narrowly-scoped exception is preferred over `NSAllowsArbitraryLoads`).
    Add a comment on the block stating it is DEV-BUILD-ONLY and must not leak into a release/EAS build
    (Pitfall G / security). Do not configure any EAS/release profile this phase.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile exec expo config --type public > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @festipal/mobile typecheck` exits 0 and `expo config` resolves the app config without error.
    - `app.json` `plugins` includes `expo-build-properties` with `android.usesCleartextTraffic: true`.
    - An iOS ATS exception for the LAN host is present under `ios.infoPlist`.
    - The block carries a dev-build-only comment; no EAS/release profile is added.
  </acceptance_criteria>
  <done>A dev build permits cleartext HTTP to the LAN API on both platforms, scoped dev-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — full core-value path on real Android + iPhone</name>
  <precondition>Android SDK + adb installed on the Windows PC; Docker Desktop running with `docker compose up -d`; local DB pushed + seeded; API running via `pnpm --filter @festipal/api dev` with `OTP_EMAIL_TRANSPORT=mailpit`; apps/mobile `.env` `EXPO_PUBLIC_API_URL` set to the PC's LAN IP:8081; phone + PC on the same Wi-Fi; (iOS) Mac with Xcode + free Apple-ID provisioning and the repo checked out.</precondition>
  <what-built>
    The full Expo app shell: Metro/pnpm build, Lingui i18n (DE/EN + German fallback), auth + API
    clients with cookie forwarding, splash-held four-state guard, real email-OTP login, festivals
    list from the real `GET /festivals`, gate-less save→enter, festival home placeholder, and
    dev-build cleartext config for the LAN API.
  </what-built>
  <how-to-verify>
    On a REAL Android device (`npx expo run:android` on the Windows PC) AND a REAL iPhone
    (`npx expo run:ios` on the Mac), against the running local API over LAN:
    1. Cold start: confirm the splash holds and NO wrong route flashes before the auth screen appears.
    2. Log in: enter your email, tap "Send code", read the 6-digit code from Mailpit's web inbox at
       http://<pc-lan-ip>:8025, enter it, and confirm you get in (first-time account: pass the
       minimal profile stub). Confirm wrong/expired-code errors show localized messages.
    3. Cookie forwarding: confirm the festivals list loads (an authenticated `GET /festivals`
       returns data, not a 401) and shows the real "Frequency 2026".
    4. Core path: tap Save, then Enter — confirm you reach the festival home placeholder and can
       navigate back to the list (no dead-end).
    5. Kill-and-relaunch: FORCE-QUIT the app (kill the process, not hot-reload), relaunch, and
       confirm you are still authenticated and land past the auth screen.
    6. i18n: set the device system language to German, relaunch, confirm the UI chrome is German;
       set it to English, confirm English; set it to a third language (e.g. French), confirm the
       German fallback (D-07).
    Report any step that fails with the platform (Android/iOS) it failed on.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass on both devices, or describe which step/platform failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev device → local API over LAN (cleartext HTTP) | Session cookie + OTP traffic cross shared Wi-Fi in dev only. |
| Release build config | Must never inherit the dev cleartext allowance. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Information Disclosure | Cleartext LAN HTTP | low | accept | Dev-build-only, documented accepted risk (D-10); production uses HTTPS. Guarded by a dev-only comment so it is not copied into a release config. |
| T-03-15 | Tampering | Release config inheritance | medium | mitigate | No EAS/release profile configured this phase; the cleartext flag is annotated dev-only (Pitfall G). |
| T-03-SC | Tampering | npm install (`expo-build-properties`) | low | mitigate | Official Expo package; confirm `npm view expo-build-properties version` at install. |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile typecheck` + `expo config` resolve.
- Blocking on-device UAT: all six checks pass on both a real Android device and a real iPhone.
</verification>

<success_criteria>
The dev build reaches the LAN API; the full OTP → festival → save → enter → home path works on real
hardware; the session survives force-quit-and-relaunch; the UI localizes by device language with a
German fallback — all four phase success criteria confirmed on-device.
</success_criteria>

<output>
Create `.planning/phases/03-mobile-app-shell-i18n-foundation/03-06-SUMMARY.md` when done.
</output>
