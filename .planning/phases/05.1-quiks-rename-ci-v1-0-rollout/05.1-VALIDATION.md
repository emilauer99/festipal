---
phase: 05.1
slug: quiks-rename-ci-v1-0-rollout
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-10
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `05.1-RESEARCH.md` § Validation Architecture. Task-level rows are filled by the planner.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.10 (both `apps/mobile` and `apps/api`) |
| **Config file** | `apps/mobile/vitest.config.ts` (node env, `lib/` scope) · `apps/api/vitest.config.ts` + `apps/api/test/setup.ts` (needs running Postgres) |
| **Quick run command** | `pnpm --filter <mobile-pkg> test` (11 files, 94 tests, ~0.5 s — no device/DB) |
| **Full suite command** | `pnpm test` (root, Turbo across all packages; `apps/api` needs the `docker compose` stack up) |
| **Estimated runtime** | ~0.5 s quick · full suite dominated by `apps/api` DB spin-up |

> Package filter name changes with the scope rename (ADR-024) — plans must use the post-rename filter after the rename task lands.

---

## Sampling Rate

- **After every task commit:** `pnpm --filter <mobile-pkg> test` + `pnpm --filter <mobile-pkg> lint`
- **After every plan wave:** `pnpm lint && pnpm typecheck && pnpm test` (root)
- **Before `/gsd-verify-work`:** Full suite green **plus** mandatory on-device acceptance (Android, both color modes, login/logout, deep link, cold start) — node-env vitest alone is not sufficient evidence for RN routing/native behavior
- **Max feedback latency:** ~30 seconds (quick run) / minutes (root suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _(filled by planner)_ | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `apps/mobile/lib/__tests__/theme.test.ts` — unit coverage for the new theme/color-resolution logic (pure function, node-testable like the existing `lib/__tests__/*` files; use `select-next-festival.test.ts` as the pattern)
- [ ] `apps/api/test/signout-origin.spec.ts` — content update, not a new file: the hardcoded `'festipal://'` EXPO_ORIGIN must move to the new scheme in the same commit as the scheme rename, otherwise the negative control asserts against a dead string
- [ ] Framework install: none — Vitest is already configured in both apps

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Sunset gradient renders correctly on the hero surface | D-06 / D-05 | No snapshot or visual-regression tooling exists in this project | Build from `apps/mobile`, inspect Home hero in light and dark mode on device |
| Deep link + cold start after scheme rename | D-05 | Native URL scheme behavior is not observable in node-env vitest | Cold start via new scheme URL on device; force-quit + relaunch |
| Light (Papier) default surface across all existing screens | D-01 | Visual/perceptual | Walk every existing screen on device in both modes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick run)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
