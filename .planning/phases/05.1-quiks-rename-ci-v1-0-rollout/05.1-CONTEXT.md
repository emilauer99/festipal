# Phase 05.1: quiks Rename & CI v1.0 Rollout - Context

**Gathered:** 2026-08-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Der Code trägt Namen und Corporate Identity von **quiks** — Ende zu Ende:

1. **ADR-024 Rename:** npm-Scope `@festipal/*` → `@quiks/*` (7 Pakete), Expo `name`/`slug`/`scheme`,
   iOS/Android Bundle-ID `at.festipal.app` → `at.quiks.app` (nativer Rebuild), MMKV-Storage-IDs,
   `docker-compose.yml` (DB + Volume), Repo-Verzeichnis + Git-Remote.
2. **ADR-023 CI v1.0:** Token-Swap in `packages/ui/src/tokens.ts` — Beere `#E8559F` als Primär,
   Amber `#FFC53D` als Sekundär, Sunset (150°, Amber → Beere) als einziger Verlauf, CI-Typo-Rollen.
3. **Hell-first:** Papier `#F7F5F2` als Standardfläche in `apps/mobile`, Dunkelmodus als „Nachtschicht",
   systemgesteuert — heute vollständig unverdrahtet, also echte Neuarbeit.
4. Alle bestehenden Screens (Phasen 3–5) laufen danach weiterhin durch Lint, Typecheck und Tests.

**Nicht in dieser Phase:** neue Features, neue Screens, Umbau von Navigation oder Datenfluss.
Layout-Anpassungen nur so weit, wie die neuen Typo-Größen sie erzwingen. Store-Listings, EAS-Profile
und Domains sind laut ADR-024 ausdrücklich nicht abgedeckt.

</domain>

<decisions>
## Implementation Decisions

### Hell-Modus (Modi)

- **D-01:** Der Modus **folgt dem System** (`useColorScheme()`) — hell ist Standard, dunkel wenn das
  Gerät dunkel steht. Kein In-App-Umschalter in dieser Phase (es gibt bis Phase 6 keinen Settings-Ort
  dafür). — **Reversibility:** costly — zieht den Umbau aller 17 tokenkonsumierenden Dateien in
  `apps/mobile` von statischem `StyleSheet.create` auf modusabhängige Farbauflösung nach sich; ein
  Rückbau auf „fix hell" wäre nur durch erneutes Anfassen derselben Dateien möglich.
- **D-02:** Der **Splash folgt dem Modus**: Papier-Fläche + Ink-Wortmarke im Hellmodus, Ink-Fläche +
  helles Wort im Dunkelmodus. Punkt bleibt in beiden Modi Beere. Text lautet `quiks.` (heute
  `festipal.` in `apps/mobile/app/_layout.tsx` `SplashView`, Zeile ~428).
- **D-03:** Die schwebende Nav (`FloatingNav`) wird im Hellmodus **milchig hell** — `glassFill` =
  `rgba(247,245,242,.78)` gemäß CI-Modus-Tabelle (`docs/brand/quiks-ci-v1.md` §6). Das heutige
  `lightColors`-Erbe (dunkles Glas per Spread) wird ersetzt. Folge: Nav-Icons und -Labels müssen im
  Hellmodus Ink-Farbe führen, sonst verschwinden sie auf hellem Glas.
- **D-04:** Karten grenzen sich im Hellmodus **nur über feine Border** ab (`borderSubtle`, hell
  `rgba(7,8,11,.08)`) — **keine neuen Shadow-/Elevation-Tokens**. Bewusste Abweichung vom
  CI-Wortlaut „Schatten sehr weich" (§6), zugunsten identischer Kartenstruktur in beiden Modi.

### Sunset, Wortmarke, Icons

- **D-05:** Sunset wird in dieser Phase an **zwei** Stellen eingesetzt: im **Zeichen/der Wortmarke**
  und in der **Home-Hero-Karte** („nächstes Festival", `FestivalCard` hero-Variante aus Phase 5).
  Sonst nirgends — das CI erlaubt den Verlauf ausschließlich für Zeichen und Hero. Textfarben auf
  der Verlaufsfläche müssen in beiden Modi lesbar sein.
- **D-06:** Der Verlauf wird über das bereits installierte **`react-native-svg` (~15.15.5)** gerendert
  — keine neue Dependency, kein `expo-linear-gradient`. Vorbild ist das CI-Bundle selbst
  (`docs/quiks_CI.html`, Zeilen 23–33): `<linearGradient>` mit Stops `#FFC53D` → `#E8559F`, das
  Zeichen ist **Text** (`q`, Outfit 800, `letter-spacing -30`) mit Verlaufsfüllung, der Punkt ein
  separater Kreis in Beere. Kein Grafik-Asset nötig.
- **D-07:** Die **App-Icons werden neu generiert** (heute Expo-Platzhalter: `icon.png`,
  `android-icon-foreground/background/monochrome.png`, `favicon.png`, `splash-icon.png`).
  Bildmarke: **`q` im Sunset-Verlauf auf Papier `#F7F5F2`, Punkt in Beere** — wörtliche Umsetzung
  des CI-Thumbnails. `android.adaptiveIcon.backgroundColor` (heute Expo-Hellblau `#E6F4FE`) wird
  auf Papier mitgezogen; die monochrome Ebene ist eine Ein-Farb-Variante des Zeichens.

### Typografie

- **D-08:** Die CI-Typo-Rollen werden **voll angeglichen** (ADR-023 §7): Display 44/800, Titel 26/700,
  Body 15/400, Label 13,5/700, Mono 15/500, Micro 10,5 caps — inklusive Outfit-Tracking −4 % bis −2 %.
  — **Reversibility:** costly — größere Headlines verschieben Zeilenumbrüche, Kartenhöhen und die
  6-Box-OTP-Reihe auf allen Screens der Phasen 3–5; ein Rückbau bedeutet einen zweiten Durchgang
  durch dieselben Screens plus erneute Geräteabnahme.
- **D-09:** Die fünf Rollen, die das CI nicht benennt (`title3`, `bodyStrong`, `bodySm`, `otpDigit`,
  `countdown`), werden **proportional an die neue Skala mitgezogen**, damit die Abstände schlüssig
  bleiben (Zwischenstufe zwischen Titel 26 und Body 15; Mono-Rollen an 15/500 ausgerichtet). Diese
  Werte sind abgeleitet, nicht CI-belegt — im Code entsprechend kennzeichnen.
- **D-10:** Der aus Phase 5 vertagte **Font-Weight-Fidelity-Fix wird in diese Phase geholt**
  (`.planning/phases/05-festival-selection-home/05-UI-REVIEW.md`, Finding 2): ein
  Rollen-zu-Schriftdatei-Resolver (`title2` → `Outfit_600SemiBold`, `label`/`bodyStrong` →
  `PlusJakartaSans_600SemiBold`, `title3`/`micro` → neu zu ladendes `PlusJakartaSans_700Bold`)
  ersetzt das heutige „exakte Familie + zusätzlich numerisches `fontWeight`"-Muster, das auf dem
  Gerät Faux-Bold erzeugt. Begründung: die Typo wird ohnehin angefasst — zwei Durchgänge wären
  Verschwendung.
- **D-11:** Limette `#74CC1F` und Violett `#5A4DFF` fliegen **ersatzlos** aus `tokens.ts`. Kein
  `ciFallbacks`-Export: der Festival-Theming-Vertrag (`--ci-primary` & Co., ADR-015 §3) existiert im
  Code heute nicht (0 Treffer in `packages/ui` und `apps/mobile`), und die Werte stehen in ADR-023
  §5 sowie `docs/brand/quiks-ci-v1.md` §3.

### Rename-Radius

- **D-12:** **Voller Code-Rename in einem Rutsch** — Teil-Rename ist laut ADR-024 unzulässig, weil
  `scheme` ↔ `trustedOrigins` und Bundle-ID ↔ nativer Build gekoppelt sind. Konkret gekoppelt:
  `apps/mobile/app.json` `scheme: "festipal"` und `apps/api/src/auth/auth.instance.ts` Zeile 29
  (`'festipal://'` in `trustedOrigins`) müssen **im selben Schritt** wandern, sonst bricht der
  better-auth-Origin-Check und damit Login/Logout.
- **D-13:** **`docker-compose.yml` wird mit umbenannt** (`POSTGRES_USER`/`PASSWORD`/`DB` →`quiks`,
  Volume `festipal-postgres-data` → `quiks-postgres-data`) — inklusive lokalem Reset: Volume weg,
  Container neu, Migration + Seed erneut, `DATABASE_URL` in **`apps/api/.env` UND `packages/db/.env`**
  nachziehen (beide Keys, `DATABASE_URL_UNPOOLED` gewinnt bei Migrationen).
  — **Reversibility:** costly — der lokale Dev-Datenbestand (Seeds, Testaccounts, OTP-Historie) wird
  mit dem Volume gelöscht und ist nur über Migration + Seed wiederherstellbar, nicht zurückrollbar.
- **D-14:** MMKV-Storage-IDs (`festipal-avatar`, `festipal-active-festival`) bekommen einen **harten
  Schnitt** auf `quiks-*` — **keine** Einmal-Migration. Avatar-URI und zuletzt betretenes Festival
  gehen auf bestehenden Dev-Installationen verloren; das ist in ADR-024 so vorhergesagt und außer
  dem Entwicklergerät hat niemand die App installiert.
- **D-15:** **Repo-Verzeichnis + Git-Remote werden am Phasenende umbenannt** (nicht mittendrin) —
  als letzter Schritt nach dem Merge, mit einer dokumentierten Reihenfolge: GitHub-Repo umbenennen →
  Remote-URL setzen → lokaler Ordner → IDE-Datasources (`.idea/dataSources*`, enthalten heute
  `festipal`). Grund für „am Ende": ein Ordner-Rename mitten in der Phase bricht laufende Pfade,
  offene Terminals und Metro/Watcher.
  — **Reversibility:** costly — betrifft Remote-URLs, lokale Klone und IDE-Konfiguration außerhalb
  des Repos; GitHub legt zwar Redirects an, aber jede lokale Umgebung muss von Hand nachgezogen werden.

### Claude's Discretion

- **Theme-Zugriffsmuster:** Wie die dynamische Farbauflösung technisch aussieht (Context/`useTheme()`-Hook
  in `packages/ui` vs. `apps/mobile`, `createStyles(colors)`-Factory vs. Inline-Overrides), entscheidet
  die Planung — sichtbar im Plan begründen. Bindend ist nur: **keine Rohwerte in Komponenten**, alles
  über semantische Alias-Tokens (ADR-015, weiterhin in Kraft).
- **Icon-Rasterizer:** Default ist `@resvg/resvg-js` als devDependency plus ein committetes
  Generator-Skript, das die Outfit-TTF aus `@expo-google-fonts/outfit` explizit lädt; die erzeugten
  PNGs werden als Artefakte mitcommittet, damit der Build nicht vom Generator abhängt. Alternative
  Wege sind zulässig, solange das Ergebnis reproduzierbar und nicht handgemalt ist.
- **Status-Farben:** `success` (heute `#8FDA3B`, aus der Limette-Rampe) und `warning` (heute
  `#FFC53D`, jetzt identisch mit Amber/Sekundär) sind im CI nicht geregelt. Auflösung — insbesondere
  die Doppelbelegung Amber = Sekundär und Warnung — liegt bei der Planung; `danger` `#FF4D5E` und
  `info` `#5FB4FF` sind bereits CI-konform und bleiben.
- **Plan-Schnitt und -Reihenfolge:** ob Rename und CI-Swap getrennte Pläne werden oder gebündelt,
  entscheidet die Planung. Hinweis: der Rename ist mechanisch und breit (1856 Treffer über das Repo),
  der CI-Swap ist eng und urteilsintensiv — eine Trennung hält die Diffs lesbar.
- **Verifikation:** Umfang der Geräteabnahme. Bindend ist nur, dass sie stattfindet (siehe Specifics).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Marke & CI (bindend)

- `docs/brand/quiks-ci-v1.md` — verbindliche Rendition der CI v1.0. §3 Farbe, §4 Typografie,
  §6 Modus-Tabelle (Fläche/Karte/Text/Glas), §8 Token-Mapping (CI-Rolle → realer Tokenname in
  `packages/ui/src/tokens.ts`), §9 die vier offenen Punkte, die genau diese Phase schließt.
- `docs/brand/README.md` — Einordnung der Brand-Dokumente.
- `docs/quiks_CI.html` — visuelle Vorlage (Claude-Design-Bundle). **Wichtig:** das eingebettete
  Stylesheet ist das ALTE festipal-Design-System und wird ausdrücklich NICHT portiert (ADR-023 §1);
  brauchbar ist die Zeichen-Konstruktion in Zeilen 23–33 (SVG-Verlauf + Text-`q` + Punkt).

### Entscheidungen (ADRs)

- `docs/DEVELOPMENT_DECISIONS.md` ADR-023 (Zeile 545 ff.) — CI v1.0: Beere/Amber, Sunset,
  hell-first, Wortmarke `quiks.`, Typo-Rollen; löst ADR-015 in genau diesen Punkten ab.
- `docs/DEVELOPMENT_DECISIONS.md` ADR-024 (Zeile 583 ff.) — Rename-Umfang als Checkliste mit
  Kopplungen (Scope, Expo, Bundle-ID, MMKV, docker-compose, Repo/Remote) und dem, was NICHT
  umbenannt wird (historische Asset-Dateinamen unter `docs/concept/designs/`).
- `docs/DEVELOPMENT_DECISIONS.md` ADR-015 — weiter in Kraft: Schriftfamilien, Layout/Abstände/Radien,
  Motion, Voice & Tone, Lucide-Ikonografie, „nur semantische Aliase in Komponenten".
- `docs/DEVELOPMENT_DECISIONS.md` ADR-022 — eigene RN-Primitiven, kein Drittanbieter-UI-Kit.

### Zustand aus Phase 5

- `.planning/phases/05-festival-selection-home/05-UI-REVIEW.md` Finding 2 — der vertagte
  Font-Weight-Fidelity-Mangel, der über D-10 in diese Phase kommt.
- `.planning/phases/05-festival-selection-home/05-CONTEXT.md` — Design-Entscheidungen der
  Screens, die jetzt umgefärbt werden.
- `.planning/WINDOWS.md` — offene `unrun-verify`-Einträge; Punkt 17 (fehlende DE-Übersetzungen für
  Home/Friends/Profile/coming-soon in `FloatingNav`) liegt in einer Datei, die diese Phase ohnehin
  anfasst.

### Betroffene Code-Anker

- `packages/ui/src/tokens.ts` — einzige Angriffsfläche für Farbe und Typo.
- `apps/mobile/app.json` — `name`/`slug`/`scheme`/Bundle-IDs/Icons/`adaptiveIcon.backgroundColor`;
  `extra._comment` dokumentiert die scheme ↔ trustedOrigins-Kopplung.
- `apps/api/src/auth/auth.instance.ts` Zeile 29 — `'festipal://'` in `trustedOrigins`.
- `docker-compose.yml` Zeilen 9–10, 17–19, 27, 29, 47 — DB-Credentials, Volume, Healthcheck, Kommentare.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`react-native-svg ~15.15.5`** ist in `apps/mobile` bereits installiert (Peer von
  `lucide-react-native`) — trägt Sunset-Verlauf und Zeichen ohne neue Dependency.
- **`lightColors`** existiert in `packages/ui/src/tokens.ts` (Zeilen 158–176) mit vollständigem
  Rollen-Set — ist aber ein reines Erbe der alten Palette und muss auf die CI-Modus-Tabelle
  korrigiert werden (u. a. `bgApp` `#FFFFFF` → Papier `#F7F5F2`, `glassFill` aktuell per Spread
  dunkel geerbt).
- **`FontsReadyProvider` / `useFontsReady` / `resolveFontFamily`** (`apps/mobile/lib/fonts.ts`,
  `lib/fonts-context.tsx`, aus 04-07) — der vorhandene Ort für den Rollen-zu-Schriftdatei-Resolver
  aus D-10.
- **`@expo-google-fonts/outfit`** liefert die TTF, die der Icon-Generator (D-07) laden kann.

### Established Patterns

- **Null hartkodierte Hex-Werte in `apps/mobile`** — 17 Dateien beziehen Farbe ausschließlich über
  `colors`/`typeRoles` aus `@festipal/ui`. Genau deshalb hat der Farbwechsel eine einzige
  Angriffsfläche; genau deshalb ist der Modus-Umbau aber auch in allen 17 Dateien fällig.
- **Statisches `StyleSheet.create` auf Modulebene** ist das durchgehende Muster — Farbwerte werden
  zur Importzeit eingefroren. Das ist die eigentliche Arbeit hinter D-01.
- **`app.json` steht bereits auf `userInterfaceStyle: "light"`**, rendert aber die Dark-Tokens —
  der Widerspruch löst sich mit dieser Phase auf.
- **i18n-Katalog ist sauber:** `festipal` kommt in `apps/mobile/locales/{de,en}/messages.po` nur in
  auskommentierten, obsoleten Einträgen vor (`#~ msgid "Welcome to festipal"`, Zeile 363/364). Keine
  aktive Übersetzung trägt den alten Namen. Markennamen werden ohnehin nicht durch Lingui geführt.
- **7 Pakete tragen den Scope:** `@festipal/{api,mobile,config,contracts,db,i18n,ui}` — plus
  `workspace:*`-Abhängigkeiten, Imports in `apps/*`, `tsconfig`-Referenzen, `eslint.config.mjs`,
  `.turbo`-Logs und Kommentare/Doc-Blöcke. Gesamttrefferzahl im Repo: ~1856 Zeilen mit `festipal`.

### Integration Points

- **`scheme` ↔ `trustedOrigins`** — die härteste Kopplung der Phase: `apps/mobile/app.json` und
  `apps/api/src/auth/auth.instance.ts` müssen synchron wandern, sonst 403 beim Sign-out und
  gebrochene Deep-Links. Der Deep-Link-Reconstructor aus 05-10 nutzt zusätzlich
  `Constants.expoConfig.scheme` mit hartem `festipal`-Fallback (`apps/mobile/lib/deep-link.ts`) —
  dieser Fallback muss mit.
- **Bundle-ID → nativer Rebuild** — `npx expo run:android` **aus `apps/mobile`**, nie aus dem
  Repo-Root (dort entsteht sonst ein veraltetes `android/` mit altem Package). Die alte Installation
  bleibt als eigene App auf dem Gerät liegen und sollte vor der Abnahme deinstalliert werden.
- **Docker-Rename → `.env`-Doppelpflege** — `apps/api/.env` und `packages/db/.env` tragen beide
  `DATABASE_URL`; Migrationen greifen zusätzlich auf `DATABASE_URL_UNPOOLED` zu.
- **`FloatingNav`** ist der einzige Glas-Verbraucher und damit der Ort, an dem D-03 sichtbar wird.
- **`FestivalCard` hero-Variante** (Phase 5, Home-Tab) ist der einzige Hero und damit der Ort, an
  dem D-05 sichtbar wird.

</code_context>

<specifics>
## Specific Ideas

- Das App-Icon soll aussehen **wie das CI-Thumbnail**: Papier-Fläche, `q` im Sunset-Verlauf,
  Beere-Punkt rechts daneben auf x-Höhe (`docs/quiks_CI.html`, Zeilen 23–33 sind die maßgebliche
  Konstruktion).
- Die Wortmarke bleibt **Text, kein Bild** — `quiks.` in Outfit 800, komplett klein, nie Versalien,
  Punkt in Beere.
- **Geräteabnahme ist Pflicht, nicht optional.** Diese Phase ändert Bundle-ID, native Konfiguration
  und Modusverhalten; die Lehre aus Phase 5 gilt unverändert: RN-/Native-Verhalten am echten Gerät
  verifizieren (`expo start -c`, Device-Logs), ein grüner node-env-Vitest-Lauf beweist hier nichts.
  Mindestens: beide Modi durchsehen, Login/Logout nach dem scheme-Wechsel, Deep-Link, Cold-Start.
- **Reihenfolge-Hinweis:** PR #10 aus Phase 5 hängt noch ungemergt am Remote und der aktuelle Branch
  ist `docs/quiks-rebrand-ci`. Der Repo-/Remote-Rename (D-15) gehört hinter diesen Merge.

</specifics>

<deferred>
## Deferred Ideas

- **In-App-Modus-Umschalter** (hell / dunkel / System, persistiert) — braucht einen Settings- oder
  Profil-Ort, den es erst ab Phase 6 gibt. Systemfolge deckt v1.0 ab.
- **Shadow-/Elevation-Tokens für Karten im Hellmodus** — bewusst verworfen (D-04); wenn Weiß-auf-Papier
  am Gerät zu flach wirkt, ist das ein eigener, kleiner Folge-Task mit CI-Rückendeckung („Schatten
  sehr weich", §6).
- **Store-Listings, EAS-Build-/Submit-Profile und Domains** — von ADR-024 ausdrücklich nicht abgedeckt,
  kein Submit existiert.
- **Historische Design-Assets** (`festipal-tokens.css`, `festipal-ds.js`, `festipal-screens.jsx` unter
  `docs/concept/designs/`) — bleiben laut ADR-024 unverändert, sind Historie.
- **`.idea/`-Datasources und lokale `.env`-Dateien** — tragen `festipal`, sind aber nicht im Repo
  versioniert bzw. rein maschinenlokal; werden im Rahmen von D-13/D-15 als Checkliste dokumentiert, nicht
  als Code-Änderung.

</deferred>

---

*Phase: 05.1-quiks-rename-ci-v1-0-rollout*
*Context gathered: 2026-08-10*
