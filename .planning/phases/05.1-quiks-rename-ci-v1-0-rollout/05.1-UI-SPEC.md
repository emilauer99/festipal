---
phase: 05.1
slug: quiks-rename-ci-v1-0-rollout
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-10
---

# Phase 05.1 — UI Design Contract

> Visual and interaction contract for the quiks rename + CI v1.0 rollout. This is a **re-theme
> phase, not a new-screen phase**: it locks the CI v1.0 token values, the hell-first (light) default
> surface, the dark ("night shift") mapping, the Sunset gradient usage rule, and the font-resolver
> contract that all 17 existing token-consuming files in `apps/mobile` must resolve through. It does
> NOT introduce new screens, navigation, or data flow. Generated by gsd-ui-researcher, verified by
> gsd-ui-checker. Binding brand source: `docs/brand/quiks-ci-v1.md` (ADR-023) — this document
> operationalizes it into token names and component contracts; it does not restate or override it.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — ADR-022 (no shadcn, no third-party RN UI kit on `apps/mobile`) |
| Preset | not applicable |
| Component library | none — owned RN primitives in `packages/ui` / `apps/mobile/components`, styled exclusively via `packages/ui/src/tokens.ts` |
| Icon library | Lucide (`lucide-react-native` 0.470), 2px stroke, 24px grid (CI §5). Sizes: 12–14 inline, 15–18 in rows, 21 in navigation — unchanged this phase. |
| Font | Outfit (display/wordmark) · Plus Jakarta Sans (body/label/title3/micro) · JetBrains Mono (mono/otpDigit/countdown) — Google Fonts OFL, bundled via `expo-font`; two new weight files load this phase (see Font Resolver Contract) |

---

## Spacing & Layout (unchanged this phase)

Confirmed unchanged — no new spacing/radii tokens are introduced or altered by the rename or CI
swap. This is the existing ADR-015-locked ramp from `packages/ui/src/tokens.ts`; it intentionally
does **not** collapse to a generic 4/8/16/... heuristic (already documented in the token file
itself as the real ported brand ramp — do not "fix" this during this phase).

| Token | Value | Usage |
|-------|-------|-------|
| `sp-0` … `sp-12` | 0, 2, 4, 6, 8, 12, 16, 20, 24, 32, 40, 56, 72 | Full `spacingScale` ramp — unchanged |
| `r-card` | 22 | Card radius (CI §5) — unchanged |
| `r-md` | 16 | Inner block radius — unchanged |
| `r-pill` | 999 | Tappable pill / segmented control / badge radius — unchanged |
| `layout.hitMin` | 44 | Minimum touch target — unchanged |

Exceptions: none. No card, hero, or glass-surface change in this phase requires a new spacing or
radius token — only color/typography/mode resolution change (see below).

---

## Typography

CI v1.0 (`docs/brand/quiks-ci-v1.md` §4, ADR-023 §7 / CONTEXT.md D-08) fully replaces the previous
size/weight values for the roles it names. The five roles the CI does not name (`title3`,
`bodyStrong`, `bodySm`, `otpDigit`, `countdown`) are proportionally derived per CONTEXT.md D-09 —
marked "derived" below, not CI-literal. Font **families** are unchanged (Outfit / Plus Jakarta Sans
/ JetBrains Mono, ADR-015). Outfit tracking stays −4% to −2% for display/title roles (CI §4).

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| `wordmark` | 56 | 800 (ExtraBold) | 1.0 | Unchanged — CI names the wordmark spec (Outfit 800, −4% tracking) but not a numeric size; existing splash-only 56px kept |
| `display2` (Display) | **44** (was 34) | **800** (was 700) | 1.06 | CI §4 direct |
| `title2` (Titel) | **26** (was 21) | **700** (was 600) | 1.2 | CI §4 direct |
| `title3` | **19** (was 17) | 700 (unchanged) | 1.0 | Derived (D-09) — interpolated between Titel 26 and Body 15 |
| `bodyStrong` | 15 (unchanged) | 600 (unchanged) | 1.45 | Derived (D-09) — same size as Body, weight variant only |
| `body` | 15 (unchanged) | 400 (unchanged) | 1.45 | CI §4 direct — "15px Fließtext, Zeilenhöhe 1,45" |
| `bodySm` | 13.5 (unchanged) | 400 (unchanged) | 1.45 | Derived (D-09) — coincidentally equals the new Label size; distinguish by weight (400 vs 700) |
| `label` | **13.5** (was 12) | **700** (was 600) | 1.3 | CI §4 direct |
| `micro` | 10.5 (unchanged), all-caps | 700 (unchanged) | 1.2 | CI §4 direct — "10,5 caps" |
| `otpDigit` | 27 (unchanged) | 500 (unchanged) | 1.0 | Aligned to CI Mono role (15/500) per D-09 — weight confirmed 500, size stays large for OTP-box legibility (a documented UI exception, not a CI violation) |
| `countdown` | 13.5 (unchanged) | 500 (unchanged) | 1.3 | Same as above |

**New role needed:** CI §4 names a generic **Mono** role (15/500, e.g. "21:30 · 48,50 € · 120 m")
that has no corresponding entry in `typeRoles` today (only `otpDigit`/`countdown` exist). Add
`mono: { size: 15, weight: 500, lineHeight: 1.3, family: fontFamilies.mono }` to `typeRoles` for
inline time/amount/distance strings — this is a **new token**, not a rename of an existing one.

---

## Font Resolver Contract (D-10, corrected against D-08)

D-10's font-file resolver replaces the old "exact family + numeric `fontWeight` override" pattern
(which produced device-rendered faux-bold) with real weight-specific `.ttf` files. **Correction:**
CONTEXT.md's D-10 prose (`title2 → Outfit_600SemiBold`, `label`/`bodyStrong` → `PlusJakartaSans_600SemiBold`)
was written against the **pre-CI** Phase-5 weights. D-08 (this phase, binding) changes `title2` to
weight **700** and `label` to weight **700** — the table below is the corrected, binding mapping the
planner and executor must use instead of D-10's literal font-file names for those two roles.

| Role | Weight | Font file | Status |
|------|--------|-----------|--------|
| `wordmark` | 800 | `Outfit_800ExtraBold` | **New** — must be loaded in `useAppFonts()` |
| `display2` | 800 | `Outfit_800ExtraBold` | **New** (shared with `wordmark`) |
| `title2` | 700 | `Outfit_700Bold` | Already loaded as `FONT_DISPLAY` — no new file needed |
| `title3` | 700 | `PlusJakartaSans_700Bold` | **New** — must be loaded |
| `label` | 700 | `PlusJakartaSans_700Bold` | **New** (shared with `title3`/`micro`) — supersedes D-10's `600SemiBold` |
| `micro` | 700 | `PlusJakartaSans_700Bold` | **New** (shared) |
| `bodyStrong` | 600 | `PlusJakartaSans_600SemiBold` | Already loaded — matches D-10 as written (derived role, not CI-weight-locked) |
| `body` | 400 | `PlusJakartaSans_400Regular` | Already loaded as `FONT_BODY` |
| `bodySm` | 400 | `PlusJakartaSans_400Regular` | Shares file with `body` — size differs via style, not font file |
| `mono` (new role) | 500 | `JetBrainsMono_500Medium` | **New** — not currently loaded (only 400Regular exists); needed for the new CI Mono role above |
| `otpDigit` / `countdown` | 500 (declared) vs. `JetBrainsMono_400Regular` (loaded) | *pre-existing gap* | **Out of scope this phase** — D-10 enumerates only `title2`/`label`/`bodyStrong`/`title3`/`micro`; do not silently expand into `otpDigit`/`countdown`. Flag as a known follow-up, do not fix ad hoc. |

Applies the existing non-blocking font contract (`apps/mobile/lib/fonts.ts` — `resolveFontFamily`
falls back to the system font until loaded; never gates splash-hide). The two new font packages
(`@expo-google-fonts/outfit` `800ExtraBold`, `@expo-google-fonts/plus-jakarta-sans` `700Bold`) are
already installed dependencies — no `pnpm add` required, only wiring into `useAppFonts()`.

---

## Color

CI v1.0 defines **two modes** (hell-first / light default, dark "night shift") — the template's
single 60/30/10 table is expanded into a light/dark pair below. Brand hues (Beere, Amber, Sunset,
status colors) are **mode-invariant**; only surface/text/glass roles switch. Values marked
"derived" are not CI-literal — they follow `docs/brand/quiks-ci-v1.md` §8's own "abgeleitet — im
Token-Phase zu bestätigen" convention; keep that same code-comment convention in `tokens.ts`.

### Mode-invariant (brand + status)

| Role → Token | Value | Usage |
|------|-------|-------|
| `primary` (Beere) | `#E8559F` | Brand mark, dot, primary buttons/CTAs, active nav pill + icon + label, Saved-badge check + text, active segmented-control indicator |
| `primaryPress` | `#B02D74` (derived) | Primary pressed/active state |
| `textOnPrimary` | `#FFFFFF` | Text/icon on Beere-filled surfaces (CI: "Text darauf weiß") |
| `secondary` (Amber) | `#FFC53D` | Sunset gradient start-stop, hints/notices, live-warmth accents |
| `secondaryPress` | `#A97400` (derived) | Secondary pressed/active state |
| `textOnSecondary` (**new token**) | `#0C0E13` (Ink) | Text/icon on Amber-filled surfaces (CI: "Text darauf Ink") |
| `gradientSunset` (**new token**) | 150°, `#FFC53D` → `#E8559F` | ONLY: brand glyph/wordmark fill, Home hero card background. No third usage anywhere (CI §3, D-05). |
| `textOnGradient` (**new token**, resolves RESEARCH Open Question 1) | `#0C0E13` (Ink) | Text/icon rendered directly on the raw Sunset gradient fill — see Sunset Gradient Contract below for the WCAG rationale |
| `success` | `#8FDA3B` (unchanged) | Status: success — proposed default, confirm at plan-phase per CONTEXT.md Claude's Discretion |
| `warning` | = `secondary` `#FFC53D` (proposed default) | Status: warning — intentionally shares Amber (CI frames Amber as "Hinweise"/hints); pairs with `textOnSecondary` |
| `danger` | `#FF4D5E` (unchanged, already CI-conform) | Status: destructive/error/live |
| `info` | `#5FB4FF` (unchanged, already CI-conform) | Status: informational |

**Removed, no replacement (D-11, binding — do not reintroduce):** Limette `#74CC1F` and Violett
`#5A4DFF` are deleted from `tokens.ts` entirely. **No `ciFallbacks` export is created this phase** —
the festival-theming contract (`--ci-primary` etc., ADR-015 §3) does not exist in code today (0
hits) and stays out of scope; the two hex values remain documented only in ADR-023 §5 / the brand
doc as historical reference for a future festival-theming feature.

### Light (default) vs. Dark ("night shift")

| Role → Token | Light | Dark | Usage |
|------|-------|------|-------|
| `bgApp` (60% dominant) | `#F7F5F2` (Papier) | `#0C0E13` (Ink) | App background / default surface |
| `surfaceCard` (30% secondary) | `#FFFFFF` | `rgba(255,255,255,.06)` (derived) | Card surfaces |
| `textPrimary` | `#0C0E13` | `#E9ECF2` | Primary text |
| `textSecondary` | `#363B49` (derived, carried over from prior `lightColors`) | `#9FA6B6` (unchanged) | Secondary text |
| `textMuted` | `#565C6E` (derived, carried over) | `#7C8394` (unchanged) | Muted/tertiary text |
| `borderSubtle` | `rgba(7,8,11,.08)` | `rgba(233,236,242,.08)` (unchanged) | Card/hairline separation — the ONLY card-boundary mechanism in light mode (D-04: no new shadow/elevation tokens this phase, deliberate deviation from CI's "Schatten sehr weich" wording) |
| `glassFill` | `rgba(247,245,242,.78)` (Papier 78%) | `rgba(12,14,19,.62)` (Ink 62% — **updates** the current dark value `rgba(20,22,29,.62)` to match CI §8's literal mapping) | `FloatingNav` glass background — the only glass consumer in the app (CI §5: "Glas nur für schwebende Ebenen") |
| `glassBorder` | `rgba(12,14,19,.12)` (derived, **new**) | `rgba(255,255,255,.16)` (unchanged) | `FloatingNav` glass hairline |

**10% accent reserved for:** `primary` (Beere) only — primary buttons/CTAs (e.g. hero "Open
festival" pill, profile-completion submit), active `FloatingNav` tab (pill fill + icon + label),
Saved-badge checkmark + label on `FestivalCard`, active `SegmentedControl` indicator, the brand dot
in the wordmark/app icon, OTP focus/active affordances. **Never** for card borders, disabled states,
or body copy — CI §3: "Maximal zwei Flächentöne pro Screen."

---

## Theme Mode Contract (D-01 — new work, not a re-color)

- **Mode source:** `useColorScheme()` (React Native core, RN 0.86.2) — no in-app override this
  phase (no Settings surface exists until Phase 6). System dark → dark tokens; system light or
  unresolved → **light tokens** (hell-first default, reversing the current codebase's static
  dark-only rendering).
- **Access pattern (Claude's Discretion per CONTEXT.md, planner decides the exact file location):**
  a single `ThemeContext`/`useTheme()` hook resolves `colors` for the active mode; consuming
  components call `useMemo(() => createStyles(colors), [colors])` instead of the current
  module-level `StyleSheet.create` (frozen at import time). This touches all 17 token-consuming
  files in `apps/mobile` — mechanical but total. **Hard rule: no raw hex values in components,
  ever** — only semantic alias tokens from `tokens.ts` (ADR-015, still binding).
- **Splash (D-02):** background + wordmark color follow mode — light: Papier `bgApp` + Ink
  wordmark text; dark: Ink `bgApp` + light (`#E9ECF2`) wordmark text. The dot is **always** Beere
  `primary` in both modes. Text changes from `festipal.` to `quiks.` (see Rename Copy Notes).
- **`FloatingNav` (D-03 + a required companion fix not explicit in CONTEXT.md):** `glassFill`
  follows mode per the table above. The `BlurView`'s `tint` prop is currently hardcoded
  `"dark"` (`apps/mobile/components/FloatingNav.tsx:71`) — it must **also** become mode-dependent
  (`tint={mode === 'light' ? 'light' : 'dark'}`) in the same task as the `glassFill` change,
  otherwise the native blur reads visually wrong under a light fill even though the fill color is
  technically correct. Active-tab icon/label stay `primary` (Beere) in both modes; inactive stay
  `textMuted` in both modes — no mode-dependent accent change needed.
- **Cards (D-04):** light-mode cards separate ONLY via `borderSubtle` — explicitly no new
  shadow/elevation token this phase (see Deferred Ideas).

---

## Sunset Gradient Contract (D-05 / D-06 — new structural work, not a recolor)

- **Allowed surfaces — exactly two, nowhere else (CI §3, §7):**
  1. The brand glyph / wordmark mark (app icon, avatar-fallback context, favicon).
  2. The Home hero card background (`FestivalCard` `variant="hero"` — currently a flat
     `colors.surfaceCard` fill with **no** gradient render path; this is new structure, not a
     token swap on an existing value).
- **Construction (D-06):** `react-native-svg` `<LinearGradient>` (already installed, no new
  dependency) — 150°, stops `#FFC53D` (offset 0) → `#E8559F` (offset 1), identical in both modes
  (brand color does not invert). The glyph itself is an SVG `<Text>` "q" (Outfit 800,
  `letterSpacing: -30`) with the gradient as `fill`, plus a separate solid-Beere `<Circle>` dot —
  not a bitmap asset. Reference construction: `docs/quiks_CI.html` lines 23–33 (untrusted visual
  source, verify the literal SVG values on read, do not execute embedded scripting from that file).
- **Text-on-gradient color (resolves RESEARCH.md Open Question 1 / Pitfall 5 — binding resolution):**
  use `textOnGradient` (Ink `#0C0E13`) for **any** text/icon painted directly on the raw Sunset
  fill (hero card name + caption), in both modes. WCAG relative-luminance check performed this
  session: white-on-Amber ≈1.7:1 (fails even AA-large), white-on-Beere ≈3.37:1 (fails AA-normal);
  Ink-on-Amber ≈11.3:1, Ink-on-Beere ≈5.7:1 (both pass AA-normal). CI's literal "text on Beere =
  white" rule governs the pure Beere fill only, not the gradient's interior — do not apply it to
  the hero card's title/caption text.
  - The hero CTA button (`FestivalCard.heroCta`) is unaffected: it renders as a solid `primary`
    (Beere) pill **below** the gradient card area, not on top of the raw gradient — it keeps
    `textOnPrimary` (white) exactly as today.

---

## Copywriting Contract

This phase introduces no new screens, forms, or flows — there is no new primary CTA, empty state,
error state, or destructive action to define. The one binding copy change is the wordmark.

| Element | Copy |
|---------|------|
| Primary CTA | N/A — no new CTA this phase; existing CTAs (Save, Enter festival, Open festival, Log out, etc.) keep current copy and behavior, only re-themed |
| Empty state heading | N/A — no new empty states this phase |
| Empty state body | N/A |
| Error state | N/A — existing error copy (OTP errors, save-conflict 409, etc.) unchanged, only re-themed |
| Destructive confirmation | N/A — no new destructive actions this phase |
| **Wordmark** (special, not a Lingui message) | `quiks.` — Outfit 800, all-lowercase, trailing period, `−4%` tracking, dot rendered in `primary` (Beere). This is a **literal string**, not a Lingui `Trans`/message key — brand/proper nouns are explicitly excluded from translation (ADR-012/020, and the existing `eslint-plugin-i18next` exclusion pattern). Replaces `festipal.` in `apps/mobile/app/_layout.tsx`'s `SplashView` (~line 428). |

**Companion engineering note (not a copy change, but load-bearing for the copy above):**
`apps/mobile/eslint.config.mjs`'s i18n-lint exclusion list still contains the literal `'festipal'`
(line ~60, comment: "brand/proper noun, never translated"). It must become `'quiks'` in the **same
commit** as the wordmark string change — otherwise the lint rule flags the new brand string as an
untranslated literal and the phase fails its own "still passes lint" success criterion.

---

## UI Considerations

Applicable state considerations resolved: **3 covered, 0 backstop, 0 unresolved, 4 dismissed**
(dismissals carry a reason string per the probe's model — silence is never a resolution). No new
list/form/media element with dynamic data states is introduced this phase; the two genuinely new
visual elements (Sunset hero background, wordmark glyph) are evaluated below. The cross-cutting
"every existing screen must render correctly in both modes" concern is **not** a per-element state
in this taxonomy (it's a global re-theme, not a new element) — it is covered instead by
`05.1-VALIDATION.md`'s Manual-Only Verifications (device walk-through, both modes).

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| long-text | `FestivalCard` hero (list-collection) | ✅ covered | Existing `numberOfLines={1}` truncation on the hero name is unchanged by the gradient background — no new long-text handling needed |
| overflow | `FestivalCard` hero (list-collection) | ✅ covered | Same single-line truncation carries over; the new gradient layer is a background fill, not a text container |
| populated | `FestivalCard` hero (list-collection) | ✅ covered | Gradient fill + `textOnGradient` (Ink) render correctly at typical hero content volume (name + date/place caption + CTA) — WCAG-verified per the Sunset Gradient Contract above |
| long-text | `WordmarkGlyph` (static-content, SVG) | ➖ dismissed | Fixed single-character "q" glyph + separate dot — not variable-length text, no long-text state exists |
| overflow | `WordmarkGlyph` (static-content, SVG) | ➖ dismissed | Fixed `viewBox="0 0 1200 800"` — glyph geometry is constant regardless of rendered size, no overflow possible |
| overflow | `FloatingNav` labels (nav) | ➖ dismissed | Unchanged from Phase 5: fixed short labels (Home/Festivals/Friends/Profil) — no new labels introduced this phase |
| long-text | `FloatingNav` labels (nav) | ➖ dismissed | Same as above — labels are static, not user- or content-driven |

---

## Registry Safety

Not applicable — `apps/mobile` has no shadcn/component-registry concept (ADR-022, RN-primitives
strategy). No third-party registry is used or proposed by this phase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| n/a | n/a | not applicable |

---

## Rename Copy & Asset Notes (bridges the design contract into ADR-024's mechanical rename)

- **Wordmark string:** `festipal.` → `quiks.` (see Copywriting Contract above) — the ONLY
  user-facing copy string this design contract changes.
- **App icon (D-07):** `q` glyph in the Sunset gradient on Papier `#F7F5F2` background, Beere dot
  — generated (via the reference SVG construction above + a rasterizer, e.g. `@resvg/resvg-js` per
  RESEARCH.md's Claude's-Discretion default), not hand-drawn, PNG artifacts committed. Android
  `adaptiveIcon.backgroundColor` moves from the current Expo placeholder `#E6F4FE` to Papier
  `#F7F5F2` to match.
- **Out of this document's scope (covered by ADR-024/CONTEXT.md D-12–D-15 instead, not a design
  decision):** npm scope rename, bundle ID, MMKV storage IDs, `docker-compose.yml`, repo/remote
  rename. This UI-SPEC does not restate those — see `05.1-CONTEXT.md` and `05.1-RESEARCH.md`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
