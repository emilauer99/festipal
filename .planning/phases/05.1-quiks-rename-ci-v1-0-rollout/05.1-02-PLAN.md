---
phase: 05.1-quiks-rename-ci-v1-0-rollout
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - docker-compose.yml
autonomous: false
requirements: [ADR-024]
user_setup:
  - service: local-postgres (docker compose)
    why: "The compose rename changes the Postgres user/password/database, so both untracked .env files must be updated by hand — .env paths are outside this environment's tool access."
    env_vars:
      - name: DATABASE_URL
        source: "apps/api/.env AND packages/db/.env — postgresql://quiks:quiks@localhost:5432/quiks"
      - name: DATABASE_URL_UNPOOLED
        source: "packages/db/.env (and apps/api/.env if present) — same local value; drizzle-kit prefers this key over DATABASE_URL for migrations (packages/db/drizzle.config.ts)"
    dashboard_config:
      - task: "Update the IDE datasource connection (JetBrains .idea/dataSources*.xml) to the new database name/user — machine-local, not versioned"
        location: "IDE > Database tool window > data source settings"
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "docker-compose.yml carries an explicit top-level `name:` field, so the Compose project name no longer derives implicitly from the repo folder name and the D-15 folder rename cannot orphan the volume created here (RESEARCH Pitfall 1 / Open Question 2)."
    - "The Postgres user, password, database, healthcheck arguments and the named volume all carry the new product name consistently — a mismatch between the healthcheck user and POSTGRES_USER makes the container report unhealthy forever."
    - "The local dev database is reset (old volume removed), recreated, migrated and re-seeded, and the full apps/api suite passes against the new credentials — proving the .env update actually took effect rather than silently falling back."
    - "Local dev credentials stay local: the compose values are never reused for Neon or Railway, and no .env file is added to version control."
  artifacts:
    - docker-compose.yml (top-level `name:`, POSTGRES_USER/PASSWORD/DB, healthcheck args, named volume, header comment with the post-rename workspace filter)
    - a reset + migrated + seeded local Postgres volume under the new name
  key_links:
    - "docker-compose.yml POSTGRES_* -> apps/api/.env + packages/db/.env DATABASE_URL / DATABASE_URL_UNPOOLED -> packages/db/drizzle.config.ts -> `db:migrate` -> apps/api test/setup.ts"
    - "docker-compose.yml top-level `name:` -> Compose project name -> which named volume `docker compose up` binds after the D-15 folder rename"
---

<objective>
Rename the local dev infrastructure (D-13) and close the Compose-project-name coupling that
RESEARCH found and CONTEXT.md does not mention: the file has no `name:` key today, so the project
name is implicitly the folder name and would silently change under the D-15 folder rename,
orphaning the very volume this plan creates.

Purpose: leave the developer with a working local stack under the new name, with the reset
documented and executed rather than half-done.
Output: renamed `docker-compose.yml`, a reset/migrated/seeded local database, and a green
apps/api suite against the new credentials.

Requirement-ID substitution (visible, recorded): no REQ-IDs exist for this phase; ADR-024 is the
traceability anchor (see plan 05.1-01 objective for the full note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-CONTEXT.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md
@.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 02 share)

| Kind | Symbol / path | Note |
|---|---|---|
| compose key | top-level `name: quiks` in `docker-compose.yml` | **new key** — the file has no `name:` today |
| compose volume | `quiks-postgres-data` | replaces the old named volume |
| env values | `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` = `quiks` | local-dev-only credentials |

<tasks>

<task type="auto">
  <name>Task 1: Rename the compose stack and pin the Compose project name explicitly</name>
  <reversibility rating="costly">Reverting the credential rename means another volume reset — the local seed/testaccount data created under either name is destroyed by the switch, and is only reproducible by re-running migrate + seed (D-13 records this rating).</reversibility>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (full file — 48 lines; note it currently has NO top-level `name:` key)
    - packages/db/drizzle.config.ts (which env key wins for migrations)
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md § Common Pitfalls 1 and § Open Questions 2
  </read_first>
  <action>
    Edit `docker-compose.yml` only. Target state:

    - Add a top-level `name: quiks` key (the file has none today). This is the RESEARCH Pitfall 1
      fix and is NOT in CONTEXT.md: without it, Compose derives the project name from the folder
      name, so the D-15 folder rename at phase end would create a second, empty volume and the
      database reset done here would be silently discarded.
    - `services.postgres.environment`: `POSTGRES_USER`, `POSTGRES_PASSWORD` and `POSTGRES_DB` all
      become `quiks`.
    - `services.postgres.volumes`: the named volume becomes `quiks-postgres-data` (keep the
      existing container mount path exactly as it is — the Postgres 18 image needs
      `/var/lib/postgresql`, not `/var/lib/postgresql/data`; changing it re-triggers the Phase-3
      startup crash-loop).
    - `services.postgres.healthcheck.test`: the `pg_isready` user and database arguments must match
      `POSTGRES_USER`/`POSTGRES_DB` exactly, or the service never reports healthy.
    - Top-level `volumes:` key: rename the declared volume to match.
    - Header/inline comments: update the product name and any workspace filter example to the
      post-rename package name (`@quiks/db`).

    Do not start, stop or reset anything in this task — Task 3 owns the reset, after the developer
    has updated the .env files.
  </action>
  <verify>
    <automated>docker compose config --quiet 2>&1 | tail -5</automated>
    <automated>docker compose config 2>&1 | grep -E "^name:|POSTGRES_(USER|DB):" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (the file is still valid YAML/Compose spec)
    - `docker compose config` output contains `name: quiks`
    - `grep -c "POSTGRES_USER: quiks" docker-compose.yml` returns 1 and the same for POSTGRES_PASSWORD/POSTGRES_DB
    - The `pg_isready` healthcheck arguments name the same user and database as the environment block
    - `grep -c "quiks-postgres-data" docker-compose.yml` returns 2 (service mount + top-level volumes declaration)
    - The Postgres data mount path inside the container is byte-identical to the pre-edit value
  </acceptance_criteria>
  <done>docker-compose.yml validates, declares an explicit project name, and names the new user/password/database/volume consistently including the healthcheck.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Developer updates the two untracked .env files to the new local credentials</name>
  <what-built>docker-compose.yml now declares user/password/database `quiks` and the named volume `quiks-postgres-data`. The two untracked `.env` files still point at the old credentials, and `.env*` paths are outside this environment's tool access — the executor cannot edit them.</what-built>
  <action>HALT and hand control to the developer: the two untracked `.env` files must be moved to the new local database credentials by hand, because this environment's tool layer refuses read and write access to `.env*` paths. Do not attempt a workaround and do not proceed to Task 3 until the resume signal is given.</action>
  <instructions>
    1. Open `apps/api/.env` and set:
       `DATABASE_URL=postgresql://quiks:quiks@localhost:5432/quiks`
       (and `DATABASE_URL_UNPOOLED` to the same value if that key is present in this file).
    2. Open `packages/db/.env` and set BOTH keys to the same value:
       `DATABASE_URL=postgresql://quiks:quiks@localhost:5432/quiks`
       `DATABASE_URL_UNPOOLED=postgresql://quiks:quiks@localhost:5432/quiks`
       (`DATABASE_URL_UNPOOLED` wins for drizzle migrations — see `packages/db/drizzle.config.ts`.)
    3. If plan 05.1-01 Task 2 reported that it could not edit the tracked `.env.example` files, apply
       the same replacement there now: `.env.example`, `apps/api/.env.example`,
       `apps/mobile/.env.example`, `packages/db/.env.example`. Only the LOCAL compose connection
       strings change — leave Neon/staging placeholders and `BETTER_AUTH_SECRET` untouched.
    4. Do not commit any `.env` file. `.env.example` files are tracked and SHOULD be committed.
    5. Confirm you are ready for the destructive reset in Task 3: `docker compose down -v` deletes
       the local Postgres volume, and with it every local seed row, test account and OTP record.
       This is D-13's accepted, non-rollbackable local data loss.
  </instructions>
  <verification>
    Both `apps/api/.env` and `packages/db/.env` exist and their DATABASE_URL (and
    DATABASE_URL_UNPOOLED where present) point at `postgresql://quiks:quiks@localhost:5432/quiks`.
    Neither file is staged for commit (`git status --porcelain` shows no `.env` entry). Any
    `.env.example` file handed over from plan 05.1-01 Task 2 has been updated and IS staged.
  </verification>
  <resume-signal>Type "approved" once both .env files carry the new connection strings and you accept the local volume reset, or describe what is blocking.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reset, recreate, migrate and re-seed the local database under the new name</name>
  <precondition>apps/api/.env and packages/db/.env both contain a DATABASE_URL pointing at the new local database (Task 2 checkpoint approved) — verify read-only by confirming both files exist and, if readable, that they no longer contain the previous database name; if the tool layer refuses to read them, treat the checkpoint approval as the assertion and say so in the SUMMARY.</precondition>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (post-Task-1 state)
    - packages/db/package.json (the exact db:migrate / db:seed / db:push script names)
    - packages/db/drizzle.config.ts (env key precedence)
    - .planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-RESEARCH.md § Runtime State Inventory
  </read_first>
  <action>
    Perform the D-13 local reset, in this order, from the repo root:

    1. `docker compose down -v` — removes containers AND the old named volume. This is the
       destructive step the Task 2 checkpoint approved.
    2. `docker compose up -d` — recreates the stack under the explicit project name with the new
       credentials; wait until the postgres service reports healthy.
    3. Run the workspace's migration script for the db package (post-rename filter
       `--filter ./packages/db`), then its seed script. Use the scripts that already exist in
       `packages/db/package.json` — do NOT invent new ones and do NOT substitute `db:push` for a
       real migration run.
    4. Re-run the API integration suite against the fresh database.

    If the migration fails with an auth/database-not-found error, the cause is almost always a
    stale `DATABASE_URL_UNPOOLED` still pointing at the old database — report it and stop rather
    than falling back to `db:push`.
  </action>
  <verify>
    <automated>docker compose ps --format "{{.Service}} {{.State}} {{.Health}}" 2>&1 | tail -5</automated>
    <automated>pnpm --filter ./apps/api test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows the postgres service `running` and `healthy` under the new project name
    - `docker volume ls | grep -c quiks-postgres-data` returns at least 1
    - The db package's migrate script exits 0 against the new database
    - The db package's seed script exits 0 and the seeded festival row is queryable
    - `pnpm --filter ./apps/api test` exits 0 (the suite connects through the new DATABASE_URL — a stale value fails here, not silently)
  </acceptance_criteria>
  <done>The local stack runs under the explicit project name with the new credentials, the schema is migrated, the seed data is back, and the API suite is green against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> local Postgres container | Plaintext local credentials over loopback; no external exposure |
| repo -> version control | `.env` files must never cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.1-05 | Information Disclosure | `docker-compose.yml` credentials + `.env` files | medium | mitigate | The compose values are local-dev-only and bound to loopback; the checkpoint explicitly instructs the developer not to commit `.env`, and only `.env.example` placeholders are tracked. Neon/Railway credentials are untouched by this plan |
| T-05.1-06 | Denial of Service | local dev database volume | low | accept | `docker compose down -v` destroys local seed/test data. Accepted by D-13; recovery is `migrate` + `seed`, executed in Task 3 within the same plan, and the destruction is gated behind a blocking human checkpoint |
| T-05.1-07 | Tampering | Compose project-name derivation | medium | mitigate | An explicit top-level `name:` pins the project name so the D-15 folder rename cannot silently rebind `docker compose up` to a different, empty volume (RESEARCH Pitfall 1) |

Schema-push gate: not applicable — this plan changes no ORM schema file. The migration run in Task 3
replays the EXISTING migrations against a freshly created database; it is a data-restore step, not a
schema change.
</threat_model>

<verification>
1. `docker compose config --quiet` exits 0 and reports project name `quiks`.
2. `docker compose ps` shows postgres running and healthy.
3. The db package migrate + seed scripts exit 0.
4. `pnpm --filter ./apps/api test` exits 0 against the new credentials.
</verification>

<success_criteria>
- Local dev infra runs under the new name with no orphaned volume risk at phase end.
- The reset is complete (migrated + seeded + suite green), not left half-done.
</success_criteria>

<output>
Create `.planning/phases/05.1-quiks-rename-ci-v1-0-rollout/05.1-02-SUMMARY.md` when done.
</output>
