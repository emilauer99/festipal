---
phase: 05.1-quiks-rename-ci-v1-0-rollout
verified: 2026-08-11T12:45:00Z
status: gaps_found
score: 14/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Die CI-Typo-Rollen sind voll angeglichen — inklusive Outfit-Tracking −4 % bis −2 % (D-08, UI-SPEC § Typography / Copywriting Contract 'Wortmarke −4% tracking')"
    status: partial
    reason: "Größen, Gewichte und der Rollen-zu-Schriftdatei-Resolver sind vollständig übernommen und gerendert. Das Tracking existiert aber nur als Token: tokens.ts berechnet letterSpacing für wordmark (−2.24), display2 (−1.76) und title2 (−0.52), doch KEIN React-Native-Consumer wendet die Werte an — alle wordmark-/display2-/title2-Styles setzen nur fontSize/lineHeight/color. Wortmarke und alle Display-/Titel-Überschriften rendern mit Tracking 0 statt der CI-Vorgabe. Kein Phasen-Artefakt dokumentiert eine bewusste Vertagung (anders als der explizit geparkte otpDigit/countdown-Gap, WINDOWS 25). Die drei Tokenwerte sind toter Code; nur der SVG-Glyph nutzt sein eigenes, separates GLYPH_TEXT.letterSpacing. Code-Review-Befund WR-01, am Code unabhängig bestätigt."
    artifacts:
      - path: "packages/ui/src/tokens.ts"
        issue: "letterSpacing-Werte in typeRoles.wordmark/display2/title2 vorhanden, aber von keinem RN-Consumer gelesen (Datenfluss: DISCONNECTED)"
      - path: "apps/mobile/app/(auth)/welcome.tsx"
        issue: "wordmark- und title2-Styles ohne letterSpacing (Zeilen ~79-90)"
      - path: "apps/mobile/app/_layout.tsx"
        issue: "SplashView-Wortmarken-Style ohne letterSpacing"
      - path: "apps/mobile/app/(auth)/email.tsx"
        issue: "display2-Style ohne letterSpacing"
      - path: "apps/mobile/app/(auth)/verify.tsx"
        issue: "display2-Style ohne letterSpacing"
      - path: "apps/mobile/app/(profile-setup)/complete-profile.tsx"
        issue: "display2-Style ohne letterSpacing"
      - path: "apps/mobile/app/(tabs)/home.tsx"
        issue: "title2-Styles (2x) ohne letterSpacing"
      - path: "apps/mobile/app/(tabs)/festivals.tsx"
        issue: "title2-Style ohne letterSpacing"
      - path: "apps/mobile/app/(festival)/f/[festivalSlug].tsx"
        issue: "title2- und display2-Styles ohne letterSpacing"
      - path: "apps/mobile/components/FestivalCard.tsx"
        issue: "Hero-title2-Style ohne letterSpacing"
    missing:
      - "letterSpacing: typeRoles.<rolle>.letterSpacing in allen wordmark-/display2-/title2-Styles setzen (ca. 10 Stellen in 9 Dateien)"
      - "ODER: eine explizite, dokumentierte Vertagungsentscheidung (Token-Docstring + WINDOWS-Eintrag), falls das RN-Anwenden bewusst auf eine Folgephase verschoben wird — dann per Override in dieser Datei akzeptieren"
  - truth: "CLAUDE.md beschreibt den Zustand nach dieser Phase, sodass keine spätere Session einer inzwischen falschen Anweisung folgt (Plan 05.1-01, RESEARCH Pitfall 8)"
    status: partial
    reason: "Die Rename-Anweisung ('names intentionally stay old') ist korrekt entfernt (0 Treffer in beiden CLAUDE.md). Aber CLAUDE.md Zeile 24 behauptet weiterhin 'Light mode is not wired up in code today (lightColors exported ... but unused in apps/mobile) — that is follow-up phase work.' Genau diese Phase hat das Wiring geliefert: apps/mobile/lib/theme.ts konsumiert lightColors, hell-first macht Light zum Default. CLAUDE.md ist bindende Arbeitsanweisung für zukünftige Sessions — der Satz kann Light-Mode-Testing überspringen oder Doppelarbeit auslösen. Code-Review-Befund WR-02, am Working Tree bestätigt."
    artifacts:
      - path: "CLAUDE.md"
        issue: "Zeilen 23-25: falsifizierte 'Light mode is not wired up'-Aussage steht noch"
    missing:
      - "Satz ersetzen, z. B.: 'Hell-first: Papier #F7F5F2 ist die Default-Fläche; apps/mobile/lib/theme.ts löst das Geräteschema auf (nur exakt dark liefert das Nachtschicht-Set, alles andere lightColors).'"
human_verification: []
---

# Phase 05.1: quiks Rename & CI v1.0 Rollout — Verification Report

**Phasenziel:** Die Codebasis trägt den quiks-Namen und die verbindliche CI v1.0 Ende zu Ende — Pakete/Bundle-IDs per ADR-024 umbenannt, `packages/ui`-Tokens auf Beere/Amber mit Sunset als einzigem Verlauf, hell-first (Light) als Default-Fläche in `apps/mobile` — und alle bestehenden Screens laufen weiterhin durch Lint, Typecheck und Tests.
**Verifiziert:** 2026-08-11
**Status:** gaps_found
**Re-Verifikation:** Nein — Erstverifikation

## Goal Achievement

Kernaussage vorab: **Das Phasenziel ist in der Substanz erreicht.** Rename, Token-Swap, Sunset-Regel, hell-first-Wiring und die grünen Gates sind alle im Code nachgewiesen, nicht nur behauptet. Zwei Fidelity-Lücken verhindern ein sauberes `passed`: das CI-Tracking wird nirgends gerendert (D-08 verlangt es ausdrücklich, keine Vertagung ist dokumentiert) und CLAUDE.md widerspricht dem gelieferten Stand. Beide sind klein und schnell schließbar.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Rename Ende zu Ende (ADR-024): kein Alt-Name außerhalb `.planning/`, `docs/`, Lockfile | ✓ VERIFIED | `git grep -l -i festipal` (mit Exclusions) = **0 Treffer**, diese Session ausgeführt |
| 2 | Scheme ↔ trustedOrigins gekoppelt gewandert; genau EIN App-Scheme-Eintrag | ✓ VERIFIED | `app.json` scheme=`quiks`, `auth.instance.ts:33` `'quiks://'` (einziger App-Scheme-Eintrag); `signout-origin.spec.ts:22` EXPO_ORIGIN=`'quiks://'` mit intakter Negativkontrolle; api-Suite 45/45 in diesem Lauf |
| 3 | Bundle-IDs `at.quiks.app` (iOS+Android), Name/Slug `quiks` | ✓ VERIFIED | `app.json` gelesen: name/slug/scheme/bundleIdentifier/package alle korrekt |
| 4 | 7 Pakete unter `@quiks/*`, Root `quiks`, Lockfile konsistent | ✓ VERIFIED | Alle 8 package.json-Namen geprüft; `pnpm install --frozen-lockfile` Exit 0 in diesem Lauf |
| 5 | MMKV-IDs harter Schnitt auf `quiks-*`, kein Migrations-Shim (D-14) | ✓ VERIFIED | `avatar-storage.ts:17` `quiks-avatar`, `active-festival-storage.ts:18` `quiks-active-festival`; kein migrate/dual-read-Code |
| 6 | tokens.ts: Beere `#E8559F` primär, Amber `#FFC53D` sekundär, Sunset als EINZIGER Verlauf, derived-Marker durchgängig | ✓ VERIFIED | Datei vollständig gelesen: `gradientSunset` einziges Gradient-Token (150°, Amber→Beere, Vektor), `textOnGradient`/`textOnSecondary`/Status-`*Text`-Varianten vorhanden, derived-Marker an jedem nicht-CI-literalen Wert |
| 7 | Limette/Violett ersatzlos gelöscht, per Test abgesichert (D-11) | ✓ VERIFIED | Einzige Treffer: Doku-Kommentar in tokens.ts + die Verbots-Assertions in `theme.test.ts` (REMOVED_LIMETTE/REMOVED_VIOLETT); mobile-Suite 140/140 in diesem Lauf |
| 8 | Hell-first-Auflösung (D-01): nur exakt `'dark'` liefert dunkel; `light`/`null`/`undefined`/`'unspecified'` → hell; Provider oberhalb des Splash | ✓ VERIFIED | `theme.ts` pur/node-importierbar; `theme.test.ts` assertiert alle Scheme-Inputs (Verhaltenstest, grün in diesem Lauf); `_layout.tsx:141` ThemeProvider + `StatusBar style="auto"` oberhalb beider Render-Zweige; `userInterfaceStyle: automatic` |
| 9 | Jeder Consumer löst Farben per Render auf statt sie beim Import einzufrieren | ✓ VERIFIED | 18 `useTheme()`-Dateien, 18 `StyleSheet.create`-Stellen — alle innerhalb `createStyles`-Factories (Review bestätigt alle 18 Callsites, Sweep bestätigt 0 Rohliterale) |
| 10 | Kein rohes Hex-/rgba-Literal in `apps/mobile/app` + `components` (ADR-015) | ✓ VERIFIED | Kombinierter Sweep diese Session: **0 Treffer** außerhalb Kommentaren |
| 11 | FloatingNav-Glas + BlurView-Tint modusabhängig (D-03) | ✓ VERIFIED | `FloatingNav.tsx:93` `tint={mode === 'light' ? 'light' : 'dark'}`; glassFill/glassBorder mit eigenen Light-Overrides in tokens.ts |
| 12 | Sunset-Hero: SVG-Layer aus dem Token, `textOnGradient`-Ink-Text, opakes `surfaceCard` darunter; kein dritter Sunset-Einsatz | ✓ VERIFIED | `FestivalCard.tsx`: `SunsetLayer` liest `colors.gradientSunset` (Zeile 141, nur hero-Branch), name/caption `textOnGradient` (241/251), Card behält `surfaceCard` (209); einzige weiteren Consumer: WordmarkGlyph + Generator (erlaubte Fläche 1) |
| 13 | Font-Resolver: jede Rolle → echte gewichtsspezifische Datei, kein numerisches fontWeight daneben (D-10) | ✓ VERIFIED | `fonts.ts` exportiert FONT_WORDMARK/TITLE_STRONG/BODY_STRONG/MONO_MEDIUM + `fontFamilyForRole`; 15 Consumer-Dateien; `fonts.test.ts` assertiert das Mapping über den Token-Key-Satz |
| 14 | Brand-Glyph: EINE Geometrie für Runtime-Komponente UND Icon-Generator; 6 PNGs in korrekten Maßen; adaptiveIcon auf Papier | ✓ VERIFIED | Generator importiert `wordmark-glyph.ts` + `@quiks/ui`-Tokens (keine Doppel-Transkription), `loadSystemFonts:false` + explizite TTF; PNG-Header-Check diese Session: alle 6 Maße OK; `adaptiveIcon.backgroundColor` = `#F7F5F2` |
| 15 | Phasen-Gate grün: frozen install, Lint, Typecheck, Tests, Mobile-Export; Geräteabnahme erfolgt | ✓ VERIFIED | Orchestrator-Gates dieser Session: install Exit 0, lint 10/10, typecheck 10/10, test 7/7 Tasks (api 45/45, mobile 140/140, i18n 6/6), mobile build Exit 0; Geräteabnahme vom USER als Ganzes mit „approved" freigegeben (2026-08-11) — menschliche Evidenz, keine Punkt-für-Punkt-Tabelle, so auch ehrlich im 07-SUMMARY protokolliert; Splash-Glyph-Entscheidung: BEHALTEN |
| 16 | CI-Typo voll angeglichen **inklusive Outfit-Tracking −4 %/−2 %** (D-08, UI-SPEC) | ✗ FAILED (partial) | Tracking-Werte existieren als Tokens (`tokens.ts:139/146/153`), aber **kein RN-Consumer wendet sie an** — Grep über app+components: einzige letterSpacing-Nutzungen sind das positive Micro-Eyebrow-Tracking und der separate SVG-Glyph-Wert. Wortmarke/Display/Titel rendern mit Tracking 0. Siehe Gaps. |
| 17 | Beide CLAUDE.md beschreiben den Nach-Phasen-Zustand | ✗ FAILED (partial) | Rename-Anweisung korrekt entfernt (0 Treffer „intentionally stay old"), aber `CLAUDE.md:24` behauptet weiterhin „Light mode is not wired up in code today" — von dieser Phase falsifiziert. Siehe Gaps. |
| 18 | WINDOWS-Ledger spiegelt die Realität; D-15 dokumentiert, nicht ausgeführt | ✓ VERIFIED | Ledger geprüft: 24/26/27/28/29 fixed mit Nachweis im reason-Feld (inkl. Unterscheidung automatisiert vs. Geräteabnahme), 25 begründet offen, 30 neu abgespalten — deckt sich mit der Abnahmeliste; `05.1-RENAME-CHECKLIST.md` existiert (7.442 B), `git remote -v` unverändert auf der alten URL |

**Score:** 14/16 Kern-Must-haves verifiziert (Truths 16+17 partial; 18 Zeilen oben, weil zwei Truths in je zwei Teilaussagen geprüft wurden)

### Required Artifacts

| Artefakt | Erwartet | Status | Details |
|----------|----------|--------|---------|
| `apps/mobile/app.json` | name/slug/scheme/Bundle-IDs/userInterfaceStyle/adaptiveIcon | ✓ VERIFIED | Alle Felder korrekt |
| `apps/api/src/auth/auth.instance.ts` | genau ein App-Scheme-Eintrag `'quiks://'` | ✓ VERIFIED | Zeile 33; `exp://`-Einträge sind vorbestehend (WR-03, Warnung unten) |
| `apps/api/test/signout-origin.spec.ts` | EXPO_ORIGIN auf neuem Scheme, Negativkontrolle intakt | ✓ VERIFIED | Zeile 22; Suite grün |
| 8× package.json | `@quiks/*` + Root `quiks` | ✓ VERIFIED | Alle geprüft |
| `packages/ui/src/tokens.ts` | CI v1.0 beide Modi, neue Tokens, derived-Marker | ✓ VERIFIED | Vollständig gelesen — Werte entsprechen UI-SPEC-Tabellen (Stichproben: bgApp, glassFill/Border, dangerText, Status-Fills, Typo-Rollen) |
| `apps/mobile/lib/theme.ts` / `theme-context.tsx` / `theme.test.ts` | Auflösungspfad + Tests | ✓ VERIFIED | Existieren, node-importierbar, Tests grün |
| `apps/mobile/lib/fonts.ts` / `fonts.test.ts` | Resolver + neue Gewichte | ✓ VERIFIED | Alle 4 neuen Konstanten + Resolver |
| `apps/mobile/components/FestivalCard.tsx` | Sunset-Layer, textOnGradient, surfaceCard darunter | ✓ VERIFIED | SunsetLayer nur im hero-Branch |
| `apps/mobile/lib/wordmark-glyph.ts` / `WordmarkGlyph.tsx` / `wordmark-glyph.test.ts` / `generate-app-icon.mjs` | eine Geometrie, zwei Consumer, deterministisch | ✓ VERIFIED | Generator liest Geometrie + Tokens, keine Doppel-Transkription |
| 6× `apps/mobile/assets/*.png` | regeneriert, exakte Maße | ✓ VERIFIED | PNG-Header-Check: 1024/1024/512/512/432/48 alle OK |
| `docker-compose.yml` | `name: quiks`, konsistente Credentials/Healthcheck/Volume | ✓ VERIFIED | Zeilen 16/23/25/33/35/53 |
| `05.1-RENAME-CHECKLIST.md` | vorhanden, nichts ausgeführt | ✓ VERIFIED | Datei existiert; Remote unverändert |
| letterSpacing-Anwendung in Consumern | UI-SPEC/D-08 | ✗ MISSING | Kein Consumer — siehe Gap 1 |
| `CLAUDE.md` | Nach-Phasen-Zustand | ⚠️ PARTIAL | Rename-Teil korrekt, Light-Mode-Satz stale — siehe Gap 2 |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `app.json` scheme | `auth.instance.ts` trustedOrigins | better-auth Origin-Check | ✓ WIRED (identischer String `quiks`; Negativkontrolle grün) |
| `app.json` scheme | `_layout.tsx` APP_SCHEME_FALLBACK → `deep-link.ts` | Constants.expoConfig.scheme | ✓ WIRED (`APP_SCHEME_FALLBACK = 'quiks'`, Zeile 123) |
| package.json-Namen | jeder Import-Specifier | pnpm workspace + Metro/tsup/tsc | ✓ WIRED (frozen install + build + typecheck Exit 0) |
| `useColorScheme()` | `resolveThemeMode` → ThemeProvider → `useTheme()` → createStyles | pro Render | ✓ WIRED (18 Consumer, Provider oberhalb Splash) |
| `tokens.gradientSunset` | FestivalCard-Hero SVG + WordmarkGlyph + Generator | ein Token, drei Leser | ✓ WIRED (keine Inline-Stops irgendwo) |
| `tokens.dangerText` | OtpBoxes-Fehlerrand + Fehlerkopie festivals/home/verify | modusabhängige Auflösung | ✓ WIRED (bare `colors.danger` als Text: 0 Treffer in den Screens) |
| `typeRoles.*.letterSpacing` | wordmark-/display2-/title2-Styles | Style-Konsum | ✗ NOT_WIRED — Token berechnet, nie gelesen (Gap 1) |
| `fontFamilyForRole` | 15 Consumer-Dateien | rollengelöste Familien ohne numerisches fontWeight | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artefakt | Datenwert | Quelle | Fließt? | Status |
|----------|-----------|--------|---------|--------|
| Alle Farb-Styles in app+components | `colors.*` | useTheme() → tokens | Ja | ✓ FLOWING |
| Sunset-Stops im Hero + Glyph + Generator | `tokens.gradientSunset` | ein Token-Objekt | Ja | ✓ FLOWING |
| `typeRoles.wordmark/display2/title2.letterSpacing` | Token-Werte −2.24/−1.76/−0.52 | tokens.ts | **Nein** | ✗ DISCONNECTED (toter Code — Gap 1) |
| Icon-PNGs | committete Artefakte | deterministischer Generator | Ja (build-time) | ✓ FLOWING |

### Behavioral Spot-Checks

Die volle Workspace-Suite lief in dieser Verifikationsrunde einmal (Orchestrator, `--force` uncached): 7/7 Tasks grün — darin die Verhaltenstests der Phase (`theme.test.ts` hell-first-Auflösung + D-11-Verbot + CI-Literale, `fonts.test.ts` Rollen-Mapping + Non-Blocking-Fallback, `wordmark-glyph.test.ts` Stop-Reihenfolge gegen das Token, `signout-origin.spec.ts` Positiv+Negativ gegen den echten Origin-Check). Keine erneuten Suite-Läufe nötig.

Zusätzlich diese Session ausgeführt: Alt-Namen-Sweep (0), Rohliteral-Sweep (0), PNG-Header-Maßprüfung (6/6 OK), package-Namen-Enumeration (8/8), Tint-Grep (modusabhängig).

### Probe Execution

Keine `scripts/*/tests/probe-*.sh` im Repo, keine Probes von Plänen deklariert — nicht anwendbar.

### Requirements Coverage

| Requirement | Source Plan | Beschreibung | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| ADR-024 (Substitution) | 05.1-01, 02, 07 | Voller Rename: Scope, Scheme, Bundle-IDs, MMKV, Compose, D-15-Checkliste | ✓ SATISFIED | Truths 1–5, 18 |
| ADR-023 (Substitution) | 05.1-03, 04, 05, 06, 07 | CI v1.0: Beere/Amber, Sunset-Regel, hell-first, Typo, Icons | ⚠️ SATISFIED mit Lücke | Truths 6–14 verifiziert; Tracking-Teil von D-08 nicht gerendert (Gap 1) |

Diese Phase hat **keine REQ-IDs** in `.planning/REQUIREMENTS.md` (geprüft: 0 Zeilen für 05.1). Die Requirement-ID-Substitution über ADR-023/ADR-024 ist in **allen sieben Plänen** sichtbar im Objective protokolliert („Requirement-ID substitution (visible, recorded)") — die fehlenden REQ-IDs sind damit kein Coverage-Versagen. Keine verwaisten Requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Keine TBD/FIXME/XXX-Marker in den Phasen-Dateien | — | Debt-Marker-Gate bestanden |
| `packages/ui/src/tokens.ts` | 139/146/153 | Tote Token-Werte (letterSpacing berechnet, nie konsumiert) | ⚠️ Warning | Gap 1 — der Docstring behauptet einen gerenderten CI-Wert, den es nicht gibt |
| `CLAUDE.md` | 24 | Von der Phase falsifizierte Instruktion | ⚠️ Warning | Gap 2 |
| `apps/api/src/auth/auth.instance.ts` | 34-35 | `exp://` + `exp://**` ungegated auch in Produktion (WR-03, vorbestehend) | ⚠️ Warning | Kein Phasen-Gap; Hardening-Follow-up empfohlen (env-gaten) |
| `apps/mobile/lib/fonts.ts` | 139/142 | Zwei geladene, nirgends referenzierte Font-Dateien (IN-01) | ℹ️ Info | Verlängert fontsLoaded-Wartezeit minimal |
| `apps/mobile/scripts/generate-app-icon.mjs` | 80/98 | `splash-icon.png`/`favicon.png` generiert, aber von keiner Config referenziert (IN-02) | ℹ️ Info | Tote Artefakte, kein Laufzeitfehler |
| diverse | — | `micro`-Rolle inkonsistent mit/ohne Caps (IN-03) | ℹ️ Info | Doku- oder Konsistenz-Follow-up |
| Repo-Root | — | untrackte `tsconfig.json` (Expo-Root-Verschmutzung) liegt noch da — 07-SUMMARY verlangt Entfernen VOR dem PR | ⚠️ Warning | `git status` zeigt `?? tsconfig.json`; vor dem Ship löschen |

### Human Verification Required

Keine offenen Punkte für diese Verifikation. Die verpflichtende Geräteabnahme wurde vom Entwickler selbst gefahren und als Ganzes freigegeben (07-SUMMARY protokolliert Umfang und Grenzen der Aussage ehrlich); die zwei bewusst NICHT abgedeckten Geräte-Ansprüche laufen korrekt als eigene offene Ledger-Einträge weiter (WINDOWS 25: Mono-Gewicht 500 vs. 400er-Datei; WINDOWS 30: Monochrome-Layer unter Androids Themed Icons) und sind keine Phasen-Gaps.

### Gaps Summary

Die Phase liefert ihr Ziel in der Substanz: Der Rename ist vollständig und beweisbar (0 Alt-Namen-Treffer, gekoppelte Scheme/Origin-Wanderung mit lebender Negativkontrolle, frozen-lockfile-Install grün), der Token-Swap trägt CI v1.0 in beiden Modi mit durchgängigen derived-Markern, Sunset existiert als genau ein Token mit genau zwei erlaubten Flächen, hell-first ist end-to-end verdrahtet und getestet, und alle Gates plus die Geräteabnahme sind grün.

Zwei Lücken bleiben:

1. **Outfit-Tracking wird nicht gerendert (WR-01, bestätigt).** D-08 ist eine protokollierte User-Entscheidung („voll angleichen … inklusive Outfit-Tracking −4 % bis −2 %"), die UI-SPEC nennt das Wortmarken-Tracking explizit. Die Werte liegen als Tokens vor, aber kein Consumer wendet sie an — die App weicht in diesem Punkt sichtbar vom CI ab, das die Phase verbindlich ausrollen soll, und nirgends ist eine Vertagung dokumentiert. Fix ist mechanisch klein (~10 Style-Stellen). **Sollte das Nicht-Anwenden gewollt sein** (Layout-Shift-Vermeidung), ist ein Override in dieser Datei plus ein dokumentierender Kommentar der saubere Weg.
2. **CLAUDE.md widerspricht dem gelieferten Stand (WR-02, bestätigt).** Ein-Satz-Fix.

Beide Gaps teilen keinen Root-Cause und sind unabhängig schließbar; zusammen sind sie deutlich unter einer Stunde Arbeit.

**Override-Vorlage, falls Gap 1 als bewusste Vertagung akzeptiert wird:**

```yaml
overrides:
  - must_have: "Die CI-Typo-Rollen sind voll angeglichen — inklusive Outfit-Tracking −4 % bis −2 % (D-08, UI-SPEC)"
    reason: "RN-Anwenden des Trackings bewusst auf Folgephase verschoben (Layout-Shift auf allen Screens); Tokens bleiben als Single Source vorbereitet"
    accepted_by: "Emil"
    accepted_at: "<ISO-Timestamp>"
```

---

_Verified: 2026-08-11T12:45:00Z_
_Verifier: Claude (gsd-verifier)_
