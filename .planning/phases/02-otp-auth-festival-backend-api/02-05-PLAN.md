---
phase: 02-otp-auth-festival-backend-api
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/api/test/auth-guard.spec.ts
  - apps/api/test/festival-isolation.spec.ts
  - apps/api/test/bodyparser-smoke.spec.ts
  - .planning/phases/02-otp-auth-festival-backend-api/02-AUTH-ANNOTATIONS.md
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "SEC-01: every non-@AllowAnonymous endpoint (getMe, completeProfile, usernameAvailability, listFestivals, saveFestival, listMyFestivals) returns 401 without a session; health + /api/auth/* work anonymously (SC-1, SC-2)"
    - "SEC-01: one deliberate public-vs-protected pass is captured as an endpoint x auth-annotation table reviewed at phase end, with each endpoint listed exactly once (SC-2)"
    - "SEC-02: cross-tenant denial is proven — visitor 1 saves festival A only; GET /me/festivals for visitor 1 returns exactly [A], never [A,B], and visitor 2 (no saves) returns [] (SC-3)"
    - "SC-4: both an OTP-verify POST to /api/auth/* AND a ts-rest POST /api/v1/festivals/:id/save receive a correctly parsed request body (bodyParser:false + re-application proven with two POSTs)"
    - "Each endpoint is exactly one of anonymous or protected — no endpoint is both-tagged and none is untagged (SEC-01/adjacency edge)"
    - statement: "The endpoint x auth-annotation table enumerates every @TsRestHandler/@Controller method once, in a stable listed order, matching the actual decorators (no drift)"
      verification: backstop
  artifacts:
    - apps/api/test/auth-guard.spec.ts
    - apps/api/test/festival-isolation.spec.ts
    - apps/api/test/bodyparser-smoke.spec.ts
    - .planning/phases/02-otp-auth-festival-backend-api/02-AUTH-ANNOTATIONS.md
  key_links:
    - "festival-isolation.spec provisions its own two throwaway festivals (D-03) — it does not use or pollute the dev seed"
    - "bodyparser-smoke asserts the ts-rest POST body half against a live dev round-trip (RESEARCH Sampling Rate), not only in-process supertest"
  prohibitions:
    - "The auth-annotation table must NOT omit any endpoint and must NOT list an endpoint as both anonymous and protected"
    - "The isolation test must NOT rely on client-side filtering to distinguish saved vs all festivals — it asserts server-side scoping"
---

<objective>
Close the phase on its two security requirements with proof, not assertion: a
comprehensive login-first guard spec over the whole endpoint set, the SEC-01 endpoint x
auth-annotation table (the "one deliberate tagging pass reviewed at phase end"), the SEC-02
cross-tenant denial test with its own throwaway fixtures, and the two-POST body-parser
proof (the /api/auth POST half plus the ts-rest POST half). This is the phase gate.

Purpose: make SEC-01 and SEC-02 falsifiable and the body-parser wiring proven end-to-end.
Output: three verification specs + the reviewed auth-annotation table.
</objective>

<artifacts_produced>
- `apps/api/test/auth-guard.spec.ts` — SEC-01 comprehensive (all protected endpoints 401 unauthenticated; anonymous baseline works)
- `apps/api/test/festival-isolation.spec.ts` — SEC-02 cross-tenant denial with self-provisioned fixtures
- `apps/api/test/bodyparser-smoke.spec.ts` — two-POST body-parser proof
- `.planning/phases/02-otp-auth-festival-backend-api/02-AUTH-ANNOTATIONS.md` — endpoint x auth-annotation table (SEC-01 deliverable)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-otp-auth-festival-backend-api/02-CONTEXT.md
@.planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md
@.planning/phases/02-otp-auth-festival-backend-api/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-01 comprehensive auth-guard spec + endpoint x auth-annotation table</name>
  <read_first>
    - apps/api/src/me/me.controller.ts + apps/api/src/festival/festival.controller.ts + apps/api/src/health/health.controller.ts (the actual decorators on every method — the table must match reality)
    - apps/api/src/auth/auth.instance.ts (the /api/auth/* routes better-auth self-manages)
    - apps/api/test/auth-guard-tracer.spec.ts (the tracer's 401/200 pattern to generalize)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md (Validation Architecture: SEC-01 test rows) + PITFALLS.md Pitfall 3 (mis-tagging failure modes)
  </read_first>
  <action>
    Write `apps/api/test/auth-guard.spec.ts`: build the Nest test app; for each protected endpoint assert 401 without a session cookie — `GET /api/v1/me`, `POST /api/v1/me/complete-profile`, `GET /api/v1/me/username-availability?username=x`, `GET /api/v1/festivals`, `POST /api/v1/festivals/<uuid>/save`, `GET /api/v1/me/festivals`; and assert the anonymous baseline works without a cookie — `GET /api/v1/health` -> 200 and `POST /api/auth/sign-in/email-otp` (request) -> success. Then write `.planning/phases/02-otp-auth-festival-backend-api/02-AUTH-ANNOTATIONS.md` as a markdown table with columns: Method | Path | Controller#method | Annotation (protected-by-default / @AllowAnonymous) | Rationale — one row per endpoint including health and the better-auth /api/auth/* group; verify each row matches the actual decorator in source (no endpoint missing, none both-tagged). Note the only @AllowAnonymous entries are health and (implicitly, wrapper-managed) /api/auth/*.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api exec vitest run test/auth-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `auth-guard.spec.ts` passes: all six /api/v1 protected endpoints return 401 without a session; /api/v1/health returns 200 and the OTP-request POST succeeds without a session
    - `02-AUTH-ANNOTATIONS.md` lists every endpoint exactly once with its annotation and rationale; health + /api/auth/* are the only anonymous rows; the table matches the source decorators
  </acceptance_criteria>
  <done>Login-first is proven over the whole endpoint set and the reviewed annotation table exists.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-02 cross-tenant denial spec + two-POST body-parser proof</name>
  <read_first>
    - apps/api/src/me/me.service.ts (listMyFestivals caller-scoped join) + apps/api/src/festival/festival.service.ts (save)
    - apps/api/test/setup.ts (test-DB helper for transaction-scoped fixtures) + apps/api/test/save-idempotency.spec.ts (session + seeded-festival pattern to reuse)
    - .planning/phases/02-otp-auth-festival-backend-api/02-RESEARCH.md ("Cross-tenant denial query shape" + "Sampling Rate": live dev round-trip required for the ts-rest POST body half) + 02-CONTEXT.md D-03 (isolation test provisions its OWN two festivals)
  </read_first>
  <action>
    Write `apps/api/test/festival-isolation.spec.ts`: within a test transaction/scope, provision two throwaway festivals A and B and two visitors 1 and 2 (own fixtures per D-03 — do NOT touch the frequency-2026 dev seed). Visitor 1 saves ONLY festival A. Assert `listMyFestivals(visitor1.id)` (or GET /me/festivals with visitor 1's session) returns exactly [A], never [A,B]; assert `listMyFestivals(visitor2.id)` returns []. This proves scope comes from the session id, not a param, and that B never leaks into visitor 1's context. Write `apps/api/test/bodyparser-smoke.spec.ts`: prove BOTH POST bodies parse — (a) `POST /api/auth/sign-in/email-otp` with a JSON body is accepted and drives the OTP flow (the /api/auth half), and (b) `POST /api/v1/festivals/:id/save` with a session receives its parsed request context and writes the row (the ts-rest half). Per RESEARCH Sampling Rate, additionally run the ts-rest POST half once against a live `pnpm --filter @festipal/api dev` round-trip (documented command in the spec header) since in-process supertest may not reproduce Express body-parser stream edges.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api exec vitest run test/festival-isolation.spec.ts test/bodyparser-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `festival-isolation.spec.ts` passes: visitor 1 (saved A only) sees exactly [A]; visitor 2 sees []; festival B never appears in visitor 1's my-festivals (SEC-02)
    - The isolation spec provisions its own A/B fixtures and does not read/write the frequency-2026 dev seed
    - `bodyparser-smoke.spec.ts` passes: both the /api/auth POST and the ts-rest /api/v1 POST receive parsed bodies (SC-4)
  </acceptance_criteria>
  <done>Cross-tenant isolation and the two-POST body-parser wiring are both proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any client → /api/v1/* | Login-first: every protected endpoint rejects unauthenticated access (401) |
| visitor session → festival-scoped reads | my_festival data is isolated by the caller's id; no cross-tenant read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Elevation of Privilege | stray @AllowAnonymous / untagged route | high | mitigate | auth-guard.spec asserts 401 on all six protected endpoints; the annotation table is reviewed at phase end (SEC-01) |
| T-02-14 | Information Disclosure | cross-tenant my_festival leak | high | mitigate | festival-isolation.spec proves caller-scoped reads with self-provisioned A/B fixtures (SEC-02, Pitfall 4) |
| T-02-15 | Spoofing | unparsed POST bodies after bodyParser:false | medium | mitigate | bodyparser-smoke.spec proves both the /api/auth and ts-rest POST halves parse; ts-rest half additionally run against a live dev round-trip (SC-4) |
</threat_model>

<verification>
- `pnpm --filter @festipal/api exec vitest run` (full apps/api suite) green
- `node apps/api/test/smoke/otp-me-smoke.mjs` green against `pnpm --filter @festipal/api dev` (re-run the end-to-end proof)
- `02-AUTH-ANNOTATIONS.md` reviewed; COVERAGE.md present (produced at plan time)
</verification>

<success_criteria>
SEC-01 (all protected endpoints 401 unauthenticated + reviewed annotation table) and SEC-02 (cross-tenant denial with own fixtures) are proven, and the two-POST body-parser wiring is verified — the phase gate is green.
</success_criteria>

<output>
Create `.planning/phases/02-otp-auth-festival-backend-api/02-05-SUMMARY.md` when done
</output>
