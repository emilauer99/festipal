---
phase: 04-visitor-auth-profile-completion
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-01"]
files_modified:
  - apps/mobile/app/(profile-setup)/complete-profile.tsx
  - apps/mobile/components/AvatarTile.tsx
  - apps/mobile/lib/avatar-storage.ts
  - apps/mobile/lib/username-suggestion.ts
  - apps/mobile/lib/__tests__/username-suggestion.test.ts
  - apps/mobile/locales/en/messages.po
  - apps/mobile/locales/de/messages.po
autonomous: true
requirements: [IDN-01]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The avatar picker (expo-image-picker: gallery + camera, with permission handling) lets the visitor choose/capture a photo shown via expo-image in the avatar-tile position; the chosen file:// URI is written to MMKV keyed by accountId (`avatar-uri:${accountId}`) and survives an app restart on the same device (D-01)."
    - "The avatar is device-local ONLY: POST /me/complete-profile body stays username + displayName (no avatar field), visitor_profile.avatar stays null, and there is no upload endpoint / object store (D-01) — cross-device sync is explicitly not built."
    - "AvatarTile shows 2-letter uppercase initials (derived from displayName, or username if displayName empty) whenever no local photo is set, and the local expo-image photo otherwise (Copywriting Contract avatar-tile fallback)."
    - "The username-taken state shows both Copywriting-Contract lines: '@{username} is already taken.' + 'Try something else, like @{suggestion}.', where {suggestion} is a generated, available candidate; on completeProfile 409 the suggestion is regenerated (IDN-01)."
    - "generateUsernameSuggestion is a pure, unit-tested function that NEVER returns more than 20 chars regardless of input displayName/username length, only emits the a-z 0-9 _ . charset, and is verified available via GET /me/username-availability before display (bounded retry) — closes the UI-SPEC long-text suggestion backstop + D-03 20-char cap."
    - statement: "A chosen avatar photo visually replaces the initials tile correctly (circular, r-pill/avatar radius) at build time — the populated avatar state is not pictured in the mockup, so this is verified by a real-picked-image visual smoke check (UI-SPEC populated/profile-completion avatar backstop)."
      verification: backstop
    - statement: "A long or multi-byte/emoji displayName (up to the 40-char server cap) does not break the avatar-tile initials derivation or the profile layout — the client soft-cap (40) + single-line truncation hold and the initials derivation handles grapheme/combining content without crashing (UI-SPEC long-text/displayName + IDN-01 encoding edge)."
      verification: backstop
    - "IDN-01 precision (rounding / overflow / tie-breaking) is N/A — flagged, not fabricated: username/displayName handling involves no numeric computation; the only bound is character length, covered by the D-03 caps and the suggestion generator's ≤20 slice."
  prohibitions:
    - "MUST NOT upload the avatar anywhere or add an `avatar` field to the complete-profile request body — device-local MMKV only, visitor_profile.avatar stays null (D-01)."
    - "MUST NOT store the avatar URI in SecureStore or build any cross-device sync — MMKV keyed by accountId is the whole mechanism (D-01); SecureStore stays session-token-only (Pitfall 2 boundary)."
    - "MUST NOT surface an unavailable username suggestion — the generator verifies availability before display and regenerates on collision (Pattern 5)."
  artifacts:
    - "apps/mobile/lib/avatar-storage.ts (MMKV wrapper: saveLocalAvatarUri/getLocalAvatarUri/clearLocalAvatarUri, keyed avatar-uri:${accountId})"
    - "apps/mobile/components/AvatarTile.tsx (initials ↔ local photo)"
    - "apps/mobile/lib/username-suggestion.ts (generateUsernameSuggestion, ≤20 chars, charset-safe)"
    - "apps/mobile/lib/__tests__/username-suggestion.test.ts"
    - "avatar action + username taken/suggestion DE/EN msgids"
---

<objective>
Complete the IDN-01 profile surface the tracer left thin: the optional device-local avatar
(expo-image-picker → MMKV → expo-image, D-01), the initials-fallback `AvatarTile`, and the full
username taken-state with a generated, verified-available suggestion (Claude's-discretion generator,
D-03-capped and unit-tested). This finishes the profile-completion screen the tracer (04-03) opened.

Purpose: IDN-01's optional-avatar half and the "try something else, like @{suggestion}" affordance —
the only genuinely-new client logic this phase (deep-link aside): the first MMKV usage in the repo and
a pure suggestion generator. Everything backend stays untouched (the write is still username +
displayName; D-01).
Output: `avatar-storage.ts`, `AvatarTile.tsx`, `username-suggestion.ts` (+ test), and the finished
complete-profile screen with avatar block + full taken-state copy.
</objective>

## Artifacts this plan produces

- **apps/mobile/lib/avatar-storage.ts** — MMKV wrapper (`createMMKV({ id: 'festipal-avatar' })`),
  `saveLocalAvatarUri`/`getLocalAvatarUri`/`clearLocalAvatarUri`, key `avatar-uri:${accountId}`.
- **apps/mobile/components/AvatarTile.tsx** — initials tile (accent tint/ring) ↔ `expo-image` local
  photo; 2-letter uppercase initials from displayName || username.
- **apps/mobile/lib/username-suggestion.ts** — `generateUsernameSuggestion(taken)` → ≤20-char,
  charset-safe, availability-verified candidate (bounded retry).
- **apps/mobile/lib/__tests__/username-suggestion.test.ts** — proves the ≤20-char + charset invariants.
- **apps/mobile/app/(profile-setup)/complete-profile.tsx** — avatar block (Choose photo / Take photo /
  optional helper) + full username taken+suggestion state + 409-regenerates-suggestion.
- **DE/EN msgids** — avatar actions/helper, username taken line 1 + suggestion line 2.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visitor-auth-profile-completion/04-CONTEXT.md
@.planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md
@.planning/phases/04-visitor-auth-profile-completion/04-PATTERNS.md
@.planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md
@apps/mobile/app/(profile-setup)/complete-profile.tsx
@apps/mobile/lib/api-client.ts
@packages/ui/src/tokens.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: username-suggestion generator (pure, ≤20-char, unit-tested)</name>
  <files>apps/mobile/lib/username-suggestion.ts, apps/mobile/lib/__tests__/username-suggestion.test.ts</files>
  <read_first>
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (Pattern 5 — sanitizeBase + suffix + slice(0,20) approach; bounded retry)
    - .planning/phases/04-visitor-auth-profile-completion/04-CONTEXT.md (D-03 — suggestion MUST cap ≤20 chars; Claude's-discretion algorithm)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## UI Considerations long-text/username-taken-suggestion backstop)
  </read_first>
  <behavior>
    - generateUsernameSuggestion('averylongdisplaynamethatexceeds') → length ≤ 20, only [a-z0-9_.]
    - generateUsernameSuggestion('Bob!! 😀') → sanitized to [a-z0-9_.], length ≤ 20
    - generateUsernameSuggestion('') → still returns a valid ≤20-char [a-z0-9_.] candidate
    - repeated calls → varied suffix (low collision), each ≤ 20 chars
  </behavior>
  <action>
Create `lib/username-suggestion.ts` per RESEARCH Pattern 5: `sanitizeBase(input)` lowercases, strips
to `[a-z0-9_.]`, and slices to leave room for a `_NNNN` suffix; `generateUsernameSuggestion(taken)`
returns `${base}${suffix}` sliced to a hard 20-char ceiling. Export a separate async
`suggestAvailableUsername(taken, checkFn)` that calls `generateUsernameSuggestion` and verifies the
candidate via the passed availability checker (so the pure generator stays runtime-free and testable),
regenerating on collision with a bounded retry (e.g. 3) before falling back to the last candidate. Add
`lib/__tests__/username-suggestion.test.ts` asserting the ≤20-char + charset invariants across the
`<behavior>` inputs (pure function, no RN runtime needed).
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test -- username-suggestion && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`generateUsernameSuggestion` never exceeds 20 chars and only emits `[a-z0-9_.]` for any input (proven by the test); `suggestAvailableUsername` verifies availability before returning and retries a bounded number of times; test + typecheck pass.</acceptance_criteria>
  <done>The suggestion generator is a tested pure function honouring the D-03 cap.</done>
</task>

<task type="auto">
  <name>Task 2: avatar-storage (MMKV) + AvatarTile component</name>
  <files>apps/mobile/lib/avatar-storage.ts, apps/mobile/components/AvatarTile.tsx, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (Code Examples — MMKV avatar-storage shape; Pattern data-flow avatar; Pitfall 3 NitroModules/native rebuild; Open Question 1 key-by-accountId)
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Copywriting Contract avatar rows; ## Color avatar-tile accent ring; ## UI Considerations populated/avatar backstop)
    - .planning/phases/04-visitor-auth-profile-completion/04-CONTEXT.md (D-01 — device-local only; Claude's-discretion MMKV key/shape + permission handling)
    - packages/ui/src/tokens.ts (accent tint/ring, r-avatar/pill radius, text tokens)
  </read_first>
  <action>
Create `lib/avatar-storage.ts` as the MMKV wrapper (RESEARCH Code Examples): `createMMKV({ id:
'festipal-avatar' })`, `saveLocalAvatarUri(accountId, uri)`, `getLocalAvatarUri(accountId)`,
`clearLocalAvatarUri(accountId)`, key `avatar-uri:${accountId}` (keyed by accountId per Open Question
1 so a second account on the same device never sees the first's photo). Store ONLY the URI string,
never image bytes; never the session token (Pitfall 2 boundary). Create `components/AvatarTile.tsx`
(ADR-022 primitive on tokens): given `displayName`, `username`, and an optional `localUri`, render the
`expo-image` photo (circular, r-avatar/pill radius) when a URI is present, else a 2-letter uppercase
initials tile (accent tint background + ring) derived from displayName (fallback username) — derive
initials defensively so multi-byte/emoji/empty content cannot crash it (IDN-01 encoding edge). Add the
avatar action/helper msgids ("Choose photo"/"Take photo" a11y/"Optional. You can add this later.") +
DE verbatim ("Foto wählen"/"Foto aufnehmen"/"Optional. Geht auch später."); run extract. (Wiring the
picker into the screen is Task 3.)
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`avatar-storage.ts` exposes save/get/clear keyed `avatar-uri:${accountId}` storing only the URI; `AvatarTile` renders initials ↔ local photo and derives initials without crashing on empty/multi-byte names; tokens + Lucide only; lint + typecheck pass.</acceptance_criteria>
  <done>The device-local avatar store + tile primitive exist; session-store boundary intact.</done>
</task>

<task type="auto">
  <name>Task 3: Wire avatar picker + full username taken-state into complete-profile.tsx</name>
  <files>apps/mobile/app/(profile-setup)/complete-profile.tsx, apps/mobile/locales/en/messages.po, apps/mobile/locales/de/messages.po</files>
  <read_first>
    - apps/mobile/app/(profile-setup)/complete-profile.tsx (self — the tracer form from 04-03; add the avatar block + finish the taken state)
    - apps/mobile/components/AvatarTile.tsx (Task 2)
    - apps/mobile/lib/avatar-storage.ts (Task 2)
    - apps/mobile/lib/username-suggestion.ts (Task 1)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (ImagePicker permission functions + mediaTypes array API; Anti-Patterns "Uploading the avatar anywhere")
    - .planning/phases/04-visitor-auth-profile-completion/04-UI-SPEC.md (## Copywriting Contract username taken lines 1+2; ## Visual Hierarchy Profile)
  </read_first>
  <action>
Add the avatar block above the username field: `AvatarTile` (reading `getLocalAvatarUri(accountId)`
on mount) + a "Choose photo" button (gallery) and an icon-only "Take photo" button (camera), each
requesting the appropriate `expo-image-picker` permission and, on a successful pick/capture, calling
`saveLocalAvatarUri(accountId, uri)` and updating the tile — 44px hit targets, optional helper caption
below (D-01: this is device-local; nothing is uploaded, the completeProfile body stays
username+displayName). Finish the username taken-state: on `available:false` (live check) or a
completeProfile 409, show line 1 "@{username} is already taken." and line 2 "Try something else, like
@{suggestion}." where `{suggestion}` comes from `suggestAvailableUsername(username, checkFn)`;
regenerate the suggestion on each fresh 409. Apply the danger 16%-tint field styling for the taken
state (UI-SPEC Color). Keep the body of the POST unchanged (no avatar). Run extract for the taken/
suggestion msgids + fill DE verbatim ("@{username} ist schon vergeben." / "Nimm etwas anderes, zum
Beispiel @{suggestion}.").
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm lint && pnpm typecheck && pnpm test</automated>
    <human-check>On the dev build: tap Choose photo → pick an image → it replaces the initials tile (circular); force-quit + relaunch to the same account → the photo persists (MMKV). Type a taken username → see both taken lines with a ≤20-char available suggestion. Confirm the complete-profile request body has no avatar field.</human-check>
  </verify>
  <acceptance_criteria>The avatar picker (gallery + camera, permissioned) persists the URI to MMKV keyed by accountId and shows it via AvatarTile surviving restart; the taken-state shows both contract lines with a verified ≤20-char suggestion; the request body carries no avatar; DE copy verbatim; lint + typecheck + test green.</acceptance_criteria>
  <done>IDN-01's optional-avatar half and the full username taken/suggestion affordance are complete and device-local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device gallery/camera → app | User-selected media file:// URI enters the app |
| MMKV (local) → app | Non-sensitive avatar URI at rest, keyed by accountId |
| client → API (username-availability) | Advisory username strings cross for the suggestion check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-05-I | Information Disclosure | MMKV avatar URI | low | mitigate | Keyed by accountId so a second account on a shared device never reads the first's photo (Open Question 1); only the URI string is stored, no session token (Pitfall 2 boundary) |
| T-4-05-T | Tampering | image-picker input | low | accept | expo-image-picker returns a sandboxed file:// URI; no upload/execution surface (device-local display only, D-01) |
| T-4-05-S | Spoofing | username suggestion | low | mitigate | Suggestion charset-restricted (a-z 0-9 _ .) + availability-verified before display; DB unique index remains the authority (Pitfall 11) |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile test` (username-suggestion invariants) + `lint` + `typecheck` green.
- Manual: pick/capture avatar → persists across force-quit; taken-state shows both lines + ≤20-char
  suggestion; request body has no avatar — recorded in SUMMARY.
</verification>

<success_criteria>
IDN-01 profile completion is fully designed: optional device-local avatar (MMKV, keyed by accountId,
survives restart, never uploaded) with initials fallback, and a full username taken-state with a
generated, verified-available, ≤20-char suggestion (unit-tested).
</success_criteria>

<output>
Create `.planning/phases/04-visitor-auth-profile-completion/04-05-SUMMARY.md` when done.
</output>
