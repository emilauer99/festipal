---
phase: 04-visitor-auth-profile-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/vitest.config.ts
  - apps/mobile/lib/fonts.ts
  - apps/mobile/lib/__tests__/fonts.test.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [AUTH-05, IDN-01]
user_setup:
  - service: local-dev-machine
    why: "The Windows Metro × pnpm install race (STATE.md Phase 3): a running Expo/Metro file-watcher makes pnpm install fail ENOENT on *_tmp_*/node_modules. Every Expo/Metro process must be stopped before the install task runs."
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "apps/mobile has a Vitest runner: `pnpm --filter @festipal/mobile test` runs and exits 0 against node-environment pure-function tests (no RN component rendering / jest-expo)."
    - "Font loading is non-blocking: useFonts() readiness gates only the `fontFamily` a StyleSheet resolves (system-font fallback until loaded), never the splash hideAsync / return-null decision (Pitfall 5, D-04)."
    - "The five newly-added native-module deps (expo-image-picker, expo-image, react-native-mmkv, react-native-nitro-modules, react-native-svg) autolink after a fresh native prebuild; MMKV's NitroModules peer is installed in the same command (Pitfall 3)."
    - statement: "The 5 [SUS]/too-new packages (lucide-react-native, expo-image-picker, expo-image, expo-font, react-native-nitro-modules) are human-confirmed legitimate against npmjs.com before install — the blocking-human legitimacy checkpoint is not auto-approvable."
      verification: backstop
  prohibitions:
    - "MUST NOT run the dependency install while any Expo/Metro process is running (Windows Metro × pnpm ENOENT race, STATE.md Phase 3)."
    - "MUST NOT install react-native-mmkv without its mandatory react-native-nitro-modules peer in the same install command (v4 fails at runtime, not install time — Pitfall 3)."
  artifacts:
    - apps/mobile/vitest.config.ts
    - apps/mobile/lib/fonts.ts
    - apps/mobile/lib/__tests__/fonts.test.ts
---

<objective>
Lay the Wave-0 foundation every later Phase-4 screen depends on: install the five new UI/native
dependency groups (icons, SVG, image picker/display + MMKV, Google Fonts), stand up the missing
Vitest runner in `apps/mobile` (the RESEARCH/VALIDATION Wave-0 gap — there is no test framework in
`apps/mobile` today), and add the non-blocking font-loading module.

Purpose: unblock the OTP/profile restyle, the MMKV avatar pipeline, and the two pure-function unit
tests (`mapOtpError`, `generateUsernameSuggestion`) that Vitest will cover in later plans. Nothing
here ships user-facing behaviour on its own — it is pure enabling infrastructure.
Output: updated `apps/mobile/package.json` + `pnpm-lock.yaml`, `app.json` image-picker config
plugin, `vitest.config.ts`, `lib/fonts.ts`, and a first passing font-module unit test.
</objective>

## Artifacts this plan produces

- **Deps (apps/mobile/package.json + pnpm-lock.yaml):** `expo-image-picker`, `expo-image`,
  `react-native-mmkv`, `react-native-nitro-modules`, `lucide-react-native`, `react-native-svg`,
  `expo-font`, `@expo-google-fonts/outfit`, `@expo-google-fonts/plus-jakarta-sans`,
  `@expo-google-fonts/jetbrains-mono`; devDep `vitest`.
- **app.json:** `expo-image-picker` config-plugin entry with Info.plist photo/camera permission strings.
- **apps/mobile/vitest.config.ts:** node-environment Vitest config (mirrors `apps/api/vitest.config.ts` shape).
- **apps/mobile/lib/fonts.ts:** `useAppFonts()` hook wrapper + exported font-family name constants
  (`FONT_DISPLAY`, `FONT_BODY`, `FONT_MONO`) with graceful system-font fallback.
- **apps/mobile/lib/__tests__/fonts.test.ts:** unit test asserting the font-family constants and the
  fallback contract.
- **package.json `test` script:** `vitest run` for `@festipal/mobile`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md
@.planning/phases/04-visitor-auth-profile-completion/04-VALIDATION.md
@apps/mobile/package.json
@apps/mobile/app.json
@apps/mobile/lib/i18n.ts
@apps/api/vitest.config.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Package legitimacy sign-off (blocking-human, NOT auto-approvable)</name>
  <decision>Approve installing the five packages RESEARCH.md flagged `[SUS] too-new` before the install runs.</decision>
  <context>
RESEARCH.md `## Package Legitimacy Audit` flagged `lucide-react-native`, `expo-image-picker`,
`expo-image`, `expo-font`, and `react-native-nitro-modules` as `[SUS]` solely on the `too-new`
signal (recent publish timestamp), NOT on low downloads / missing repo / postinstall script. All
five are official-monorepo packages (`expo/expo`, `mrousavy/*`, `lucide-icons/lucide`) with 1.4M–7.6M
weekly downloads — assessed as a false positive of the freshness heuristic. Per the Package
Legitimacy Protocol the verdict is honoured literally: a human confirms legitimacy before install.
`workflow.auto_advance` is ignored for this gate.
  </context>
  <options>
    <option id="approve">
      <name>Confirm legitimate — proceed to install</name>
      <pros>Verified each against npmjs.com (publisher = expo / mrousavy / lucide-icons, repo links resolve).</pros>
      <cons>None if the npmjs.com publisher/repo checks pass.</cons>
    </option>
    <option id="hold">
      <name>Hold — a check failed</name>
      <pros>Stops a questionable dependency entering a mobile app shipping to real visitors.</pros>
      <cons>Blocks the phase until resolved.</cons>
    </option>
  </options>
  <verify>
    <human-check>Open npmjs.com/package/&lt;name&gt; for each of the 5 packages; confirm publisher (expo / mrousavy / lucide-icons) and that the source repo link resolves. Approve only if all 5 pass.</human-check>
  </verify>
  <acceptance_criteria>A human has explicitly recorded approval (or hold) for all five [SUS] packages; the install task does not start until this is approved.</acceptance_criteria>
  <resume-signal>Select: approve or hold</resume-signal>
  <done>All five [SUS] packages are human-confirmed legitimate (or the phase is held).</done>
</task>

<task type="auto">
  <name>Task 2: Install the five new dependency groups + image-picker config plugin</name>
  <precondition>Every Expo/Metro process (including any `expo run:android`/`expo run:ios` watcher) is stopped before this task runs — the Windows Metro × pnpm install race makes install fail ENOENT on `*_tmp_*/node_modules` otherwise (STATE.md Phase 3, Pitfall 6).</precondition>
  <files>apps/mobile/package.json, apps/mobile/app.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/package.json (current dependency set — expo ~57.0.9 already present)
    - apps/mobile/app.json (current plugins array + scheme `festipal`)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (## Standard Stack Installation block; Pitfall 3 NitroModules peer; Pitfall 6 Windows race)
  </read_first>
  <action>
Install, in ONE `pnpm --filter @festipal/mobile add` command (per RESEARCH Installation block, so
MMKV's peer lands with it): `expo-image-picker`, `expo-image`, `react-native-mmkv`,
`react-native-nitro-modules`, `lucide-react-native`, `react-native-svg`, `expo-font`,
`@expo-google-fonts/outfit`, `@expo-google-fonts/plus-jakarta-sans`,
`@expo-google-fonts/jetbrains-mono`. Prefer `npx expo install &lt;pkg&gt;` resolution for the four
SDK-locked Expo packages (`expo-image-picker`, `expo-image`, `expo-font`) so they match Expo SDK 57;
treat RESEARCH's pinned versions as a floor, not a hard pin. Add the `expo-image-picker` config
plugin to `app.json` `plugins` with `photosPermission` and `cameraPermission` strings (English source
text — DE handled by store localization later; these are Info.plist/AndroidManifest strings, not
Lingui UI copy). Do NOT add any avatar/session logic here — this task is install + native config only.
Run a fresh native prebuild so MMKV/NitroModules/SVG/image-picker autolink (Pitfall 3: MMKV v4 needs
the NitroModules peer + a native rebuild and cannot run in Expo Go). Keep the session token on
SecureStore — MMKV is introduced here but wired ONLY to the avatar URI in plan 04-05 (never the
session token; Pitfall 2 boundary).
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm typecheck</automated>
    <human-check>On the real Android dev build (not Expo Go), confirm the app still cold-starts to the Festivals list after the prebuild — proving the new native modules autolinked without a TurboModule/Nitro "could not be found" crash (Pitfall 3).</human-check>
  </verify>
  <acceptance_criteria>`apps/mobile/package.json` lists all ten new packages + the NitroModules peer; `app.json` has the image-picker config plugin with permission strings; `pnpm typecheck` passes; the dev build cold-starts without a native-module-not-found error.</acceptance_criteria>
  <done>All new deps installed together, image-picker permissions declared, native prebuild picks up the modules.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest runner for apps/mobile + non-blocking font module</name>
  <files>apps/mobile/vitest.config.ts, apps/mobile/lib/fonts.ts, apps/mobile/lib/__tests__/fonts.test.ts, apps/mobile/package.json</files>
  <read_first>
    - apps/api/vitest.config.ts (existing node-environment Vitest config to mirror — `environment: 'node'` shape)
    - apps/mobile/lib/i18n.ts (existing async-bootstrap-wrapper shape to mirror for the font hook)
    - .planning/phases/04-visitor-auth-profile-completion/04-RESEARCH.md (## Anti-Patterns — "Gating splash-hide on font load"; Pitfall 5)
    - .planning/phases/04-visitor-auth-profile-completion/04-PATTERNS.md (`lib/fonts.ts` NEW section)
  </read_first>
  <action>
Add `vitest` as a devDependency to `@festipal/mobile` and create `apps/mobile/vitest.config.ts`
mirroring `apps/api/vitest.config.ts` (`environment: 'node'`, no jest-expo, scoped to pure `lib/`
function tests — no RN component rendering this phase). Add a `"test": "vitest run"` script to
`apps/mobile/package.json`. Create `lib/fonts.ts` exporting a `useAppFonts()` hook that wraps
`useFonts` from `expo-font` with the three `@expo-google-fonts/*` families, and exports font-family
name constants `FONT_DISPLAY` (Outfit), `FONT_BODY` (Plus Jakarta Sans), `FONT_MONO` (JetBrains Mono).
The module contract (enforced by Pitfall 5 / D-04): the `fontsLoaded` boolean it returns is meant to
gate ONLY which `fontFamily` a style resolves (fall back to `undefined` = RN system font until
loaded) — it must NEVER be consumed as a splash-hide / return-null gate. Add `lib/__tests__/fonts.test.ts`
asserting the three font-family constants are the expected literal names and documenting (via the
test's own assertions) that the constants are plain strings usable as a fallback-safe `fontFamily`.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>`pnpm --filter @festipal/mobile test` runs Vitest and the font test passes; `lib/fonts.ts` exports `useAppFonts`, `FONT_DISPLAY`, `FONT_BODY`, `FONT_MONO`; nothing in `lib/fonts.ts` calls `SplashScreen.hideAsync` or returns null based on font readiness.</acceptance_criteria>
  <done>Mobile Vitest runner is live and green; the non-blocking font module exists and is unit-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build pipeline | Five new third-party packages enter the mobile build |
| device storage (MMKV, introduced here) → app | Non-sensitive local media URI at rest (wired in 04-05, not here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01-SC | Tampering | npm installs (5 [SUS] pkgs) | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; verify each publisher/repo on npmjs.com; honour verdict, no silent downgrade |
| T-4-01-I | Information Disclosure | react-native-mmkv (introduced) | low | accept | MMKV holds only the non-sensitive avatar URI (04-05), never the session token — SecureStore stays the sole session store (Pitfall 2 boundary) |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile test` runs and is green (runner did not exist before this plan).
- `pnpm --filter @festipal/mobile typecheck` passes with the new deps resolved.
- Dev-build cold-start smoke on real Android confirms native autolinking (no Nitro/TurboModule crash).
- Legitimacy checkpoint recorded before install.
</verification>

<success_criteria>
Mobile test runner live + green; ten deps + NitroModules peer installed together with image-picker
permissions; non-blocking font module exists and is unit-tested; no session-store regression (MMKV
not wired to the session token).
</success_criteria>

<output>
Create `.planning/phases/04-visitor-auth-profile-completion/04-01-SUMMARY.md` when done.
</output>
