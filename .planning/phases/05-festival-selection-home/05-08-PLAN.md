---
phase: 05-festival-selection-home
plan: 08
type: execute
wave: 5
depends_on: [05-06, 05-07]
files_modified: []
autonomous: true
requirements: [FEST-01, FEST-02, FEST-03, FEST-04, HOME-01, HOME-02]
user_setup: []
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The whole monorepo passes a single clean gate after both Wave-4 screens land: pnpm typecheck, pnpm lint, and pnpm test are green across all packages/apps (REVIEW cross-plan MEDIUM — no final phase-wide gate existed)."
    - "A clean install + build/export of apps/mobile succeeds (no stale route-type or lockfile drift from the expo-blur/typedRoutes changes)."
    - "The full acceptance flow works end-to-end on a real device: login → Home hero/rail → open Festivals on Alle (via the empty-state/see-all segment=all CTA) → Save → the festival appears under Meine → enter it → Back returns to the shell (never a dead-end)."
    - "Cold-start focus + back: enter a festival, force-quit, relaunch → the festival home opens directly; Back from it lands on the Festivals tab (leaveFestival shell fallback), never exits the app."
    - "Cross-account hygiene: after logout the active-festival slug is cleared, so logging in as a different account on the same device starts on Home (not the prior account's festival)."
    - "Localization + a11y: date ranges re-format on DE/EN device-language switch (Intl, null → localized fallback), and the disabled Friends/Profil tabs announce a localized 'coming soon'/'bald verfügbar'."
    - { statement: "A logged-out deep link is replayed and WINS over the active-festival cold-start redirect after auth (deep-link precedence, REVIEW 05-05 HIGH).", verification: backstop }
  artifacts: []
  key_links:
    - "all Wave-1..4 plans -> single monorepo typecheck/lint/test gate"
    - "05-06 segment=all contract + 05-07 CTA -> Alle-segment acceptance step"
    - "05-03 leaveFestival + 05-05 cold-start replace -> cold-start-back acceptance step"
  prohibitions:
    - "Do NOT modify feature code in this plan; it is a verification-only gate. If a gate fails, file the fix into the owning plan's scope (or a gap-closure plan), not here."
    - "Do NOT skip the on-device acceptance flow because the automated gates pass; the RN navigation/interaction paths have no automated coverage (Vitest excludes RN rendering)."
---

<objective>
Final phase-wide integration gate: after both Wave-4 screens (05-06 Festivals, 05-07 Home) land, run
the complete monorepo quality suite and a scripted end-to-end acceptance flow on a real device, so the
phase ships as a coherent whole rather than seven independently-green slices. This closes the reviewer's
cross-plan finding that no plan owned the full-suite + full-acceptance run.

Purpose: prove the integrated phase satisfies all six requirements (FEST-01..04, HOME-01/02) and the
cross-plan edges (date nullability, cache shape, cold-start back, deep-link precedence, cross-tab
segment) together, on device.
Output: a green monorepo gate + a signed-off acceptance run (no new feature code).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-VALIDATION.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

- None (verification-only gate). No source files are created or modified.

<tasks>

<task type="auto">
  <name>Task 1: Full monorepo gate + clean mobile build/export</name>
  <files></files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-VALIDATION.md (the Per-Task Verification Map — confirm every requirement's automated check is represented)
    - turbo.json (the `typecheck`/`lint`/`test`/`build` task graph run across the workspace)
    - apps/mobile/package.json (the mobile build/export script + expo-blur/typedRoutes changes from 05-02)
  </read_first>
  <action>
    Run the whole-workspace quality gate: `pnpm typecheck`, `pnpm lint`, `pnpm test` (Turbo fans these
    across every package/app — packages/db, packages/contracts, packages/ui, apps/api, apps/mobile).
    All must be green. Then prove a clean mobile build path: from a clean install (`pnpm install`
    with no Metro/Expo watcher running — Windows install race), regenerate the Expo route types and run
    the mobile build/export (`pnpm --filter @festipal/mobile exec expo export` or the app's build
    script) to confirm no stale `.expo/types` route declaration or lockfile drift from the
    expo-blur/typedRoutes changes. If any gate fails, STOP and record the failing package + error for a
    gap-closure plan — do NOT patch feature code inside this verification plan.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test</automated>
  </verify>
  <done>pnpm typecheck + lint + test are green across the whole monorepo, and a clean mobile build/export succeeds with no route-type or lockfile drift.</done>
  <acceptance_criteria>
    - `pnpm typecheck`, `pnpm lint`, `pnpm test` all exit 0 across all workspace packages/apps.
    - A clean `pnpm install` + Expo route-type regen + mobile build/export completes without error.
    - No feature code is modified in this plan.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: On-device end-to-end acceptance flow (all six requirements + cross-plan edges)</name>
  <files></files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Copywriting Contract + ## Visual Hierarchy (the exact copy/layout to confirm on device)
    - .planning/ROADMAP.md §"Phase 5" (the 5 success criteria this flow must demonstrate)
    - .planning/phases/05-festival-selection-home/05-VALIDATION.md (manual verification rows)
  </read_first>
  <action>
    On a real Android device (matching the Phase 3/4 gate — Vitest excludes RN rendering, so these paths
    have no automated coverage), execute the full acceptance script and confirm each step:
    (1) Login → land on the Home tab (initialRouteName=home) showing the "Dein nächstes Festival" hero +
    "Meine Festivals" rail (or, with zero saved, the shared empty-state block).
    (2) From the empty-state CTA (or the rail "Alle" see-all) → the Festivals tab OPENS on the Alle
    segment (segment=all param), not Meine.
    (3) In Alle, tap Save on an unsaved festival → it flips to 'Gespeichert' and appears under Meine;
    a rapid double-tap enqueues exactly one save; kill the API to force a failed save → the optimistic
    insert rolls back with a visible localized error.
    (4) Enter a festival → its home shows name (H1) + locale-formatted "{dates} · {place}" + the four
    disabled coming-soon tiles; Back returns to the Festivals tab (never a dead-end).
    (5) Enter a festival, force-quit, relaunch → the festival home opens directly; Back lands on the
    Festivals tab (leaveFestival shell fallback), does not exit the app.
    (6) Logout → login as a DIFFERENT account on the same device → starts on Home, NOT the prior
    account's festival (active-festival slug cleared on logout).
    (7) A logged-out deep link (to /f/:slug) → after auth it is replayed and WINS over the
    active-festival redirect.
    (8) Toggle device language DE↔EN → date ranges re-format (and a null date shows the localized
    fallback); the disabled Friends/Profil tabs announce a localized "coming soon"/"bald verfügbar".
    Record pass/fail per step; any failure is a gap for a gap-closure plan, not an edit here.
  </action>
  <verify>
    <automated>echo "Manual on-device acceptance flow — see human-check below"</automated>
    <human-check>All 8 acceptance steps above pass on a real Android device; each step's expected result is observed and signed off (or failures recorded for gap closure).</human-check>
  </verify>
  <done>The full login→Home→Alle→save→Meine→enter→back, force-quit→active-festival, cold-start-back→shell, logout/login-other-account, deep-link-precedence, and DE/EN date + a11y flow is verified on device and signed off.</done>
  <acceptance_criteria>
    - Every one of the 8 acceptance steps is observed to pass on a real device (or its failure recorded).
    - The Alle-segment CTA, cold-start-back-to-shell, cross-account slug clear, and deep-link precedence edges are each explicitly confirmed.
    - DE/EN date re-formatting (incl. null fallback) and the disabled-tab localized a11y label are confirmed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no new data path, persistence, or trust decision is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Information Disclosure | full-phase acceptance run | high | mitigate | Step (6)/(7) explicitly verify cross-account active-festival hygiene and that the authenticated guard still gates the shell (a logged-out deep link is redirected then replayed only AFTER auth), confirming SEC-01/SEC-02 hold end-to-end across the integrated phase |
| T-05-15 | Elevation of Privilege | integrated route tree | low | accept | This plan adds no routes/code; it only exercises the already-mitigated 05-05 `Stack.Protected` guard — no new attack surface |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint && pnpm test` green monorepo-wide + clean mobile build/export (Task 1)
- On-device 8-step acceptance flow signed off (Task 2)
</verification>

<success_criteria>
- The integrated phase passes a single monorepo quality gate and the full on-device acceptance flow, demonstrating all six requirements and the cross-plan edges together.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-08-SUMMARY.md` when done.
</output>
