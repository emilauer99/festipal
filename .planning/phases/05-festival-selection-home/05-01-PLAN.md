---
phase: 05-festival-selection-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/festival.ts
  - packages/contracts/src/schemas.ts
  - packages/db/scripts/seed.ts
  - packages/db/drizzle/
  - apps/api/test/festival-isolation.spec.ts
autonomous: true
requirements: [FEST-01, HOME-02]
estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GET /festivals, GET /me/festivals and GET /festivals/:slug each return startDate, endDate and place on every festival row (FEST-01, HOME-02)."
    - "packages/contracts festivalSchema is composed on a drizzle-zod createSelectSchema(festival) base, so renaming a festival column breaks the contract typecheck instead of drifting silently (Pitfall 1/6)."
    - "The frequency-2026 seed row carries real startDate/endDate/place values that round-trip unchanged through all three read endpoints."
    - "A visitor who saved only festival A never sees festival B's startDate/endDate/place in their GET /me/festivals response (SEC-02 cross-tenant isolation for the new fields)."
    - "packages/db build/typecheck and the full apps/api supertest suite pass after the migration is applied to Neon."
  artifacts:
    - packages/db/src/schema/festival.ts (startDate/endDate/place columns + festivalSelectSchema + festivalInsertSchema)
    - packages/contracts/src/schemas.ts (festivalSchema recomposed on the drizzle-zod base)
    - packages/db/scripts/seed.ts (seed extended with dates/place)
    - packages/db/drizzle/ (new generated + applied migration for the three columns)
    - apps/api/test/festival-isolation.spec.ts (new-field round-trip + cross-tenant assertions)
  key_links:
    - "createSelectSchema(festival) -> festivalSchema in contracts -> Festival type consumed by every client screen"
    - "applied Neon migration -> live columns -> seed populates them -> api reads return them"
  prohibitions:
    - "Do NOT hand-add three fields to the existing z.object festivalSchema; replace it with a drizzle-zod-derived composition (Pitfall 1)."
    - "Do NOT add a server-side `saved` boolean to listFestivals; saved-state is client-derived downstream (Pattern 3)."
    - "Do NOT invent a getFestivalById endpoint; getFestival stays slug-keyed (Pattern 2)."
    - "Do NOT bump drizzle-zod off the pinned 0.7.1 (0.8.x breaks the workspace zod v3 pin, ADR-006)."
---

<objective>
Land the D-08 festival master-data (`startDate`, `endDate`, `place`) as a complete, drift-safe
vertical from the Drizzle schema through the ts-rest contract, the seed, an applied Neon migration,
and the live API reads — proven by an integration test that asserts the new fields round-trip through
all three festival read endpoints AND never leak cross-tenant. This is the phase tracer's backend
half: every downstream screen depends on `Festival.startDate/endDate/place` existing in the type and
in the database.

Purpose: prove the whole master-data vertical works before any UI consumes it, and close the
contract-drift gap the current hand-rolled `festivalSchema` leaves open (Pitfall 1/6).
Output: extended `festival` table + drizzle-zod bases, recomposed `festivalSchema`, seed values, an
applied migration, and green isolation/round-trip specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-RESEARCH.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@.planning/phases/05-festival-selection-home/05-VALIDATION.md
@packages/db/src/schema/visitor-profile.ts
@packages/db/src/schema/festival.ts
@packages/contracts/src/schemas.ts
@packages/db/scripts/seed.ts
@apps/api/test/festival-isolation.spec.ts
</context>

## Artifacts this phase produces (this plan)

- New columns on the `festival` table: `startDate` (date, string mode), `endDate` (date, string mode), `place` (text).
- New exports `festivalSelectSchema` and `festivalInsertSchema` in `packages/db/src/schema/festival.ts` (drizzle-zod bases with `.extend()` text overrides).
- Recomposed `festivalSchema` / `Festival` type in `packages/contracts/src/schemas.ts` (drizzle-zod-derived, `supportedLocales` kept as a manual `.extend()`).
- Extended `frequency-2026` seed row with real date/place values.
- A new generated + applied Drizzle migration under `packages/db/drizzle/`.
- New assertions in `apps/api/test/festival-isolation.spec.ts` covering the three new fields.

<tasks>

<task type="tracer">
  <name>Task 1: Add D-08 columns + drizzle-zod bases and recompose the contract festival shape</name>
  <files>packages/db/src/schema/festival.ts, packages/contracts/src/schemas.ts, packages/db/scripts/seed.ts</files>
  <read_first>
    - packages/db/src/schema/visitor-profile.ts (the exact `createSelectSchema/createInsertSchema` + `.extend()` text-column idiom and the comment block explaining why the override is mandatory)
    - packages/db/src/schema/festival.ts (current table — the target to extend)
    - packages/contracts/src/schemas.ts lines 1-14 and 24-45 (current hand-rolled `festivalSchema` and the `visitorProfilePublicSchema` composition to mirror)
    - packages/db/scripts/seed.ts (existing idempotent `onConflictDoUpdate` upsert shape)
    - .planning/phases/05-festival-selection-home/05-PATTERNS.md (the festival.ts / schemas.ts / seed.ts pattern sections with the exact target code)
  </read_first>
  <action>
    Per D-08: add three columns to the `festival` pgTable in packages/db/src/schema/festival.ts —
    `startDate` and `endDate` as `date({ mode: 'string' })` (import `date` from `drizzle-orm/pg-core`
    alongside the existing imports), and `place` as `text()` (nullable — no `.notNull()`, resolving
    RESEARCH Assumption A1 to nullable so a partially-configured festival is valid). In the same file
    add `export const festivalSelectSchema = createSelectSchema(festival).extend({ place: z.string().nullable() })`
    and `export const festivalInsertSchema = createInsertSchema(festival).extend({ place: z.string().nullable().optional() })`,
    importing `createInsertSchema`/`createSelectSchema` from `drizzle-zod` and `z` from `zod`. Carry
    over the visitor-profile explanatory comment (adapted) stating why the `place` `.extend()` override
    is required — the drizzle-zod 0.7.1 + drizzle-orm 0.45.2 text-column type-inference bug (Pitfall 2)
    collapses an un-overridden text column to `unknown`. Verify whether `startDate`/`endDate` need an
    override too and only add one if the type resolves to `unknown` without it.

    In packages/contracts/src/schemas.ts: add `festivalSelectSchema` to the EXISTING
    `@festipal/db/schema` import line (do not add a second import statement). Replace the hand-rolled
    `festivalSchema` `z.object` with `festivalSelectSchema.pick({ id, slug, name, defaultLocale,
    cashlessUrl, startDate, endDate, place })` chained with `.extend({ supportedLocales:
    z.array(localeSchema) })` — `supportedLocales` stays a manual extend because it is aggregated
    server-side from `festival_locale`, not a column on `festival`. Keep `export type Festival =
    z.infer<typeof festivalSchema>` unchanged in position. Add a short comment citing D-08 and Pitfall
    1/6 (drift detection via the drizzle-zod base).

    In packages/db/scripts/seed.ts: extend the `frequency-2026` `.values({...})` and the
    `.onConflictDoUpdate({ set: {...} })` with `startDate: '2026-08-13'`, `endDate: '2026-08-16'`,
    `place: 'Wiesen, Burgenland'` (raw ISO date strings, no Date objects — `date({ mode: 'string' })`).
    Leave the surrounding `try/finally { await db.$client.end(); }` unchanged.

    Do NOT run the migration in this task — schema files only.
  </action>
  <reversibility rating="costly">D-08 touches the published packages/contracts festival shape plus a DB migration and every festival consumer in one coordinated edit; no production data exists yet so the migration is low-risk, but it is a contract change (CONTEXT rates it costly).</reversibility>
  <verify>
    <automated>pnpm --filter @festipal/db typecheck && pnpm --filter @festipal/contracts typecheck && pnpm --filter @festipal/db build</automated>
  </verify>
  <done>festival.ts exports `festivalSelectSchema`/`festivalInsertSchema` with the three new columns; contracts `Festival` type carries `startDate`/`endDate`/`place` (not `unknown`); seed supplies real values; all three typecheck/build commands pass.</done>
  <acceptance_criteria>
    - `Festival['startDate']`, `Festival['endDate']`, `Festival['place']` resolve to concrete types (string / string | null), not `unknown`, under `tsc --noEmit`.
    - `festivalSchema` in contracts is derived from `festivalSelectSchema` (no standalone `z.object` festival shape remains).
    - `festivalInsertSchema` is exported and re-exported via the existing `packages/db/src/schema/index.ts` `export *` barrel (no barrel edit needed).
    - The seed row for `frequency-2026` sets all three new fields in both `.values` and the conflict `set`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate and apply the migration, then run the seed against Neon</name>
  <files>packages/db/drizzle/</files>
  <read_first>
    - packages/db/drizzle.config.ts (uses DATABASE_URL_UNPOOLED else DATABASE_URL; out=./drizzle)
    - packages/db/package.json scripts (`db:generate`, `db:migrate`, `db:seed`)
    - packages/db/drizzle/ existing migrations (0000..0002 + meta) — the naming/journal convention the new migration joins
  </read_first>
  <precondition>DATABASE_URL_UNPOOLED (or DATABASE_URL) points at the reachable Neon dev branch (Phases 1-4 already migrated against it). [user_setup: env]</precondition>
  <action>
    This is the [BLOCKING] schema-gate task: it MUST run after Task 1's schema edits are complete and
    BEFORE Task 3's live-DB assertions. Run `pnpm --filter @festipal/db db:generate` to emit a new
    migration for the three additive columns (additive column adds are non-interactive — no rename
    disambiguation prompt is expected). Inspect the generated SQL to confirm it only ADDs the three
    columns and touches nothing else. Then run `pnpm --filter @festipal/db db:migrate` to apply it to
    Neon, then `pnpm --filter @festipal/db db:seed` to upsert the extended `frequency-2026` row. If
    `db:generate` unexpectedly prompts interactively (it should not for pure column additions), stop
    and surface the prompt rather than guessing an answer.
  </action>
  <verify>
    <automated>ls packages/db/drizzle/*.sql | tail -1 && pnpm --filter @festipal/db db:seed</automated>
  </verify>
  <done>A new migration file exists under packages/db/drizzle/, is applied to Neon without error, and the seed run completes populating the three new fields on frequency-2026.</done>
  <acceptance_criteria>
    - Exactly one new `.sql` migration is generated, and it only adds `start_date`/`end_date`/`place` columns (snake_case per drizzle.config casing).
    - `db:migrate` exits 0 (migration applied to Neon).
    - `db:seed` exits 0 and the frequency-2026 row now has non-null start/end dates and place.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove new-field round-trip and cross-tenant isolation in the api integration suite</name>
  <files>apps/api/test/festival-isolation.spec.ts</files>
  <read_first>
    - apps/api/test/festival-isolation.spec.ts (full file — the `createTestApp`/`createTestDatabase` setup, the `randomUUID()`-suffixed fixture inserts at lines ~102-118, the "visitor 1 sees exactly [A]" assertion at ~145-154, and the FK-safe `afterAll` cleanup at ~133-143)
    - .planning/phases/05-festival-selection-home/05-PATTERNS.md (festival-isolation.spec.ts extension section — the exact fixture + assertion additions)
    - .planning/phases/05-festival-selection-home/05-VALIDATION.md (Per-Task Verification Map: FEST-01/HOME-02/SEC-02 rows)
  </read_first>
  <action>
    Extend the existing isolation spec (do not create a parallel spec, do not replace it). Add
    `startDate: '2026-08-13'`, `endDate: '2026-08-16'`, `place: 'Wiesen, Burgenland'` to festival A's
    fixture insert and distinct values to festival B's fixture (e.g. different dates + place). Add
    assertions that (a) the new fields round-trip unchanged through `GET /festivals` (browse),
    `GET /me/festivals` (caller-scoped) and `GET /festivals/:slug` (single) for the festival(s) the
    test visitor can see; and (b) the cross-tenant guard: a visitor who saved only festival A receives
    festival A's dates/place and NEVER festival B's dates/place in `GET /me/festivals` (assert B's
    distinct place/date values are absent). Reuse the existing FK-safe `afterAll` cleanup order
    (`myFestival` -> `visitorProfile` -> `festival`) unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/api test</automated>
  </verify>
  <done>The full apps/api supertest suite passes, including the new round-trip assertions on all three read endpoints and the cross-tenant non-leak assertion for the D-08 fields.</done>
  <acceptance_criteria>
    - The spec asserts `startDate`/`endDate`/`place` on a row returned by each of `GET /festivals`, `GET /me/festivals`, `GET /festivals/:slug`.
    - The spec asserts festival B's distinct place/date values do not appear in the visitor's `GET /me/festivals` response.
    - `pnpm --filter @festipal/api test` is green (no regressions in the existing 8-file suite).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API (`/api/v1`) | Authenticated visitor requests cross into festival-scoped reads |
| API -> Neon Postgres | Scoped queries (`festivalId`/`visitorId`) read the new master-data columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `GET /me/festivals` returning new D-08 fields | high | mitigate | Task 3 extends `festival-isolation.spec.ts` with a cross-tenant assertion that festival B's dates/place never appear in a visitor who saved only festival A (SEC-02) |
| T-05-04 | Tampering (Input Validation, ASVS V5) | new `startDate`/`endDate`/`place` fields | medium | mitigate | Fields flow through the drizzle-zod `createSelectSchema`/`.extend()` base (Task 1), not a hand-redeclared shape — the contract validates them like every other field |
| T-05-02 | Information Disclosure | `GET /festivals/:slug` (public browse read) | low | accept | Single-festival read is already gate-less/public-per-authenticated-visitor (ADR-014); adding date/place fields exposes no cross-tenant data (each row is its own tenant's public identity) |
</threat_model>

<verification>
- `pnpm --filter @festipal/db typecheck && pnpm --filter @festipal/contracts typecheck && pnpm --filter @festipal/db build` (Task 1)
- New migration applied + `pnpm --filter @festipal/db db:seed` green (Task 2)
- `pnpm --filter @festipal/api test` green including new round-trip + cross-tenant assertions (Task 3)
</verification>

<success_criteria>
- `Festival` type carries `startDate`/`endDate`/`place` derived from a drizzle-zod base (drift-safe).
- Neon has the three columns applied and seeded with real values.
- The isolation spec proves round-trip through all three read endpoints and no cross-tenant leak.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-01-SUMMARY.md` when done.
</output>
