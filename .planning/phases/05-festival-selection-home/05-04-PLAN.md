---
phase: 05-festival-selection-home
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - apps/mobile/components/FestivalCard.tsx
  - apps/mobile/components/SegmentedControl.tsx
autonomous: true
requirements: [FEST-01, FEST-02]
estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FestivalCard is a flat, non-photo owned primitive: surfaceCard bg, borderSubtle 1px, radiiScale r-card, a header row (name title3 + saved Badge OR quiet Save affordance) and a bodySm/textMuted '{dates} · {place}' caption formatted via formatDateRange (FEST-01, UI-SPEC Component Contract)."
    - "A saved festival shows a 'Gespeichert'/'Saved' Badge (fillBrandQuiet bg, primary check + text); an unsaved festival shows a tappable Save affordance (fillQuiet bg, plus + 'Speichern'/'Save') — the two are mutually exclusive, and tapping Save does not bubble to the card's enter press (FEST-02 saved distinction)."
    - "FestivalCard exposes a hero variant (title2 name, full-width primary 'Festival öffnen' CTA below the card) reusing the same header row (UI-SPEC Home hero variant)."
    - "SegmentedControl is an owned pill primitive with options/value/onChange props (mirroring festipal-ds.js prop names), surfaceInset track, pill radius, label typography (FEST-02)."
    - "The festival name truncates with numberOfLines={1} + ellipsis on both variants (long-text)."
    - "Both primitives are built as owned RN components in the imported Claude Design mockups' visual language (mirroring festipal-ds.js prop shapes, ADR-015 fidelity), not a third-party UI kit (D-01, ADR-022)."
  artifacts:
    - apps/mobile/components/FestivalCard.tsx (flat list variant + hero variant, saved/unsaved states)
    - apps/mobile/components/SegmentedControl.tsx (options/value/onChange owned primitive)
  key_links:
    - "FestivalCard saved prop <- client-derived savedIds set (computed in the screens plan)"
    - "FestivalCard caption <- formatDateRange(startDate, endDate, locale) + place"
  prohibitions:
    - "Do NOT copy festipal-ds.js's Photo-backed FestivalCard; build the flat non-photo variant (UI-SPEC Scope note #1 — no cover images this phase)."
    - "Do NOT render a ticket/countdown/wallet capsule or Kommend/Vergangen framing (D-05, UI-SPEC #2/#3)."
    - "Do NOT build an unsave/toggle action; the saved Badge is a status indicator only (UI-SPEC #11)."
    - "Do NOT use the violet secondary color or JetBrains Mono anywhere (UI-SPEC #10/#12)."
    - "Do NOT invent divergent prop names for SegmentedControl; use options/value/onChange (UI-SPEC Component library note)."
    - "Do NOT hardcode user-facing strings; badge/CTA/save copy comes via Lingui from the caller or t()."
---

<objective>
Build the two shared, owned list primitives every festival surface reuses: the flat (non-photo)
`FestivalCard` (list + hero variants, saved/unsaved states) and the `SegmentedControl` (Meine/Alle).
Pure presentation on shared tokens (ADR-022) — no data fetching, no business logic — so the Home and
Festivals screens in the next wave are thin compositions over these.

Purpose: extract the card/segment visual contract once, correctly, so both screens stay consistent
and thin.
Output: `FestivalCard.tsx`, `SegmentedControl.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@apps/mobile/app/festivals/index.tsx
@apps/mobile/components/AvatarTile.tsx
@apps/mobile/lib/date-range.ts
@docs/concept/designs/festival/festipal-ds.js
</context>

## Artifacts this phase produces (this plan)

- `apps/mobile/components/FestivalCard.tsx` — flat list variant + hero variant, saved-badge / Save-affordance states.
- `apps/mobile/components/SegmentedControl.tsx` — `options` / `value` / `onChange` owned pill primitive.

<tasks>

<task type="auto">
  <name>Task 1: FestivalCard (flat list + hero variants, saved/unsaved)</name>
  <files>apps/mobile/components/FestivalCard.tsx</files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract "FestivalCard (flat, non-photo variant)" + "Home hero variant", ## Typography (title3/title2/bodySm/micro roles), ## Color (fillQuiet/fillBrandQuiet/borderSubtle/primary/textMuted), ## Copywriting Contract (saved badge "Saved"/"Gespeichert", Save "Save"/"Speichern", hero CTA "Open festival"/"Festival öffnen")
    - apps/mobile/app/festivals/index.tsx (current inline `renderRow` markup at ~88-113 — the row structure being extracted; the token destructuring at line 17; the Save `<Trans>Save</Trans>` msgid to reuse)
    - apps/mobile/components/AvatarTile.tsx (owned-primitive conventions: token destructure, resolveFontFamily + useFontsReady)
    - apps/mobile/lib/date-range.ts (formatDateRange signature for the caption)
    - docs/concept/designs/festival/festipal-ds.js (FestivalCard/Badge/Button prop shapes — mirror props, not the Photo implementation)
  </read_first>
  <action>
    Create apps/mobile/components/FestivalCard.tsx as an owned RN primitive. The base card is a
    `Pressable` (`onPress` enters the festival, always enabled — gate-less) with `surfaceCard`
    background, `borderSubtle` 1px border, `radiiScale['r-card']` radius, `spacingScale['sp-6']`
    padding, containing: (1) a header row (flex-row, space-between) with the festival name on the left
    (`title3`, `textPrimary`, `numberOfLines={1}` + ellipsis) and on the right EITHER a "Saved"/
    "Gespeichert" `Badge` (fillBrandQuiet bg, `primary` `check` icon + text, `micro`) when `saved`, OR a
    quiet Save `Pressable` (fillQuiet bg, `radiiScale['r-pill']`, `layout.hitMin` height, `plus` icon +
    "Save"/"Speichern" text) when not saved; and (2) a caption row (`spacingScale['sp-4']` gap) with
    `"{formatDateRange(startDate, endDate, locale)} · {place}"` in `bodySm`/`textMuted`,
    `numberOfLines={1}`. The Save affordance's `onPress` must stop propagation to the card press (guard
    so tapping Save never also enters). Props: the festival, `saved: boolean`, `locale: string`,
    `onEnter`, `onSave`. Add a `variant="hero"` (or a `hero` boolean) that renders the name as `title2`
    and a full-width primary `Button` ("Festival öffnen"/"Open festival", `arrow-right`) below the card
    (the hero header row still shows the badge/save affordance). Copy comes via Lingui — reuse the
    existing `Save` msgid; new msgids ("Saved"/"Gespeichert", hero CTA) authored per the Copywriting
    Contract. Destructure `colors`, `typeRoles`, `layout`, `radiiScale`, `spacingScale` at module top;
    resolve fonts via `resolveFontFamily` + `useFontsReady`.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
  </verify>
  <done>FestivalCard renders flat + hero variants with mutually-exclusive saved-badge/Save-affordance, a formatted dates·place caption, name truncation, and a non-bubbling Save press; typecheck + lint pass.</done>
  <acceptance_criteria>
    - The card is flat (no `expo-image`/Photo backdrop); name uses `numberOfLines={1}`.
    - `saved` true -> Badge only; `saved` false -> Save affordance only (never both).
    - Tapping Save does not trigger the card's `onEnter`.
    - Hero variant renders `title2` name + a full-width "Festival öffnen" primary CTA below the card.
    - No violet/secondary color, no mono font, no ticket/countdown capsule.
    - typecheck + lint pass (no literal-string violations).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SegmentedControl (Meine/Alle owned pill primitive)</name>
  <files>apps/mobile/components/SegmentedControl.tsx</files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract references + ## Color (surfaceInset track, fillBrandQuiet/textSecondary selected states), ## Typography (label role), ## Spacing Scale (radiiScale r-pill)
    - docs/concept/designs/festival/festipal-ds.js SegmentedControl (the `options`/`value`/`onChange` prop shape to mirror)
    - apps/mobile/components/AvatarTile.tsx (token/font conventions)
  </read_first>
  <action>
    Create apps/mobile/components/SegmentedControl.tsx as an owned pill primitive with props `options`
    (array of `{ value, label }`), `value`, and `onChange(value)` — mirror these names exactly, do not
    invent divergent ones. Render a `surfaceInset` track with `radiiScale['r-pill']`; each option is a
    `Pressable` with `label` typography; the selected option gets the active treatment (accent/
    fillBrandQuiet tint pill behind it per UI-SPEC), the unselected uses `textSecondary`. Labels are
    passed in already-localized by the caller (the caller wraps them via Lingui) — the component itself
    renders `label` nodes and hardcodes no copy. Destructure `colors`, `typeRoles`, `radiiScale`,
    `spacingScale` at module top; resolve fonts via the fonts-context pattern.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
  </verify>
  <done>SegmentedControl renders an options/value/onChange pill with a selected-state treatment; typecheck + lint pass with no hardcoded copy in the component.</done>
  <acceptance_criteria>
    - Props are exactly `options` / `value` / `onChange`.
    - The track uses `surfaceInset` + `radiiScale['r-pill']`; the selected item uses the accent tint per UI-SPEC.
    - No user-facing string is hardcoded inside the component (labels arrive via props).
    - typecheck + lint pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational primitives; no data fetch, no persistence, no trust decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | FestivalCard rendering festival fields | low | accept | Renders only already-scoped public festival identity passed in as props; the server-side festivalId scoping (SEC-02, proven in 05-01) is the sole isolation boundary — these primitives add no new data path |
| T-05-08 | Tampering | Save affordance press bubbling into enter | low | mitigate | The inner Save `Pressable` stops propagation so a save tap cannot also trigger gate-less entry (deterministic, no double-action) |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile typecheck && lint` (both tasks)
- Visual verification of both primitives happens on-device inside the screens that consume them (05-06/05-07)
</verification>

<success_criteria>
- FestivalCard (flat + hero) and SegmentedControl exist as owned token-based primitives with the exact UI-SPEC contract, ready for the Home and Festivals screens.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-04-SUMMARY.md` when done.
</output>
