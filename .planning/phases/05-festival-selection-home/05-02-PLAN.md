---
phase: 05-festival-selection-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/tokens.ts
  - apps/mobile/package.json
  - apps/mobile/app.json
  - pnpm-lock.yaml
autonomous: true
requirements: [FEST-01, FEST-02]
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "packages/ui exports a radiiScale with r-card (22px), r-md (16px) and r-pill (999px) mirroring the existing spacingScale export shape, AND radiiScale is a member of the public `tokens` aggregate object (so `tokens.radiiScale` resolves) — not merely a loose export (REVIEW 05-02 MEDIUM)."
    - "colors and lightColors carry the new borderSubtle, borderBrand, fillQuiet, fillBrandQuiet, glassFill and glassBorder roles with the exact values in UI-SPEC ## Color, for both schemes (glass roles stay dark-glass in both)."
    - "expo-blur is installed in apps/mobile pinned to the SDK-57 version train (~57.0.x), verified as an official Expo package."
    - "experiments.typedRoutes is enabled in apps/mobile/app.json so the phase's route rename/move surfaces bad literal paths as compile errors (Pitfall 6, Open Question 1)."
  artifacts:
    - packages/ui/src/tokens.ts (radiiScale export + new color roles)
    - apps/mobile/package.json (expo-blur dependency)
    - apps/mobile/app.json (experiments.typedRoutes: true)
  key_links:
    - "radiiScale + new color roles -> consumed by FestivalCard / SegmentedControl / ComingSoonTile / FloatingNav in later plans"
    - "expo-blur BlurView -> FloatingNav glass backdrop"
  prohibitions:
    - "Do NOT replace or re-round the existing Phase-4 spacingScale/typeRoles/layout tokens; extend only (UI-SPEC Scope note)."
    - "Do NOT overload the generic radii object; add a separate radiiScale export mirroring spacingScale."
    - "Do NOT add a second/competing token set or a third brand palette (UI-SPEC)."
    - "Do NOT add any UI-component library; expo-blur is an effect package, not a UI kit (ADR-022)."
---

<objective>
Extend the shared design system with exactly the tokens this phase's new primitives need (a real
`radiiScale`, translucent border/fill roles, and the glass-nav surface) and install the single new
dependency (`expo-blur`), plus enable typed routes so the Wave-2/3 route restructure fails loud on a
bad literal path. This is pure foundation: no screens, no behavior — it unblocks FestivalCard,
SegmentedControl, ComingSoonTile and FloatingNav in later plans.

Purpose: give every later UI plan a single source of truth for the phase's new visual tokens and the
blur primitive, without forking the brand system.
Output: extended `packages/ui/src/tokens.ts`, `expo-blur` in `apps/mobile`, `typedRoutes` on.
</objective>

<review_incorporation>
Cross-AI review (05-REVIEWS.md, Codex) findings addressed in THIS plan:
- **MEDIUM — `tokens.radiiScale` not guaranteed by a loose export.** INCORPORATED: Task 1 explicitly
  requires adding `radiiScale` to the public `tokens` aggregate object (it enumerates fields); an
  acceptance criterion asserts `tokens.radiiScale['r-card']` resolves.
- **MEDIUM — typed-route types may be stale under plain `tsc`.** INCORPORATED: Task 2 regenerates the
  Expo Router route types (start the dev server briefly or `pnpm --filter @festipal/mobile exec expo
  export --source-maps=false --output-dir /tmp/expo-typegen` — whichever regenerates `.expo/types`)
  BEFORE relying on the typecheck, then runs `pnpm --filter @festipal/mobile typecheck`. The
  route-rename safety net only holds if the generated declarations are fresh.
- **LOW — manual version pin can desync the lockfile.** INCORPORATED: Task 2 installs via
  `npx expo install expo-blur` (Expo picks the SDK-57-correct version in one operation) and verifies
  the lockfile carries it — no separate manual edit + second install.
- **LOW — final verification mentions mobile typecheck but Task 2 didn't run it.** INCORPORATED:
  Task 2's automated verify now runs `pnpm --filter @festipal/mobile typecheck`.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@.planning/phases/05-festival-selection-home/05-RESEARCH.md
@packages/ui/src/tokens.ts
@apps/mobile/app.json
@docs/concept/designs/festival/festipal-tokens.css
</context>

## Artifacts this phase produces (this plan)

- New `radiiScale` export in `packages/ui/src/tokens.ts`: `r-card` (22px), `r-md` (16px), `r-pill` (999px).
- New color roles on `colors` and `lightColors`: `borderSubtle`, `borderBrand`, `fillQuiet`, `fillBrandQuiet`, `glassFill`, `glassBorder`.
- `expo-blur` dependency added to `apps/mobile/package.json` + lockfile.
- `experiments.typedRoutes: true` in `apps/mobile/app.json`.

<tasks>

<task type="auto">
  <name>Task 1: Add radiiScale + translucent color roles to the shared tokens</name>
  <files>packages/ui/src/tokens.ts</files>
  <read_first>
    - packages/ui/src/tokens.ts (full file — the existing `colors`/`lightColors`/`spacingScale`/`radii`/`layout`/`typeRoles` export shapes; mirror the spacingScale export style for radiiScale and the existing color-role key style for the new roles)
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Spacing Scale ("New additions needed" radiiScale table) and ## Color ("New additions needed" table) — the exact token names + dark/light values
    - docs/concept/designs/festival/festipal-tokens.css (source values for `--glass-*`, `--r-*`, border/fill roles — confirm consistency, do not fork)
  </read_first>
  <action>
    Add a new `radiiScale` export to packages/ui/src/tokens.ts mirroring the existing `spacingScale`
    export pattern (a `const` object with string keys), with `'r-card': 22`, `'r-md': 16`, `'r-pill':
    999`. Do NOT modify the generic `radii` object. Then add `radiiScale` as a member of the public
    `tokens` aggregate object (the aggregate explicitly enumerates its fields — a loose export is NOT
    enough for `tokens.radiiScale` to resolve; REVIEW 05-02 MEDIUM). Extend `colors` (dark) and `lightColors` with the
    six new roles from UI-SPEC ## Color: `borderSubtle` (dark `rgba(233,236,242,.08)` / light
    `rgba(7,8,11,.08)`), `borderBrand` (`rgba(116,204,31,.45)` both), `fillQuiet` (dark
    `rgba(233,236,242,.06)` / light `rgba(7,8,11,.05)`), `fillBrandQuiet` (`rgba(116,204,31,.16)`
    both), `glassFill` (`rgba(20,22,29,.62)` — same value in both schemes; the nav stays dark-glass in
    light mode per `.fp-glass`), `glassBorder` (`rgba(255,255,255,.16)` both). Keep the existing key
    style/casing. Ensure `radiiScale` and the new roles are exported through the package's public
    surface (whatever `index.ts`/barrel `packages/ui` already uses for `tokens`).
  </action>
  <verify>
    <automated>pnpm --filter @festipal/ui typecheck && pnpm --filter @festipal/ui build</automated>
  </verify>
  <done>tokens.ts exports `radiiScale` (r-card/r-md/r-pill) and the six new color roles on both `colors` and `lightColors`; the package typechecks and builds.</done>
  <acceptance_criteria>
    - `tokens.radiiScale['r-card']`, `['r-md']`, `['r-pill']` are accessible and numeric.
    - Each of `borderSubtle`, `borderBrand`, `fillQuiet`, `fillBrandQuiet`, `glassFill`, `glassBorder` exists on both `colors` and `lightColors`.
    - The existing `spacingScale`/`typeRoles`/`layout`/`radii` exports are unchanged (extend-only).
    - `pnpm --filter @festipal/ui typecheck && build` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install expo-blur (SDK-57 train) and enable typed routes</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/package.json (existing `expo-*` version pins to match the SDK-57 train, e.g. expo-image ~57.0.2)
    - apps/mobile/app.json (the `expo` object — where `experiments` goes; existing `plugins` array is unchanged)
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md ## Package Legitimacy Audit (expo-blur verdict OK / Approved — first-party Expo package, no human checkpoint required)
    - .planning/WINDOWS.md note on Metro/pnpm install race (stop Expo/Metro before installing on this machine)
  </read_first>
  <action>
    Install expo-blur into apps/mobile with `npx expo install expo-blur` run from `apps/mobile` (the
    Expo-compatible installer resolves the SDK-57-correct version in ONE operation and writes the
    lockfile — do NOT `pnpm add` then hand-edit the version range, which can desync package metadata
    from the lockfile, REVIEW 05-02 LOW). expo-blur is a first-party Expo package already verified
    legitimate in RESEARCH's Package Legitimacy Audit (verdict OK) — no human-verify checkpoint is
    required. If the install fails with an ENOENT on a `*_tmp_*/node_modules` path, ensure no
    Expo/Metro watcher is running and retry (known Windows install race). Then add `"experiments": {
    "typedRoutes": true }` to the `expo` object in apps/mobile/app.json (Open Question 1 — off today),
    so the route rename/move in later plans surfaces stale literal paths as compile errors rather than
    runtime "no route found" warnings. Because typed routes emit generated declarations under
    `.expo/types` (consumed by tsconfig `include`), REGENERATE those declarations before the typecheck
    can be trusted as a route safety net: run the Expo route-types generation (start the dev server
    briefly so it writes `.expo/types`, or `pnpm --filter @festipal/mobile exec expo customize` /
    an `expo export` pass — whichever regenerates the types on this SDK), then confirm
    `pnpm --filter @festipal/mobile typecheck` passes with the fresh declarations (REVIEW 05-02 MEDIUM).
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/mobile/package.json');if(!p.dependencies['expo-blur'])throw new Error('expo-blur missing');const a=require('./apps/mobile/app.json');if(!a.expo.experiments||a.expo.experiments.typedRoutes!==true)throw new Error('typedRoutes not enabled');console.log('ok',p.dependencies['expo-blur'])" && pnpm --filter @festipal/mobile typecheck</automated>
  </verify>
  <done>expo-blur is a dependency of apps/mobile on the SDK-57 train (installed via expo install), the lockfile is updated, app.json has experiments.typedRoutes true, the .expo/types route declarations are regenerated, and mobile typecheck passes.</done>
  <acceptance_criteria>
    - `apps/mobile/package.json` dependencies include `expo-blur` on the SDK-57 (`~57.0.x`) train, installed via `expo install`.
    - `pnpm-lock.yaml` reflects the new dependency (no version/lockfile desync).
    - `apps/mobile/app.json` `expo.experiments.typedRoutes` is `true`.
    - The Expo route-type declarations under `.expo/types` are freshly regenerated and `pnpm --filter @festipal/mobile typecheck` passes.
    - The existing `plugins` array in app.json is unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> apps/mobile build | A new dependency (`expo-blur`) enters the app bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | expo-blur npm install | high | mitigate | RESEARCH Package Legitimacy Audit verifies expo-blur as an official Expo monorepo package (same publisher/repo as 6 already-installed expo-* deps), verdict OK; version pinned to the SDK-57 train + lockfile review. No `[ASSUMED]`/`[SUS]` package, so no blocking-human checkpoint is required |
| T-05-05 | Information Disclosure | new visual tokens | low | accept | Tokens are static presentation values (colors/radii); no data, secrets, or trust decision encoded |
</threat_model>

<verification>
- `pnpm --filter @festipal/ui typecheck && build` (Task 1)
- expo-blur present + typedRoutes enabled assertion (Task 2)
- `pnpm --filter @festipal/mobile typecheck` should still pass after install
</verification>

<success_criteria>
- Shared tokens carry radiiScale + the six new roles, extend-only.
- expo-blur installed on the correct train; typed routes on.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-02-SUMMARY.md` when done.
</output>
