---
phase: 05-festival-selection-home
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - apps/mobile/components/FloatingNav.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/home.tsx
  - apps/mobile/app/(tabs)/festivals.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/festivals/index.tsx
  - apps/mobile/app/festivals/_layout.tsx
autonomous: true
requirements: [FEST-04, HOME-01]
estimate:
  tokens: 80000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A global floating bottom tab bar (FloatingNav) renders 4 items — Home, Festivals, Friends, Profil — with Home + Festivals live (navigable) and Friends + Profil visibly disabled (0.4 opacity, no-op onPress, accessibilityState disabled) as the app's root shell (D-02); this places Profile/Friends in the global shell, not the festival home (D-03, reconciling ROADMAP HOME-03), and trims the mockup's 5-tab GLOBAL_NAV (Artists omitted, Mehr relabeled Profil) in the design's visual language (D-01)."
    - "The disabled Friends/Profil tabs' accessibilityLabel is routed through Lingui and appends '— coming soon' / '— bald verfügbar', not a hardcoded literal (Pitfall 4, I18N-01)."
    - "The authenticated route tree registers a (tabs) group (replacing the old flat festivals group) plus the (festival) stacked group; the old app/festivals/* files are deleted and no stale '/festivals' or '/(festival)' literal path remains (Pitfall 6)."
    - "On cold start, if an active-festival slug is persisted, the app opens that festival's home (/f/:slug) directly; otherwise it lands on the Home tab — resolved without holding the splash any longer than today (D-06, HOME-01)."
    - "Entering a festival and pressing back returns to the tab shell (Festivals tab), never a dead-end (FEST-04)."
    - "The floating nav uses expo-blur BlurView for the glass backdrop (glassFill/glassBorder), positioned at layout.navInset above the safe-area bottom, and scrollable screens keep layout.scrollBottomPad clearance."
  artifacts:
    - apps/mobile/components/FloatingNav.tsx (owned custom tab bar)
    - apps/mobile/app/(tabs)/_layout.tsx (Tabs with custom tabBar)
    - apps/mobile/app/(tabs)/home.tsx (placeholder screen, filled in 05-07)
    - apps/mobile/app/(tabs)/festivals.tsx (moved from app/festivals/index.tsx, filled to Meine/Alle in 05-06)
  key_links:
    - "app/_layout.tsx root guard -> registers (tabs) + (festival) for authenticated state"
    - "getActiveFestivalSlug() on cold start -> router.replace(/f/:slug) one-shot"
    - "Tabs state.routes -> FloatingNav renders live tabs; Friends/Profil are decorative non-routes"
  prohibitions:
    - "Do NOT use the experimental expo-router/ui headless Tabs API; use stable Tabs + a custom tabBar render prop (RESEARCH Pattern 1)."
    - "Do NOT add Tabs.Screen routes for Friends/Profil; they are decorative disabled buttons with no navigation target this phase (Phase 6)."
    - "Do NOT include an Artists tab (own later milestone)."
    - "Do NOT add a new async gate before SplashScreen.hideAsync(); the MMKV read is synchronous — bootstrapped stays the sole splash gate (RESEARCH Pattern 4)."
    - "Do NOT build an in-festival tab bar; the festival home is a stacked screen over the shell (D-02)."
    - "Do NOT leave both the old festivals group and the new (tabs) group; delete the old files (RESEARCH State of the Art)."
---

<objective>
Stand up the app's global navigation frame: a custom floating bottom tab bar (Home · Festivals live;
Friends · Profil disabled "coming soon") as the new authenticated root shell, replacing the flat
`festivals` route group with a `(tabs)` group, and wire the active-festival cold-start focus so a
returning visitor lands straight in their festival's home. The festival home stays a stacked screen
over the shell with a non-dead-end back.

Purpose: deliver the shell scaffold now (D-02) so Phase 6 and later content phases mount into it, and
serve the core value "reach everything from one home screen" via the cold-start focus (D-06).
Output: `FloatingNav`, the `(tabs)` group + layout, the root-guard swap, and the cold-start redirect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-RESEARCH.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@apps/mobile/app/_layout.tsx
@apps/mobile/app/festivals/index.tsx
@apps/mobile/app/festivals/_layout.tsx
@apps/mobile/app/(festival)/_layout.tsx
@apps/mobile/lib/active-festival-storage.ts
@docs/concept/designs/festival/festipal-ds.js
</context>

## Artifacts this phase produces (this plan)

- `apps/mobile/components/FloatingNav.tsx` — owned custom tab bar (2 live + 2 disabled items, glass backdrop).
- `apps/mobile/app/(tabs)/_layout.tsx` — `Tabs` with `tabBar={props => <FloatingNav {...props} />}`.
- `apps/mobile/app/(tabs)/home.tsx` — placeholder Home screen (enriched in 05-07).
- `apps/mobile/app/(tabs)/festivals.tsx` — the moved Festivals screen (rewritten to Meine/Alle in 05-06).
- Root-guard registration swap + active-festival cold-start redirect in `apps/mobile/app/_layout.tsx`.

<tasks>

<task type="auto">
  <name>Task 1: FloatingNav custom tab bar (2 live + 2 disabled, glass backdrop)</name>
  <files>apps/mobile/components/FloatingNav.tsx</files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract "Tab bar (FloatingNav)" (floating pill, layout.navHeight/navInset, glassFill/glassBorder, active tint-pill + strokeWidth 2.4, disabled 0.4 opacity), ## Copywriting Contract (tab labels Home/Festivals/Friends/Profil), ## Typography (micro label, not uppercase)
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md Pattern 1 (Tabs + custom tabBar prop shape) + Pitfall 4 (accessibilityLabel is a lint gap — route through t())
    - node_modules/expo-router Tabs / tabBar type definitions (verify the actual SDK-57 prop shape of state/descriptors/navigation/insets before writing prop types — RESEARCH Assumption A4)
    - docs/concept/designs/festival/festipal-ds.js FloatingNav/GLOBAL_NAV (visual/prop reference, web — mirror shape not implementation)
    - apps/mobile/lib/fonts.ts + fonts-context.tsx (font resolution)
  </read_first>
  <action>
    Create apps/mobile/components/FloatingNav.tsx, an owned primitive that receives React Navigation
    bottom-tabs props (`state`, `descriptors`, `navigation`, `insets`) from `Tabs`' `tabBar` render
    prop. Render a floating pill: `position: absolute`, `left/right: layout.screenPad`, `bottom:
    layout.navInset + safe-area-bottom`, `height: layout.navHeight`, `borderRadius:
    radiiScale['r-pill']`, backed by expo-blur's `BlurView` (intensity tuned to approximate
    `--glass-blur: 22px` / `--glass-saturate: 180%`) with `glassFill` overlay + `glassBorder` 1px
    border. Render the TWO live tabs from `state.routes` (home `home`, festivals `tent`): the active one
    gets `colors.primary` icon (`strokeWidth={2.4}`) + label + a `fillBrandQuiet` tint-pill behind it;
    inactive-enabled gets `textMuted` + `strokeWidth={2}`; tapping navigates via `navigation` (standard
    bottom-tabs onPress with the emit/navigate guard). Render TWO decorative items NOT in `state.routes`
    (Friends `users`, Profil `user-round`) at 0.4 opacity, `onPress` no-op, `accessibilityState={{
    disabled: true }}`, and `accessibilityLabel` composed via Lingui `t()` to append "— coming soon" /
    "— bald verfügbar" (do NOT hardcode — the lint rule excludes accessibilityLabel, Pitfall 4). Labels
    are `micro`, not uppercase. Ensure each item meets `layout.hitMin`.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
  </verify>
  <done>FloatingNav renders a glass floating pill with 2 live navigable tabs and 2 disabled decorative items whose a11y labels are localized "coming soon"; typecheck + lint pass.</done>
  <acceptance_criteria>
    - Live tabs derive from `state.routes`; Friends/Profil are rendered as extra non-route items.
    - Disabled items have `accessibilityState={{ disabled: true }}`, a no-op press, 0.4 opacity, and a `t()`-composed "coming soon"/"bald verfügbar" label.
    - The backdrop uses expo-blur `BlurView` + `glassFill`/`glassBorder`.
    - typecheck + lint pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: (tabs) group + layout, move Festivals screen, swap the root-guard registration</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/home.tsx, apps/mobile/app/(tabs)/festivals.tsx, apps/mobile/app/_layout.tsx, apps/mobile/app/festivals/index.tsx, apps/mobile/app/festivals/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (the Stack.Protected authenticated block at ~213-224 registering `festivals` + `(festival)` — swap `festivals` -> `(tabs)`)
    - apps/mobile/app/festivals/index.tsx (the full screen to MOVE into (tabs)/festivals.tsx, carrying forward the 05-03 enter-by-slug + saveActiveFestivalSlug wiring and the logout header per UI-SPEC Scope note #8)
    - apps/mobile/app/festivals/_layout.tsx and apps/mobile/app/(festival)/_layout.tsx (the existing group layouts — the (tabs) layout mirrors the plain-Stack style but uses Tabs)
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md Pattern 1 (the (tabs)/_layout.tsx shape) + Recommended Project Structure + Pitfall 6 (grep every '/festivals' and '/(festival)' literal)
  </read_first>
  <action>
    Create apps/mobile/app/(tabs)/_layout.tsx as `<Tabs tabBar={props => <FloatingNav {...props} />}
    screenOptions={{ headerShown: false }}>` with `<Tabs.Screen name="home" />` and `<Tabs.Screen
    name="festivals" options={{ headerShown: true, title: t\`Festivals\` }} />` (home has no header;
    Festivals keeps its plain title + the logout header control). MOVE the existing
    app/festivals/index.tsx into apps/mobile/app/(tabs)/festivals.tsx (adjust relative import depths —
    it sits at app/(tabs)/ now), preserving the logout header (AUTH-04, UI-SPEC #8), the query/error
    patterns, and the 05-03 enter-by-slug + `saveActiveFestivalSlug` wiring; this move keeps the screen
    functional as-is (the Meine/Alle rewrite is 05-06). Create apps/mobile/app/(tabs)/home.tsx as a
    minimal placeholder screen (a simple heading routed through Lingui; enriched to the real hero+rail
    in 05-07) so the Home tab renders. DELETE apps/mobile/app/festivals/index.tsx and
    apps/mobile/app/festivals/_layout.tsx (do not leave both groups — RESEARCH State of the Art). In
    apps/mobile/app/_layout.tsx swap `<Stack.Screen name="festivals" />` for `<Stack.Screen
    name="(tabs)" />` inside the authenticated `Stack.Protected` block (keep `<Stack.Screen
    name="(festival)" />`). Grep the whole apps/mobile/app + apps/mobile/lib tree for `'/festivals'` and
    `'/(festival)'` literal paths and update any stale ones to the new shape (with typedRoutes on from
    05-02, stale literals should already surface as type errors). Ensure `scrollBottomPad` clearance
    exists on the moved screen so content clears the floating nav.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
    <manual>On device: after login, the floating tab bar renders with Home + Festivals active and Friends + Profil dimmed/disabled; tapping Friends/Profil does nothing; the Festivals tab shows the existing list and can still enter a festival; back from a festival returns to the Festivals tab (FEST-04).</manual>
  </verify>
  <done>The authenticated shell is a (tabs) group with a custom floating nav; the Festivals screen is moved and still functional; the old festivals group is gone; no stale literal paths remain; typecheck + lint pass.</done>
  <acceptance_criteria>
    - `app/(tabs)/_layout.tsx` uses stable `Tabs` + `tabBar` render prop (no `expo-router/ui`).
    - Only `home` and `festivals` have `Tabs.Screen` entries; no Friends/Profil/Artists screens.
    - `app/festivals/index.tsx` and `app/festivals/_layout.tsx` are deleted; root guard registers `(tabs)` + `(festival)`.
    - No `'/festivals'` literal path remains; typecheck (typed routes) + lint pass.
    - On-device: shell renders, Festivals tab works, back from a festival is non-dead-end.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Active-festival cold-start focus redirect (D-06 / HOME-01)</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (the existing one-shot pending-destination effect at ~186-190 and the `splashHiddenRef` one-shot idiom at ~87/194-199 — mirror the guard-once-per-cold-start pattern; the `bootstrapped` splash gate at ~192)
    - apps/mobile/lib/active-festival-storage.ts (getActiveFestivalSlug — synchronous MMKV read)
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md Pattern 4 (wire into the existing bootstrap effect, no new async gate) + Architecture Patterns diagram (cold-start branch)
  </read_first>
  <action>
    Extend the existing authenticated-transition effect in apps/mobile/app/_layout.tsx so that, once
    `authState.status === 'authenticated'`, AFTER the pending-destination replay check (a captured
    deep-link href still wins), it reads `getActiveFestivalSlug()` and, if present, calls
    `router.replace(\`/f/${activeSlug}\`)` to open the festival home directly on cold start (D-06). If
    no slug is persisted, do nothing (the guard's default authenticated route is the Home tab). Guard
    the redirect so it fires exactly once per cold start (a ref, same idiom as `splashHiddenRef` /
    `consumePendingDestination`), so tab navigation later in the session is never hijacked back into a
    festival. Do NOT add any new async gate before `SplashScreen.hideAsync()` — the MMKV read is
    synchronous and `bootstrapped` stays the sole splash gate.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
    <manual>On device (Phase 3/4 gate): enter a festival -> force-quit the app -> relaunch -> the app opens that festival's home directly (not the Home tab). Then navigate back to the shell and confirm normal tab navigation is not re-hijacked into the festival on subsequent taps.</manual>
  </verify>
  <done>Cold start opens the persisted active festival's home directly (once), deep-link replay still takes precedence, no slug lands on the Home tab, and the splash is not held longer; typecheck + lint pass.</done>
  <acceptance_criteria>
    - The redirect reads `getActiveFestivalSlug()` and `router.replace('/f/:slug')` only when a slug is present.
    - It is guarded to fire once per cold start (ref), after the pending-destination check.
    - No new gate is added before `SplashScreen.hideAsync()`.
    - On-device: force-quit-and-relaunch opens the festival home; no-slug opens Home tab.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device-local MMKV -> cold-start navigation | The persisted active-festival slug picks the launch target |
| route guard | Authenticated route tree gates which groups mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | active-festival slug driving cold-start redirect | low | accept | The slug only selects a `/f/:slug` target reachable via the already-public gate-less browse; the authenticated `Stack.Protected` guard still gates the whole tree, so a tampered slug cannot escalate past auth or reach another tenant's scoped data |
| T-05-09 | Elevation of Privilege | (tabs) route-guard swap | high | mitigate | The `(tabs)` group is registered ONLY inside the `authState.status === 'authenticated'` `Stack.Protected` block (unchanged four-state guard from Phase 4); an unauthenticated/no-profile session still cannot mount the shell — verified on-device (deep-link-while-logged-out remains redirected) |
| T-05-10 | Spoofing | disabled Friends/Profil tabs | low | mitigate | The disabled tabs have no navigation target (not in `state.routes`) and a no-op press, so they cannot route to any unbuilt/unprotected screen; a11y state marks them disabled |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile typecheck && lint` (all tasks; typed routes catch stale paths)
- On-device: shell renders with 2 live + 2 disabled tabs; Festivals works; back non-dead-end (Task 2)
- On-device: force-quit-and-relaunch opens the active festival home (Task 3)
</verification>

<success_criteria>
- The global floating tab shell is the authenticated root; the old festivals group is removed.
- Cold start focuses the active festival; entering-then-back is never a dead-end.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-05-SUMMARY.md` when done.
</output>
