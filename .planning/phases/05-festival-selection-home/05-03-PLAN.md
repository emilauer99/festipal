---
phase: 05-festival-selection-home
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - apps/mobile/lib/date-range.ts
  - apps/mobile/lib/__tests__/date-range.test.ts
  - apps/mobile/lib/active-festival-storage.ts
  - apps/mobile/lib/festival-queries.ts
  - apps/mobile/lib/festival-navigation.ts
  - apps/mobile/components/ComingSoonTile.tsx
  - apps/mobile/app/(festival)/f/[festivalSlug].tsx
  - apps/mobile/app/(festival)/index.tsx
  - apps/mobile/app/festivals/index.tsx
autonomous: true
requirements: [HOME-02, FEST-04]
estimate:
  tokens: 85000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Entering any festival opens a slug-keyed festival home at /f/:festivalSlug that renders its identity (name as display2 H1) and key facts (formatted date range + place) from getFestival(slug) (HOME-02)."
    - "The festival home always exposes a working back affordance backed by the shared leaveFestival(router) helper — router.canGoBack() ? router.back() : router.replace('/festivals') — so back is non-dead-end even on a cold-start that entered via router.replace('/f/:slug') and left no in-app history (FEST-04, REVIEW 05-03/05-05 HIGH)."
    - "The date range is rendered by a pure formatDateRange(start, end, locale) that accepts string | null (DATE-NULLABILITY): with both dates present it returns two Intl.DateTimeFormat format() calls joined by an en-dash (never the single-call range API, Pitfall 3); with a null date it returns a localized 'Termin folgt'/'Dates TBA' fallback. Dates are parsed date-only (YYYY-MM-DD components → local date, NOT new Date(str) which is UTC and shifts the day in negative offsets). Covered by a Vitest unit test (REVIEW 05-03 HIGH/MEDIUM)."
    - "The festival home renders authoritative data from the getFestival(slug) query result and branches explicitly on four states: pending (loading text), transport error (query.isError → network-error pattern), data.status === 404 (not-found pattern, still with a working Back), and data.status === 200 (render) — a 404 is a SUCCESSFUL React Query result, not query.status === 'error' (REVIEW 05-03 MEDIUM)."
    - "Instant paint is seeded from a separately-derived cachedFestival (scanned from the ['festivals'] / ['me','festivals'] caches) rendered while the authoritative getFestival query resolves — NOT via initialData/placeholderData of the wrong shape; the ['festivals'] cache holds full ts-rest response objects, not bare Festival values (REVIEW 05-03 HIGH)."
    - "The festival home renders a static 2x2 'coming soon' menu (Timetable/Lageplan/Cashless/News) built from an owned, non-interactive ComingSoonTile primitive (D-07); it is a stacked screen with NO in-festival tab bar and no Profile/Friends nav — those live in the global shell (D-03)."
    - "The festival home and its primitives follow the imported Claude Design mockups' visual language (DashboardScreen slice, ADR-015 fidelity) (D-01)."
    - "Entering a festival persists its slug via lib/active-festival-storage.ts (MMKV), mirroring lib/avatar-storage.ts's lazy-require idiom, for the cold-start focus wired in a later plan (D-06)."
    - { statement: "While getFestival(slug) is pending, the festival home shows the existing festivals/index.tsx loading-text pattern (no new skeleton) and does not crash.", verification: backstop }
    - { statement: "A getFestival 404 (deleted/invalid slug) renders the existing network-error pattern and still exposes the back affordance required by FEST-04, without crashing.", verification: backstop }
    - { statement: "A long organizer-entered place truncates with numberOfLines={1} rather than breaking the caption layout, and the date range renders correctly on-device (iOS+Android) via the two-format() fallback.", verification: backstop }
  artifacts:
    - apps/mobile/lib/date-range.ts (formatDateRange pure fn, null-safe + date-only parse)
    - apps/mobile/lib/__tests__/date-range.test.ts
    - apps/mobile/lib/active-festival-storage.ts (save/get/clear active slug)
    - apps/mobile/lib/festival-queries.ts (shared festivalKeys query-key factory + unwrapOk response-unwrap helper — REVIEW cross-plan MEDIUM)
    - apps/mobile/lib/festival-navigation.ts (shared leaveFestival(router) back-with-shell-fallback helper — REVIEW 05-03/05-05 HIGH)
    - apps/mobile/components/ComingSoonTile.tsx (owned primitive)
    - apps/mobile/app/(festival)/f/[festivalSlug].tsx (festival home, replaces the static index placeholder)
  key_links:
    - "getFestival(slug) -> festivalQuery.data (status-branched) -> identity + formatDateRange(place) render"
    - "cachedFestival (scanned from caches) -> instant paint while the authoritative query resolves"
    - "festivals list Enter -> saveActiveFestivalSlug(slug) + router.push(/f/:slug) -> festival home"
    - "festival home header Back -> leaveFestival(router) -> canGoBack ? back : replace('/festivals')"
  prohibitions:
    - "Do NOT use Intl.DateTimeFormat.prototype.formatRange (unreliable on Hermes/iOS, Pitfall 3)."
    - "Do NOT parse festival dates with new Date('YYYY-MM-DD') (interpreted as UTC → wrong calendar day in negative offsets); split into y/m/d components and build a local date (REVIEW 05-03 MEDIUM)."
    - "Do NOT seed the getFestival query via initialData/placeholderData shaped as a bare Festival; the ['festivals'] cache holds full ts-rest response objects. Render a separately-derived cachedFestival for instant paint and keep the query result authoritative (REVIEW 05-03 HIGH)."
    - "Do NOT treat a getFestival 404 as query.status === 'error'; a non-200 ts-rest response is a SUCCESSFUL React Query result — branch on data.status === 404 (REVIEW 05-03 MEDIUM)."
    - "Do NOT use bare router.back() for the festival-home Back; use the shared leaveFestival(router) helper so cold-start entry (no in-app history) still lands on the shell, never exiting the app (REVIEW 05-03/05-05 HIGH)."
    - "Do NOT invent a getFestivalById endpoint or route by UUID; the route is slug-keyed (Pattern 2)."
    - "Do NOT top-level import react-native-mmkv in active-festival-storage.ts; lazy-require inside the accessor so the module stays importable under the node-env Vitest runner (avatar-storage.ts idiom)."
    - "Do NOT render or fetch cashlessUrl on the Cashless coming-soon tile; the tiles are fully static this phase (ADR-011 hide-when-unset only becomes load-bearing when Cashless goes live)."
    - "Do NOT make the ComingSoonTile navigable; it is a disabled View, not a Pressable (D-07)."
    - "Do NOT hardcode any user-facing string incl. tile labels; route through Lingui t()/<Trans>."
---

<objective>
Complete the phase tracer: wire the D-08 master-data all the way to a real client consumer. Build the
slug-keyed festival home (`/f/:festivalSlug`) that fetches `getFestival(slug)` and renders the
festival's identity + formatted dates + place, plus the static "coming soon" menu, and prove
end-to-end on device that a seeded festival's real dates/place surface in the UI. This closes the
vertical (seed -> migration -> contract -> API -> mobile screen) proven halfway by 05-01, and lands
the pure `formatDateRange` helper (Hermes-safe) and the active-festival persistence used by the
cold-start focus later.

Purpose: de-risk the whole phase architecture on one thin end-to-end path before expanding into the
tab shell + list + save breadth.
Output: `formatDateRange` (+ test), `active-festival-storage`, `festival-queries` (keys + unwrapOk),
`festival-navigation` (leaveFestival), `ComingSoonTile`, and the festival home screen.
</objective>

<review_incorporation>
Cross-AI review (05-REVIEWS.md, Codex) findings addressed in THIS plan:
- **HIGH — `initialData` shape is wrong.** INCORPORATED: Task 3 no longer seeds the query with a bare
  `Festival`. A `findCachedFestivalBySlug` helper derives a `cachedFestival` for instant paint; the
  screen renders `query.data?.body ?? cachedFestival` while the authoritative `getFestival` query
  (its own ts-rest response) stays the source of truth. No `initialData`/`placeholderData` of the
  wrong shape.
- **HIGH — nullable dates can't feed `formatDateRange(string, string)`.** INCORPORATED (DATE-NULLABILITY,
  locked in 05-01): `formatDateRange(start: string | null, end: string | null, locale)` returns a
  localized "Termin folgt"/"Dates TBA" fallback when a date is null; the caption tolerates null place.
- **MEDIUM — `new Date('YYYY-MM-DD')` parses as UTC.** INCORPORATED: Task 1 parses date-only strings by
  y/m/d components into a local date; the test covers a negative-offset-sensitive date.
- **MEDIUM — a 404 is a successful React Query result.** INCORPORATED: Task 3 branches explicitly on
  pending / transport-error (`query.isError`) / `data.status === 404` / `data.status === 200`.
- **MEDIUM — `router.back()` alone fails the cold-start edge.** INCORPORATED: a shared
  `lib/festival-navigation.ts` `leaveFestival(router)` = `router.canGoBack() ? router.back() :
  router.replace('/festivals')` backs the header Back; 05-05 relies on this after its
  cold-start `router.replace('/f/:slug')`.
- **LOW — initial-data freshness ambiguity.** RESOLVED by dropping cache-seeding of the query entirely
  (the query always fetches; cachedFestival is only a paint hint).
- **Cross-plan MEDIUM — repeated ts-rest response unwrapping.** INCORPORATED: `lib/festival-queries.ts`
  exports `festivalKeys` (query-key factory) + `unwrapOk(response)` (throws on non-200), reused by the
  05-06 save mutation so cache-shape/unwrap logic lives in one place.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-festival-selection-home/05-RESEARCH.md
@.planning/phases/05-festival-selection-home/05-PATTERNS.md
@.planning/phases/05-festival-selection-home/05-UI-SPEC.md
@apps/mobile/lib/avatar-storage.ts
@apps/mobile/lib/otp-error.ts
@apps/mobile/app/(festival)/index.tsx
@apps/mobile/app/festivals/index.tsx
@apps/mobile/lib/api-client.ts
</context>

## Artifacts this phase produces (this plan)

- `apps/mobile/lib/date-range.ts` — `formatDateRange(start, end, locale)` pure helper (null-safe, date-only local parse, two `Intl` `format()` calls, en-dash join).
- `apps/mobile/lib/__tests__/date-range.test.ts` — Wave-0 unit test for the helper (de/en + null + negative-offset date-only).
- `apps/mobile/lib/active-festival-storage.ts` — `saveActiveFestivalSlug` / `getActiveFestivalSlug` / `clearActiveFestivalSlug` (MMKV, id `festipal-active-festival`).
- `apps/mobile/lib/festival-queries.ts` — `festivalKeys` query-key factory + `unwrapOk(response)` (throws a typed error on non-200; shared response-unwrap).
- `apps/mobile/lib/festival-navigation.ts` — `leaveFestival(router)` back-with-shell-fallback helper.
- `apps/mobile/components/ComingSoonTile.tsx` — owned, non-interactive disabled-tile primitive.
- `apps/mobile/app/(festival)/f/[festivalSlug].tsx` — the festival home screen (replaces the static `(festival)/index.tsx` placeholder, which is deleted).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatDateRange (null-safe, date-only, tested) + active-festival storage + shared festival-queries/navigation helpers</name>
  <files>apps/mobile/lib/date-range.ts, apps/mobile/lib/__tests__/date-range.test.ts, apps/mobile/lib/active-festival-storage.ts, apps/mobile/lib/festival-queries.ts, apps/mobile/lib/festival-navigation.ts</files>
  <read_first>
    - apps/mobile/lib/avatar-storage.ts (full file — the exact lazy-require MMKV idiom to mirror; use a NEW storage id and key namespace)
    - apps/mobile/lib/otp-error.ts and apps/mobile/lib/__tests__/otp-error.test.ts (the pure-function + node-env Vitest test pattern to mirror; no RN imports)
    - apps/mobile/lib/api-client.ts (the ts-rest client whose calls return `{ status, body }` response objects — the shape `unwrapOk` narrows)
    - .planning/phases/05-festival-selection-home/05-RESEARCH.md Pitfall 3 (why formatRange is avoided) and Pattern 4 (active-festival storage)
    - apps/mobile/vitest.config.ts (node-env, `lib/**/__tests__/**/*.test.ts` scope)
  </read_first>
  <behavior>
    - formatDateRange('2026-08-13','2026-08-16','de') returns a single string containing both formatted dates joined by " – " (en-dash with surrounding spaces), month rendered per the locale.
    - formatDateRange(...,'en') renders the same two dates with English month formatting.
    - formatDateRange(null, '2026-08-16', locale) OR (start, null, locale) OR (null, null, locale) returns the localized "dates TBA" fallback string (no "Invalid Date", no throw).
    - The day component is stable regardless of runtime timezone: formatDateRange('2026-08-13','2026-08-13','en') renders "Aug 13, 2026" (not Aug 12) — i.e. parsed date-only, not UTC-shifted.
  </behavior>
  <action>
    Create apps/mobile/lib/date-range.ts exporting a pure `formatDateRange(start: string | null, end:
    string | null, locale: string): string`. If EITHER date is null/empty, return a localized fallback
    label (a plain constant the caller can also import, e.g. exported `DATES_TBA_KEY` — but since this
    is a pure lib with no Lingui runtime, return the two possible strings by locale: 'de' → 'Termin
    folgt', else 'Dates TBA'; the SCREEN may alternatively pass an already-localized fallback — pick one
    and keep it consistent). Otherwise parse each `YYYY-MM-DD` by splitting into year/month/day integers
    and constructing `new Date(y, m - 1, d)` (LOCAL date — never `new Date(str)`, which is UTC and shifts
    the calendar day in negative offsets, REVIEW 05-03 MEDIUM), format each with ONE `new
    Intl.DateTimeFormat(locale, { day: 'numeric', month: 'short', year: 'numeric' })` via two separate
    `.format()` calls (never the single-call range API, Pitfall 3), and join with an en-dash plus
    surrounding spaces. Create apps/mobile/lib/__tests__/date-range.test.ts mirroring otp-error.test.ts
    (pure input/output, no RN imports) covering all four behaviors above including the null fallbacks and
    the timezone-stable day.

    Create apps/mobile/lib/active-festival-storage.ts as a verbatim structural copy of
    avatar-storage.ts's lazy-`require('react-native-mmkv')` accessor, storage id
    `festipal-active-festival`, key `active-festival-slug`, exporting `saveActiveFestivalSlug(slug)`,
    `getActiveFestivalSlug(): string | undefined`, `clearActiveFestivalSlug()` (require INSIDE the
    accessor so the module imports under Vitest).

    Create apps/mobile/lib/festival-queries.ts exporting (a) a `festivalKeys` query-key factory —
    `all: ['festivals'] as const`, `mine: ['me','festivals'] as const`, `detail: (slug) => ['festival',
    slug] as const` — the single source for these keys across screens; and (b) `unwrapOk<T>(response:
    { status: number; body: unknown }): T` that returns `response.body as T` when `status === 200` and
    otherwise throws a typed `SaveFestivalError`/`ApiResponseError` carrying `response.status` (used by
    the 05-06 save mutation so a non-200 ts-rest result becomes a real rejection). Keep it framework-free
    (no React import) so it is unit-testable and reusable.

    Create apps/mobile/lib/festival-navigation.ts exporting `leaveFestival(router: Router): void` =
    `router.canGoBack() ? router.back() : router.replace('/festivals')` — the deterministic
    non-dead-end Back used by the festival home header and after any cold-start `router.replace('/f/:slug')`
    (REVIEW 05-03/05-05 HIGH). Type `router` from expo-router's `useRouter` return type.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile test && pnpm --filter @festipal/mobile typecheck</automated>
  </verify>
  <done>date-range.test.ts passes for de + en + null fallback + timezone-stable day; active-festival-storage exposes save/get/clear via a lazy MMKV require; festival-queries exports festivalKeys + unwrapOk; festival-navigation exports leaveFestival; mobile test + typecheck are green.</done>
  <acceptance_criteria>
    - `formatDateRange` accepts `string | null`, returns a localized fallback on a null date, parses date-only into a LOCAL date, and never calls `formatRange`; covered by de/en/null/timezone-stable assertions.
    - `active-festival-storage.ts` uses a lazy `require('react-native-mmkv')` inside its accessor and a distinct storage id.
    - `festival-queries.ts` exports `festivalKeys` (all/mine/detail) and `unwrapOk` (throws on non-200 with the status attached).
    - `festival-navigation.ts` exports `leaveFestival(router)` = canGoBack ? back : replace('/festivals').
    - `pnpm --filter @festipal/mobile test && typecheck` are green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ComingSoonTile owned primitive (static disabled tile)</name>
  <files>apps/mobile/components/ComingSoonTile.tsx</files>
  <read_first>
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract "Coming-soon tile" (2x2 grid, View not Pressable, surfaceCard bg, borderSubtle, radiiScale r-md, centered muted icon + label + "Bald"/"Soon" badge in fillQuiet/textMuted) and ## Color (fillQuiet/textMuted roles)
    - apps/mobile/components/AvatarTile.tsx (an existing owned-primitive component for the token-destructuring + fontFamily-via-fonts-context conventions)
    - apps/mobile/lib/fonts.ts + apps/mobile/lib/fonts-context.tsx (resolveFontFamily / useFontsReady pattern used by every restyled component)
  </read_first>
  <action>
    Create apps/mobile/components/ComingSoonTile.tsx as an owned RN primitive (ADR-022) per the UI-SPEC
    Component Contract: a non-interactive `View` (NOT Pressable) with `surfaceCard` background,
    `borderSubtle` 1px border, `radiiScale['r-md']` radius, a centered Lucide icon (`textMuted`, 22px) +
    a label (`title3`, `textMuted`) + a small "Soon"/"Bald" badge in the corner (`micro`, `fillQuiet`
    bg, `textMuted` fg — deliberately NOT accent). Props take the icon component, the label node, and
    the badge text (all passed in; the tile does not hardcode copy). Destructure tokens
    (`colors`, `typeRoles`, `radiiScale`, `spacingScale`) at module top per the codebase convention and
    resolve font families via `resolveFontFamily` + `useFontsReady`. No press feedback, no navigation;
    if focusable at all, set `accessibilityState={{ disabled: true }}`.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
  </verify>
  <done>ComingSoonTile renders a static muted tile from props (icon/label/badge), is non-interactive, typechecks and lints clean (no literal-string violations in its own copy — labels come from props).</done>
  <acceptance_criteria>
    - The component is a `View`, never a `Pressable`/`TouchableOpacity`.
    - It reads `radiiScale['r-md']`, `fillQuiet`, `textMuted`, `surfaceCard`, `borderSubtle` from tokens.
    - It accepts icon/label/badge via props (no hardcoded user-facing string inside the component).
    - typecheck + lint pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Festival home screen at /f/:festivalSlug + wire gate-less entry from the list</name>
  <files>apps/mobile/app/(festival)/f/[festivalSlug].tsx, apps/mobile/app/(festival)/index.tsx, apps/mobile/app/festivals/index.tsx</files>
  <read_first>
    - apps/mobile/app/(festival)/index.tsx (current static placeholder — the Back-affordance pattern to keep the spirit of; this file is DELETED and replaced by the dynamic route)
    - apps/mobile/app/festivals/index.tsx (current `handleEnter` at ~81-86 does `router.push('/(festival)')`; the query/loading/error text pattern to reuse for the festival home's states)
    - .planning/phases/05-festival-selection-home/05-PATTERNS.md (festival home slug-keyed query pattern + findCachedFestivalBySlug) and 05-RESEARCH.md Pattern 2
    - .planning/phases/05-festival-selection-home/05-UI-SPEC.md ## Component Contract (festival home identity: display2 name, bodySm caption "{dates} · {place}"), ## Copywriting Contract (coming-soon tile labels + "Bald"/"Soon" + Back a11y), and ## UI Considerations festival-home-identity backstops (loading/error/long-text)
    - apps/mobile/lib/date-range.ts + apps/mobile/lib/active-festival-storage.ts + apps/mobile/lib/festival-queries.ts + apps/mobile/lib/festival-navigation.ts (Task 1) and apps/mobile/components/ComingSoonTile.tsx (Task 2)
    - apps/mobile/lib/i18n.ts (how the active UI locale is obtained to pass into formatDateRange)
  </read_first>
  <action>
    Create apps/mobile/app/(festival)/f/[festivalSlug].tsx and DELETE apps/mobile/app/(festival)/index.tsx.
    Read the slug via `useLocalSearchParams<{ festivalSlug: string }>()`. Fetch with `useQuery({
    queryKey: festivalKeys.detail(festivalSlug), queryFn: () => apiClient.getFestival({ params: { slug:
    festivalSlug } }) })` — do NOT pass `initialData`/`placeholderData`. Separately compute a
    `cachedFestival` via a `findCachedFestivalBySlug(queryClient, slug)` helper that scans the
    `festivalKeys.all` (`['festivals']`) and `festivalKeys.mine` (`['me','festivals']`) caches — each of
    which holds a FULL ts-rest response object `{ status, body }`, so read `cache?.status === 200 ?
    cache.body.find(f => f.slug === slug) : undefined` (REVIEW 05-03 HIGH — the cache does NOT hold bare
    Festival values). Branch the render explicitly into FOUR states (REVIEW 05-03 MEDIUM): (1)
    `query.isPending` AND no `cachedFestival` → the existing festivals loading-text pattern; (2)
    `query.isError` (transport failure) → the existing network-error pattern WITH a working Back; (3)
    `query.data?.status === 404` → a not-found message WITH a working Back (a 404 is a SUCCESSFUL React
    Query result, never `query.status === 'error'`); (4) render from `festival = query.data?.status ===
    200 ? query.data.body : cachedFestival`. Render: the festival name as the `display2` H1 (identity);
    a key-facts caption in `bodySm`/`textMuted` reading `"{formatDateRange(festival.startDate,
    festival.endDate, activeLocale)}"` followed by `" · {place}"` ONLY when `festival.place` is non-null
    (null-safe, DATE-NULLABILITY), `numberOfLines={1}` — pass the active UI locale from i18n, not a
    hardcoded 'de'; and a 2x2 grid of four `ComingSoonTile`s (Timetable `calendar-clock`, Lageplan
    `map-pin`, Cashless `wallet`, News `newspaper`) with labels + the "Soon"/"Bald" badge via Lingui
    `t()`/<Trans> (the Cashless tile is fully static — do NOT read cashlessUrl). Provide the Back
    affordance via a `Stack.Screen` header back (accessible "Back"/"Zurück" label) whose handler calls
    `leaveFestival(router)` (Task 1 helper: `canGoBack ? back : replace('/festivals')`) so
    entering-then-back — including on a cold-start that entered via `router.replace('/f/:slug')` and left
    no history — returns to the shell, never a dead-end or app exit (FEST-04, REVIEW 05-03/05-05 HIGH).

    In apps/mobile/app/festivals/index.tsx: change `handleEnter` to accept the tapped festival's slug,
    call `saveActiveFestivalSlug(slug)` (D-06 persistence) and `router.push(\`/f/${slug}\`)` (typed
    route). Update `renderRow`'s Enter affordance to pass `item.slug`. This is the minimal entry
    rewiring; the full Meine/Alle rewrite of this screen happens in a later plan.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/mobile typecheck && pnpm --filter @festipal/mobile lint</automated>
    <manual>On device (real Android, matching Phase 3/4 gate): enter the seeded frequency-2026 festival from the list -> the festival home shows the name as H1, the caption "13. Aug 2026 – 16. Aug 2026 · Wiesen, Burgenland" (locale-formatted), and four disabled coming-soon tiles; back returns to the list. Toggle device language DE/EN and confirm the date range re-formats. Confirm a wrong slug (dev) shows the error pattern with a usable Back.</manual>
  </verify>
  <done>Entering a festival opens /f/:slug rendering real D-08 dates/place + the coming-soon menu; back is never a dead-end; typecheck + lint pass and the on-device tracer check confirms real dates/place render.</done>
  <acceptance_criteria>
    - The route file is `app/(festival)/f/[festivalSlug].tsx` and the old `(festival)/index.tsx` is deleted.
    - The screen fetches via `getFestival({ params: { slug } })` (slug-keyed, no UUID route) with NO `initialData`/`placeholderData`; instant paint comes from a separately-derived `cachedFestival` that reads `.body` from the status-200 cached response.
    - The render branches on pending / `query.isError` / `data.status === 404` / `data.status === 200`; the 404 branch shows a not-found message with a working Back.
    - The date range is produced by null-safe `formatDateRange` with the active UI locale (not a literal string, not `formatRange`); ` · {place}` is only appended when place is non-null.
    - The four coming-soon tiles render via `ComingSoonTile` and none reads `cashlessUrl`.
    - The Back handler calls `leaveFestival(router)` (canGoBack ? back : replace('/festivals')).
    - The list's Enter affordance persists the slug via `saveActiveFestivalSlug` and pushes `/f/:slug`.
    - typecheck + lint pass; on-device tracer manual check signed off.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device-local MMKV -> navigation target | The persisted active-festival slug drives the launch/entry target |
| client -> API | `getFestival(slug)` reads a single festival's public identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | active-festival MMKV slug | low | accept | The persisted slug only ever constructs a `getFestival(slug)` request, itself gate-less public-per-authenticated-visitor browse data (ADR-014); persisting it grants no privilege the visitor lacked via the browse endpoint |
| T-05-03 | Information Disclosure | Cashless coming-soon tile | low | accept | The tile is fully static/disabled this phase (no data fetch, no `cashlessUrl` render); ADR-011 hide-when-unset only becomes load-bearing when a later phase makes Cashless live — documented as a forward constraint |
| T-05-06 | Denial of Service | getFestival 404 / hung request | low | mitigate | The screen renders the existing error pattern and keeps the Back affordance on 404 (FEST-04 backstop); no infinite spinner path is introduced |
</threat_model>

<verification>
- `pnpm --filter @festipal/mobile test` (Task 1: formatDateRange)
- `pnpm --filter @festipal/mobile typecheck && lint` (Tasks 2, 3)
- On-device tracer check: seeded festival's real dates/place render on the festival home; back works (Task 3 manual)
</verification>

<success_criteria>
- The full D-08 vertical is proven end-to-end: seed -> API -> festival home shows real dates/place.
- formatDateRange is Hermes-safe and tested; active-festival slug persists on entry.
- The festival home renders identity + key facts + a static coming-soon menu with a non-dead-end back.
</success_criteria>

<output>
Create `.planning/phases/05-festival-selection-home/05-03-SUMMARY.md` when done.
</output>
