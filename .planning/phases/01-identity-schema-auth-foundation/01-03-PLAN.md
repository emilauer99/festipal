---
phase: 01-identity-schema-auth-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - packages/db/src/schema/my-festival.ts
  - packages/db/src/schema/index.ts
  - packages/db/drizzle/
autonomous: false
requirements: [PLAT-01]
user_setup: []

must_haves:
  truths:
    - "A my_festival table exists with composite primary key (visitorId, festivalId): visitorId text FK -> visitor_profile.accountId ON DELETE CASCADE, festivalId uuid FK -> festival.id ON DELETE CASCADE, savedAt timestamp, camp nullable text (reserved) (SC-1, D-04)."
    - "my_festival is the ONLY link between global identity and tenant (festival) data — it is always keyed/queried by visitorId; the global Account carries no festivalId FK (SC-2, ADR-014/016)."
    - "drizzle-zod insert+select base schemas are exported for my_festival from the packages/db barrel (SC-3, D-02)."
    - "The full schema (auth + visitor_profile + my_festival) generates and applies cleanly against the Neon dev branch (SC-1)."
    - "Inserting two visitor_profile rows whose usernames differ only in case fails the second insert with a Postgres unique-violation (23505), proving the lower(username) index is live in the applied database (SC-2)."
  prohibitions:
    - "my_festival.visitorId must FK to visitor_profile.accountId (NOT directly to user.id) — this encodes the 'completed profile before you can save a festival' invariant at the schema layer (D-04); it must not be re-pointed at user.id."
    - "my_festival must NOT be reshaped into an org/membership/role table — it is a gate-less save edge; no invite/role/active-festival columns (PITFALL 1, ADR-014)."
  artifacts:
    - packages/db/src/schema/my-festival.ts
    - packages/db/drizzle/ (final migration adding my_festival)
  key_links:
    - "my_festival.visitorId (text FK) -> visitor_profile.accountId — the save-membership edge and the profile-before-save invariant."
    - "my_festival.festivalId (uuid FK) -> festival.id — the single global<->tenant bridge (ADR-014)."
---

<objective>
EXPANSION on the proven skeleton. Add the `my_festival` gate-less save-membership join table
(the one bridge between global identity and tenant data), derive its drizzle-zod base, generate
the final migration, apply the complete schema to Neon, and prove the live case-insensitive
username uniqueness constraint with a real duplicate-insert.

Purpose: Completes SC-1 (all tables migrate cleanly), SC-2 (my_festival as the only global<->tenant
link + verified live unique index), and closes the phase's schema surface. The pipeline it rides
(table -> migration -> drizzle-zod -> Neon) was already proven by the Plan 02 tracer.
Output: `my-festival.ts` (+ drizzle-zod bases), the final migration applied to Neon, and a live
uniqueness verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-schema-auth-foundation/01-CONTEXT.md
@.planning/phases/01-identity-schema-auth-foundation/01-RESEARCH.md
@.planning/phases/01-identity-schema-auth-foundation/01-PATTERNS.md
@packages/db/src/schema/visitor-profile.ts
@packages/db/src/schema/festival.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define my_festival (composite-PK gate-less save join) + drizzle-zod base</name>
  <files>packages/db/src/schema/my-festival.ts, packages/db/src/schema/index.ts</files>
  <read_first>
    - packages/db/src/schema/tag.ts (tagTranslation — the closest composite-PK, two-FK join-table analog)
    - packages/db/src/schema/festival.ts (festivalLocale composite-PK shape; festival.id FK target — uuid)
    - packages/db/src/schema/visitor-profile.ts (visitor_profile.accountId FK target — text; drizzle-zod colocated-base pattern to mirror)
    - packages/db/src/schema/_shared.ts (note: my_festival uses a single savedAt timestamp, NOT the full `...timestamps` pair — intentional per D-04)
    - packages/db/src/schema/index.ts (barrel to extend)
    - .planning/phases/01-identity-schema-auth-foundation/01-PATTERNS.md (my-festival.ts pattern assignment; mixed text/uuid FK note)
    - .planning/phases/01-identity-schema-auth-foundation/01-CONTEXT.md (D-04 — my_festival shape, mixed id types, camp reserved)
  </read_first>
  <action>
Create `packages/db/src/schema/my-festival.ts` mirroring the tagTranslation/festivalLocale
composite-PK join shape. Define `myFestival` pgTable named `my_festival` with columns: visitorId
text notNull referencing visitor_profile.accountId with onDelete cascade; festivalId uuid notNull
referencing festival.id with onDelete cascade; savedAt timestamp (with timezone) notNull defaultNow;
camp nullable text (reserved, no UI this cycle). Third-argument constraint array:
`(t) => [primaryKey({ columns: [t.visitorId, t.festivalId] })]`. Use the terse column style (global
snake_case casing). Add a JSDoc block citing ADR-014/016 and D-04 explaining this is the gate-less
save edge and the only global<->tenant link, and noting the intentional mixed id types (text
visitorId + uuid festivalId).

Colocate the drizzle-zod bases in the same file: export `myFestivalInsertSchema =
createInsertSchema(myFestival)` and `myFestivalSelectSchema = createSelectSchema(myFestival)`.

Extend `packages/db/src/schema/index.ts` with `export * from './my-festival';`.

Do NOT add invite/role/active-festival columns (gate-less save, not org membership). Do NOT
FK visitorId to user.id (must be visitor_profile.accountId). Do NOT add a ts-rest route (Phase 2).
Run typecheck + build.
  </action>
  <verify>
    <automated>pnpm --filter @festipal/db typecheck && pnpm --filter @festipal/db build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema/my-festival.ts` defines `my_festival` with `primaryKey({ columns: [t.visitorId, t.festivalId] })`.
    - visitorId is `text` referencing `visitor_profile.accountId` with onDelete cascade; festivalId is `uuid` referencing `festival.id` with onDelete cascade; both are notNull; savedAt is a notNull timestamp; camp is nullable text.
    - `myFestivalInsertSchema` and `myFestivalSelectSchema` are exported and reachable from the `@festipal/db/schema` barrel.
    - No invite/role/active-festival columns exist; visitorId does not reference user.id directly.
    - `pnpm --filter @festipal/db typecheck` and `pnpm --filter @festipal/db build` exit 0.
  </acceptance_criteria>
  <done>my_festival exists as a gate-less composite-PK save join keyed on visitor_profile.accountId and festival.id, with drizzle-zod bases exported and the package typechecking + building.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate + apply the final migration and verify the live unique index</name>
  <files>packages/db/drizzle/</files>
  <precondition>The Neon dev branch is reachable via DATABASE_URL_UNPOOLED (per ADR-005). If unset/unreachable, halt — do not fabricate a migration apply or the uniqueness proof.</precondition>
  <read_first>
    - packages/db/drizzle.config.ts (out dir, DATABASE_URL_UNPOOLED, casing)
    - packages/db/src/client.ts (createDatabase helper — reuse for the throwaway duplicate-insert script)
    - .planning/phases/01-identity-schema-auth-foundation/01-RESEARCH.md (§Sampling Rate — Phase gate: real db:migrate + manual duplicate-insert; §Validation Architecture SC-2 row)
  </read_first>
  <action>
Run `pnpm --filter @festipal/db db:generate` to emit the final migration under
`packages/db/drizzle/` (it will contain the my_festival CREATE TABLE + its two FKs + composite PK).
Then run `pnpm --filter @festipal/db db:migrate` against the Neon dev branch and confirm the FULL
schema (auth + visitor_profile + my_festival) applies with exit 0 — this satisfies SC-1
("migrate cleanly against Neon").

Then prove SC-2's constraint is live against the applied database: using a throwaway script
(reuse `createDatabase(process.env.DATABASE_URL_UNPOOLED)` from client.ts, or drizzle-kit studio),
insert a `user` row + a `visitor_profile` row with a username, then attempt a second visitor_profile
insert (with its own user) whose username differs only in letter case. Confirm the second insert is
rejected with Postgres error code 23505 (unique_violation) raised by
`visitor_profile_username_lower_unq`. Clean up the throwaway rows afterward. Record the observed
error in the SUMMARY. (No Vitest framework is stood up this phase — CONCERNS.md confirms none exists
project-wide; a scripted/studio check is the appropriate gate for a schema-only phase.)
  </action>
  <verify>
    <automated>pnpm --filter @festipal/db db:generate && pnpm --filter @festipal/db db:migrate</automated>
    <human-check>Run the duplicate-insert script/studio check and confirm the case-variant second insert fails with error 23505 from visitor_profile_username_lower_unq; clean up the rows.</human-check>
  </verify>
  <acceptance_criteria>
    - A final migration file under `packages/db/drizzle/` contains CREATE TABLE my_festival with the composite PK and both cascade FKs (text visitorId -> visitor_profile, uuid festivalId -> festival).
    - `pnpm --filter @festipal/db db:migrate` applies the full schema against Neon and exits 0.
    - A duplicate case-variant username insert is rejected with Postgres 23505 raised by visitor_profile_username_lower_unq; the observation is recorded in the SUMMARY.
    - Re-running `db:generate` on the unchanged schema produces no new migration (idempotency — the flagged $COVERAGE idempotency edge, confirmed).
  </acceptance_criteria>
  <done>The complete identity/membership schema migrates cleanly against Neon and the case-insensitive username uniqueness is proven live in the database.</done>
</task>

</tasks>

<artifacts_produced>
Symbols/files this plan creates (excluded from drift verification as newly-created):
- Files: `packages/db/src/schema/my-festival.ts`; the final migration under `packages/db/drizzle/`
- Table: `my_festival` (composite PK visitorId+festivalId)
- drizzle-zod bases: `myFestivalInsertSchema`, `myFestivalSelectSchema`
- Barrel addition: `export * from './my-festival'`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| global identity -> tenant (festival) data | my_festival is the single sanctioned bridge; any other cross-link would break tenant isolation |
| profile-completion invariant | my_festival.visitorId -> visitor_profile.accountId encodes "must have a profile before saving" at the schema level |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-XT | Elevation of Privilege / Information Disclosure | Cross-tenant leakage via an incorrect global<->tenant link | high | mitigate | my_festival is the ONLY link and is always keyed by visitorId; global Account has no festivalId; FK targets asserted in acceptance (Phase 2's TenantGuard queries this correct relation) |
| T-01-INV | Tampering | visitorId re-pointed at user.id, bypassing the profile-before-save invariant | medium | mitigate | Acceptance asserts visitorId FKs to visitor_profile.accountId, not user.id; recorded as a prohibition |
| T-01-UN | Spoofing | Username uniqueness not actually enforced in the live DB | high | mitigate | Live duplicate-insert proof asserts 23505 from the functional index against the applied Neon schema |
</threat_model>

<verification>
- `pnpm --filter @festipal/db typecheck && build` exit 0.
- Final `db:generate` + `db:migrate` apply the full schema to Neon (exit 0); re-generate is a no-op.
- Live case-variant duplicate insert rejected with 23505.
</verification>

<success_criteria>
The full identity/membership schema (auth + visitor_profile + my_festival) migrates cleanly against
Neon (SC-1); my_festival is the only global<->tenant link keyed by visitorId (SC-2); the
lower(username) unique index is proven live in the database (SC-2).
</success_criteria>

<output>
Create `.planning/phases/01-identity-schema-auth-foundation/01-03-SUMMARY.md` when done
(record the final migration filename, the Neon apply result, and the observed 23505 uniqueness proof).
</output>
